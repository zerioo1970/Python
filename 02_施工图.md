# Python 3 新手完全教程 · 施工图

> **这是什么**：写正文时照着本文件干活，不需要再做设计决策。
> 第一部分是**编写规范**（模板、示例标准、术语表），第二部分是**每章图纸**（知识点清单、代码示例清单、样板示例、易错点）。
> 教程总纲与完整目录见 `01_目录.md`。
>
> **核心原则：不设字数上限。** 唯一标准是把概念讲清楚——宁可长，不可漏。

---

# 第一部分　编写规范

## 1. 每章正文的统一模板

每章正文按以下固定结构撰写，小节序号与 `01_目录.md` 的详细目录对应：

```markdown
# 第 N 章　章节标题

> **本章目标**：一句话说明读完能做什么（动词开头，可验证）
> **前置章节**：第 X 章
> **预计用时**：X 小时
> **优先级**：★★★

## 开篇：为什么需要它
用 3~6 行讲清"不学它会怎样"。禁止用"XX 是 Python 的重要特性"这类空话，
必须给一个具体的、读者能共情的麻烦场景。

## N.1 小节标题
    讲解 → 示例 → 说明输出 → （必要时）对比错误写法

## ...

## 本章易错点
表格形式：错误写法 | 症状/报错 | 正确写法 | 原因

## 本章小结
- 3~8 条要点，每条一行
- 一张"本章 API 速查表"（函数/方法 | 作用 | 最短用法）

## 下一章预告
一句话：这一章留下的什么问题，下一章解决。
```

### 章内节奏要求

- **不设字数上限。** 唯一标准是把概念讲清楚——宁可长，不可漏。
  某章确实长到影响阅读时，**拆章**（如第 5 章拆成 5a / 5b）而不是删内容或降级到工具箱。
- **讲解与代码交替出现**，禁止连续 3 段以上纯文字。
- 每个小节至少 1 个示例；纯概念小节（如"为什么需要它"）可以没有。

---

## 2. 概念讲解的深度标准

> 本节是"不设字数上限"的落地标准。宁可长，不可漏——但**长必须长在这四件事上**，不是长在废话上。

### 2.1 每个概念必答四问

任何一个知识点，正文里必须依次回答清楚：

| 问 | 要求 | 反例（不合格） |
|---|---|---|
| **是什么** | 一句话定义 + 一个最小可运行示例 | 只给定义不给示例 |
| **为什么** | 为什么这样设计 / 为什么会出现这个现象。**只给直觉解释与类比，不做数学推导** | "记住就行""这是语言规定" |
| **什么时候用** | 至少一个具体场景，且是读者能想象的日常任务 | 只讲语法不讲用途 |
| **容易错在哪** | 至少一组 ❌/✅ 对照；确实没坑就明确写"这个没什么坑" | 留给读者自己踩 |

**★★★ 级别的概念，额外必须补两项**：

- **边界情况**：空值、零、负数、越界、类型不匹配、中文时分别会怎样
- **常见误解**：新手通常以为它是什么，而实际不是（这一条往往比正确解释更有价值）

### 2.2 讲解手法（六条硬要求）

1. **先现象，后原因。** 先让读者亲眼看到 `print(0.1 + 0.2)` 输出 `0.30000000000000004`，再解释为什么。顺序反了读者没有动机听。
2. **抽象概念必须给类比。** 类比优先用**已经学过的编程概念**，其次用生活常识。给完类比要立刻补一句"这个类比在哪里不成立"，防止读者过度延伸。
3. **禁止跳步。** 不允许出现未经解释就使用的术语。用到"可哈希""可迭代""不可变"这类词，要么当场用一句话解释，要么明确写"详见第 N 章，这里只需知道……"。
4. **不回避难点。** 遇到真正难懂的（不可变性、深浅拷贝、闭包、GIL），宁可多写三五百字，不许用"以后你就懂了""这里比较复杂"糊过去。
5. **原理讲到"够用"为止。** 判断标准：读者能据此**预测**代码行为、**排查**相关 bug 就够了。不需要讲 CPython 实现细节，那属于专项 F。
6. **每个概念结束时给一句"记住这条"。** 用一句可复述的话收口，读者合上书能想起来的就是这一句。

### 2.3 什么情况允许写短

不设上限不等于处处都要长。以下情况允许一两句带过，但**必须交代为什么不细讲**：

| 情况 | 处理方式 | 例子 |
|---|---|---|
| ★ 优先级且日常极少用 | 用途 + 一个最小示例 + 一句"用到再查" | `complex`、`frozenset`、`calendar` |
| 【面试向】原理 | 讲清现象 + 一句结论，原理指向专项 | 小整数缓存、字符串驻留 |
| 有更好替代的旧写法 | 讲到"能看懂别人代码"就够，明确说新代码别用 | `%` 格式化、`os.path`、`unittest` |

**注意**：允许短的是**篇幅**，不是**四问**。哪怕两句话带过，也必须说清"是什么"和"什么时候用"。

---

## 3. 代码示例标准

### 3.1 编号与命名

- 编号格式 `N-M`：`N` 为章号，`M` 为章内序号，从 1 开始连续。
- 引用其他章示例写作"参见示例 13-4"。
- 每个示例必须有**一行标题**，动词或名词短语，说明它演示什么，不要写"示例 1"。

```markdown
**示例 5-3　用切片取字符串的一部分**
```

### 3.2 难度梯度（硬要求）

每章示例按下面三段递进，前段不能出现后段的语法：

| 段位 | 特征 | 长度 |
|---|---|---|
| **A 最简形式** | 只演示本节一个语法点，无干扰要素，可在 REPL 里一行验证 | 1~5 行 |
| **B 组合使用** | 与已学章节内容组合（如本节语法 + 循环 + 判断） | 5~15 行 |
| **C 接近真实** | 一个能独立运行的小片段，解决一个具体小问题 | 15~30 行 |

**三段的数量不设固定比例。** 各章性质差别很大，硬凑比例反而坏事：

| 章节性质 | 典型分布 | 原因 |
|---|---|---|
| 前几章（1~8）：概念多、每个都简单 | **A 段为主** | 还没有足够的已学内容可供"组合"，B 段天然凑不出来 |
| 中间章（9~15）：容器与函数 | A / B 均衡 | 开始能与前面的语法组合 |
| 后面章（16~37）：概念复杂、偏工程 | **B / C 段为主** | 单个语法点少，但组合场景多，需要接近真实的完整片段 |

**唯一的硬要求**：每章必须有 A 段（先让读者看清最简形态），且**至少有一个 C 段**（让读者看到它在真实代码里长什么样）。数量按"讲清楚需要多少"来定。

- **只能向后引用已学内容**。第 9 章的示例不得出现第 14 章的函数定义、第 26 章的类。
- C 段示例的数量按需要定，不设上下限。
- 单个示例**不超过 30 行**。超了就拆成两个示例。

### 3.3 注释规范

原则：**注释解释"为什么"和"容易看错的地方"，不解释"是什么"。**

```python
# ✅ 该加注释的地方
total = sum(prices) / len(prices)   # len 为 0 时会抛 ZeroDivisionError，见 15.3
data = raw.strip().split(",")       # strip 先去掉行尾换行，否则最后一项带 \n

# ❌ 不该加注释的地方
count = 0        # 把 count 设为 0        ← 废话
print(name)      # 打印 name             ← 废话
```

**判定标准：满足下列任一条的行，就是"重要行"，必须有注释。**

| 情况 | 例子 |
|---|---|
| 出现本章新讲的语法或 API 的那一行 | 第 5 章里第一次出现 `s.split(",")` |
| 行为有隐含约定：返回新对象 vs 原地修改 | `s.upper()` 返回新串；`lst.sort()` 原地改且返回 `None` |
| 运行结果反直觉 | `-7 // 2`、`0.1 + 0.2`、`int(3.9)`、`round(2.5)` |
| 参数含义不自明 | `range(1, 10, 2)`、`open(path, "a", encoding="utf-8")`、`s.replace("a", "b", 1)` |
| 涉及版本差异、平台差异、或与其他语言习惯不同 | `-7 % 2` 在 C#/Java 里结果不同 |
| 埋了前置条件或可能出错 | 可能抛异常、依赖外部文件存在、需要联网 |

具体要求：

1. **重要行必须全覆盖**，其余行不要加注释（见上方 ❌ 示例）。
2. **注释密度不设上下限**，以"重要行全覆盖"为准。A 段示例通常 1~2 条，C 段可能十几行里有五六条。
3. **注释写在行尾**（短）或**行上方**（长于 30 字）。不用块注释堆在开头。
4. **结果反直觉时，注释要写出正确预期**，例如 `print(-7 // 2)   # => -4，注意不是 -3`。
5. 注释一律用中文，代码中的标识符一律用英文（示例数据可以是中文字符串）。

### 3.4 输出标注

- 单行输出用 `# =>` 紧跟在语句后：
  ```python
  print(10 / 3)      # => 3.3333333333333335
  ```
- 多行输出用独立的输出块：
  ```python
  for i in range(3):
      print(i, i * i)
  ```
  ```text
  0 0
  1 1
  2 4
  ```
- 报错示例标注异常类型与关键信息，不贴完整栈：
  ```python
  int("abc")   # ❌ ValueError: invalid literal for int() with base 10: 'abc'
  ```

### 3.5 错误示范

- 一律用 `# ❌` 和 `# ✅` 成对出现，**错误写法必须紧跟正确写法**，不允许单独出现错误代码（防止读者误抄）。
- 每章"易错点"小节汇总本章所有 ❌，形成表格。

### 3.6 示例数据约定

- 人名用 `小明` `小红` `Alice` `Bob`；不用真实姓名。
- 数字用小整数，避免读者算不过来。金额用 `19.9` 这类两位小数。
- 文件名统一用 `data.txt` / `data.csv` / `config.json` / `report.xlsx`。
- 目录路径用相对路径，不写 `C:\Users\...` 或 `/home/xxx`。
- 网络示例统一用 `https://httpbin.org/get` 或 `https://api.github.com`，并注明"需联网"。
- 涉及数据库统一用 `sqlite3` + 文件 `demo.db`，不要求读者装数据库。

### 3.7 可运行性

- 每个示例必须**独立可运行**：不依赖前一个示例定义的变量。若必须依赖，在示例标题后注明"（接示例 N-M）"。
- 用到第三方库时，在示例前用一行说明：`> 需先安装：pip install requests`
- 写文件的示例要在同目录写，且**不得删除用户已有文件**；示例结束提示清理。

---

## 4. 术语与用词

### 4.1 中英文对照（全书统一，首次出现给英文，之后只用中文或只用英文）

| 中文 | 英文 | 全书主用 |
|---|---|---|
| 变量 | variable | 中文 |
| 函数 / 方法 | function / method | 中文 |
| 参数（定义时）/ 实参（调用时） | parameter / argument | 中文，需区分时注明 |
| 列表 / 元组 / 字典 / 集合 | list / tuple / dict / set | **英文**（避免歧义） |
| 可变 / 不可变 | mutable / immutable | 中文 |
| 可迭代对象 / 迭代器 | iterable / iterator | 中文 |
| 推导式 | comprehension | 中文 |
| 装饰器 | decorator | 中文 |
| 上下文管理器 | context manager | 中文 |
| 类型注解 | type hint / annotation | 中文 |
| 魔术方法 / 特殊方法 | dunder / magic method | 中文用"特殊方法"，括注"魔术方法" |
| 异常 / 报错 | exception / error | 中文 |
| 栈回溯 | traceback | **Traceback** |
| 虚拟环境 | virtual environment | 中文 |
| 解包 | unpacking | 中文 |

### 4.2 用词禁忌

- 禁止"显而易见""众所周知""简单来说不过是"——新手不觉得简单。
- 禁止"以后你就懂了"——要么现在讲，要么明确写"详见第 X 章"。
- 禁止无出处的性能断言（"这样写快很多"）。要给数量级或让读者用 `timeit` 自测。
- 提到"最佳实践"必须说明适用条件。

---

## 5. 交叉引用规则

- 向后引用（还没讲）：写"详见第 X 章"，并且**当前章必须能不依赖它读懂**。
- 向前引用（已讲过）：写"参见 N.M 节"。
- 涉及坑的地方，统一指向 `T7-F 新手最常踩的 30 个坑`。
- 涉及报错，统一指向 `T7-A 常见报错速查表`。
- 有其他语言经验的对照，统一放在章末的折叠块里，不打断正文：

```markdown
<details>
<summary>给有 C# / SQL 经验者</summary>

Python 的 `dict` 相当于 C# 的 `Dictionary<TKey, TValue>`，但……
</details>
```

---

## 6. 文件与目录约定

```
Python3新手教程/
├── 01_目录.md          ← 总纲：定位、读法、四阶段完整目录
├── 02_施工图.md        ← 本文件：编写规范 + 每章图纸
├── chapters/           ← 各章正文
│   ├── ch01_认识Python与环境搭建.md
│   └── ...
├── toolbox/            ← 工具箱正文（T1~T7，随查手册）
│   ├── T5_编辑器实用指南.md
│   └── T6_pip疑难速查.md
├── tools/              ← 写作辅助脚本（输出质检、目录同步）
├── projects/           ← 三个验收项目的参考实现
└── assets/             ← 图片（尽量少用）
```

- 章节文件名：`ch{两位章号}_{中文标题去掉标点}.md`
- 专项文件名：`spec{字母}_{标题}.md`；工具箱：`box{编号}_{标题}.md`
- 图片能用表格或代码说明的就不画图；必须画时用 Mermaid，且**最宽不超过 2 列节点**，用 `graph TB`，单节点标签每行不超过 20 个中文字符。

---

## 6.5 写作手法：两条贯穿全书的线

这两条不是可选的修饰，是本教程的结构性设计。**写任何一章都要检查这两条。**

### 6.5.1 伏笔回收链

同一个原理会在多章以不同面貌出现，**必须显式回收**，让读者意识到"这几处怪事是同一件事"。

已建立的最长一条链（写到相关章节时必须回指）：

```
3.1  变量是标签不是盒子
  → 9.20  [[0]*3]*3 改一行变三行
  → 9.21  b = a 之后改 b，a 也变
  → 10.4  元组不可变，里面的列表却能改
  → 11.18 字典 copy() 之后内层还共享
  → 13.5  统一讲透（浅拷贝 / 深拷贝 / 判断标准）
  → 14.9  函数参数传的也是引用
```

**写法要求**：在回收处明确写出"回收 X.Y 节"，并说明它们为什么是同一件事。

### 6.5.2 承诺兑现链

**前面故意用"不够好的写法"，学了新工具后回来重写同一段代码。** 这让读者亲身体验"学新东西是为了解决旧痛点"，而不是被灌一堆孤立语法。

已建立的链条：

| 埋在哪 | 用的凑合写法 | 在哪兑现 |
|---|---|---|
| 6.7 | `input` 只能假设用户老实 | 8-27（`while` + `isdigit`）→ 14-35（包成函数）→ **15-21（`try` + `float`，三行且支持所有合法数字）** |
| 9-34 | 用 `count` 数词频（每次扫全表） | 11-23（字典计数，附效率对比） |
| 7.3 | 一长串 `elif` | 11.13（字典查找表）→ **14.16（字典存函数 = 命令分发器）** |
| 16-30 | 自己拼格式存数据 | 17-12（三行 JSON） |
| 16-28 | `a` 模式手工追加运行记录 | 21-22（logging，多了级别/轮转） |
| 17.9 | `datetime` 存不进 JSON | 23-25（存 ISO 字符串） |
| 1.6 | 装了库却 import 失败 | 19.13（虚拟环境是彻底解法） |

**写法要求**：埋的时候要明确写一句"这样还不够好，第 N 章会解决"；兑现的时候要把两段代码并排对比。

---

## 6.6 每章必须有的「给有其他语言经验者」折叠块

**每一章正文末尾（易错点之前）都要有一个折叠块**，用 `<details>` 包起来，不打断主线阅读：

```markdown
<details>
<summary><b>给有 C# / Java / SQL 经验的读者</b></summary>

| 你熟悉的 | Python |
|---|---|
| ... | ... |

（然后是 2~3 条「需要特别注意的」）

</details>
```

**内容要求**：

1. **对照表列本章相关的差异**，不要泛泛而谈
2. **重点写"看着一样其实不同"的地方**——这比列语法对照有用得多。已写过的例子：
   - C# 的 `Substring(start, length)` 第二个参数是**长度**，Python 切片 `s[start:end]` 是**结束位置**
   - SQL 的 `SUBSTRING` 下标从 **1** 开始，Python 从 **0** 开始
   - `-7 % 2` 在 C#/Java 得 `-1`，Python 得 `1`
   - `Math.Round(2.5)` 得 3，`round(2.5)` 得 **2**
   - C# 的 `x is int` 是类型判断，Python 的 `is` 是**身份比较**
3. **不要回避劣势**——该说"Python 少了编译器的保护，要靠测试和类型注解补"就直说

---

## 6.7 实测流程（硬性要求）

规范 7 节要求"所有输出必须与真实运行结果一致"。**具体怎么做**：

| 步骤 | 做法 |
|---|---|
| **① 用哪个版本实测** | **Python 3.12**（与教程推荐版本一致）。报错文本随版本变化很大，3.9 和 3.12 差异明显——凡涉及版本差异的地方，要在正文里标注 |
| **② 写作前先跑一遍** | 把本章要用的输出**先在终端跑出来**，再往正文里填。不要凭记忆写，尤其是浮点数、报错文本、对齐输出 |
| **③ 写完自动核对** | `python3.12 tools/check_outputs.py`——它会抽出所有 ```python 代码块，逐行核对 `# =>` 标注，并核对「代码块紧跟 ```text 输出块」的多行输出 |
| **④ 处理"易变输出"** | `id()`、时间、随机数、`is` 比较的结果会被脚本自动跳过（它们依赖运行环境或执行方式），这类输出要在正文里注明"你运行时结果会不同" |

**已经因此发现并修正的真实问题**（说明这个流程是必要的）：

- `19.9 * 3` 实际输出 `59.699999999999996`（第 1 章会撞上浮点误差，已换成 `19.9 * 2`）
- 价格表的空格数手工估算错了（多行输出块核对功能就是为此加的）
- `257 is 257` 在**整块执行时是 `True`、逐行执行时是 `False``——这个发现反而强化了第 4 章的论点
- `newline=""` 的 CSV 坑在 Linux 上**复现不出来**（只在 Windows 出现），必须标注平台差异
- 第 2 章图纸原有的"括号未闭合时报错行号会骗你"在 3.10+ **已不成立**

---

## 7. 每章完成前的自检清单

写完一章，逐条打勾才算完成：

- [ ] 章内示例编号连续，无跳号
- [ ] 示例有 A 段起步、有至少一个 C 段收尾（**比例不作要求**）
- [ ] 没有任何示例引用了后面章节才讲的语法
- [ ] 每个示例独立可运行，已实际运行过一遍
- [ ] 所有输出标注与真实运行结果一致（**必须实跑，不能凭记忆写**）
- [ ] 每个 ❌ 都紧跟一个 ✅
- [ ] "易错点"表格覆盖了正文所有 ❌
- [ ] "本章小结"含 API 速查表
- [ ] 术语用词符合第 3 节对照表
- [ ] **本章涉及的概念点全部讲到，没有为了控制篇幅而省略**
- [ ] **每个知识点都答齐了四问**：是什么 / 为什么 / 什么时候用 / 容易错在哪（见 2.1）
- [ ] **★★★ 概念额外讲了边界情况与常见误解**
- [ ] **每个示例的"重要行"都有注释**（判定见 3.3）
- [ ] 每个概念收口处有一句可复述的"记住这条"
- [ ] **跑过 `python3.12 tools/check_outputs.py` 且全部通过**（见 6.7）
- [ ] **有「给有其他语言经验者」折叠块**（见 6.6）
- [ ] **检查过伏笔回收与承诺兑现链**（见 6.5），该回指的地方都写了"回收 X.Y 节"
- [ ] 交叉引用的章节号真实存在
- [ ] `01_目录.md` 的写作进度已更新

## 8. 附：需要向读者交代的示例约定

以下几条要在第 1 章或前言里明确告诉读者：

- **示例编号**：`章号-序号`，例如 `5-3` 是第 5 章第 3 个示例。同一章内示例**由简到复杂**递进。
- **每个示例都是可直接运行的完整代码**，不用补上下文，不用装第三方库（需要装的会显式说明）。
- **注释只标在关键处**，不给显而易见的代码加注释。
- **输出用 `# =>` 标注**，方便对照：

```python
name = "小明"
age = 18
# f-string：花括号里直接写变量名，Python 3.6+ 支持
print(f"{name} 今年 {age} 岁")   # => 小明 今年 18 岁
```

- **错误示范会明确标记**，不会让人误抄：

```python
# ❌ 错误写法：默认参数用了可变对象，会在多次调用间共享
def add_item(item, box=[]):
    box.append(item)
    return box

# ✅ 正确写法
def add_item(item, box=None):
    if box is None:
        box = []
    box.append(item)
    return box
```

- **版本要求**：默认 Python **3.10 及以上**。用到更高版本特性时会标注，例如`（3.12+）`。

---

---

# 第二部分　每章图纸

## 本文件的读法

每章包含五块：

| 块 | 作用 |
|---|---|
| **章头信息表** | 目标、前置、用时、**可用语法边界** |
| **知识点清单** | 必须讲到的点，逐条对应正文小节；带 ★ 优先级 |
| **代码示例清单** | 要写哪些示例、什么顺序、什么段位、核心写法 |
| **样板示例** | 挑 1~2 个写出完整代码，示范讲解密度与注释密度 |
| **易错点** | ❌ 症状 ✅ 原因，正文"易错点"小节直接用 |

**「可用语法边界」是本文件最重要的约束。** 它规定该章示例只能用哪些语法。新手教程最常见的事故是第 5 章的示例里出现了第 14 章才讲的函数，读者当场卡住。凡边界之外确实必须提前用到的，记为**预支**，正文里必须写一句"这里先照抄，第 N 章会详细讲"。

**段位标记**（定义见规范 3.2）：
- **A** 最简形式，1~5 行，只演示一个点
- **B** 组合使用，5~15 行，与已学内容组合
- **C** 接近真实，15~30 行，每章最多 2 个

---

# 阶段一　基础入门（第 1~15 章）

**阶段目标**：能写出解决具体问题的单文件脚本。
**阶段验收**：项目 A（猜数字 / 计算器 / 成绩管理，至少完成两个）。
**阶段内的语法解锁顺序**：`print` → 变量 → 运算符 → 字符串 → `input` → `if` → 循环 → 四大容器 → 函数 → 异常。**任何一章的示例都不得越过这个顺序。**

---

## 第 1 章　认识 Python 与环境搭建 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 装好 Python 并验证成功；能用 REPL、脚本、Jupyter 三种方式运行代码；能用 pip 装一个库 |
| 前置 | 无 |
| 预计用时 | 2~3 小时（大部分花在装软件） |
| 可用语法边界 | `print`、字符串与数字字面量、算术运算。**预支**：变量赋值（1.5 节顺带出现，注明"第 3 章详解"） |
| 特别说明 | 本章一半内容是终端操作而非 Python 代码，示例清单中用「终端」标注 |
| 段位分布 | A 段 8 个、B 段 2 个、C 段 1 个。第 1 章还没有语法可供"组合"，A 段为主是正常的（规范 3.2 已说明比例不作要求） |
| **打样记录** | 正文已完成（`chapters/ch01_认识Python与环境搭建.md`，647 行 / 约 5000 中文字）。所有输出均实测。打样中发现并修正：① 示例 1-6 的 `19.9 * 3` 有浮点误差 ② 示例顺序按叙述逻辑重排（版本验证要在 REPL 之前）③ 网络示例的 URL 需换成 python.org |

### 知识点清单

| # | 知识点 | 必须讲到 | 优先级 |
|---|---|---|---|
| 1.1 | Python 是什么 | 解释型：写完直接运行，不用编译成 exe；解释器就是 `python` 这个程序；`.py` 就是纯文本文件，用记事本也能打开 | ★★ |
| 1.2 | 为什么只学 Python 3 | Python 2 已于 2020 年停止维护；**识别信号**：网上代码里 `print "hi"` 没括号的就是 Python 2，不要照抄 | ★★ |
| 1.3 | 能做什么、不适合做什么 | 擅长：自动化脚本、数据处理、Web 后端、AI；不擅长：手机 App、高性能游戏、对启动速度敏感的场景。**给一句实话**：Python 慢，但绝大多数场景瓶颈不在语言 | ★ |
| 1.4 | 安装与 PATH | 官网下载 3.12/3.13；**Windows 必须勾选 Add python.exe to PATH**（用加粗+截图说明位置）；macOS 别用系统自带的旧版本；验证 `python --version` | ★★★ |
| 1.5 | 三种运行方式 | REPL：`>>>` 提示符、表达式自动回显、`exit()` 退出、适合验证一行；脚本：`python hello.py`、**必须先 cd 到文件所在目录**；Jupyter：Cell 分段执行、变量跨 Cell 存活、适合学习和做实验 | ★★★ |
| 1.6 | 编辑器 | VS Code：装 Python 扩展 + 右下角选解释器（**新手第一个坑**：装了库但解释器选错，import 失败）；PyCharm 社区版；**不要用记事本**（不显示缩进、易存成 .txt） | ★★ |
| 1.7 | pip 装第三方库 | `pip install requests`；`pip list` 看装了什么；`pip show requests` 看装在哪；国内镜像一行命令；一句话说清"标准库自带、第三方库要装" | ★★ |
| 1.8 | 本章之后的学习方式 | 三条硬规矩（自己敲、故意写错一次、验收项目必须做）；卡住时的四步排查 | ★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 1-1 | 确认自己装的是哪个版本 | A · 终端 | `python --version`；Windows 上可能要用 `py --version`；配一张"报错对号入座"表 | `python --version` |
| 1-2 | 把 REPL 当计算器用 | A · 终端 | 启动 `python`、`>>>` 提示符、表达式自动回显、`exit()` 退出 | `>>> 2 + 3` |
| 1-3 | 第一个脚本 hello.py | A | 存文件 → `cd` 到目录 → `python hello.py`；顺带讲 `.py.txt` 和忘记 cd 两个坑 | `print("Hello, Python!")` |
| 1-4 | **REPL 和脚本的关键区别**（样板） | A | REPL 里 `2 + 3` 显示 5；脚本里同样一行**什么都不输出**，必须 `print` | `print(2 + 3)` |
| 1-5 | 在 Jupyter 里分段执行 | B | 装 notebook、Shift+Enter、**上一个 Cell 的变量在下一个 Cell 还活着**；说明它适合学习不适合交付 | `jupyter notebook` |
| 1-6 | 让 print 算一笔账 | A | `print` 里可以放算术表达式。**必须用 `19.9 * 2`（得 39.8），不要用 `19.9 * 3`**——后者实际输出 `59.699999999999996`，浮点误差要到 3.4 才讲，第 1 章撞上它会分散注意力 | `print(19.9 * 2)` |
| 1-7 | 连续多个 print 输出一张名片 | A | 一行一句按顺序执行；空 `print()` 打空行 | `print()` |
| 1-8 | **打印一张购物小票**（样板） | C | 走完"编辑→保存→运行"全流程；15 行左右。**数字必须选无浮点误差的**（`2.5*3=7.5`、`6.0*2=12.0`、`12.8*1=12.8`、合计 `32.3`，均已实测）；末尾埋两个伏笔：`"=" * 28`（第 5 章）和 `12.0` 为何带 `.0`（第 3 章） | 多行 `print` + 算术 |
| 1-9 | 故意打错字看报错 | A | `pirnt("hi")` → NameError 三行；**3.12+ 会多一行 `Did you mean: 'print'?`，老版本没有，要注明** | `pirnt("hi")` |
| 1-10 | 三行代码访问一个网站 | B · 需联网 | `pip install requests` 后三行拿到状态码，体感"装库=扩展能力"。**用 `https://www.python.org`（实测返回 200），不要用 httpbin.org（实测 503）** | `requests.get(url).status_code` |
| 1-11 | pip 的三个常用命令 | A · 终端 | `pip list` / `pip show` / `pip uninstall`；**重点讲 `pip show` 输出里的 `Location`**，它是诊断"装了库却 import 失败"的关键 | `pip show requests` |

### 样板示例

**示例 1-4　REPL 和脚本的关键区别**

在 REPL 里输入 `2 + 3`，回车立刻看到结果：

```text
>>> 2 + 3
5
```

把同样一行写进 `test.py` 文件再运行，**屏幕上什么都没有**：

```python
2 + 3
```

```text
$ python test.py
$
```

原因：REPL 会自动把每个表达式的结果显示出来（这叫"回显"，方便你做实验）；而脚本不会，它只做你明确要求的事。想看到结果，必须用 `print` 说出来：

```python
print(2 + 3)   # => 5
```

> **记住这条**：以后你写脚本发现"程序跑完了却没输出"，第一个要检查的就是——忘了 `print`。

**示例 1-8　打印一张购物小票**

新建文件 `receipt.py`，输入以下内容后运行 `python receipt.py`：

```python
# 这一章还没讲变量和格式化，所以先用最笨的方式：把要显示的内容直接写进 print
# 第 3 章学了变量、第 5 章学了 f-string 之后，这段代码会被大幅简化

print("=" * 28)          # "=" * 28 表示把等号重复 28 次，用来画分隔线
print("        小明便利店")
print("=" * 28)
print("矿泉水   2.5 x 3 =", 2.5 * 3)     # print 里逗号分隔的多项会用空格连起来
print("面包     6.0 x 2 =", 6.0 * 2)
print("鸡蛋    12.8 x 1 =", 12.8 * 1)
print("-" * 28)
print("合计：", 2.5 * 3 + 6.0 * 2 + 12.8 * 1)
print("=" * 28)
print("谢谢光临，欢迎再来")
```

```text
============================
        小明便利店
============================
矿泉水   2.5 x 3 = 7.5
面包     6.0 x 2 = 12.0
鸡蛋    12.8 x 1 = 12.8
----------------------------
合计： 32.3
============================
谢谢光临，欢迎再来
```

跑通这个，说明你的环境完全正常，可以进第 2 章了。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 装完 Windows 上找不到 python | `'python' 不是内部或外部命令` | 重装勾选 Add to PATH，或改用 `py` 命令 | PATH 没配 |
| 用记事本存成 `hello.py.txt` | `can't open file 'hello.py'` | 打开"显示文件扩展名"，另存时选"所有文件" | 记事本默认追加 .txt |
| 在其他目录运行脚本 | `No such file or directory` | 先 `cd` 到脚本所在目录 | 用的是相对路径 |
| 代码里用了中文全角引号 `“”` | `SyntaxError: invalid character` | 切换到英文半角输入法 | 中文输入法自动转全角 |
| VS Code 里 import 装好的库失败 | `ModuleNotFoundError` | 右下角/命令面板重新选解释器，选到装库的那个环境 | 编辑器用的解释器和 pip 装库的解释器不是同一个 |
| REPL 里粘贴多行代码 | `IndentationError` / 只执行了一部分 | 多行代码写进 `.py` 文件或 Jupyter | REPL 对空行和缩进敏感 |

### 下一章预告

环境通了，但你刚才已经见过两次报错了。第 2 章先教两件事：Python 的语法规矩（尤其缩进），以及**怎么读报错信息**——这是整本书里最先用得上、也最省时间的技能。

---

## 第 2 章　语法规则与读懂报错 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 掌握缩进、注释、命名三条基本规矩；能读懂 Traceback 并定位到出错行；认识十个最常见报错 |
| 前置 | 第 1 章 |
| 预计用时 | 2 小时 |
| 可用语法边界 | `print`、字面量、算术、变量赋值。**预支**：`if` 与缩进块（2.2 节讲缩进必须有块，只用最简 `if True:` 形式，注明"第 7 章详解"）；`import keyword`（2.5 节查关键字表，注明"第 18 章详解"） |
| 编写要点 | 本章是全书"抗挫折训练"，**所有报错都要真实运行并贴出真实输出**，不能凭记忆编造报错文字 |

### 知识点清单

| # | 知识点 | 必须讲到 | 优先级 |
|---|---|---|---|
| 2.1 | Hello World 逐行拆解 | `print` 是函数、括号表示调用、引号表示文本、**大小写敏感**（`Print` 不认） | ★★★ |
| 2.2 | 缩进 | 用缩进代替花括号；统一 4 个空格；同一块必须对齐；**Tab 和空格不能混**；编辑器设置"Tab 转空格" | ★★★ |
| 2.3 | 注释 | `#` 单行；行尾注释前留两空格；三引号字符串当注释块用（说明它其实不是注释）；**注释是写给三个月后的自己看的** | ★★ |
| 2.4 | 语句与换行 | 一行一句、不写分号；行太长用 `\` 续行；**括号内可以自然换行**（更推荐） | ★★ |
| 2.5 | 命名规范 | 字母数字下划线、不能数字开头、区分大小写、不能用关键字；`snake_case` 惯例；**取名要能读懂**（`a1` vs `student_count`） | ★★★ |
| 2.6 | 读 Traceback | **从下往上读**：最后一行是异常类型和原因，往上找 `File "...", line N` 定位；多层调用时**只看属于自己文件的那几行** | ★★★ |
| 2.7 | 十个必认报错 | `SyntaxError`、`IndentationError`、`NameError`、`TypeError`、`ValueError`、`IndexError`、`KeyError`、`AttributeError`、`ZeroDivisionError`、`ModuleNotFoundError`——各给一句"通常意味着什么" | ★★★ |
| 2.8 | 排错的基本套路 | **语法错误优先于运行时错误**（文件解析不过，一行都不执行）；一次只改一处、改完立刻重跑；最小复现法；报错行号在老版本可能不准（3.9 的括号未闭合指向下一行，3.10+ 已修好） | ★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 2-1 | Hello World 逐行拆解 | A | 函数名、括号、引号各自的作用 | `print("Hello")` |
| 2-2 | 大小写敏感三连 | A | `Print` ❌ / `print` ✅；`TRUE` ❌ / `True` ✅ | `Print("hi")` |
| 2-3 | 缩进对了和错了 | A | 同一块内多缩进一格就报错 | `IndentationError` |
| 2-4 | Tab 和空格混用 | A | 肉眼一样、Python 不认 | `TabError` |
| 2-5 | 三种写注释的方式 | A | `#`、行尾注释、三引号块 | `# 说明` |
| 2-6 | 长行怎么换行 | A | `\` 续行 vs 括号内换行 | `(...\n...)` |
| 2-7 | 合法与非法的变量名 | A | `2name` ❌、`user-age` ❌、`class` ❌、`user_age` ✅ | `user_age = 18` |
| 2-8 | 查一查哪些词不能用 | A | `import keyword; print(keyword.kwlist)` | `keyword.kwlist` |
| 2-9 | 读懂三行的 Traceback | B | 拼错变量名 → NameError，指着哪一行、说了什么 | 从下往上读 |
| 2-10 | 读懂多层的 Traceback | B | `int("abc")` → ValueError，栈里有不属于你的行，怎么筛 | `int("abc")` |
| 2-11 | 十个报错速认 | B | 十个 1~2 行片段，每个只触发一种报错，配一句"通常意味着" | 见知识点 2.7 |
| 2-12 | 括号没闭合：新旧版本的报错差别 | B | 实测：**3.9 报第 2 行 `invalid syntax`（误导人），3.10+ 报第 1 行 `'(' was never closed`（准确）**。原图纸假设「行号总是骗你」已不成立，改为借此说明升级 Python 的实际好处 | `'(' was never closed` |
| 2-13 | 修好一段有三处错误的代码 | C | 按 Traceback 一次改一处、每次重跑，体验真实排错节奏 | 迭代排错 |

### 样板示例

**示例 2-9　读懂三行的 Traceback**

把下面代码存成 `bug.py`（注意第 3 行故意把 `name` 拼成了 `naem`）：

```python
name = "小明"
print("准备打招呼")
print(naem)
```

运行后：

```text
准备打招呼
Traceback (most recent call last):
  File "bug.py", line 3, in <module>
    print(naem)
          ^^^^
NameError: name 'naem' is not defined. Did you mean: 'name'?
```

**从下往上读**：

1. **最后一行最重要**：`NameError: name 'naem' is not defined` —— 有个叫 `naem` 的名字没定义过。Python 3.12 起还会贴心地问 `Did you mean: 'name'?`
2. **往上一行看位置**：`File "bug.py", line 3` —— 出错在 bug.py 第 3 行，下面还把出错的代码和位置箭头都打出来了。
3. **第一行 `Traceback (most recent call last)`** 是固定套话，意思是"下面按调用顺序列出来，最后一次调用在最下面"。**所以真正的错误信息永远在最下面。**

另外注意：`准备打招呼` 是打印出来了的。这说明**程序是执行到第 3 行才崩的**，前两行正常跑完了——报错的位置就是程序停下的位置。

> **新手最常犯的错**：一看到红字就整段删掉重写。其实报错已经把行号和原因都告诉你了，读它比重写快得多。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `Print("hi")` | `NameError: name 'Print' is not defined` | 全小写 `print` | Python 区分大小写 |
| 块内缩进不一致 | `IndentationError: unexpected indent` | 同一块统一 4 空格 | 缩进决定代码块归属 |
| Tab 和空格混用 | `TabError: inconsistent use of tabs` | 编辑器开"Tab 转空格" | 两者字符不同 |
| `class = 1` | `SyntaxError: invalid syntax` | 换名 `class_name` / `category` | `class` 是关键字 |
| `user-age = 18` | `SyntaxError` | 用下划线 `user_age` | 减号是运算符 |
| 括号忘闭合 | 报错行号指向**下一行** | 往上一行找没闭合的括号 | 解析器一直等着括号闭合 |
| 只看报错第一行 | 抓不到真正原因 | **从最后一行往上读** | 关键信息在末尾 |

### 下一章预告

规矩清楚了，现在可以正式开始装数据了。第 3 章讲变量和四种基本数据类型——以及一个会让很多人第一次怀疑计算机的现象：`0.1 + 0.2` 居然不等于 `0.3`。

---

## 第 3 章　变量与基本数据类型 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会用变量存取数据；分清 int / float / bool / None；会做类型转换并知道什么时候转换会失败 |
| 前置 | 第 2 章 |
| 预计用时 | 2~3 小时 |
| 可用语法边界 | `print`、字面量、算术、变量赋值、`type`、`isinstance`、`id`、`round`。**不得使用**：f-string（第 5 章）、`if`（第 7 章）、任何容器（第 9 章起） |
| 编写要点 | 3.1 节的"标签不是盒子"比喻要贯穿全书，后面第 13 章拷贝语义、第 14 章参数传递都会回来引用它 |

### 知识点清单

| # | 知识点 | 必须讲到 | 优先级 |
|---|---|---|---|
| 3.1 | 变量是标签，不是盒子 | `=` 是"贴标签"不是"相等"；一个对象可以贴多个标签；用 `id()` 验证；**这个模型决定了后面一堆坑能不能想通** | ★★★ |
| 3.2 | 动态类型 | 同一个变量先存数字后存字符串完全合法；好处（灵活）与代价（错误留到运行时才暴露，→ 第 33 章类型注解） | ★★ |
| 3.3 | 整数 int | 任意精度（不会溢出，可以算 2**1000）；下划线分隔 `1_000_000`；二/八/十六进制字面量 | ★★ |
| 3.4 | 浮点数 float | `0.1 + 0.2` 的现象与原因：**用"十进制写不出 1/3"类比"二进制写不出 0.1"讲清，不做数学推导**；`round(x, n)` 用法；`round(2.5)` 得 2 而 `round(3.5)` 得 4 的银行家舍入现象**及其原因**；科学计数法；**为什么金额不能用 float**（累加误差示例）→ 正确做法见工具箱 T2 `decimal` | ★★★ |
| 3.5 | 布尔值 bool | `True` / `False` **首字母大写**；`bool` 其实是 int 的子类（`True + 1 == 2`），作为趣味知识提一句 | ★★★ |
| 3.6 | None | 表示"没有值"；不等于 `0`、不等于 `""`、不等于 `False`（虽然它们都为假）；典型用途：还没算出来的结果、函数没有返回值（→ 第 14 章） | ★★★ |
| 3.7 | 显式类型转换 | `int()` / `float()` / `str()` / `bool()`；`int("3.5")` 会失败而 `int(float("3.5"))` 可以；`int(3.9)` 是截断不是四舍五入 | ★★★ |
| 3.8 | 隐式类型转换 | `1 + 2.0` 自动变 float；`"1" + 1` **不会**自动转（→ TypeError）；Python 在这点上比 JS 严格，是好事 | ★★ |
| 3.9 | 查看类型 | `type(x)` 看是什么、`isinstance(x, int)` 判断是不是；日常用 `isinstance` | ★★ |
| 3.10 | 多变量赋值与删除 | `a, b = 1, 2`；`a = b = 0`（**留一句提醒**：对可变对象这样写有坑，第 13 章讲）；`del a` | ★★ |
| 3.11 | 复数 complex | 给出字面量写法 `3+4j` 与 `abs()` 求模，说明用途（信号处理、电路计算），明确写"日常开发用不到，用到再查"——**按 2.3 允许写短，但四问中的"是什么/什么时候用"仍要交代** | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 3-1 | 存一个值，再用它 | A | 赋值 → 使用 → 重新赋值 | `age = 18` |
| 3-2 | 同一个变量换成另一种类型 | A | 动态类型：先数字后字符串，合法 | `x = 1` → `x = "hi"` |
| 3-3 | 用 id() 看清"标签"这件事 | A | `b = a` 后两者 `id` 相同 → 同一个对象 | `id(a) == id(b)` |
| 3-4 | 大得离谱的整数 | A | `2 ** 100` 不溢出；`1_000_000` 下划线 | `2 ** 100` |
| 3-5 | 二进制、十六进制怎么写 | A | `0b1010`、`0o17`、`0xff` 与 `bin()`/`hex()` | `0xff` |
| 3-6 | 0.1 + 0.2 不等于 0.3 | A | 打印出 `0.30000000000000004`，先给现象 | `print(0.1 + 0.2)` |
| 3-7 | round 保留两位 | A | `round(3.14159, 2)`；`round(2.5)` 得 2 的怪现象 | `round(x, 2)` |
| 3-8 | True 和 true | A | `true` ❌ NameError，必须大写开头 | `True` |
| 3-9 | None 长什么样 | A | `print(None)`、`type(None)` | `None` |
| 3-10 | type 和 isinstance | B | 两者输出对照，说明日常用哪个 | `isinstance(x, int)` |
| 3-11 | 四种转换一次看完 | B | `int` / `float` / `str` / `bool` 各转几个值 | `int("42")` |
| 3-12 | 转换失败的两种情况 | B | `int("abc")` ❌、`int("3.5")` ❌ → `int(float("3.5"))` ✅ | `int(float(s))` |
| 3-13 | int() 是截断，不是四舍五入 | B | `int(3.9)` → 3、`int(-3.9)` → -3；要四舍五入用 `round` | `int(3.9)` |
| 3-14 | 什么会被当成"假" | B | `bool(0)`、`bool("")`、`bool(None)` 全是 False；`bool("0")` 是 True（**坑**） | `bool("0")` |
| 3-15 | 数字和文字能不能相加 | B | `1 + 2.0` ✅ 自动转；`"1" + 1` ❌ TypeError → 显式转 | `str(1) + "1"` |
| 3-16 | 一行赋好几个变量 | B | `a, b = 1, 2`；`a = b = 0` | `a, b = 1, 2` |
| 3-17 | 摄氏度转华氏度 | C | 变量命名 + 算术 + `round` + 类型转换走一遍 | `c * 9 / 5 + 32` |
| 3-18 | 算圆的周长和面积 | C | 常量命名（`PI = 3.14159`，说明 Python 没有真常量，靠大写约定） | `PI * r ** 2` |

### 样板示例

**示例 3-3　用 id() 看清"标签"这件事**

很多教材说"变量是一个装数据的盒子"。这个比喻在 Python 里是**错的**，会害你后面栽跟头。Python 的变量是**贴在对象上的标签**。

```python
a = [1, 2, 3]        # 这里先借用一下列表，第 9 章会详细讲
b = a                # 不是"复制一份给 b"，而是"给同一个对象再贴一张标签"

print(id(a))         # => 140712834567296  （每次运行数字都不同，看的是"是否相同"）
print(id(b))         # => 140712834567296  与 a 完全一样
print(a is b)        # => True             is 判断的是"是不是同一个对象"
```

`id()` 返回对象在内存中的身份编号。两个变量 `id` 相同，说明它们贴在**同一个对象**上，不是两份数据。

对数字和字符串这类**不可变**对象，你感觉不到区别：

```python
x = 10
y = x
y = 20          # 这不是"改了那个 10"，而是把 y 撕下来贴到 20 上
print(x)        # => 10   x 不受影响
```

但对**可变**对象（列表、字典），区别是致命的：

```python
a = [1, 2, 3]
b = a
b.append(4)     # 通过 b 修改了对象本身
print(a)        # => [1, 2, 3, 4]   a 也变了！因为它们本来就是同一个对象
```

> **现在只要记住这句话**：`=` 是贴标签，不是复制。第 13.4 节会专门讲怎么真正复制一份，以及不知道这件事会写出什么样的 bug。

**示例 3-12　转换失败的两种情况**

```python
print(int("42"))            # => 42     纯数字的字符串可以转

int("abc")                  # ❌ ValueError: invalid literal for int() with base 10: 'abc'
int("3.5")                  # ❌ ValueError: invalid literal for int() with base 10: '3.5'
```

第二个尤其反直觉：`"3.5"` 明明是个数字，为什么 `int()` 不认？因为 `int()` 只接受**整数形式**的字符串，小数点它不管。要转就分两步：

```python
print(int(float("3.5")))    # => 3      先转成浮点数 3.5，再截断成 3
print(round(float("3.5")))  # => 4      想四舍五入用 round（注意 round(2.5) 得 2，见 3.4）
```

> 这个坑在处理用户输入和读文件时天天遇到（比如 Excel 里导出的 `"5.0"`）。第 15 章学了异常处理后，正确姿势是用 `try` 把转换包起来。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `true` / `false` | `NameError` | `True` / `False` | 首字母必须大写 |
| `int("3.5")` | `ValueError` | `int(float("3.5"))` | `int()` 不认小数点 |
| `int(3.9)` 期望得 4 | 得到 3 | 用 `round(3.9)` | `int()` 是截断 |
| `"1" + 1` | `TypeError: can only concatenate str` | `int("1") + 1` 或 `"1" + str(1)` | Python 不做字符串与数字的隐式转换 |
| `0.1 + 0.2 == 0.3` | 结果是 `False` | 比较用 `round()` 或 `math.isclose()`（第 4.14 示例） | 二进制无法精确表示 0.1 |
| 用 float 算金额 | 累加后出现 `0.9999999` | 用 `decimal`（工具箱 T2） | 同上 |
| 把 `None` 写成 `"None"` | 判断永远不成立 | 直接用 `None`，判断用 `is None` | 一个是空值，一个是三字符文本 |
| 变量没赋值就使用 | `NameError` | 先赋初值 | Python 不自动初始化 |

### 下一章预告

有了数据就要开始算。第 4 章讲运算符——重点是三个反直觉的地方：`/` 和 `//` 的区别、`-7 // 2` 为什么是 `-4`、以及 `is` 和 `==` 到底差在哪。


---

## 第 4 章　运算符与表达式 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 熟练使用算术、比较、逻辑三类运算符；分清 `/` 与 `//`、`is` 与 `==`；知道浮点数不能直接比较 |
| 前置 | 第 3 章 |
| 预计用时 | 2~3 小时 |
| 可用语法边界 | 第 3 章全部 + 所有运算符 + `abs`、`divmod`、`math.isclose`。**不得使用**：`if`（第 7 章）、循环（第 8 章）、容器（第 9 章起）。**预支**：`import math`（4.14 示例，注明"第 18 章详解"） |
| 编写要点 | 本章能演示"逻辑判断"却还没学 `if`，示例要善用**直接打印布尔表达式的结果**（如 `print(year % 4 == 0)`），这本身是很好的思维训练 |

### 知识点清单

| # | 知识点 | 必须讲到 | 优先级 |
|---|---|---|---|
| 4.1 | 算术运算符 | `+ - * /`；**`/` 永远返回 float**（`4 / 2` 得 `2.0` 不是 `2`）；`//` 整除、`%` 取模、`**` 幂 | ★★★ |
| 4.2 | 整除与取模的负数行为 | `-7 // 2 == -4`（**向下取整，不是截断**）、`-7 % 2 == 1`（结果符号跟除数）；`divmod` 一次拿两个结果 | ★★ |
| 4.3 | 幂运算 | `2 ** 10`；`x ** 0.5` 开平方；`-2 ** 2 == -4`（**优先级坑**：先幂后取负） | ★★ |
| 4.4 | 复合赋值 | `+= -= *= /= //= %= **=`；说明 `x += 1` 就是 `x = x + 1` 的简写（**对列表有微妙差别，第 13 章再说**） | ★★★ |
| 4.5 | 比较运算符 | `== != > < >= <=` 返回 bool；字符串也能比大小（按字符编码顺序，`"apple" < "banana"`） | ★★★ |
| 4.6 | 链式比较 | `0 <= score <= 100` 是 Python 特有的写法，比 `x >= 0 and x <= 100` 更好读 | ★★★ |
| 4.7 | 逻辑运算符 | `and` / `or` / `not`；**优先级 not > and > or**；括号能省但不建议省 | ★★★ |
| 4.8 | 短路求值 | `and` 遇假即停、`or` 遇真即停；**实用价值**：`name or "游客"` 取默认值 | ★★ |
| 4.9 | and / or 返回操作数 | `1 and 2` 得 `2` 不是 `True`；这不是 bug 而是设计，是上面"取默认值"写法的原理 | ★★ |
| 4.10 | 成员运算 in | `"a" in "abc"`；对容器的用法第 9 章起大量出现 | ★★★ |
| 4.11 | 身份运算 is | `is` 比"是不是同一个对象"，`==` 比"值是否相等"；**只在与 `None` 比较时用 `is`**；`a is b` 对小整数和短字符串的诡异表现：**现象和原因都要讲清**——Python 预先复用了这些对象，所以 `is` 会碰巧为真；不展开 CPython 实现细节（→ 专项 B）。**必须让读者得出结论**：这种"碰巧"正是不能用 `is` 比值的理由 | ★★★ |
| 4.12 | 浮点数比较 | 不能 `==`；正确做法 `abs(a - b) < 1e-9` 或 `math.isclose(a, b)` | ★★★ |
| 4.13 | 优先级 | 给一张精简优先级表（只列常用的十来个）；结论：**不确定就加括号，加括号从不是错** | ★★ |
| 4.14 | 位运算 | `& | ^ ~ << >>` 各一行示例并说明各自作用；给**一个真实用途**（权限位掩码 / 状态标志位），让读者知道它不是摆设；说明业务代码少见，但读别人代码会遇到 | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 4-1 | 算术运算符全体亮相 | A | 七个运算符各来一行 | `7 + 2` … `7 ** 2` |
| 4-2 | `/`、`//`、`%` 有什么区别 | A | `7/2=3.5`、`7//2=3`、`7%2=1`；`4/2` 得 `2.0` | `7 // 2` |
| 4-3 | 负数的整除和取模 | A | `-7//2 = -4`、`-7%2 = 1`，**先给现象再给一句解释** | `-7 // 2` |
| 4-4 | 开平方不用 math | A | `9 ** 0.5` | `x ** 0.5` |
| 4-5 | `-2 ** 2` 到底等于几 | A | 得 `-4`；要 4 得写 `(-2) ** 2` | `(-2) ** 2` |
| 4-6 | 复合赋值省点字 | A | `x += 1`、`x *= 2` | `x += 1` |
| 4-7 | 比较的结果是 True/False | A | 直接 print 比较表达式 | `print(3 > 2)` |
| 4-8 | 字符串也能比大小 | A | `"apple" < "banana"`；`"A" < "a"` | `"A" < "a"` |
| 4-9 | 链式比较：一句话判断区间 | A | `0 <= 85 <= 100` | `0 <= x <= 100` |
| 4-10 | and / or / not | A | 三个各两行 | `True and False` |
| 4-11 | 用 or 取默认值 | B | `name = "" ` → `name or "游客"` 得 `"游客"` | `name or "游客"` |
| 4-12 | and 和 or 返回的不是 True/False | B | `print(1 and 2)` → 2；`print(0 or "x")` → "x" | `1 and 2` |
| 4-13 | in 判断包含 | B | `"@" in email` | `"@" in s` |
| 4-14 | is 和 == 的区别 | B | 两个内容相同的长字符串：`==` 为 True、`is` 为 False；`x is None` 的正确用法 | `x is None` |
| 4-15 | 浮点数千万别用 == 比 | B | `0.1 + 0.2 == 0.3` ❌ → `math.isclose` ✅ | `math.isclose(a, b)` |
| 4-16 | 优先级三个常见误会 | B | `2 + 3 * 4`、`not 3 == 3`、`1 or 0 and 0` | 加括号 |
| 4-17 | 判断闰年（不用 if） | C | 纯布尔表达式算出 True/False，训练"把规则翻译成表达式"的能力 | `(y%4==0 and y%100!=0) or y%400==0` |
| 4-18 | 把秒数换算成 时:分:秒 | C | `//` 和 `%` 的经典配合；`divmod` 版本对照 | `divmod(total, 60)` |
| 4-19 | 位运算见面即可 | A | 五个位运算各一行 | `5 & 3` |

### 样板示例

**示例 4-3　负数的整除和取模**

```python
print(7 // 2)      # => 3     符合直觉
print(-7 // 2)     # => -4    注意！不是 -3
print(7 % 2)       # => 1
print(-7 % 2)      # => 1     注意！不是 -1
```

`//` 叫**地板除**（floor division），意思是"往小的方向取整"，不是"砍掉小数"。`-3.5` 往小的方向走是 `-4`，所以 `-7 // 2` 得 `-4`。

取模的结果符号**跟除数一致**：除数 `2` 是正的，所以余数也是正的。这和 C#、Java 的行为**不一样**（那些语言里 `-7 % 2` 得 `-1`），从其他语言转过来的人要特别注意。

想要"砍掉小数"的效果，用 `int()`：

```python
print(int(-7 / 2))   # => -3    先算成 -3.5，再截断
```

> **什么时候会踩到**：用 `//` 算分页数、算索引偏移时，如果数据里出现了负数，结果会和预期差 1。

**示例 4-17　判断闰年（不用 if）**

闰年规则：能被 4 整除但不能被 100 整除，**或者**能被 400 整除。

我们还没学 `if`，但完全可以先把规则翻译成一个布尔表达式，直接打印结果：

```python
year = 2024

# 拆成三个条件分别看，先确认自己理解对了
div_by_4 = year % 4 == 0        # 比较运算符优先级高于 =，所以这里不用括号
div_by_100 = year % 100 == 0
div_by_400 = year % 400 == 0

print(div_by_4, div_by_100, div_by_400)      # => True False False

# 合成一句：注意 and 优先级高于 or，下面的括号是为了让人读得懂，不加也对
is_leap = (div_by_4 and not div_by_100) or div_by_400
print(f"{year} 是闰年吗？{is_leap}")           # => 2024 是闰年吗？True
```

换几个年份自己试：`2000` → True、`1900` → False、`2023` → False。

> **这个练习的真正价值**：编程里大部分"判断"的难点不在 `if` 怎么写，而在**能不能把自然语言的规则准确翻译成表达式**。先拆成几个中间变量再合并，比一口气写一个长表达式更不容易错，也更好调试。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 以为 `4 / 2` 得 `2` | 得到 `2.0`，后续拼字符串或当索引用时出错 | 要整数用 `4 // 2` | `/` 恒返回 float |
| 以为 `-7 // 2` 是 `-3` | 差 1 的诡异 bug | 想截断用 `int(-7 / 2)` | `//` 是向下取整 |
| `-2 ** 2` 以为得 4 | 得 `-4` | 写 `(-2) ** 2` | `**` 优先级高于取负 |
| `if x == None` | 能用但不规范，遇到重载 `__eq__` 的对象会出错 | `if x is None` | `None` 是单例 |
| `0.1 + 0.2 == 0.3` | `False` | `math.isclose(a, b)` | 浮点精度 |
| `a is b` 判断值相等 | 短字符串"碰巧"为 True，长字符串为 False，看起来时好时坏 | 值比较一律用 `==` | `is` 比的是身份 |
| `not a == b` 理解成 `(not a) == b` | 逻辑反了 | 实际是 `not (a == b)`，想清楚就加括号 | 比较优先级高于 `not` |
| `1 == True` | 得 `True`，误判类型 | 需要严格区分时用 `isinstance` | `bool` 是 `int` 的子类 |

### 下一章预告

数字算完了，接下来是程序里出现频率最高的数据——文本。第 5 章讲字符串，重点是切片和 f-string 这两个天天要用的工具，以及一个必须现在就理解的性质：**字符串改不了**。

---

## 第 5 章　字符串 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会创建各种字符串；熟练索引与切片；掌握十来个高频方法；能用 f-string 做格式化输出；理解不可变性 |
| 前置 | 第 4 章 |
| 预计用时 | 4~5 小时（本章内容多，建议分两次） |
| 可用语法边界 | 第 4 章全部 + 所有字符串方法 + f-string。**不得使用**：`if`（第 7 章）、循环（第 8 章）、列表（第 9 章）。**注意**：`split` 返回列表，示例中只能 `print` 它或用索引取元素，**不能对它做列表操作** |
| 编写要点 | 本章是阶段一里最"有用"的一章，读者学完立刻能处理文本。方法讲解要按**查判 → 变换 → 拆合 → 格式化**分组，不要按字母序罗列 |

### 知识点清单

| # | 知识点 | 必须讲到 | 优先级 |
|---|---|---|---|
| 5.1 | 三种引号 | 单双引号等价；**内含引号时换用另一种**；三引号用于多行文本；多行字符串里的缩进会被保留（常见困扰） | ★★★ |
| 5.2 | 转义字符 | `\n` `\t` `\\` `\"` `\'`；`print` 时才生效，直接看变量值显示的是转义形式 | ★★★ |
| 5.3 | 原始字符串 | `r"C:\new\table"`；**Windows 路径不加 r 会变成换行和制表符**；正则里也要用（第 35 章） | ★★★ |
| 5.4 | 不可变性 | `s[0] = "X"` 报错；**所有"修改"方法都返回新字符串，原串不动**；忘了接收返回值是新手第一大坑 | ★★★ |
| 5.5 | 索引 | 正索引从 0、负索引从 -1；越界 `IndexError`；`s[len(s)]` 必错 | ★★★ |
| 5.6 | 切片 | `s[start:stop:step]`；**左闭右开**；三个都能省；负步长 `[::-1]` 反转；**切片越界不报错**（与索引不同，这是重要区别） | ★★★ |
| 5.7 | 查与判 | `len`、`in`、`find`（找不到返回 -1）、`index`（找不到报错）、`count`、`startswith`/`endswith`、`isdigit`/`isalpha`/`isspace` | ★★★ |
| 5.8 | 变换 | `strip`/`lstrip`/`rstrip`（默认去空白，也可指定字符）、`replace`（可限次数）、`lower`/`upper`/`title`、`zfill`、`ljust`/`rjust`/`center` | ★★★ |
| 5.9 | 拆与合 | `split`（指定分隔符 vs 无参数按任意空白拆，**行为不同**）、`splitlines`、`join`（注意是"分隔符.join(可迭代对象)"这个反直觉的调用方式） | ★★★ |
| 5.10 | f-string | 花括号放变量、放表达式；`{x=}` 自带变量名（调试神器，3.8+）；格式说明符 `:.2f` `:>10` `:,` `:.1%` `:05d`；`{{` `}}` 转义花括号 | ★★★ |
| 5.11 | 旧式格式化 | `.format()` 与 `%` 各给一例，**说明只为看懂老代码，新代码一律 f-string** | ★ |
| 5.12 | 字符与字节 | `len("你好")` 是 2（字符数）；`len("你好".encode("utf-8"))` 是 6（字节数）；`encode`/`decode` 与 UTF-8；**为读文件章节（第 16 章）打底** | ★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 5-1 | 单引号、双引号、三引号 | A | 三种写法等价性与各自场合 | `"""多行"""` |
| 5-2 | 字符串里要放引号怎么办 | A | 换用另一种引号 vs 转义 | `'他说"好"'` |
| 5-3 | `\n` 和 `\t` | A | print 出来才换行；不 print 看到的是 `\n` | `"第一行\n第二行"` |
| 5-4 | Windows 路径为什么要加 r | A | `"C:\new"` ❌ 变成换行 → `r"C:\new"` ✅ | `r"C:\new\table"` |
| 5-5 | 字符串改不了 | A | `s[0] = "X"` ❌ TypeError | `TypeError` |
| 5-6 | upper() 之后原串没变 | A | **必须接收返回值**，❌/✅ 对照 | `s = s.upper()` |
| 5-7 | 正着数和倒着数 | A | `s[0]`、`s[-1]`、越界报错 | `s[-1]` |
| 5-8 | 切片：取一段出来 | A | `s[0:3]`、省略写法、左闭右开 | `s[:3]` |
| 5-9 | 一句话把字符串反过来 | A | `s[::-1]` | `s[::-1]` |
| 5-10 | 切片越界不报错 | A | `s[0:100]` 正常返回，与 `s[100]` ❌ 对比 | `s[0:100]` |
| 5-11 | 有多长、包不包含 | A | `len`、`in` | `"@" in email` |
| 5-12 | find 和 index 的区别 | A | 找不到时一个返回 -1、一个报错 | `s.find("x")` |
| 5-13 | 判断开头、结尾、是否全是数字 | A | `startswith`、`endswith`、`isdigit` | `s.isdigit()` |
| 5-14 | 去掉用户输入的多余空格 | B | `strip` 三兄弟；带参数去指定字符 | `s.strip()` |
| 5-15 | 批量替换 | B | `replace`；限制替换次数；链式调用 | `s.replace("a","b",1)` |
| 5-16 | 大小写转换的四个方法 | B | `lower`/`upper`/`title`/`capitalize` | `s.title()` |
| 5-17 | 把一行文本拆开 | B | `split(",")`；再用索引取其中一项 | `s.split(",")` |
| 5-18 | split() 不带参数的特殊行为 | B | 连续空格、制表符、换行都算分隔，且不产生空项 | `s.split()` |
| 5-19 | join：把几段拼成一句 | B | `"-".join(...)`；解释为什么是分隔符在前 | `", ".join(parts)` |
| 5-20 | f-string 基本用法 | B | 放变量、放表达式、放方法调用 | `f"{name} 今年 {age} 岁"` |
| 5-21 | f-string 的调试写法 | B | `f"{total=}"` 自动带上变量名 | `f"{total=}"` |
| 5-22 | 数字格式化：小数、千分位、百分比、补零 | B | `:.2f`、`:,`、`:.1%`、`:05d` | `f"{x:.2f}"` |
| 5-23 | 让文字对齐：左中右 | B | `:<10`、`:^10`、`:>10` | `f"{name:<10}"` |
| 5-24 | 老代码里的 format 和 % | B | 各一例，能看懂即可 | `"{}".format(x)` |
| 5-25 | 中文占几个字节 | B | `len` 数字符、`encode` 后 `len` 数字节；`decode` 还原 | `s.encode("utf-8")` |
| 5-26 | 清洗一条用户信息 | C | `strip` + `split` + `title` + f-string 组合，处理 `"  zhang san , 28 , beijing "` | 方法链 |
| 5-27 | 打印一张对齐的价格表 | C | 用 f-string 宽度与千分位画出整齐表格，回收第 1 章的购物小票 | `f"{n:<8}{p:>10,.2f}"` |

### 样板示例

**示例 5-6　upper() 之后原串没变**

这是新手最常踩的坑，没有例外——几乎每个人都要栽一次。

```python
name = "alice"
name.upper()              # 调了，但结果被丢掉了
print(name)               # => alice     完全没变！
```

原因在 5.4 讲过：**字符串是不可变的**，`upper()` 不可能修改 `name`，它只能**造一个新字符串还给你**。你不接住，它就没了。

```python
# ❌ 错误：以为方法会修改原字符串
name = "alice"
name.upper()
print(name)               # => alice

# ✅ 正确：接收返回值
name = "alice"
name = name.upper()       # 把新字符串重新贴到 name 这个标签上
print(name)               # => ALICE
```

所有字符串方法都是这个规矩，`strip`、`replace`、`lower` 全都一样：

```python
raw = "  hello  "
raw.strip()               # 白干
print(f"[{raw}]")         # => [  hello  ]

clean = raw.strip()       # 用新变量接住，原变量想留就留
print(f"[{clean}]")       # => [hello]
```

> **怎么记住**：以后写完 `.strip()`、`.replace()` 这类方法，回头看一眼——**这行有没有等号**？没有等号基本就是白写了。
> **对照**：第 9 章的列表方法 `append`、`sort` 恰恰相反，它们直接改原列表、返回 `None`。写成 `lst = lst.sort()` 会得到 `None`，这是另一个方向的同类坑。

**示例 5-27　打印一张对齐的价格表**

第 1 章我们用一堆 `print` 硬拼了一张小票，对不齐还难改。现在用 f-string 重写：

```python
# 格式说明符速记：
#   {值:<8}   左对齐，占 8 个字符宽
#   {值:>10}  右对齐，占 10 个字符宽
#   {值:,.2f} 千分位分隔 + 保留两位小数（金额标准写法）

shop = "小明便利店"
line = "-" * 30

print(f"{shop:^30}")                    # ^ 表示居中，宽度 30
print(line)
print(f"{'商品':<10}{'单价':>8}{'金额':>10}")   # 表头也用同样的宽度，才能对齐
print(line)

name1, price1, count1 = "矿泉水", 2.5, 3
name2, price2, count2 = "进口面包", 68.0, 2
name3, price3, count3 = "鸡蛋", 12.8, 1

# 注意：三行的格式说明符必须完全一致，对齐才不会乱
print(f"{name1:<10}{price1:>8.2f}{price1 * count1:>10,.2f}")
print(f"{name2:<10}{price2:>8.2f}{price2 * count2:>10,.2f}")
print(f"{name3:<10}{price3:>8.2f}{price3 * count3:>10,.2f}")

print(line)
total = price1 * count1 + price2 * count2 + price3 * count3
print(f"{'合计':<10}{'':>8}{total:>10,.2f}")   # 空字符串占位，保持列对齐
```

```text
          小明便利店
------------------------------
商品            单价        金额
------------------------------
矿泉水          2.50      7.50
进口面包       68.00    136.00
鸡蛋           12.80     12.80
------------------------------
合计                     156.30
```

> **注意中文对齐的真相**：中文字符在终端里占两个字符宽，但 Python 的 `:<10` 是按**字符个数**算的，所以中英文混排时看起来还是会歪。要真正对齐得用 `wcwidth` 之类的第三方库，或者干脆改用第 9 章之后的表格化输出。这是个已知的、无法用 f-string 单独解决的问题，先知道原因即可，不必现在纠结。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `s.upper()` 不赋值 | 原串没变化 | `s = s.upper()` | 字符串不可变，方法返回新串 |
| `s[0] = "X"` | `TypeError: 'str' object does not support item assignment` | `s = "X" + s[1:]` | 同上 |
| `"C:\new"` | 输出里出现换行 | `r"C:\new"` 或 `"C:\\new"` | `\n` 被当转义 |
| `s[3:6]` 以为含第 6 个字符 | 少一个字符 | 记住左闭右开，要含就写 `s[3:7]` | 切片区间约定 |
| `s.find("x")` 返回 0 当成"没找到" | 逻辑反了 | 没找到是 `-1`，`0` 表示在开头找到了 | `find` 的返回值约定 |
| 忘了 f-string 的 `f` | 原样输出 `{name}` | 补上 `f` 前缀 | 没有 `f` 就是普通字符串 |
| `f"{d["k"]}"` | 3.12 之前 `SyntaxError` | 外面用单引号 `f'{d["k"]}'` | 旧版本 f-string 不允许同种引号嵌套 |
| 用 `len()` 判断中文字符串的存储长度 | 字节数不符 | 需要字节数时用 `len(s.encode("utf-8"))` | `len` 数的是字符 |
| `"".join(1, 2)` | `TypeError` | `"".join(["1", "2"])`，且元素必须是字符串 | `join` 接收一个可迭代对象，且不自动转类型 |

### 下一章预告

到这里你的程序还只能运行你写死的数据。第 6 章加上 `input`，程序就能和人对话了——同时会遇到一个必踩的坑：`input` 拿到的**永远是字符串**，哪怕用户输的是数字。

---

## 第 6 章　输入与输出 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 掌握 `print` 的全部常用参数；理解 `input` 永远返回字符串并会正确转换；会清洗用户输入；知道输出缓冲和标准流的存在 |
| 前置 | 第 5 章（f-string、字符串方法） |
| 预计用时 | 2~3 小时 |
| 可用语法边界 | 第 5 章全部 + `print` / `input` 全部参数 + `sys.stdout` / `sys.stderr`。**不得使用**：`if`（第 7 章）、任何循环（第 8 章）、容器操作（第 9 章起）。**注意**：`split` 返回列表，本章只能对它做索引取值，不能做列表操作。**预支**：`import sys`（6.9 节，注明"第 18 章详解"） |
| 目录调整 | 原 6.3"输入校验与重试循环"**已移出**——重试需要 `while`（第 8 章）和 `try`（第 15 章）。本章只讲"转换与清洗"，完整的校验重试放在示例 8-14 和 15-13 |
| 编写要点 | 本章是读者第一次写出"会和人对话"的程序，成就感很强，要把这个情绪用上：开篇就让程序问一句话。同时 6.6 的坑是新手 100% 会踩的，篇幅要给足 |
| **打样记录** | 正文已完成（`chapters/ch06_输入与输出.md`）。质检通过：行内标注 9 处、输出块 6 个全部一致（含 `input` 的代码块无法自动核对，已用伪终端单独实测）。<br>**打样中发现并修正**：① 名片示例的对齐输出图纸里估算错了（实际左 12 右 13 个空格）② `flush` 的差别需要"在 sleep 期间去读输出"才能证明，已补实测数据 ③ 重定向时 stderr 比 stdout 先出现，这个现象正好把 6.10 与 6.11 连起来，已补进 6.11 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 6.1 | `print` 输出多个值 | 逗号分隔的多个参数**自动用空格连接**且末尾自动换行；**为什么**：`print` 的设计目标是"随手就能看到东西"；**常见误解**：以为逗号是字符串拼接，其实是传了多个参数 | ★★★ |
| 6.2 | `sep` 参数 | 改变多个参数之间的连接符；`sep=""` 表示紧挨着；典型场景：打印路径、拼日期 `print(2026, 8, 1, sep="-")` | ★★ |
| 6.3 | `end` 参数 | 默认 `end="\n"`；`end=""` 让下一个 `print` 接着同一行；**边界情况**：连续用 `end=""` 后忘记补一次换行，输出会和下一段粘在一起 | ★★★ |
| 6.4 | 空 `print()` | 打一个空行；说明它其实是"零个参数 + 默认换行" | ★★ |
| 6.5 | 三种输出风格对比 | ① 逗号传多参 ② `+` 拼接 ③ f-string。**结论：一律用 f-string**；**为什么**：`+` 要求两边都是字符串（`"年龄" + 18` 直接报错），逗号无法控制格式；f-string 兼顾可读性与格式控制 | ★★★ |
| 6.6 | `input` 基础 | 参数是提示语；**执行到 `input` 程序会停下来等**（这是新手第一次遇到"程序暂停"，要说清）；提示语末尾留一个空格更好看 | ★★★ |
| 6.7 | **`input` 永远返回字符串** | 用户输入 `10`，拿到的是 `"10"` 而不是 `10`；**为什么**：Python 无法猜测你想要什么类型，索性一律给字符串；**容易错在哪**：直接参与算术会得到 `"105"` 这种拼接结果或 TypeError；**边界情况**：用户直接回车得到空字符串 `""` | ★★★ |
| 6.8 | 输入的清洗与转换 | `strip()` 去掉误敲的空格（**必做**，用户很容易多打空格）；`lower()` 统一大小写；`int()` / `float()` 转换；转换失败会抛 `ValueError`（→ 第 15 章正确处理）；`isdigit()` 可以预判但**不能处理负数和小数**，所以它不是可靠方案 | ★★★ |
| 6.9 | 一次输入多个值 | `input().split()` 拆开后用索引取；**边界情况**：用户少输一项会 `IndexError`；说明这是"临时凑合"的写法，第 9 章学了列表才有正规做法 | ★★ |
| 6.10 | 输出缓冲与 `flush` | 为什么有时 `print` 的东西"没马上出来"；`flush=True` 强制立刻输出；**什么时候用**：做进度提示、日志实时刷屏、程序卡住时定位到哪一步 | ★★ |
| 6.11 | 标准输入输出与标准错误 | `stdout` / `stderr` 的区别与用途（正常结果 vs 错误信息应该分开）；`print(..., file=sys.stderr)`；命令行重定向 `python a.py > out.txt` 只重定向 stdout，报错依然会显示在屏幕上——**这解释了为什么日志和结果要分流**（→ 第 21 章 logging） | ★★ |
| 6.12 | `print` 写进文件 | `print(..., file=f)` 是能用的，但正规做法是第 16 章的 `open` + `write`；这里只让读者知道 `file` 参数存在 | ★ |
| 6.13 | 中文对齐的现实 | 回收第 5 章的遗留问题：f-string 宽度按字符数算，而终端里中文占两格，所以中英混排会歪；**结论**：知道原因即可，不必现在解决 | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 6-1 | 一次输出好几个值 | A | 逗号分隔自动加空格；**点明逗号是"传多个参数"不是"拼接"**（对比 `print("小明" + 18)` 会报错） | `print("小明", 18)` |
| 6-2 | 自定义分隔符 | A | `sep="-"` 拼日期（顺带指出 `2026-8-1` 月份不补零，要补零得用 f-string）；`sep=""` 紧挨着 | `sep="-"` |
| 6-3 | 让几次 print 输出在同一行 | A | `end=""`；`end="..."` 也可以是别的内容 | `end=""` |
| 6-4 | `end=""` 的经典事故 | A | ❌ 三段输出粘成一行 → ✅ 该结束处补 `print()` | `print()` |
| 6-5 | 输出空行 | A | `print()` = 零参数 + 默认换行；**强调空行对交互程序观感的作用** | `print()` |
| 6-6 | 三种风格对照 | B | 逗号 / `+` / f-string 输出同一句话 + 优劣对照表；`"年龄" + 18` ❌ TypeError | `f"{name} 今年 {age} 岁"` |
| 6-7 | 让程序问你一句话 | A | **必须讲清三件事**：程序会真的停下来等、括号里是提示语、返回值要用等号接住 | `input("请输入名字：")` |
| 6-8 | 提示语末尾留个空格 | A | 英文冒号后留空格、中文全角冒号不用 | `input("姓名: ")` |
| 6-9 | input 拿到的到底是什么类型 | A | `type()` 得 `<class 'str'>`；用 `repr()` 看清引号 | `repr(a)` |
| 6-10 | **两个数字相加，结果却拼在一起了**（样板） | B | ❌ 得 `105` → ✅ `int(input(...))`；**顺带演示减法直接 TypeError**，说明"加法比减法更危险"（不报错却给错结果） | `int(input(...))` |
| 6-11 | 用户多打了空格 | A | `int(" 10 ")` 能过、`int("1 0")` ❌；**处理文本时 strip 必做**（`"小明 " == "小明"` 是 False） | `.strip()` |
| 6-12 | `int()` 什么时候会失败 | A | 三种：空输入、非数字、**小数形式**（回收 3.8）；要小数用 `float()` | `float("10.5")` |
| 6-13 | 算 BMI | B | `float(input())` + 计算 + `round`；实测输出 `22.9` | `round(bmi, 1)` |
| 6-14 | 让 YES / yes / Yes 都能识别 | B | `.strip().lower()` 连着调用——处理用户输入的标准搭配 | `.strip().lower()` |
| 6-15 | `isdigit()` 的局限 | A | 六个取值逐个测：负数、小数都是 False → **结论：不能当数字校验用**，可靠做法见第 15 章 | `.isdigit()` |
| 6-16 | 用空格分隔一次输入三项 | B | `input().split()` + 下标取值；少输一项 → `IndexError`；**明说这是"临时凑合"写法** | `input().split()` |
| 6-17 | **`flush` 的实际差别** | B | `end=""` + `sleep` 时前 2 秒屏幕空白；加 `flush=True` 立刻显示。**实测数据：不加 flush 时 sleep 期间读到 `''`，加了读到 `'处理中...'`** | `flush=True` |
| 6-18 | 把结果和错误分开输出 | B | `file=sys.stderr`；**用 `> out.txt` 重定向演示两条通道的区别**；再讲一个现象：重定向时 stderr 反而先出现（缓冲差异，把 6.10 和 6.11 连起来） | `file=sys.stderr` |
| 6-19 | 用 print 往文件里写 | A | `file=f`；提一句它比 `write()` 自动带换行；正规做法指向第 16 章 | `file=f` |
| 6-20 | **交互式名片生成器**（样板） | C | 三次 `input` + `strip` + 空行 + **嵌套花括号 `f"{name:^{width-2}}"`**（格式说明符里可以放变量，第 5 章没讲）。**注意：实测输出中"工程师"行左 12 右 13 个空格**——28 宽居中 3 字符无法平分，不要手工估算 | `f"{x:^{w}}"` |
| 6-21 | 汇率换算小工具 | C | `float(input())` + `,.2f` 千分位 + 右对齐；**指出"人民币："比"美元："多一字所以宽度要减 2**——中文对齐问题的具体表现；末尾提示输入非数字会崩（第 15 章解决） | `f"{x:>10,.2f}"` |

### 样板示例

**示例 6-10　两个数字相加，结果却拼在一起了**

这是每个人学 `input` 时必踩的坑，没有例外。

```python
a = input("第一个数字：")      # 用户输入 10
b = input("第二个数字：")      # 用户输入 5
print(a + b)                  # => 105   ！不是 15
```

为什么是 `105`？因为 `input` **永远返回字符串**，`a` 是 `"10"` 而不是 `10`。字符串的 `+` 是拼接，所以 `"10" + "5"` 得 `"105"`。

用 `type()` 亲眼确认一次：

```python
a = input("第一个数字：")
print(type(a))                # => <class 'str'>   果然是字符串
```

正确做法是显式转换：

```python
# ❌ 错误：直接对 input 的结果做算术
a = input("第一个数字：")
b = input("第二个数字：")
print(a + b)                  # => 105

# ✅ 正确：先转成数字再算
a = int(input("第一个数字："))   # int(...) 包在外面，一步完成输入和转换
b = int(input("第二个数字："))
print(a + b)                  # => 15
```

**换个方向也会出问题**——如果需要的是文本，却把数字直接拼上去：

```python
age = 18
print("年龄" + age)            # ❌ TypeError: can only concatenate str (not "int") to str
print("年龄" + str(age))       # ✅ 显式转成字符串
print(f"年龄{age}")            # ✅ 更推荐：f-string 自动处理类型
```

> **记住这条**：`input` 出来的是字符串，要算术就先 `int()` 或 `float()`；要拼字符串就用 f-string，别用 `+`。
>
> **顺带说个边界情况**：如果用户什么都没输就直接回车，你拿到的是空字符串 `""`，`int("")` 会抛 `ValueError`。现在还没学异常处理，所以本章的示例都假设用户会老实输入数字；第 15 章会教怎么优雅地应对乱输。

**示例 6-19　交互式名片生成器**

把本章学的东西和第 5 章的 f-string 格式化合起来用：

```python
# 依次问三个问题。strip() 是必须的：用户很容易在名字后面多敲一个空格
name = input("姓名：").strip()
title = input("职位：").strip()
phone = input("电话：").strip()

width = 30                      # 名片宽度，统一用一个变量，改起来方便

print()                         # 空行，把输入区和输出区分开，观感差别很大
print("+" + "-" * (width - 2) + "+")            # 上边框：加号做四角，减号做横线
print("|" + f"{name:^{width - 2}}" + "|")       # 嵌套花括号：宽度本身也用变量算出来
print("|" + f"{title:^{width - 2}}" + "|")      # ^ 表示居中
print("|" + f"{phone:^{width - 2}}" + "|")
print("+" + "-" * (width - 2) + "+")
```

运行效果（输入 `小明` / `工程师` / `13800138000`）：

```text
姓名：小明
职位：工程师
电话：13800138000

+----------------------------+
|             小明             |
|            工程师             |
|        13800138000         |
+----------------------------+
```

注意两个细节：

1. **`f"{name:^{width - 2}}"` 里出现了两层花括号**——外层是 f-string 的占位符，内层 `{width - 2}` 是把宽度也做成动态的。这个写法第 5 章没讲，是本章新东西，值得记住：**格式说明符本身也可以用变量**。
2. **中文那两行看起来还是没对齐**——这就是 6.13 说的问题：`^28` 按 28 个**字符**居中，但终端里一个中文占两格宽，所以中文少的行显得偏。原因清楚就行，这个问题用 f-string 单独解决不了。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `input()` 结果直接做算术 | `"10" + "5"` 得 `"105"`；`"10" - "5"` 直接 TypeError | `int(input(...))` | input 恒返回字符串 |
| `"年龄" + 18` | `TypeError: can only concatenate str` | `f"年龄{18}"` 或 `str(18)` | Python 不做隐式转换 |
| 用户输入空值 | `int("")` → `ValueError` | 第 15 章用 `try` 处理 | 空字符串不是数字 |
| 用户输入带空格 | `int("1 0")` → `ValueError` | 先 `strip()`，中间有空格的属于真无效输入 | `strip` 只去两端 |
| 用 `isdigit()` 做数字校验 | 负数、小数被判为非数字 | 用 `try` + `float()`（第 15 章） | `isdigit` 只认纯数字字符 |
| 连续 `end=""` 忘了补换行 | 后面的输出粘在同一行 | 最后加一个 `print()` | `end=""` 取消了自动换行 |
| 以为 `print(a, b)` 是拼接 | 想改分隔符却不知从何改 | 用 `sep=` 参数，或改用 f-string | 逗号是传多个参数 |
| 提示语不带空格 `input("姓名:")` | 光标紧贴冒号，看着难受 | `input("姓名: ")` | 纯观感问题，但值得讲 |
| 在某些 IDE 里 `input` 没反应 | 程序像卡住了 | 改用终端运行，或检查 IDE 的"在终端中运行"设置 | 部分 IDE 的运行窗口不支持交互输入 |

### 下一章预告

现在程序能问话了，但它还不会**根据回答做不同的事**——不管用户输什么，代码都一条道跑到黑。第 7 章加上 `if`，程序才算真正有了判断力。顺便解决第 3 章留下的一个悬念：为什么 `0`、`""`、`None` 都被当成"假"。

---

## 第 7 章　条件判断 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会写单分支、双分支、多分支判断；理解 Python 的真假规则；会把嵌套判断改写成扁平结构；知道 `match-case` 存在 |
| 前置 | 第 6 章 |
| 预计用时 | 3~4 小时 |
| 可用语法边界 | 第 6 章全部 + `if` / `elif` / `else` / 三元表达式 / `pass` / `match-case`（仅认知）+ `math.isclose`。**不得使用**：任何循环（第 8 章）、容器（第 9 章起）、函数定义（第 14 章） |
| 目录调整 | 原 7.4"嵌套判断与**提前返回**式扁平化"中的"提前返回"需要函数，**移到第 14 章**（示例 14-15）。本章只讲不依赖函数的扁平化手段：`elif` 替代嵌套、`and` 合并条件、边界值排序 |
| 编写要点 | 本章的难点不是语法（`if` 三行就讲完了），而是**把规则准确翻译成条件**和**条件的先后顺序**。篇幅重点要放在 7.3 的顺序问题和 7.8 的扁平化上，不要在语法形式上多花笔墨。第 4 章的"不用 if 判断闰年"示例要在这里回收对照 |
| **打样记录** | 正文已完成（`chapters/ch07_条件判断.md`，约 5900 字 / 24 个示例）。质检通过：行内标注 22 处、输出块 6 个全部一致。<br>**示例顺序调整**：`match-case`（原 7-24）提前为 **7-21**，因为它在 7.16 节、位于三个综合示例之前；BMI / 运费 / 登录顺延为 7-22 / 7-23 / 7-24。<br>**实测补充**：① `if x = 5:` 在 3.12 的报错比预想更友好——`SyntaxError: invalid syntax. Maybe you meant '==' or ':=' instead of '='?`，正文已用真实文本 ② 运费的 7 组边界值已逐个实跑验证，结果全部填进正文表格 ③ BMI 四个分支各测一次（16.3 / 22.9 / 26.1 / 31.0），四档都进到了 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 7.1 | `if` 单分支 | 语法三要素：条件、冒号、缩进块；**为什么要冒号**（它是"下面有一个块"的标记，Python 里所有引出块的语句都以冒号结尾，这个规律后面 `for`/`def`/`class` 全都适用）；条件为假时整块被跳过 | ★★★ |
| 7.2 | `if` / `else` | 二选一，`else` 后面直接跟冒号，不带条件；**常见误解**：以为 `else` 也能写条件（那是 `elif`） | ★★★ |
| 7.3 | `if` / `elif` / `else` | **顺序至关重要**：从上往下逐个判断，**第一个成立的执行完就整体结束**，后面的一律不再看；**容易错在哪**：把宽松条件写在前面（先判 `>= 60` 会让 90 分也被判成"及格"）；`else` 可省略，但省了就意味着"都不满足时什么也不做" | ★★★ |
| 7.4 | 条件里可以放什么 | 任何表达式都行：比较、逻辑运算、函数调用结果、甚至一个光秃秃的变量；Python 会把它的值当成真或假来用 | ★★★ |
| 7.5 | 真假规则（Falsy / Truthy） | **假**：`False`、`0`、`0.0`、`""`、`None`、空列表/元组/字典/集合；**真**：其余一切；**为什么这样设计**：让"有没有内容"的判断写起来更短（`if name:` 比 `if name != "":` 更常见）；**常见误解**：`"0"`、`"False"`、`" "`（一个空格）**都是真**，因为它们是非空字符串 | ★★★ |
| 7.6 | `if x:` 还是 `if x != "":` | 两种风格的适用场合：判断"有没有内容"用前者；需要区分 `0` 和 `None` 时**必须**用后者（`if count:` 会把 `0` 当成"没有"，这是真实 bug 的来源）；**这一条要给具体反例，不能只讲风格** | ★★★ |
| 7.7 | 三元表达式 | `值1 if 条件 else 值2`；**读法**：先读中间的条件；**什么时候用**：简单的二选一赋值；**什么时候别用**：嵌套超过一层就该改回 `if` | ★★ |
| 7.8 | 嵌套判断与扁平化 | 三种手段：① 平级互斥的条件改用 `elif` ② 层层递进的条件用 `and` 合并 ③ 区间判断按边界值从大到小（或从小到大）**单向排序**，避免重复条件；**为什么要扁平**：嵌套三层以上人就读不动了，且容易漏分支 | ★★★ |
| 7.9 | 区间判断的正确写法 | 链式比较 `0 <= x <= 100`（回收 4.6）；**边界值必须逐个确认**：`>` 和 `>=` 差一个数，就是"90 分算不算优秀"的区别；建议写测试值把每个边界都试一遍 | ★★★ |
| 7.10 | 与 `None` / 浮点数比较 | `if x is None:` 而不是 `== None`（回收 4.11）；浮点数判断用 `math.isclose`（回收 4.12）；**这两条在第 4 章讲过原理，这里讲实际怎么写** | ★★★ |
| 7.11 | `pass` 占位 | `if` 块不能为空，想"先放着不写"就用 `pass`；**什么时候用**：搭框架、临时屏蔽逻辑；**注意**：`pass` 不是"什么都不做的注释"，它是一条真语句 | ★★ |
| 7.12 | `=` 和 `==` 写错会怎样 | `if x = 5:` 直接 `SyntaxError`；**说明这是好事**：C / C#/ Java 里这个错误能编译通过并造成隐蔽 bug，Python 从语法层面禁止了；顺带提一句 3.8+ 的海象运算符 `:=` 是唯一例外（→ 第 37 章） | ★★ |
| 7.13 | `match-case` 认知 | Python 3.10+ 才有；**只做认知**：给一个最简示例，说明它适合"一个值对多种取值"的场景，能力远超 C 的 switch（可以匹配结构）；**为什么现在不细讲**：日常用 `if/elif` 和字典分发（第 11 章）更多，完整用法见第 37 章 | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 7-1 | 第一个 if | A | 条件、冒号、缩进块三要素 | `if score >= 60:` |
| 7-2 | 条件不成立时整块跳过 | A | 在块内外各放一个 `print`，对比执行结果 | 缩进决定归属 |
| 7-3 | if / else 二选一 | A | `else:` 不带条件 | `else:` |
| 7-4 | if / elif / else 判成绩等级 | A | 四个分支，从高到低排 | `elif score >= 80:` |
| 7-5 | elif 顺序写反的后果 | A | ❌ 先判 `>= 60` 导致 90 分也被判"及格" → ✅ 从高到低 | 顺序即逻辑 |
| 7-6 | 条件里放个光秃秃的变量 | A | `if name:` 与 `if 0:` 对照 | `if name:` |
| 7-7 | 到底什么算"假" | A | 逐个测 `0` / `0.0` / `""` / `None`，各配一个 `print` | 真假规则 |
| 7-8 | `"0"` 居然是真的 | A | `if "0":` 会执行；`if " ":` 也会执行 | 非空字符串恒为真 |
| 7-9 | `if count:` 把 0 当成了"没有" | B | ❌ 数量为 0 时被当成"没填" → ✅ `if count == 0:` 区分开 | 显式比较 |
| 7-10 | 三元表达式：一行搞定二选一 | B | 先读中间条件的读法 | `"及格" if s >= 60 else "不及格"` |
| 7-11 | 三元嵌套的可读性红线 | B | ❌ 嵌套两层已经难读 → ✅ 改回 `if/elif` | 可读性优先 |
| 7-12 | 用链式比较判区间 | B | `0 <= score <= 100` vs `score >= 0 and score <= 100` | `0 <= x <= 100` |
| 7-13 | 边界值要一个个试 | B | `>` 和 `>=` 的差别：90 分算不算优秀；列出边界测试值 | `>=` vs `>` |
| 7-14 | 嵌套改 elif | B | ❌ 三层嵌套 → ✅ 平级 `elif` | 扁平化手段① |
| 7-15 | 嵌套改 and | B | ❌ `if a:` 里套 `if b:` → ✅ `if a and b:` | 扁平化手段② |
| 7-16 | 判断 None 的正确写法 | B | `is None` vs `== None`；`if x is not None:` | `is None` |
| 7-17 | 浮点数判断别用 == | B | `math.isclose` 实战 | `math.isclose(a, b)` |
| 7-18 | pass 占个位置 | A | 空块报 IndentationError → 用 `pass` | `pass` |
| 7-19 | 把 == 写成 = 会怎样 | A | `if x = 5:` → SyntaxError；说明这是语言在保护你 | `SyntaxError` |
| 7-20 | 多条件忘加括号 | B | `a or b and c` 的实际结合方式；加括号让意图明确 | `(a or b) and c` |
| 7-22 | BMI 分级 | C | `input` + `float` + 四分支 + f-string 输出建议 | 综合 |
| 7-23 | 快递运费计算 | C | 区间判断 + 边界值 + 首重续重规则，**每个边界都要给测试值** | 区间分支 |
| 7-24 | 简易登录判断 | C | `strip().lower()` 清洗 + 多条件组合 + 三元输出 | 条件组合 |
| 7-21 | match-case 长什么样 | A | 最简三分支示例，一句话说明 3.10+ 才有 | `match cmd:` |

### 样板示例

**示例 7-5　elif 顺序写反的后果**

先看一段"看起来很对"的代码：

```python
score = 90

# ❌ 错误：把最宽松的条件放在了最前面
if score >= 60:
    print("及格")
elif score >= 80:
    print("良好")
elif score >= 90:
    print("优秀")
```

```text
及格
```

90 分被判成了"及格"。为什么？因为 `elif` 是**从上往下逐个判断，第一个成立的执行完就整体结束**。`90 >= 60` 成立，所以打印"及格"，下面两个 `elif` 根本不会被检查。

正确写法是**从最严格到最宽松**排列：

```python
score = 90

# ✅ 正确：从高到低排，先被最严格的条件拦住
if score >= 90:
    print("优秀")
elif score >= 80:
    print("良好")
elif score >= 60:
    print("及格")
else:
    print("不及格")
```

```text
优秀
```

注意排好序之后，第二个条件其实不需要写成 `80 <= score < 90`——能走到 `elif score >= 80` 这一行，就说明前面的 `score >= 90` 已经不成立了，`< 90` 是自动保证的。**这是 `elif` 链最大的好处：条件之间自带"排除前面所有情况"的语义，能省掉一大堆重复判断。**

> **记住这条**：写 `elif` 链之前先问自己——**我的条件是从严到宽排的吗？** 顺序错了，语法完全正确，结果完全错误，而且不报错。这类 bug 最难发现。
>
> **自查方法**：给每个分支挑一个代表值试一遍。上面这段就该用 `95 / 85 / 70 / 30` 各跑一次，四个分支都进到了才算验证过。

**示例 7-23　快递运费计算**

一个真实规则：首重 1 公斤 8 元，续重每公斤 3 元；满 100 元包邮；超过 30 公斤走货运不接单。

```python
weight = float(input("重量（公斤）："))
amount = float(input("订单金额（元）："))

# 边界值想清楚再动手：30 公斤本身接不接？规则说"超过 30 不接"，所以 30 是接的，用 > 而不是 >=
if weight > 30:
    print("超重，请走货运")
elif amount >= 100:                       # 满 100 包邮：注意"满"就是 >=，100 元本身算包邮
    print("满 100 包邮，运费 0 元")
elif weight <= 1:                         # 1 公斤以内只收首重
    print("运费 8.00 元")
else:
    # 走到这里说明 1 < weight <= 30，续重部分要向上取整计费
    # 现实中不足 1 公斤按 1 公斤算，这里先用简化规则：直接按小数算
    extra = weight - 1                    # 超出首重的部分
    fee = 8 + extra * 3
    print(f"运费 {fee:.2f} 元")            # 金额一律保留两位小数
```

**必须逐个验证的边界值**（写完就该照着试一遍）：

| 输入 | 预期 | 检验点 |
|---|---|---|
| 重 0.5，金额 50 | 8.00 元 | 首重下限 |
| 重 1，金额 50 | 8.00 元 | **1 公斤走首重还是续重**（`<= 1` 决定的） |
| 重 1.5，金额 50 | 9.50 元 | 续重计算对不对 |
| 重 30，金额 50 | 95.00 元 | **30 公斤接单**（`> 30` 决定的） |
| 重 31，金额 50 | 超重 | 拒单下限 |
| 重 40，金额 200 | 超重 | **超重优先于包邮**——顺序决定的，如果把包邮判断放前面，40 公斤的大件会被判包邮 |
| 重 2，金额 100 | 包邮 | "满 100"含不含 100 |

最后一行尤其要注意：**分支顺序表达的是业务优先级**。这段代码里"超重拒单"排在"包邮"前面，意味着超重比包邮优先——这是业务决定的，不是随便排的。

> **记住这条**：多分支代码写完，先列一张边界值表逐个试。分支越多越要试，因为不进那个分支的时候，写错了也不会报错。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `elif` 从宽到严排 | 语法正确、结果全错、不报错 | 从严到宽排，用代表值逐分支验证 | 第一个成立的分支就终止判断 |
| `if x = 5:` | `SyntaxError: invalid syntax` | `if x == 5:` | 赋值不是表达式（这是 Python 在保护你） |
| `if` 后忘冒号 | `SyntaxError: expected ':'` | 补上冒号 | 冒号是块的起始标记 |
| `if` 块内没有语句 | `IndentationError: expected an indented block` | 放一个 `pass` | 块不能为空 |
| `if count:` 判断"有没有填" | 用户填了 `0` 被当成没填 | `if count == 0:` 或 `if count is None:` 区分 | `0` 是 Falsy |
| `if s == "0":` 写成 `if s:` | `"0"` 恒为真，判断永远成立 | 显式比较字符串内容 | 非空字符串恒为真 |
| `if x == None:` | 能跑，但不规范 | `if x is None:` | `None` 是单例，用身份比较 |
| `if 0.1 + 0.2 == 0.3:` | 永远不成立 | `math.isclose(a, b)` | 浮点精度 |
| `if a or b and c:` 以为先算 or | 结合方式是 `a or (b and c)` | 加括号写明意图 | `and` 优先级高于 `or` |
| 三元嵌套两层以上 | 一周后自己都读不懂 | 改回 `if/elif` | 可读性 |
| 区间判断漏边界 | 90 分不算优秀这类 off-by-one | 列边界值表逐个试 | `>` 与 `>=` 差一个数 |
| 缩进多了一级 | 语句被归到了上一个块里，逻辑错乱 | 检查缩进对齐 | 缩进决定归属 |

### 下一章预告

程序会判断了，但还只能判断一次。要处理"一批数据"或者"反复问到用户输对为止"，就得让代码转起来。第 8 章讲循环——同时兑现第 6 章欠下的那个承诺：**输入校验与重试**。

---

## 第 8 章　循环 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会用 `for` 和 `while` 两种循环并知道何时用哪个；掌握 `break` / `continue`；会写 `while True` 交互骨架；能排查死循环；会用 `enumerate` / `zip` / `reversed`；理解 `break` 只跳一层 |
| 前置 | 第 7 章 |
| 预计用时 | 4~5 小时（本章是阶段一的分水岭，建议分两次） |
| 可用语法边界 | 第 7 章全部 + `for` / `while` / `break` / `continue` / `range` / `enumerate` / `zip` / `reversed` / 循环 `else`。**不得使用**：列表的增删方法（第 9 章）、函数定义（第 14 章）、`try`（第 15 章）。**注意**：本章可遍历的对象只有**字符串**和 `range`；`sorted` 返回列表，移到第 9 章讲。**预支**：`import random`（示例 8-25，注明"第 18 章详解"） |
| 目录调整 | 8.5 原含 `sorted`，**移到 9.4**（排序需要先理解列表）。本章保留 `enumerate` / `zip` / `reversed`，全部用字符串和 `range` 演示 |
| 编写要点 | 本章有两个真正的难点，篇幅要压在这里：① **死循环的成因与排查**（新手第一次遇到"程序不停了"会慌，要教 Ctrl+C 并讲清三种成因）② **`break` 只跳出一层**。另外"累加器模式"（8.18）虽然后面有 `sum`/`max` 可用，但必须先手写一遍，否则读者不理解循环怎么"攒结果" |
| **打样记录** | 正文已完成（`chapters/ch08_循环.md`，约 7000 字 / 29 个示例）。质检通过：行内标注 11 处、输出块 13 个全部一致。<br>**实测手段**：① 死循环用 `timeout -s INT 1 python3.12 loop.py` 模拟 `Ctrl+C`，拿到真实的 `KeyboardInterrupt` 输出 ② 猜数字用 `random.seed(42)` 固定答案（82）后模拟用户输入，得到可复现的真实对话；**正文代码不含 seed**，并注明"你运行时答案会不同"<br>**发现**：`while...else` 有一个比图纸里的"查找失败"更有说服力的用法——**猜数字的次数上限**（`else` 精确表达"不是猜对退出，而是次数耗尽"）。正文用它作为 `else` 的主要示例，查找失败的例子降为引入 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 8.1 | `for` 的语法与"可迭代对象" | `for 变量 in 可迭代对象:`；**可迭代对象 = 能一个一个交出元素的东西**（当场给这个定义，不许留着不解释）；目前学过的可迭代对象：字符串、`range`；第 9 章起的容器全都是 | ★★★ |
| 8.2 | 遍历字符串 | 逐个字符取出；**循环变量的命名**（`ch`、`char` 而不是 `i`）；循环变量在循环结束后依然存在（值是最后一个元素） | ★★★ |
| 8.3 | `range` 的三种形式 | `range(5)` / `range(1, 6)` / `range(1, 10, 2)`；**左闭右开**（和切片一致，这是 Python 的统一约定，讲清"为什么统一"能减少记忆负担）；负步长倒序 | ★★★ |
| 8.4 | `range` 不是列表 | `print(range(5))` 输出 `range(0, 5)` 而不是 `[0,1,2,3,4]`；**为什么**：它按需生成，`range(10**9)` 也不占内存；**什么时候会踩到**：想直接查看内容时（`list(range(5))` 可以看，但列表在第 9 章） | ★★ |
| 8.5 | 遍历元素 vs 遍历索引 | Python 风格是**直接遍历元素**，只有确实需要位置时才用索引；反例：从其他语言带来的 `for i in range(len(s)): s[i]` 写法虽然能跑，但不地道 | ★★★ |
| 8.6 | `while` 循环 | 条件为真就一直转；**三件套**：初始化、条件、更新——**漏掉更新就是死循环**；`while` 的条件在每轮开始时检查，所以可能一次都不执行 | ★★★ |
| 8.7 | `for` 和 `while` 怎么选 | **次数已知或有明确的遍历对象 → `for`；不知道要转几次、由条件决定 → `while`**；给一张对照表配典型场景（遍历文本用 for，等用户输对用 while） | ★★★ |
| 8.8 | `break` | 立刻跳出当前循环；**什么时候用**：找到了就不用继续找；配合 `while True` 是最常见的组合 | ★★★ |
| 8.9 | `continue` | 跳过本轮剩下的语句，直接进入下一轮；**常见误解**：以为它会跳出循环；**典型场景**：过滤掉不合格的数据 | ★★★ |
| 8.10 | `pass` 在循环里 | 占位，与第 7 章一致；说明 `pass` 和 `continue` 完全不同（一个啥也不做但继续往下执行，一个直接跳到下一轮） | ★★ |
| 8.11 | `while True` + `break` 骨架 | 交互式程序的标准结构；**为什么不写成 `while 条件`**：条件要在循环体中间才能判断时，`while True` + `break` 更清晰；这是**合法且常用的无限循环**，不是坏味道 | ★★★ |
| 8.12 | 死循环的成因与排查 | 三种成因：① 条件永真（`while 1 > 0`）② 忘了更新循环变量 ③ 更新方向写反（该减写成了加）；**必须教会 `Ctrl+C` 中断**；排查手段：在循环里 `print` 出循环变量，一眼就能看出它没在变 | ★★★ |
| 8.13 | 累加器模式 | 循环外初始化、循环内累积——求和、计数、找最大值全是这个模式；**为什么要手写**：后面 `sum`/`max` 一行就能做，但不手写一遍就不理解"结果是怎么攒出来的"；**边界情况**：找最大值的初始值不能随便设 0（负数场景会错），应该用第一个元素或 `float("-inf")` | ★★★ |
| 8.14 | `enumerate` | 同时拿到序号和元素；`start=1` 让序号从 1 开始（打印清单时常用）；**为什么不用 `range(len(x))`**：`enumerate` 更短、更不容易错、对所有可迭代对象都适用 | ★★★ |
| 8.15 | `zip` | 并行遍历两个（或多个）序列；**边界情况**：长度不等时**以最短的为准，多出来的被静默丢弃**（这是常见的静默 bug 来源，必须点明）；3.10+ 有 `strict=True` 可以让它报错 | ★★ |
| 8.16 | `reversed` | 倒序遍历；与 `range(n-1, -1, -1)` 对照（前者好读得多）；与切片 `[::-1]` 的区别（`reversed` 不复制数据） | ★★ |
| 8.17 | 嵌套循环 | 外层转一次、内层转一整轮；**总次数 = 外层次数 × 内层次数**（这个乘法关系要说清，它是性能问题的起点）；典型用途：二维结构、图形打印、两两配对 | ★★★ |
| 8.18 | **`break` 只跳出一层** | 内层 `break` 只结束内层循环，外层继续；**为什么**：`break` 的定义就是"结束**当前**循环"；**怎么跳出两层**：① 标志变量 ② 把内层逻辑抽成函数用 `return`（→ 第 14 章）；Python **没有** `goto` 也没有带标签的 break | ★★★ |
| 8.19 | 循环的 `else` 子句 | **循环正常结束（没被 `break`）才执行 `else`**；用途：查找失败时的处理（"找遍了都没找到"）；**Python 特有**，其他语言没有，所以读者看到会困惑；**说实话**：这个语法争议很大，很多团队禁用它，会用即可，不推荐主动用 | ★ |
| 8.20 | 循环变量的作用域 | 循环结束后循环变量还在（这点和 C#/Java 不同）；`for i in range(0)` 一次都不执行时，`i` 根本不存在，之后用它会 `NameError` | ★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 8-1 | 逐个字符打印 | A | `for ch in "Python":` | `for ch in s:` |
| 8-2 | 数五次 | A | `for i in range(5):` | `range(5)` |
| 8-3 | range 的三种写法 | A | `range(5)` / `range(1,6)` / `range(1,10,2)` | 左闭右开 |
| 8-4 | 倒着数 | A | `range(10, 0, -1)` | 负步长 |
| 8-5 | range 打印出来不是列表 | A | `print(range(5))` → `range(0, 5)` | 惰性生成 |
| 8-6 | while 数到 5 | A | 初始化 / 条件 / 更新三件套 | `while i <= 5:` |
| 8-7 | 忘了更新变量：死循环 | A | ❌ 少了 `i += 1` → 教 Ctrl+C → ✅ 补上更新 | `i += 1` |
| 8-8 | 条件一次都不成立 | A | `while False:` 循环体一次不执行 | 先判断后执行 |
| 8-9 | break 提前退出 | A | 找到目标字符就停 | `break` |
| 8-10 | continue 跳过本轮 | A | 跳过空格字符 | `continue` |
| 8-11 | continue 不是 break | A | 两段对照，输出差别 | 对照 |
| 8-12 | while True + break 骨架 | B | 交互菜单的标准结构 | `while True:` |
| 8-13 | 手写求和 | B | 累加器：循环外初始化、循环内累积 | `total += n` |
| 8-14 | 手写找最大值 | B | ❌ 初始值设 0 遇负数出错 → ✅ 用第一个元素或 `-inf` | 初始值陷阱 |
| 8-15 | 手写计数 | B | 数字符串里有几个数字字符 | `count += 1` |
| 8-16 | enumerate 拿序号 | B | 同时要位置和内容 | `for i, ch in enumerate(s):` |
| 8-17 | 让序号从 1 开始 | B | `enumerate(s, start=1)` | `start=1` |
| 8-18 | 不地道的写法对照 | B | ❌ `for i in range(len(s))` → ✅ `enumerate` | Python 风格 |
| 8-19 | zip 并行遍历 | B | 学号与姓名（两个字符串）配对 | `zip(a, b)` |
| 8-20 | zip 长度不等会静默丢数据 | B | ❌ 长的那个多出来的被丢掉 → ✅ 3.10+ 用 `strict=True` 报错 | `strict=True` |
| 8-21 | reversed 倒着遍历 | B | 与 `range(n-1,-1,-1)` 对照可读性 | `reversed(s)` |
| 8-22 | 嵌套循环打印矩形 | B | 外层行、内层列、`end=""` 控制换行 | 嵌套 + `end=""` |
| 8-23 | 九九乘法表 | B | 嵌套 + f-string 宽度对齐 | `f"{i*j:>4}"` |
| 8-24 | break 只跳出了一层 | B | ❌ 以为能跳出双层 → ✅ 标志变量；说明 Python 没有带标签的 break | `found = True` |
| 8-25 | 循环的 else 子句 | A | 找遍了没找到时执行 `else` | `for...else` |
| 8-26 | 循环变量在循环后还活着 | A | 循环结束 `print(i)` 仍有值；`range(0)` 时 `i` 不存在 → NameError | 作用域 |
| 8-27 | 输入校验重试 | C | **兑现第 6 章承诺**：`while True` + `isdigit` 校验 + `break`；说明为什么这还不够完美（→ 第 15 章 `try`） | 校验骨架 |
| 8-28 | 猜数字游戏 | C | `random.randint` + `while True` + 多分支 + 计数 + `break`，**这就是验收项目 A1 的核心** | 综合 |
| 8-29 | 打印金字塔 | C | 嵌套循环 + 空格与星号数量的规律推导 | 嵌套 + 字符串乘法 |

### 样板示例

**示例 8-7　忘了更新变量：死循环**

先看这段代码，**运行前请先记住怎么停下它：按 `Ctrl+C`**。

```python
# ❌ 错误：循环体里没有改变 i，条件永远成立
i = 1
while i <= 5:
    print(i)
```

```text
1
1
1
1
1
... 无穷无尽
```

屏幕会疯狂刷 `1`。按 **`Ctrl+C`** 中断，你会看到：

```text
KeyboardInterrupt
```

这不是报错，是"你手动打断了程序"的提示。

原因很简单：`while` 每轮开始时检查 `i <= 5`，而循环体里 `i` 从头到尾都是 `1`，条件永远成立。**`while` 循环必须有三件套**：

```python
# ✅ 正确：初始化、条件、更新，三个都不能少
i = 1              # ① 初始化
while i <= 5:      # ② 条件
    print(i)
    i += 1         # ③ 更新 ← 漏掉这一行就是死循环
```

死循环一共就三种成因，遇到时按顺序查：

| 成因 | 例子 | 排查方法 |
|---|---|---|
| 忘了更新循环变量 | 上面这段 | 在循环里 `print(i)`，看它有没有在变 |
| 条件永远为真 | `while 1 > 0:` 且循环体里没有 `break` | 检查条件是否包含会变化的变量 |
| 更新方向写反 | `while i > 0:` 里写了 `i += 1` | 看变量是在朝着"让条件不成立"的方向走吗 |

> **记住这条**：写 `while` 的时候，**先把更新那一行写出来**，再写循环体的其他内容。顺序反了就容易忘。
>
> **`for` 循环为什么不会有这个问题**？因为 `for` 的"更新"是 Python 自动做的——它按顺序从可迭代对象里取下一个，取完就自然结束。**这也是"能用 for 就别用 while"的原因**。

**示例 8-27　输入校验重试（兑现第 6 章的承诺）**

第 6 章我们只能"假设用户会老实输入数字"。现在有了循环，可以真的做到"输错就重新问"：

```python
while True:                                  # 合法的无限循环：条件要在循环体中间才能判断
    raw = input("请输入年龄（0-120）：").strip()   # strip 必做，用户很容易多敲空格

    if not raw.isdigit():                    # isdigit 只认纯数字字符，能挡住字母和空输入
        print("请输入数字，不要输入其他字符")
        continue                             # 跳过本轮剩下的检查，直接重新问

    age = int(raw)                           # 走到这里 raw 一定能转成整数，转换是安全的

    if age > 120:                            # 数值范围是第二道关，和格式检查分开写更清楚
        print("年龄不能超过 120")
        continue

    break                                    # 两道关都过了，跳出循环

print(f"你的年龄是 {age} 岁")                  # 循环里赋值的 age，循环外依然可用（见 8.20）
```

跑一遍试试，依次输入 `abc`、空回车、`200`、`35`：

```text
请输入年龄（0-120）：abc
请输入数字，不要输入其他字符
请输入年龄（0-120）：
请输入数字，不要输入其他字符
请输入年龄（0-120）：200
年龄不能超过 120
请输入年龄（0-120）：35
你的年龄是 35 岁
```

这段代码有三个值得单独说的设计：

1. **格式检查和范围检查分开写**，各配各自的提示语。合成一个 `if` 也能跑，但用户就不知道自己错在哪了。
2. **`continue` 而不是嵌套 `if`**。如果写成"格式对了就进去检查范围"，会多一层缩进；用 `continue` 提前挡掉不合格的输入，主流程始终保持在最外层——这是第 7.8 节扁平化思想在循环里的应用，也是很多老手的习惯写法。
3. **`break` 放在所有检查之后**，位置就代表"全部通过"。

> **这段代码还不够好**：`isdigit()` 挡不住负数和小数（`-5`、`3.5` 都会被判为"不是数字"），要正确处理这些输入，得用第 15 章的 `try` + `int()`。到时候我们会回来把这段重写一遍，代码会更短也更可靠。
>
> **记住这条**：`while True` + 校验 + `continue` + `break` 是交互式输入的标准骨架，闭着眼睛都要能写出来。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `while` 里忘了更新循环变量 | 死循环，屏幕疯狂刷屏 | `Ctrl+C` 中断，补上 `i += 1` | 条件永远成立 |
| 更新方向写反 | 死循环 | 检查变量是否朝"条件不成立"走 | `i > 0` 配 `i += 1` 永不结束 |
| 以为 `continue` 会跳出循环 | 循环没停，逻辑错乱 | 跳出用 `break` | 两者语义完全不同 |
| 内层 `break` 想跳出双层 | 外层还在继续转 | 标志变量，或抽函数用 `return`（第 14 章） | `break` 只结束当前循环 |
| `for i in range(len(s)): s[i]` | 能跑但不地道，容易越界写错 | `for ch in s:` 或 `enumerate(s)` | Python 直接遍历元素 |
| `zip` 两个长度不等的序列 | 多出来的数据被**静默丢弃**，不报错 | 3.10+ 用 `zip(a, b, strict=True)` | zip 以最短为准 |
| 找最大值初始值设 `0` | 全是负数时结果错成 `0` | 用第一个元素或 `float("-inf")` 初始化 | 0 不是"最小可能值" |
| `range(1, 5)` 以为含 5 | 少循环一次 | 要含 5 写 `range(1, 6)` | 左闭右开 |
| `print(range(5))` 想看内容 | 输出 `range(0, 5)` | `list(range(5))`（第 9 章） | range 是惰性的 |
| 循环体忘了缩进 | `IndentationError` 或语句跑到循环外只执行一次 | 检查缩进 | 缩进决定归属 |
| 一次都没进循环还用循环变量 | `NameError` | 循环外先给变量赋初值 | `range(0)` 时变量不会被创建 |
| 嵌套循环里内外层用了同一个变量名 | 外层计数被内层改掉，结果诡异 | 内外层用不同变量名（`i` / `j`） | 变量互相覆盖 |

### 下一章预告

到这里，语言的"骨架"（判断 + 循环）就齐了。但你手上还只有单个的数字和字符串——没法存"一批"东西。第 9 章讲列表，它是 Python 里最常用的数据结构，也是第一次让你能一次性拿住一堆数据。同时会遇到第 3 章埋下的那个伏笔：**`b = a` 之后改 b，a 也跟着变了**。

---

## 第 9 章　列表 list ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会创建、索引、切片列表；熟练增删改查；分清 `sort` 与 `sorted`、`append` 与 `extend`；知道遍历时不能改列表；会写最简列表推导式 |
| 前置 | 第 8 章 |
| 预计用时 | 5~6 小时（本章是全书最常用的数据结构，建议分两次） |
| 可用语法边界 | 第 8 章全部 + 列表全部操作 + `sorted` / `sum` / `max` / `min` / `len` / `list()` / `copy` 模块。**不得使用**：字典与集合（第 11、12 章）、函数定义（第 14 章）、`try`（第 15 章）、推导式的进阶写法（第 25 章） |
| 目录调整 | `sorted` 从 8.5 移入本章 9.10；新增 9.14（遍历时修改列表的陷阱）、9.18（列表与字符串互转），这两个都是高频实战问题，原目录漏了 |
| 编写要点 | 本章的核心不是方法列表，而是三组必须讲透的对比：① **列表可变 vs 字符串不可变**（第 5 章的反面，两章要互相引用）② **`sort` 原地改并返回 `None` vs `sorted` 返回新列表**（与 5.6 的 `s.upper()` 恰好相反，是同一坑的两个方向）③ **`append` vs `extend`**。另外 9.19 只给"改 b 影响 a"的现象，**原理和解法留给第 13 章**，不要提前讲深 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 9.1 | 创建列表 | `[]` 字面量、`list()`、空列表 `[]`；**可以混装不同类型**（`[1, "a", True]`），但实践中通常只装同类东西，因为混装的列表处理起来要不停判断类型 | ★★★ |
| 9.2 | 索引与切片 | 与字符串**完全一致**（正负索引、左闭右开、步长、`[::-1]`）；**为什么一致**：它们都是"序列"，Python 让所有序列共享同一套下标规则，学一次用到处（这个统一性要点明，能省一半记忆量） | ★★★ |
| 9.3 | **列表是可变的** | 与字符串形成对照：`s[0] = "X"` 报错，`lst[0] = "X"` 正常；**为什么设计成可变**：列表是用来装"会变的一批数据"的，每次改都造新列表代价太大；**代价**：可变带来共享修改的坑（9.19、第 13 章） | ★★★ |
| 9.4 | 增：`append` | 在末尾加**一个**元素；返回 `None`（原地修改）；最常用的列表方法 | ★★★ |
| 9.5 | **`append` vs `extend`** | `append(x)` 把 x 整个当一个元素塞进去；`extend(x)` 把 x **拆开**逐个塞进去；**容易错在哪**：`append([1,2])` 得到嵌套列表 `[..., [1,2]]`；`extend("abc")` 会拆成三个字符（字符串也是可迭代的！）——这个"惊喜"必须演示 | ★★★ |
| 9.6 | 增：`insert` | 按位置插入；`insert(0, x)` 插到开头；**性能提示**：在开头插入要挪动后面所有元素，数据量大时慢（真正的解法是第 34 章的 `deque`，这里只需知道有这回事） | ★★ |
| 9.7 | 增：`+` 和 `*` | `+` 拼接产生**新列表**（与 `extend` 原地修改的区别）；`*` 重复；`[0] * 5` 快速造初始列表 | ★★ |
| 9.8 | 删：`pop` | 不带参数删末尾并**返回被删的元素**；带索引删指定位置；**边界情况**：空列表 `pop()` 抛 `IndexError` | ★★★ |
| 9.9 | 删：`remove` | 按**值**删，且**只删第一个**匹配项；**边界情况**：值不存在抛 `ValueError`；要删全部相同值得用循环或推导式 | ★★★ |
| 9.10 | 删：`del` 与 `clear` | `del lst[0]` 删单个、`del lst[1:3]` 删一段、`del lst` 删掉整个变量；`clear()` 清空但列表还在；**三者的区别要讲清** | ★★ |
| 9.11 | 改：索引赋值与切片赋值 | `lst[0] = x` 改一个；`lst[1:3] = [7,8,9]` 可以**改变长度**（替换的段和新内容长度不必相等），这是列表特有的能力，`lst[1:3] = []` 等于删除 | ★★ |
| 9.12 | 查：`in` / `index` / `count` | `in` 判断存在（最常用）；`index` 返回位置，**找不到抛 `ValueError`**（和字符串 `find` 返回 -1 不同，列表**没有** `find`）；`count` 数出现次数 | ★★★ |
| 9.13 | 统计：`len` / `sum` / `max` / `min` | 回收第 8 章手写的累加器，一行搞定；**边界情况**：空列表 `sum([])` 得 `0`，但 `max([])` 抛 `ValueError`；`max` 对混装类型会报 TypeError | ★★★ |
| 9.14 | **`sort` vs `sorted`** | `lst.sort()` **原地排序，返回 `None`**；`sorted(lst)` **返回新列表，原列表不动**；**容易错在哪**：`lst = lst.sort()` 会把 `lst` 变成 `None`——这与 5.6 的 `s.upper()` 不赋值恰好相反，两个坑要放在一起讲，读者才能建立"哪些方法原地改、哪些返回新对象"的判断力 | ★★★ |
| 9.15 | 排序参数 | `reverse=True` 降序；`key=len` 按长度排、`key=str.lower` 忽略大小写；**混装类型排序会报错**（数字和字符串没法比大小） | ★★★ |
| 9.16 | `reverse()` / `reversed()` / `[::-1]` | 三种"倒过来"：原地反转、返回迭代器（第 8 章用过）、切片产生新列表；**什么时候用哪个**给一张小表 | ★★ |
| 9.17 | 遍历列表 | `for x in lst`、`enumerate(lst)`；回收第 8 章 | ★★★ |
| 9.18 | **遍历时修改列表的陷阱** | 边遍历边 `remove` 会**跳过元素**（因为删除后后面的元素前移，而下标继续往后走）；**必须演示这个诡异结果**；三种正确做法：① 倒序遍历 ② 遍历副本 `for x in lst[:]` ③ **推荐**：用推导式生成新列表 | ★★★ |
| 9.19 | 二维列表 | 列表里装列表；`grid[1][2]` 双下标；嵌套循环遍历；**什么时候用**：表格数据、矩阵、棋盘 | ★★ |
| 9.20 | `[[0]*3]*3` 陷阱 | 改一行会**三行全变**；**原因**：`*` 复制的是"引用"，三行其实是同一个列表；**正确做法**：`[[0]*3 for _ in range(3)]`（推导式在 9.22 讲，这里可以先给写法）；**这个坑与第 13 章拷贝语义同源，要明确指路** | ★★★ |
| 9.21 | `b = a` 的现象 | 改 `b` 会影响 `a`（回收 3.3 的 `id()` 演示）；**本章只给现象和"暂时的自保办法"**（`b = a[:]`），完整解释和 `deepcopy` 留给 13.4 | ★★★ |
| 9.22 | 列表推导式（最简形式） | `[表达式 for 变量 in 可迭代对象]` 和带 `if` 的过滤形式；**必须与等价的 `for` 循环写法对照**，让读者看清它只是"循环 + append"的缩写；**什么时候别用**：逻辑复杂、需要多行时老实写循环（进阶写法见第 25 章） | ★★★ |
| 9.23 | 列表与字符串互转 | `s.split(",")` 得列表、`",".join(lst)` 拼回字符串（`join` 要求元素全是字符串）、`list("abc")` 拆成单字符；**这三个是文本处理的日常操作** | ★★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 9-1 | 创建一个列表 | A | 字面量、空列表、混装 | `[1, 2, 3]` |
| 9-2 | 取第一个和最后一个 | A | `lst[0]` / `lst[-1]`；越界 IndexError | `lst[-1]` |
| 9-3 | 切一段出来 | A | 与字符串切片规则完全一致 | `lst[1:3]` |
| 9-4 | 列表能改，字符串不能 | A | `lst[0] = 99` ✅ 对照 `s[0] = "X"` ❌ | 可变 vs 不可变 |
| 9-5 | append 加一个 | A | 末尾追加 | `lst.append(4)` |
| 9-6 | append 一个列表会发生什么 | A | ❌ 得到嵌套 `[1,2,[3,4]]` → ✅ 用 `extend` | `extend` |
| 9-7 | extend 一个字符串的"惊喜" | A | `extend("abc")` 拆成三个字符 | 字符串可迭代 |
| 9-8 | insert 插到中间 | A | `insert(0, x)` 插开头 | `lst.insert(1, x)` |
| 9-9 | 拼接和重复 | A | `+` 产生新列表、`[0] * 5` 造初始列表 | `[0] * 5` |
| 9-10 | pop 拿出最后一个 | A | 返回被删元素；空列表 pop 报错 | `lst.pop()` |
| 9-11 | remove 按值删 | A | 只删第一个匹配；不存在报 ValueError | `lst.remove(2)` |
| 9-12 | del 和 clear | A | 删一个 / 删一段 / 清空 / 删变量 | `del lst[0]` |
| 9-13 | 改一个元素 | A | 索引赋值 | `lst[0] = 99` |
| 9-14 | 切片赋值能改长度 | B | `lst[1:3] = [7,8,9]`；`lst[1:3] = []` 删一段 | 切片赋值 |
| 9-15 | in / index / count | A | 列表没有 `find`，`index` 找不到会报错 | `lst.index(x)` |
| 9-16 | 一行求和求最值 | A | `sum` / `max` / `min` / `len`；回收第 8 章手写版 | `sum(lst)` |
| 9-17 | 空列表的边界 | A | `sum([])` 得 0；`max([])` ❌ ValueError | 边界情况 |
| 9-18 | sort 排序后原列表变了 | A | 原地排序 | `lst.sort()` |
| 9-19 | `lst = lst.sort()` 把列表弄丢了 | A | ❌ 得到 `None` → ✅ 分开写或用 `sorted` | 返回 None |
| 9-20 | sorted 不动原列表 | A | 对照 `sort`，两个列表都打印出来 | `sorted(lst)` |
| 9-21 | 降序和按长度排 | B | `reverse=True`、`key=len`、`key=str.lower` | `key=len` |
| 9-22 | 混装类型排序会报错 | A | `[1, "a"].sort()` ❌ TypeError | 类型不可比 |
| 9-23 | 三种倒过来的方式 | B | `reverse()` / `reversed()` / `[::-1]` 对照 | `[::-1]` |
| 9-24 | 遍历列表 | A | `for x in lst` / `enumerate` | `enumerate(lst)` |
| 9-25 | 边遍历边删除的诡异结果 | B | ❌ 元素被跳过 → ✅ 倒序 / 遍历副本 / 推导式 | `for x in lst[:]` |
| 9-26 | 二维列表 | B | 创建、`grid[1][2]`、嵌套循环打印 | `grid[1][2]` |
| 9-27 | `[[0]*3]*3` 改一个变三个 | B | ❌ 三行联动 → ✅ `[[0]*3 for _ in range(3)]` | 引用复制 |
| 9-28 | `b = a` 之后改 b，a 也变了 | B | 现象 + `id()` 验证；临时解法 `b = a[:]`；指向第 13 章 | `b = a[:]` |
| 9-29 | 列表推导式 vs for 循环 | B | 同一需求两种写法对照，说明它就是缩写 | `[x*2 for x in lst]` |
| 9-30 | 推导式带 if 过滤 | B | 只留偶数 | `[x for x in lst if x % 2 == 0]` |
| 9-31 | 字符串和列表互转 | B | `split` / `join` / `list()`；`join` 元素必须是字符串 | `",".join(lst)` |
| 9-32 | 成绩统计 | C | `sum` / `max` / `min` / 平均分 / `sorted` 取前三 / f-string 报表 | 综合 |
| 9-33 | 购物清单管理 | C | `while True` 菜单 + `append` / `remove` + `enumerate` 编号显示，**验收项目 A3 的雏形** | 综合 |
| 9-34 | 文本词频雏形 | C | `split` + 遍历 + `count` 统计；**顺带指出 `count` 在循环里用是低效的**（每次都要扫全表），正确做法见第 11 章字典 | 综合 |

### 样板示例

**示例 9-19　`lst = lst.sort()` 把列表弄丢了**

第 5 章我们学过：`s.upper()` 不改原字符串，必须写 `s = s.upper()` 接住返回值。列表这里**恰好相反**，同样的习惯会害人：

```python
scores = [88, 95, 70]

# ❌ 错误：sort() 是原地排序，返回的是 None
scores = scores.sort()
print(scores)          # => None    列表没了！
```

`sort()` 直接在原列表上排好序，**不需要也没有返回新列表**，它返回 `None`。你把 `None` 赋回去，原列表就被覆盖了。

正确写法有两种，取决于你想不想保留原顺序：

```python
# ✅ 写法一：原地排序，不要赋值
scores = [88, 95, 70]
scores.sort()                  # 直接改 scores 本身，这一行不能有等号
print(scores)                  # => [70, 88, 95]

# ✅ 写法二：要保留原列表，用 sorted() 生成新的
scores = [88, 95, 70]
ordered = sorted(scores)       # sorted 返回新列表
print(ordered)                 # => [70, 88, 95]
print(scores)                  # => [88, 95, 70]   原列表纹丝不动
```

**把两章的坑并排放在一起看，规律就出来了**：

| 操作 | 行为 | 返回值 | 正确用法 |
|---|---|---|---|
| `s.upper()`（字符串） | 造一个新字符串 | **新字符串** | `s = s.upper()` 必须赋值 |
| `s.replace(...)`（字符串） | 造一个新字符串 | **新字符串** | 必须赋值 |
| `lst.sort()`（列表） | 原地改 | **`None`** | 不要赋值 |
| `lst.append(x)`（列表） | 原地改 | **`None`** | 不要赋值 |
| `lst.reverse()`（列表） | 原地改 | **`None`** | 不要赋值 |
| `sorted(lst)`（内置函数） | 造一个新列表 | **新列表** | 必须赋值 |

> **记住这条**：**不可变类型（字符串）的方法一律返回新对象，必须赋值；可变类型（列表）的方法多半原地修改并返回 `None`，不要赋值。**
>
> **一个通用的自查技巧**：写完一行 `.某方法()`，如果不确定，就 `print` 一下它的返回值。看到 `None` 就说明它是原地修改的，那一行不该有等号。

**示例 9-25　边遍历边删除的诡异结果**

需求：删掉列表里所有的 0。看起来最自然的写法是错的：

```python
nums = [1, 0, 0, 2, 3]

# ❌ 错误：一边遍历一边删除
for n in nums:
    if n == 0:
        nums.remove(n)

print(nums)        # => [1, 0, 2, 3]     还剩一个 0！
```

为什么漏掉一个？因为 `for` 循环内部是**按下标往后走**的，而 `remove` 把后面的元素**往前挪了一格**：

| 轮次 | 下标 | 当前列表 | 取到的元素 | 动作 |
|---|---|---|---|---|
| 1 | 0 | `[1, 0, 0, 2, 3]` | `1` | 不删 |
| 2 | 1 | `[1, 0, 0, 2, 3]` | `0` | 删掉 → 列表变 `[1, 0, 2, 3]` |
| 3 | 2 | `[1, 0, 2, 3]` | `2` | **第二个 0 已经挪到下标 1，被跳过了** |
| 4 | 3 | `[1, 0, 2, 3]` | `3` | 不删 |

删除让"当前位置"和"下一个元素"错开了一格，于是紧跟在被删元素后面的那个元素永远不会被检查。

三种正确做法，**推荐第三种**：

```python
# ✅ 做法一：倒着遍历。从后往前删，挪动的都是已经处理过的部分
nums = [1, 0, 0, 2, 3]
for i in range(len(nums) - 1, -1, -1):     # 从最后一个下标倒数到 0
    if nums[i] == 0:
        del nums[i]
print(nums)        # => [1, 2, 3]

# ✅ 做法二：遍历一份副本，改原列表。nums[:] 是切片拷贝，遍历它不受删除影响
nums = [1, 0, 0, 2, 3]
for n in nums[:]:                          # 注意这个 [:]，去掉就又错了
    if n == 0:
        nums.remove(n)
print(nums)        # => [1, 2, 3]

# ✅ 做法三（推荐）：不删了，直接生成一个新列表
nums = [1, 0, 0, 2, 3]
nums = [n for n in nums if n != 0]         # 推导式：留下所有不等于 0 的
print(nums)        # => [1, 2, 3]
```

第三种最好，原因有三：**没有下标，就没有下标错乱的可能**；一行讲清意图（"留下不等于 0 的"）；不修改原数据，更不容易牵连别处。

> **记住这条**：**遍历一个列表的时候，不要增删这个列表。** 需要筛选就生成新列表——这个原则对字典和集合同样适用（第 11、12 章会再遇到，字典在遍历中改动甚至会直接报错）。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `lst = lst.sort()` | `lst` 变成 `None` | `lst.sort()` 不赋值，或用 `sorted()` | `sort` 原地改并返回 `None` |
| `lst = lst.append(x)` | 同上 | `lst.append(x)` | 同上 |
| `append` 一个列表 | 得到嵌套列表 | 用 `extend` 或 `+` | `append` 只加一个元素 |
| `extend("abc")` | 拆成了 `'a','b','c'` | 想整体加就 `append("abc")` | 字符串是可迭代的 |
| 边遍历边删除 | 元素被静默跳过 | 用推导式生成新列表 | 下标与元素错位 |
| `lst.remove(x)` 删所有 | 只删掉第一个 | 用推导式过滤 | `remove` 只删第一个匹配 |
| `lst.index(x)` 找不存在的值 | `ValueError` | 先 `if x in lst` 判断 | 列表没有 `find` |
| `max([])` | `ValueError` | 先判断非空 | 空序列没有最大值 |
| `[1, "a"].sort()` | `TypeError` | 保证列表元素类型一致 | 数字和字符串不可比较 |
| `[[0]*3]*3` | 改一行三行全变 | `[[0]*3 for _ in range(3)]` | `*` 复制的是引用 |
| `b = a` 后改 `b` | `a` 也变了 | `b = a[:]`（浅拷贝，详见 13.4） | 两个名字指向同一列表 |
| `",".join([1,2])` | `TypeError` | `",".join(str(x) for x in lst)` | `join` 不自动转类型 |
| 用 `count` 在循环里统计词频 | 数据量大时明显变慢 | 用字典计数（第 11 章） | 每次 `count` 都扫全表 |

### 下一章预告

列表能装能改，很好用——但正因为它能改，把它交给别人时你就没法保证数据不被动。第 10 章讲元组：一个**故意不让你改**的列表。听起来像是功能变少了，但正是这个限制让它能干一件列表干不了的事（第 11 章你会看到）。

---

## 第 10 章　元组 tuple ★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会创建元组（包括单元素元组）；理解不可变的**范围与边界**；会用解包；知道什么时候该用元组而不是列表 |
| 前置 | 第 9 章 |
| 预计用时 | 2~3 小时 |
| 可用语法边界 | 第 9 章全部 + 元组全部操作 + `hash()` / `divmod()` / `tuple()`。**不得使用**：字典与集合（第 11、12 章）——讲"元组能作字典键"时**用 `hash()` 演示可哈希性**，不要用 `dict` 举例；函数定义（第 14 章）——讲"返回多值"时用内置的 `divmod` 举例 |
| 目录调整 | 新增 10.4（不可变的边界：元组里装列表）、10.7（元组只有两个方法）、10.9（星号解包）、10.12（选型决策表）。原目录 4 个小节偏少，讲不透"为什么需要不可变" |
| 编写要点 | 本章最容易被写成"列表的残废版"，那就白讲了。真正要讲透的是**为什么需要一个不能改的序列**——三个理由：① 传出去不怕被人改 ② 可哈希才能当字典的键 ③ 语义上表达"这是一条固定记录"。其中②是元组**不可替代**的用途，第 11 章会用到，这里必须打好底。另外 10.4 的"不可变边界"是新手普遍的误解，要专门演示 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 10.1 | 创建元组 | `(1, 2, 3)`；空元组 `()`；`tuple()` 转换 | ★★★ |
| 10.2 | **逗号才是关键，括号不是** | `t = 1, 2, 3` 不加括号也是元组；**为什么要知道这个**：它解释了为什么 `return a, b` 返回的是元组（第 14 章）、为什么 `a, b = b, a` 能交换变量；括号只在有歧义时才必须加（如函数调用的参数里） | ★★★ |
| 10.3 | 单元素元组的逗号 | `(1)` 是数字 `1`，`(1,)` 才是元组；**为什么**：括号本身还有"分组运算"的含义，Python 只能靠逗号区分；**容易错在哪**：写配置、写单元素参数时漏掉逗号，类型就变了 | ★★★ |
| 10.4 | 索引与切片 | 与列表、字符串完全一致；切片返回的是**元组** | ★★★ |
| 10.5 | 不可变意味着什么 | 不能改元素、不能增删；**没有** `append` / `remove` / `sort`（调用会 `AttributeError` 而不是 `TypeError`，这个区别可以顺带讲：不是"不允许改"，是"根本没这个方法"） | ★★★ |
| 10.6 | **不可变的边界（重要）** | 元组不可变指的是"**装的东西不能换**"，不是"装的东西内部不能变"；`t = (1, [2, 3])` 里那个列表照样能 `append`；**为什么会这样**：元组存的是引用，引用没变就算没变（回收 3.3 的标签比喻）；**什么时候会踩到**：以为把数据放进元组就安全了 | ★★★ |
| 10.7 | 元组只有两个方法 | 只有 `count` 和 `index`（因为其他方法都是改数据的）；**这是个好记忆点**：方法少正是不可变的直接体现 | ★★ |
| 10.8 | `+` 和 `*` | 都产生**新元组**；`t += (4,)` 看起来像"追加"，实际是造了个新元组再重新贴标签——**这一点要说清**，否则读者会以为元组能改 | ★★ |
| 10.9 | **可哈希：元组能做而列表不能做的事** | 用 `hash((1,2))` 成功、`hash([1,2])` 抛 `TypeError` 演示；**什么叫可哈希**：能算出一个固定的"指纹"，前提是内容不会变；**为什么重要**：只有可哈希的东西才能当字典的键、集合的元素（第 11、12 章）；**边界情况**：装了列表的元组**也不可哈希**（`hash((1,[2]))` 报错），因为内容会变 | ★★★ |
| 10.10 | 解包 | `x, y = point`；**个数必须完全匹配**，多了少了都抛 `ValueError`（错误信息会明确写 too many / not enough values）；解包对列表、字符串同样有效（`a, b, c = "xyz"`） | ★★★ |
| 10.11 | 交换变量 | `a, b = b, a`；**原理**：右边先打包成元组，再整体解包给左边——所以不需要临时变量；这是 Python 相比 C#/Java 最讨喜的小特性之一 | ★★★ |
| 10.12 | 星号解包 | `first, *rest = t` 把剩下的收进列表（**注意 `rest` 是列表不是元组**）；`*mid` 可以在中间；**什么时候用**：只关心头尾、丢弃中间部分时 | ★★ |
| 10.13 | 函数返回多值的本质 | 用内置的 `divmod(17, 5)` 演示：它返回 `(3, 2)` 一个元组，配合解包 `q, r = divmod(17, 5)` 就像"返回了两个值"；**这解释了第 14 章的多返回值机制**，提前打底 | ★★★ |
| 10.14 | 遍历与解包遍历 | `for x in t`；元组列表配合解包 `for name, score in records`——**这是处理"一批记录"的标准写法**，实战中极常用 | ★★★ |
| 10.15 | 元组与列表互转 | `list(t)` / `tuple(lst)`；**典型用法**：需要临时修改一个元组，就转成列表改完再转回去 | ★★ |
| 10.16 | 选型：元组还是列表 | 给一张决策表：数据条数会变 → 列表；一条固定记录（坐标、RGB、数据库一行）→ 元组；需要当键 → 必须元组；**一条实用经验**：元组表达"结构"（每个位置含义不同），列表表达"集合"（每个元素地位相同） | ★★★ |
| 10.17 | 具名元组一瞥 | `namedtuple` 让 `p.x` 代替 `p[0]`；**只做认知**，说明它解决"元组下标看不懂"的问题，完整用法见第 34 章；也提一句现代替代品是第 29 章的 `dataclass` | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 10-1 | 创建一个元组 | A | `(1, 2, 3)`、空元组 | `()` |
| 10-2 | 括号其实可以省 | A | `t = 1, 2, 3` 也是元组，`type()` 验证 | `t = 1, 2` |
| 10-3 | 单元素元组必须加逗号 | A | ❌ `(1)` 是 int → ✅ `(1,)` | `(1,)` |
| 10-4 | 索引和切片 | A | 与列表一致；切片结果还是元组 | `t[1:3]` |
| 10-5 | 元组不能改 | A | `t[0] = 9` ❌ TypeError | `TypeError` |
| 10-6 | 元组没有 append | A | ❌ `AttributeError`（不是"不允许"，是"没有这方法"） | `AttributeError` |
| 10-7 | 元组里的列表照样能改 | B | `t = (1, [2, 3])` → `t[1].append(4)` ✅ 成功；元组本身没变 | 不可变的边界 |
| 10-8 | `t += (4,)` 不是追加 | B | `id()` 对比：得到的是新元组 | `t += (4,)` |
| 10-9 | 元组能算指纹，列表不能 | B | `hash((1,2))` ✅ / `hash([1,2])` ❌ / `hash((1,[2]))` ❌ | `hash(t)` |
| 10-10 | 元组只有两个方法 | A | `count` / `index`；用 `dir()` 对比列表方法数量 | `t.count(x)` |
| 10-11 | 解包成几个变量 | A | `x, y = (3, 4)` | `x, y = point` |
| 10-12 | 个数不对会报错 | A | ❌ 多了/少了都 `ValueError`，看清错误提示的措辞 | `ValueError` |
| 10-13 | 一行交换两个变量 | A | `a, b = b, a`，不需要临时变量 | `a, b = b, a` |
| 10-14 | 只要头尾，中间丢掉 | B | `first, *rest = t`；`rest` 是列表 | `first, *rest = t` |
| 10-15 | 字符串也能解包 | A | `a, b, c = "xyz"` | 解包通用性 |
| 10-16 | divmod 一次返回商和余数 | B | `q, r = divmod(17, 5)`；说明这就是"返回多值"的真相 | `divmod(a, b)` |
| 10-17 | 遍历一批记录 | B | 元组列表 + `for name, score in records` 解包遍历 | 解包遍历 |
| 10-18 | 给记录排序 | B | `sorted(records, key=...)` 按第二个元素排（`key` 的完整用法见第 25 章，这里给最简形式） | `key=` |
| 10-19 | 元组和列表互转 | A | 转成列表改完再转回来 | `tuple(lst)` |
| 10-20 | 选型对照 | B | 同一份数据用元组和列表各写一次，比较语义差别 | 决策表 |
| 10-21 | 坐标点计算 | C | 元组表示点 + 解包 + 距离公式（`** 0.5` 开方，回收 4.4） | 综合 |
| 10-22 | 学生成绩报表 | C | 元组列表 + 排序 + 解包遍历 + f-string 对齐输出，**比第 9 章的纯列表版本更清晰**（每条记录是个结构） | 综合 |
| 10-23 | namedtuple 长什么样 | A | `Point = namedtuple("Point", "x y")` → `p.x`；一瞥即可 | `namedtuple` |

### 样板示例

**示例 10-7　元组里的列表照样能改**

新手最常见的误解是"把数据放进元组就锁住了"。看看实际情况：

```python
t = (1, [2, 3])       # 元组里装了一个列表

t[0] = 99             # ❌ TypeError: 'tuple' object does not support item assignment
```

改元素不行，这符合预期。但是：

```python
t = (1, [2, 3])
t[1].append(4)        # ✅ 居然成功了
print(t)              # => (1, [2, 3, 4])
```

元组的内容"看起来"变了。这算不算违背了不可变？**不算。** 回到第 3.3 节的标签比喻：

- 元组里存的不是那个列表本身，而是**指向列表的一个引用**（一根线）
- 不可变保证的是：**这根线不能改接到别的对象上**
- 但线那头的对象自己变胖变瘦，元组管不着

```python
t = (1, [2, 3])
print(id(t[1]))       # => 140234... （记住这个编号）
t[1].append(4)
print(id(t[1]))       # => 140234... 编号没变，还是同一个列表对象
```

所以准确的说法是：**元组保证"装的是哪几个对象"不变，不保证"这些对象的内容"不变。**

这个区别有实际后果——它决定了元组能不能当字典的键：

```python
print(hash((1, 2)))          # => 一个整数，成功
print(hash((1, [2, 3])))     # ❌ TypeError: unhashable type: 'list'
```

装了列表的元组**不可哈希**，因为内容会变，算出来的"指纹"就不可靠了。Python 干脆禁止对它求哈希。

> **记住这条**：**元组的不可变是"浅"的。** 想要真正锁死一份数据，元组里就只能装不可变的东西（数字、字符串、其他这样的元组）。
>
> **实践建议**：需要"一条不会被改的记录"时，用**只装不可变元素的元组**；如果记录里必须有一个可变的部分，那就别指望元组保护你，靠约定或者第 29 章的 `dataclass(frozen=True)`。

**示例 10-17　遍历一批记录**

这是元组在实战中最高频的用法：**用元组表示"一条记录"，用列表装"很多条记录"**。

```python
# 每个元组是一条记录，三个位置的含义固定：姓名、科目、分数
# 用列表装它们，因为记录条数是会变的
records = [
    ("小明", "数学", 92),
    ("小红", "语文", 88),
    ("小刚", "英语", 76),
]

# 解包遍历：直接把三个位置拆成三个有名字的变量，比 r[0] r[1] r[2] 好读得多
for name, subject, score in records:
    print(f"{name:<6}{subject:<6}{score:>4}")
```

```text
小明    数学      92
小红    语文      88
小刚    英语      76
```

对比一下不解包的写法，差别很明显：

```python
# ❌ 能跑，但三个月后你不记得 r[1] 是科目还是分数
for r in records:
    print(f"{r[0]:<6}{r[1]:<6}{r[2]:>4}")

# ✅ 解包后每个变量都有名字，代码自己说明了含义
for name, subject, score in records:
    print(f"{name:<6}{subject:<6}{score:>4}")
```

加上排序也很自然：

```python
# key 指定"按什么排"，这里 t[2] 是分数；reverse=True 表示从高到低
# key 的完整用法（含 lambda）在第 25 章，这里先照这个形式用
for name, subject, score in sorted(records, key=lambda t: t[2], reverse=True):
    print(f"{name:<6}{score:>4}")
```

```text
小明      92
小红      88
小刚      76
```

> **记住这条**：**元组表达"结构"，列表表达"集合"。** 一条记录里每个位置的含义不同（姓名、科目、分数）→ 元组；一堆地位相同的东西 → 列表。这个区分不是语法要求，但它能让代码的意图一眼看明白。
>
> **下一步的改进方向**：`t[2]` 这种下标还是不够直观。第 11 章的字典可以用 `r["score"]`，第 29 章的 `dataclass` 可以用 `r.score`——但元组胜在轻量，一条记录就三个字段时它仍然是好选择。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `(1)` 当成元组用 | 它是整数 `1`，`len()` 直接报错 | `(1,)` | 括号还有分组含义，靠逗号区分 |
| `t[0] = 9` | `TypeError: does not support item assignment` | 转成列表改，或重建元组 | 元组不可变 |
| `t.append(x)` | `AttributeError: no attribute 'append'` | 用列表，或 `t + (x,)` | 元组根本没有这个方法 |
| 以为放进元组数据就安全了 | 里面的列表被别处改掉 | 元组里只装不可变元素 | 不可变是浅层的 |
| `hash((1, [2]))` | `TypeError: unhashable type: 'list'` | 元组里改用元组代替列表 | 含可变元素则不可哈希 |
| 解包个数不匹配 | `ValueError: too many values to unpack` | 数清个数，或用 `*rest` 兜底 | 解包要求严格对应 |
| 以为 `t += (4,)` 是原地追加 | `id` 变了，别处引用的还是老元组 | 明确知道这是重新赋值 | 产生了新元组 |
| 用元组存"条数会变"的数据 | 每次增删都要重建，代码难看 | 改用列表 | 选型错误 |
| `first, *rest = t` 以为 `rest` 是元组 | 后续按元组用会出错 | 记住 `rest` 是**列表** | 星号解包固定收成列表 |

### 下一章预告

列表和元组都是"按位置取数据"——想拿分数得先知道它在第几位。可现实中我们更想说"给我小明的分数"。第 11 章讲字典，用**名字**取数据。同时你会看到本章 10.9 埋下的伏笔终于派上用场：字典的键必须可哈希，所以元组能当键，列表不能。

---

## 第 11 章　字典 dict ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会创建字典与嵌套字典；掌握三种取值方式并知道各自的失败行为；熟练遍历；会用字典做计数、分组、查找表三件事；知道遍历时不能增删 |
| 前置 | 第 10 章（尤其 10.9 可哈希） |
| 预计用时 | 5~6 小时（与第 9 章并列为阶段一最重的两章，建议分两次） |
| 可用语法边界 | 第 10 章全部 + 字典全部操作 + `sorted` / `zip` / `len`。**不得使用**：集合（第 12 章）、函数定义（第 14 章）、`try`（第 15 章）。**受限使用**：`key=lambda ...` 只用最简形式并注明"完整用法见第 25 章"（与 10.18 保持一致） |
| 目录调整 | 新增 11.11（遍历时增删直接报错）、11.20（按值排序）、11.22（视图对象是"活的"）、11.23（浅拷贝问题指路）。原目录 7 个小节撑不住这一章的实际内容 |
| 编写要点 | 本章有两处必须写透：① **三种取值方式的失败行为不同**（`[]` 抛异常、`get` 返回 `None`、`setdefault` 会顺手写进去），新手大量的 `KeyError` 都源于分不清这三个；② **字典的三大高频用法**（计数、分组、查找表），这是从"会语法"到"会用"的分水岭。其中"查找表替代 `if/elif`"是全书第一次让读者感受到"原来代码可以这样写"，篇幅要给足。另外要兑现 9-34 留下的"`count` 低效"问题 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 11.1 | 字典是什么 | 键值对的映射；**与列表的根本区别**：列表按**位置**取（`lst[0]`），字典按**名字**取（`d["name"]`）；**什么时候用**：数据有天然的标识符（学号、用户名、商品编码）时；**类比**：现实中的字典/电话簿——你不会数到第 37 页找"苹果"，你直接翻到"苹"字 | ★★★ |
| 11.2 | 创建字典 | 字面量 `{"a": 1}`、`dict(a=1)`、`dict(zip(keys, values))`、空字典 `{}`；**注意**：`{}` 是空字典**不是空集合**（第 12 章会对比） | ★★★ |
| 11.3 | 键的要求 | ① **唯一** ② **必须可哈希**（回收 10.9）；可以当键的：字符串、数字、元组（只装不可变元素的）；不能当键的：列表、字典、集合；**值没有任何限制**，可以是任何东西 | ★★★ |
| 11.4 | 键重复会怎样 | 字面量里写重复键不报错，**后面的静默覆盖前面的**；**什么时候会踩到**：从两份数据合并字典时丢数据而不知道 | ★★ |
| 11.5 | **取值三法及其失败行为** | `d[k]`：取不到抛 `KeyError`（**适合"这个键必须存在"的场景，让它早点崩比拿到错数据好**）；`d.get(k)`：取不到返回 `None`，`d.get(k, 默认值)` 返回默认值（**适合"可能没有"的场景**）；`d.setdefault(k, 默认值)`：取不到就**顺手把默认值写进字典**再返回（**有副作用，这是它最容易被误用的地方**）；三者必须放在一张表里对照 | ★★★ |
| 11.6 | `in` 判断的是**键** | `"a" in d` 查的是键，不是值；查值要 `in d.values()`；**这是高频误解**，必须演示 | ★★★ |
| 11.7 | 增和改是同一个语法 | `d[k] = v`：键不存在就新增，存在就覆盖；**好处**：不用先判断；**代价**：想"只在不存在时才写入"得用 `setdefault` 或先 `in` 判断，否则会误覆盖已有数据 | ★★★ |
| 11.8 | `update` 合并 | 用另一个字典或关键字参数批量更新；**冲突时后来的覆盖原有的**；3.9+ 还可以用 `d1 | d2` 合并出新字典 | ★★ |
| 11.9 | 删除 | `del d[k]`（不存在抛 `KeyError`）、`d.pop(k)`（返回被删的值）、`d.pop(k, 默认值)`（**不存在也不报错，最安全**）、`popitem()`（删最后一对）、`clear()` | ★★★ |
| 11.10 | 遍历 | **直接 `for k in d` 遍历的是键**（不是键值对，容易搞错）；`d.keys()` / `d.values()` / `d.items()`；`for k, v in d.items()` 是**最常用的形式**（回收第 10 章解包遍历） | ★★★ |
| 11.11 | **遍历时增删直接报错** | `RuntimeError: dictionary changed size during iteration`；**与列表对比**：列表边遍历边删是"静默出错"（第 9.18），字典是"直接报错"——**字典的行为其实更友好**，错误早暴露；**解法**：遍历 `list(d)` 或 `list(d.items())` 的副本 | ★★★ |
| 11.12 | 长度与空判断 | `len(d)`；`if d:` 判断非空（回收 7.5 真假规则） | ★★ |
| 11.13 | 字典推导式 | `{k: v for ...}`；典型用途：键值互换、批量转换值、过滤字典；**与列表推导式对照**（只是多了 `k:` 部分） | ★★ |
| 11.14 | 嵌套字典 | 字典里装字典/列表 → 就是 JSON 的结构（**这一点要点明**，第 17 章读 JSON 时会直接用到）；`d["a"]["b"]` 层层取值 | ★★★ |
| 11.15 | 嵌套取值的安全写法 | 层层 `get`：`d.get("a", {}).get("b")`；**为什么**：中间任何一层缺失都会 `KeyError`；**说实话**：套三层以上就该考虑重新设计数据结构了 | ★★ |
| 11.16 | **高频用法①：计数** | `d[x] = d.get(x, 0) + 1` 的标准写法；**必须对比第 9 章用 `count` 的写法**：`count` 每次都要扫一遍整个列表，1 万个词就是 1 万次全表扫描；字典计数只扫一遍；**这是全书第一次让读者体会"选对数据结构"的威力**；顺带提一句 `collections.Counter` 更简单（→ 第 34 章） | ★★★ |
| 11.17 | **高频用法②：分组** | `d.setdefault(key, []).append(item)`；**这是 `setdefault` 唯一真正好用的场合**；同样提一句 `defaultdict`（→ 第 34 章） | ★★★ |
| 11.18 | **高频用法③：查找表替代 `if/elif`** | 把"值 → 结果"的映射从一串 `elif` 改成一个字典 + `get`；**为什么更好**：新增分支只需加一行数据、不用改逻辑；**什么时候不适用**：分支条件是区间判断（`score >= 90`）而不是等值匹配；**这是"数据驱动"思维的入门** | ★★★ |
| 11.19 | 插入顺序 | Python **3.7+ 保证按插入顺序**遍历；**为什么要讲**：老代码里的 `OrderedDict` 就是为了这个（→ 第 34 章）；网上老教程说"字典无序"是过时信息 | ★★ |
| 11.20 | 按值排序 | `sorted(d.items(), key=lambda kv: kv[1], reverse=True)`；**注意结果是列表不是字典**（字典本身不"排序"，你排的是它的 items）；这是词频统计、排行榜的标准写法 | ★★★ |
| 11.21 | 字典与列表互转 | `list(d)` 得键列表、`list(d.items())` 得元组列表、`dict(元组列表)` 反向构造、`dict(zip(a, b))` | ★★ |
| 11.22 | 视图对象是"活的" | `keys()` / `values()` / `items()` 返回的不是快照，字典变了它们跟着变；**演示**：先拿到 `ks = d.keys()`，再往 `d` 里加东西，`ks` 也多了；**什么时候会踩到**：以为存下来的是当时的副本 | ★★ |
| 11.23 | 拷贝问题 | `d2 = d1` 是同一个字典（回收 9.21）；`d1.copy()` 是浅拷贝，**嵌套的内层还是共享的**；完整解释与 `deepcopy` 见 13.4 | ★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 11-1 | 创建一个字典 | A | 字面量、空字典、`dict(a=1)` | `{"name": "小明"}` |
| 11-2 | 按名字取值 | A | `d["name"]`；与列表 `lst[0]` 对照 | `d["name"]` |
| 11-3 | 键不存在会报错 | A | ❌ `KeyError`，看清报错里给出的键名 | `KeyError` |
| 11-4 | get 取不到不报错 | A | 返回 `None` | `d.get("x")` |
| 11-5 | get 带默认值 | A | `d.get("x", 0)` | `d.get(k, 0)` |
| 11-6 | 三种取值方式对照 | B | `[]` / `get` / `setdefault` 各自在键不存在时的行为，**并演示 setdefault 会改字典** | 对照表 |
| 11-7 | in 查的是键不是值 | A | ❌ `"小明" in d` 为 False → ✅ `in d.values()` | `in d.values()` |
| 11-8 | 新增和修改是同一句 | A | 同一个赋值语法两种效果 | `d[k] = v` |
| 11-9 | 键重复后面覆盖前面 | A | 字面量里写两个相同键，不报错 | 静默覆盖 |
| 11-10 | 什么能当键 | A | 字符串/数字/元组 ✅；列表 ❌ `unhashable type` | `d[(1,2)] = x` |
| 11-11 | 三种删除方式 | A | `del` / `pop` / `clear`，各自的返回值 | `d.pop(k)` |
| 11-12 | pop 带默认值最安全 | A | `d.pop(k, None)` 不存在也不报错 | `d.pop(k, None)` |
| 11-13 | update 合并字典 | B | 冲突时后来的赢；3.9+ 的 `|` 写法 | `d1.update(d2)` |
| 11-14 | 直接遍历得到的是键 | A | `for k in d` 打印出来是键 | `for k in d:` |
| 11-15 | keys / values / items | B | 三者输出对照 | `d.items()` |
| 11-16 | 遍历键值对 | A | `for k, v in d.items()` | `d.items()` |
| 11-17 | 遍历时删除直接报错 | B | ❌ `RuntimeError` → ✅ 遍历 `list(d)` 副本；与第 9 章列表的"静默出错"对比 | `list(d)` |
| 11-18 | 判断字典是不是空的 | A | `len(d)` / `if d:` | `if d:` |
| 11-19 | 字典推导式 | B | 键值互换、值批量转换、过滤 | `{v: k for k, v in d.items()}` |
| 11-20 | 用两个列表拼出字典 | B | `dict(zip(names, scores))` | `dict(zip(a, b))` |
| 11-21 | 嵌套字典：一份学生档案 | B | `d["小明"]["数学"]`；指出这就是 JSON 的形状 | `d[k1][k2]` |
| 11-22 | 嵌套取值别被中间层坑了 | B | ❌ 中间层缺失 `KeyError` → ✅ 层层 `get` | `d.get("a", {}).get("b")` |
| 11-23 | 词频计数：字典 vs count | B | 两种写法对照 + 效率解释，**兑现 9-34 的承诺** | `d[w] = d.get(w, 0) + 1` |
| 11-24 | 按班级分组学生 | B | `setdefault(key, []).append(x)` | `setdefault(k, []).append(v)` |
| 11-25 | 用字典干掉一串 elif | B | ❌ 五个 `elif` → ✅ 一个查找表 + `get` 兜底 | `handlers.get(cmd, 默认)` |
| 11-26 | 按值排序出排行榜 | B | `sorted(d.items(), key=lambda kv: kv[1], reverse=True)`；结果是列表 | `sorted(d.items(), key=...)` |
| 11-27 | keys() 是"活的" | B | 先取 `ks`，再改字典，`ks` 跟着变 | 视图对象 |
| 11-28 | 字典按插入顺序遍历 | A | 3.7+ 的保证；一句话说明网上"字典无序"是老信息 | 插入顺序 |
| 11-29 | 通讯录管理 | C | `while True` 菜单 + 增删查改 + 嵌套字典 + `items` 遍历显示 | 综合 |
| 11-30 | 词频统计完整版 | C | `split` + 清洗标点 + 字典计数 + 按值排序 + 输出 Top 5 | 综合 |
| 11-31 | 学生成绩管理 | C | 字典套字典 + 遍历统计各科平均分 + f-string 报表，**验收项目 A3 的核心结构** | 综合 |

### 样板示例

**示例 11-23　词频计数：字典 vs count**

第 9 章统计词频时我们用了 `count`，当时留了一句"这样写效率不好"。现在把两种写法并排放在一起。

先看第 9 章的写法：

```python
words = "苹果 香蕉 苹果 橘子 香蕉 苹果".split()

# ❌ 能出正确结果，但做了大量重复工作
for w in words:
    print(w, words.count(w))     # 每次 count 都要把整个列表从头扫一遍
```

```text
苹果 3
香蕉 2
苹果 3
橘子 1
香蕉 2
苹果 3
```

两个问题：**同一个词被重复统计了**（"苹果"算了三次），而且 `count` 每次都要扫一遍整个列表。6 个词还看不出什么，**如果是 1 万个词，就是 1 万次全表扫描，总共约 1 亿次比较**。

字典的写法只扫一遍：

```python
words = "苹果 香蕉 苹果 橘子 香蕉 苹果".split()

counter = {}                              # 键是词，值是出现次数
for w in words:
    counter[w] = counter.get(w, 0) + 1    # 关键一行：没见过这个词就从 0 起算

print(counter)                            # => {'苹果': 3, '香蕉': 2, '橘子': 1}
```

关键就是 `counter.get(w, 0) + 1` 这一行。拆开看：

```python
counter = {}
w = "苹果"

# 第一次遇到"苹果"：字典里没有，get 返回默认值 0，加 1 得 1
counter[w] = counter.get(w, 0) + 1
print(counter)          # => {'苹果': 1}

# 第二次遇到：get 返回已有的 1，加 1 得 2
counter[w] = counter.get(w, 0) + 1
print(counter)          # => {'苹果': 2}
```

如果不用 `get`，就得先判断，啰嗦得多：

```python
# 这样写也对，但多了三行
if w in counter:
    counter[w] = counter[w] + 1
else:
    counter[w] = 1
```

**为什么字典快？** 一句话：字典靠"键的哈希值"直接算出数据存在哪儿，不用逐个比较（这就是第 10.9 节"可哈希"的意义）。而列表的 `count` 只能一个一个看过去。前者的耗时几乎和数据量无关，后者和数据量成正比。

| 写法 | 1 万个词的大致工作量 | 结果形式 |
|---|---|---|
| `list.count()` 在循环里 | 约 1 亿次比较 | 有重复行，还要自己去重 |
| 字典计数 | 约 1 万次哈希查找 | 天然去重，键唯一 |

> **记住这条**：**"统计每个 X 出现多少次"——看到这句话就该想到字典。** 这是字典最经典的用途。
>
> **还能更简单**：标准库的 `collections.Counter` 一行就能做完（`Counter(words)`），第 34 章会讲。但现在**必须先自己写一遍**，否则你不会理解 `Counter` 到底替你做了什么。

**示例 11-25　用字典干掉一串 elif**

需求：根据用户输入的命令输出对应的说明。第 7 章我们只会这么写：

```python
cmd = input("请输入命令：").strip().lower()

# ❌ 能用，但每加一个命令就要改一次逻辑代码
if cmd == "add":
    print("添加一条记录")
elif cmd == "del":
    print("删除一条记录")
elif cmd == "list":
    print("列出全部记录")
elif cmd == "help":
    print("显示帮助")
elif cmd == "quit":
    print("退出程序")
else:
    print("未知命令")
```

这段代码的问题不在于它错，而在于**"命令有哪些"这件事被埋在了逻辑里**。想加一个命令，得改代码结构；想知道支持哪些命令，得把整段读一遍。

改成查找表：

```python
cmd = input("请输入命令：").strip().lower()

# 把"命令 → 说明"这份对应关系单独拿出来，它是数据，不是逻辑
actions = {
    "add": "添加一条记录",
    "del": "删除一条记录",
    "list": "列出全部记录",
    "help": "显示帮助",
    "quit": "退出程序",
}

# get 的第二个参数天然承担了原来 else 的角色
print(actions.get(cmd, "未知命令"))
```

好处非常具体：

| 方面 | `if/elif` 版 | 查找表版 |
|---|---|---|
| 加一个命令 | 插一段 `elif`，改逻辑 | 加一行数据 |
| 想知道支持哪些命令 | 通读整段代码 | 看字典，或 `print(list(actions))` 直接列出来 |
| 命令数量到 20 个 | 60 行 `elif` | 字典 20 行 + 逻辑 1 行 |
| 支持"命令列表"功能 | 得手工再维护一份 | `for c in actions:` 直接遍历 |

顺手就能加一个"列出所有命令"的功能，`if/elif` 版做不到这么轻松：

```python
print("支持的命令：", ", ".join(actions))     # 直接遍历字典的键
# => 支持的命令： add, del, list, help, quit
```

**什么时候不该用查找表？** 当分支条件不是"等值匹配"而是**区间判断**时：

```python
# ❌ 这种情况用不了查找表：键是范围而不是具体值
if score >= 90:
    grade = "优秀"
elif score >= 80:
    grade = "良好"
```

区间判断只能老实用 `if/elif`（第 7 章）。**判断标准很简单：能写成"某个值 → 某个结果"的一对一映射，就用字典；需要比大小、判范围，就用 `if`。**

> **记住这条**：**一长串 `elif` 里如果每个条件都是 `x == 某个值`，那它应该是一个字典。**
>
> **这个套路能走得更远**：字典的值不光能存字符串，还能存**函数**——那样连"要执行什么动作"都能查表决定，一个字典就是一个完整的命令分发器。这是验收项目 A2（命令行计算器）要用的核心技巧，第 14 章学完函数后就能写出来。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `d[k]` 取不存在的键 | `KeyError` | 用 `d.get(k, 默认值)`，或先 `if k in d` | `[]` 要求键必须存在 |
| `"值" in d` 想查值 | 永远是 `False` | `"值" in d.values()` | `in` 对字典查的是键 |
| 用 `setdefault` 只为了取值 | **字典被意外写入了新键** | 只取值用 `get` | `setdefault` 有副作用 |
| 遍历字典时增删 | `RuntimeError: dictionary changed size` | 遍历 `list(d)` 或 `list(d.items())` | 迭代中结构不能变 |
| `for k, v in d:` | `ValueError: too many values to unpack` | `for k, v in d.items():` | 直接遍历只给键 |
| 用列表当键 | `TypeError: unhashable type: 'list'` | 改用元组当键 | 键必须可哈希 |
| 字面量里写重复键 | 前面的**静默丢失** | 检查数据来源 | 后者覆盖前者 |
| `d[k] = v` 想"只在不存在时写" | 覆盖了已有数据 | 先 `if k not in d`，或用 `setdefault` | 赋值不区分新增和修改 |
| 以为 `sorted(d)` 能排出字典 | 得到的是**键的列表** | `sorted(d.items(), key=...)` 得元组列表 | 字典本身不排序 |
| 把 `keys()` 当快照存起来 | 字典变了它也变了 | `list(d.keys())` 固化成列表 | 视图对象是"活的" |
| `d2 = d1` 以为复制了 | 改 `d2` 影响 `d1` | `d1.copy()`（浅）或 `deepcopy`（深，13.4） | 同一个对象两个名字 |
| `d1.copy()` 后改嵌套内层 | 原字典的内层也变了 | `copy.deepcopy(d1)` | 浅拷贝只复制第一层 |

### 下一章预告

字典的键有两个特点：**唯一**、**可哈希**。如果我们只要键、不要值呢？那就是集合——一个专门用来"去重"和"求交集并集"的容器。第 12 章会很轻松，它是阶段一里最短的一章。

---

## 第 12 章　集合 set ★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会创建集合并知道空集合的正确写法；会用集合去重（含保序去重）；熟练四种集合运算并能用它解决"找共同/找差异"的实际问题；知道集合的成员测试比列表快 |
| 前置 | 第 11 章 |
| 预计用时 | 2~3 小时（阶段一最短的一章） |
| 可用语法边界 | 第 11 章全部 + 集合全部操作 + `frozenset`。**不得使用**：函数定义（第 14 章）、`try`（第 15 章）、`timeit`（第 40 章，效率对比只用文字说明和 `time` 粗测） |
| 目录调整 | 新增 12.6（去重保序的实用技巧）、12.7（成员测试的效率）、12.11（运算符与方法的区别）、12.13（数据对比场景）。原目录 5 个小节只讲了语法，没讲"集合到底解决什么问题" |
| 编写要点 | 本章语法极简单，讲解的重点应该放在**什么时候该想到用集合**——两个信号：① 出现"去重"② 出现"两批数据比对"。12.13 的新旧名单对比是最能体现集合价值的例子，很多人写了几年代码还在用双层循环做这件事，这个示例要写足。另外"集合 = 只有键没有值的字典"这个类比要在开篇就给出，能省一半解释 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 12.1 | 集合是什么 | 无序、元素不重复；**最好的类比**：就是"只有键、没有值的字典"——这解释了它的三个特性（元素唯一、必须可哈希、查找极快）；**什么时候用**：去重、判断"在不在里面"、两批数据比对 | ★★★ |
| 12.2 | 创建集合 | `{1, 2, 3}`、`set(可迭代对象)`；**空集合必须写 `set()`**，`{}` 是空字典——**这是集合唯一的语法坑**，必须专门演示 | ★★★ |
| 12.3 | 元素必须可哈希 | 能放：数字、字符串、元组；不能放：列表、字典、集合（回收 10.9、11.3）；**为什么**：和字典键同一个原因——要靠哈希值快速定位 | ★★★ |
| 12.4 | 无序意味着什么 | **不能索引**（`s[0]` 报 `TypeError`）、不能切片、遍历顺序不保证；**边界情况**：小整数集合打印出来看着像有序，那是巧合，不能依赖 | ★★★ |
| 12.5 | 增删 | `add`（加一个）、`update`（批量加，能吃任何可迭代对象）、`remove`（不存在抛 `KeyError`）、`discard`（**不存在也不报错**）、`pop`（**随机删一个并返回，很少用且危险**）、`clear` | ★★★ |
| 12.6 | **去重** | `set(lst)` 是最常用的去重手段；**代价：丢失原有顺序**；**保序去重的标准技巧**：`list(dict.fromkeys(lst))`（利用 3.7+ 字典保持插入顺序，回收 11.19）；这两种写法要放在一起对照，让读者按需选择 | ★★★ |
| 12.7 | 成员测试快 | `x in s` 靠哈希直接定位，`x in lst` 要从头扫；**数据量小时没差别，几万条时差距是几十倍到几百倍**；**什么时候值得转成集合**：要反复判断"在不在"的时候（只判断一次就不值得转） | ★★★ |
| 12.8 | 四种集合运算 | `&` 交集（都有的）、`|` 并集（合起来）、`-` 差集（**我有他没有的，有方向**）、`^` 对称差（只在一边出现的）；**必须配一张对照表 + 具体例子**，光讲名字记不住；**最容易错的是差集方向**：`a - b` 和 `b - a` 完全不同 | ★★★ |
| 12.9 | 子集与超集 | `<=` 子集、`>=` 超集、`<` / `>` 真子集/真超集、`isdisjoint()` 判断毫无交集；**典型场景**：权限检查（"用户拥有的权限"是否覆盖"这个操作需要的权限"） | ★★ |
| 12.10 | 运算符 vs 方法 | `a & b` 要求**两边都是集合**；`a.intersection(lst)` **可以吃列表**；同理 `|` 对 `union`、`-` 对 `difference`；**什么时候用方法**：另一边是列表且不想多写一次 `set()` | ★★ |
| 12.11 | 集合推导式 | `{x for x in ...}`；注意 `{}` 是字典所以空推导式没有歧义（有内容就能区分） | ★★ |
| 12.12 | 遍历集合 | `for x in s`；**顺序不保证，需要固定顺序就 `sorted(s)`**（返回列表） | ★★ |
| 12.13 | **实战场景：两批数据比对** | 新旧名单找"新增/删除/不变"——三行差集搞定；**必须对比双层循环的写法**：`for x in new: for y in old:` 不仅慢，而且容易漏情况；**这是集合最有价值的用途**，也是很多人不知道能这么做的地方 | ★★★ |
| 12.14 | `frozenset` | 不可变的集合，**因此可哈希**，能当字典的键、能放进别的集合里；**什么时候用**：需要"一组元素"作为字典键时（如用"标签组合"做键）；日常少见，认得出即可 | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 12-1 | 创建一个集合 | A | `{1, 2, 3}`；`set("hello")` 拆成字符并去重 | `{1, 2, 3}` |
| 12-2 | 空集合的正确写法 | A | ❌ `s = {}` 得到字典（`type()` 验证）→ ✅ `set()` | `set()` |
| 12-3 | 重复元素自动消失 | A | `{1, 1, 2}` → `{1, 2}` | 自动去重 |
| 12-4 | 集合不能按位置取 | A | ❌ `s[0]` → `TypeError: not subscriptable` | 无序 |
| 12-5 | 顺序不保证 | A | 打印含字符串的集合，顺序与写入不同 | 无序 |
| 12-6 | add 和 update | A | 加一个 vs 批量加；`update` 能吃列表和字符串 | `s.update(lst)` |
| 12-7 | remove 和 discard | A | ❌ `remove` 删不存在的抛 `KeyError` → ✅ `discard` 静默 | `s.discard(x)` |
| 12-8 | pop 是随机删的 | A | 每次运行删掉的可能不同；说明它几乎没有正当用途 | `s.pop()` |
| 12-9 | 集合不能装列表 | A | ❌ `{[1,2]}` → `unhashable type: 'list'` → ✅ 装元组 | `{(1,2)}` |
| 12-10 | 一行给列表去重 | A | `list(set(lst))`；**顺序变了** | `list(set(lst))` |
| 12-11 | 去重还想保持原顺序 | B | ❌ `set` 打乱顺序 → ✅ `list(dict.fromkeys(lst))` | `dict.fromkeys(lst)` |
| 12-12 | in 对集合和列表的速度差别 | B | 用 `time` 粗测十万次查找的耗时差；说明什么时候值得转集合 | `x in s` |
| 12-13 | 交集：两边都有的 | A | `a & b` | `a & b` |
| 12-14 | 并集：合起来 | A | `a | b` | `a \| b` |
| 12-15 | 差集有方向 | A | `a - b` 与 `b - a` 结果不同，**并排打印对比** | `a - b` |
| 12-16 | 对称差：只在一边的 | A | `a ^ b`；等价于 `(a-b) | (b-a)` | `a ^ b` |
| 12-17 | 四种运算一张表 | B | 同一组数据跑四种运算，输出对照 | 对照表 |
| 12-18 | 运算符和方法的区别 | B | ❌ `a & [1,2]` 报错 → ✅ `a.intersection([1,2])` 可以 | `a.intersection(lst)` |
| 12-19 | 子集与权限检查 | B | `需要的权限 <= 用户的权限` | `required <= owned` |
| 12-20 | 判断两组数据完全不相干 | A | `a.isdisjoint(b)` | `isdisjoint` |
| 12-21 | 集合推导式 | B | 从一批数据里提取不重复的属性 | `{x[1] for x in records}` |
| 12-22 | 遍历集合并固定顺序 | B | `for x in sorted(s)` | `sorted(s)` |
| 12-23 | 新旧名单对比 | C | 三行差集算出"新增/删除/保留"，对比双层循环写法 | `new - old` |
| 12-24 | 两个班的选课分析 | C | 交集找共同课、差集找特有课、并集算总课程数 | 综合 |
| 12-25 | frozenset 长什么样 | A | 作字典键；一瞥即可 | `frozenset({1,2})` |

### 样板示例

**示例 12-11　去重还想保持原顺序**

`set()` 去重最简单，但有个代价：

```python
names = ["小明", "小红", "小明", "小刚", "小红"]

print(list(set(names)))     # => ['小刚', '小明', '小红']   顺序乱了！
```

顺序变了，而且**每次运行可能不一样**（取决于哈希值）。如果这份名单是要打印给人看的，或者顺序本身有意义（比如报名先后），这就不能接受。

保序去重的标准写法是借用字典：

```python
names = ["小明", "小红", "小明", "小刚", "小红"]

# dict.fromkeys 用这些元素当键造一个字典，重复的键自动合并
# 字典从 3.7 起保持插入顺序（见 11.19），所以顺序就保住了
unique = list(dict.fromkeys(names))
print(unique)               # => ['小明', '小红', '小刚']   顺序与首次出现一致
```

`dict.fromkeys(names)` 造出的是 `{'小明': None, '小红': None, '小刚': None}`，我们只要它的键。可以打印出来看清楚：

```python
print(dict.fromkeys(names))     # => {'小明': None, '小红': None, '小刚': None}
```

两种写法怎么选：

| 需求 | 写法 | 说明 |
|---|---|---|
| 只是去重，不在乎顺序 | `set(lst)` | 最快最短；后续还要做集合运算时更要用它 |
| 去重且保持首次出现的顺序 | `list(dict.fromkeys(lst))` | 慢一点点，但顺序可预测 |
| 去重后要排序输出 | `sorted(set(lst))` | 顺序由排序规则决定，与原顺序无关 |

> **记住这条**：**`set` 去重会打乱顺序，`dict.fromkeys` 去重会保住顺序。** 给人看的数据基本都要保序。
>
> **一个常见的隐蔽 bug**：有人用 `list(set(lst))` 去重后直接输出，本地跑了几次看着顺序正常就上线了，结果换台机器顺序就变了。集合的顺序**从来没有承诺过**，看着有序纯属巧合。

**示例 12-23　新旧名单对比**

这是集合最实用的场景：数据同步时要知道"哪些是新增的、哪些被删了、哪些没动"。

先看很多人的写法：

```python
old = ["小明", "小红", "小刚"]
new = ["小红", "小刚", "小华", "小丽"]

# ❌ 双层循环：能出结果，但代码长、慢、还容易漏情况
added = []
for n in new:
    found = False
    for o in old:
        if n == o:
            found = True
            break
    if not found:
        added.append(n)
print(added)          # => ['小华', '小丽']
```

九行代码只算出了"新增"，还得再写九行算"删除"。用集合三行搞定全部：

```python
old = {"小明", "小红", "小刚"}
new = {"小红", "小刚", "小华", "小丽"}

added = new - old         # 新名单有、老名单没有 → 新增
removed = old - new       # 老名单有、新名单没有 → 删除
kept = old & new          # 两边都有 → 保留不变

print(f"新增 {len(added)} 人：{sorted(added)}")     # => 新增 2 人：['小丽', '小华']
print(f"删除 {len(removed)} 人：{sorted(removed)}")  # => 删除 1 人：['小明']
print(f"保留 {len(kept)} 人：{sorted(kept)}")        # => 保留 2 人：['小刚', '小红']
```

三个运算的方向不能记混，画出来就很清楚：

```text
        old                    new
   ┌─────────────┐      ┌─────────────┐
   │   小明       │      │             │
   │        ┌────┼──────┼────┐        │
   │        │ 小红 小刚  │        │
   │        └────┼──────┼────┘        │
   │             │      │  小华 小丽   │
   └─────────────┘      └─────────────┘
     old - new            new - old
     （删除）              （新增）
          old & new（保留）在中间重叠区
```

**注意 `sorted()` 那一步是必须的**——集合无序，直接打印顺序不可预测，给人看的输出一定要排序。

如果原始数据是列表（通常是从文件或数据库读来的），转一下就行：

```python
old_list = ["小明", "小红", "小刚"]
new_list = ["小红", "小刚", "小华", "小丽"]

added = set(new_list) - set(old_list)     # 临时转成集合做运算
print(sorted(added))                       # => ['小华', '小丽']
```

> **记住这条**：**看到"两批数据要比对"，第一反应就该是集合的 `-` 和 `&`，而不是双层循环。**
>
> **实际用途举例**：同步数据库时算增量、对比配置文件的差异、检查哪些必填字段没提供（`必填字段 - 已提供字段`）、找出重复上报的记录。这些活儿用集合都是三五行的事。
>
> **一个前提**：集合运算会去重，也会丢掉顺序和"出现次数"。如果你需要"小明出现了两次"这种信息，那要用第 11 章的字典计数，不是集合。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `s = {}` 想建空集合 | 得到的是**空字典** | `s = set()` | `{}` 被字典占用了 |
| `s[0]` | `TypeError: not subscriptable` | 集合不能索引，需要顺序就 `sorted(s)` | 集合无序 |
| 依赖集合的遍历顺序 | 本地正常、换机器就变 | 要固定顺序就 `sorted()` | 顺序从未被承诺 |
| `list(set(lst))` 后直接输出 | 顺序不可预测 | `list(dict.fromkeys(lst))` | 同上 |
| `{[1, 2]}` | `unhashable type: 'list'` | 改用元组 `{(1, 2)}` | 元素必须可哈希 |
| `s.remove(x)` 删不存在的元素 | `KeyError` | 用 `s.discard(x)` | `remove` 要求元素存在 |
| 差集方向搞反 | 算出来的"新增"其实是"删除" | 记住 `a - b` 是"a 有 b 没有" | 差集有方向 |
| `a & [1, 2]` | `TypeError` | `a.intersection([1, 2])` | 运算符要求两边都是集合 |
| 用 `s.pop()` 想删指定元素 | 删掉的是随机一个 | 用 `remove` / `discard` | `pop` 不接受参数 |
| 用集合处理需要计数的场景 | 丢失了"出现几次"的信息 | 改用字典计数（11.16） | 集合只记"有没有" |
| 只判断一次就把大列表转集合 | 转换本身的开销超过了收益 | 反复判断才值得转 | 转换要遍历一遍 |

### 下一章预告

四个容器都学完了：列表、元组、字典、集合。第 13 章不教新容器，做两件事：**一是给你一张选型表**，遇到需求知道该用哪个；**二是把从第 3 章一路埋到现在的那颗地雷挖出来**——`b = a` 之后改 `b`，`a` 为什么会跟着变，以及怎么真正复制一份数据。这一章不学，你迟早要为它熬一个通宵。

---

## 第 13 章　容器综合与拷贝语义 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 遇到需求能选对容器；理清可迭代/序列/映射三个概念；**彻底搞懂 `b = a` 为什么会互相影响，会用浅拷贝和深拷贝** |
| 前置 | 第 9~12 章（四个容器都学完） |
| 预计用时 | 4~5 小时 |
| 可用语法边界 | 第 12 章全部 + `copy` 模块 + `any` / `all`。**不得使用**：函数定义（第 14 章）、`try`（第 15 章）。**预支**：`import copy`（13.12 起，注明"第 18 章详解 import"） |
| 目录调整 | 新增 13.4（`any` / `all` 对空序列的反直觉行为）、13.10（三种拷贝的对照实验）、13.13（`deepcopy` 的代价）、13.15（列表套字典这一最常见数据形状的拷贝陷阱）。原目录只列了拷贝的四个要点，没有覆盖"什么时候需要深拷贝"和"深拷贝的代价" |
| 编写要点 | **本章是阶段一的技术制高点。** 从 3.3 的"标签不是盒子"、9.21 的"`b = a` 现象"、9.20 的 `[[0]*3]*3`、10.4 的"元组不可变边界"、11.23 的"`copy()` 只复制一层"，五条伏笔全部在 13.5~13.15 收网。写作时要显式回收这些引用，让读者感到"原来前面那些怪事是同一件事"。<br><br>**关键约束**：只讲"怎么判断、怎么做"，**不讲引用计数和 GC 原理**（那在专项 B）。判断标准就一句话：改了 b 会不会影响 a |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 13.1 | 四大容器选型 | 一张对照表：可变性、有序性、能否重复、按什么取值、典型用途；再给一个**决策流程**：要不要键值对应？→ 数据会不会变？→ 要不要去重？→ 要不要保持顺序？**必须配"同一个需求用四种容器各写一遍"的对照示例**，光看表记不住 | ★★★ |
| 13.2 | 三个概念的层次 | **可迭代对象**（能一个个交出元素：字符串、四大容器、`range`、文件对象）⊃ **序列**（有顺序、能按下标取：字符串、列表、元组）；**映射**（键值对：字典）；集合两者都不是；**为什么要分清**：决定了哪些操作能用——能不能索引、能不能切片、`in` 查的是什么 | ★★ |
| 13.3 | 通用函数一览 | `len` / `min` / `max` / `sum` / `sorted` / `reversed` / `enumerate` / `zip` / `in`——**同一批函数对四种容器都能用**，这是 Python 的一致性设计带来的好处；**边界情况**：`sum` 只能加数字；`min/max` 对空容器报错；`sorted(字典)` 排的是键；`reversed` 不能用于集合（无序） | ★★★ |
| 13.4 | `any` 和 `all` | `any` 有一个真就为真、`all` 全部为真才为真；**反直觉的边界**：空容器时 `any([])` 是 `False`，`all([])` 是 **`True`**；**为什么**：`all` 的语义是"没有反例"，空容器里当然找不到反例；**什么时候会踩到**：校验一批数据时，数据为空却通过了检查 | ★★ |
| 13.5 | 回到"标签"模型 | 复述 3.3：变量是贴在对象上的标签，`=` 是贴标签不是复制；**为什么 Python 这样设计**：复制一份大列表要付出时间和内存代价，绝大多数情况下你并不需要复制，语言默认选了便宜的那个 | ★★★ |
| 13.6 | `b = a` 的真相 | `id()` 验证是同一个对象；改 `b` 就是改那个对象，`a` 看到的当然也变了；**这一节要把第 9、11 章遇到的现象统一解释掉** | ★★★ |
| 13.7 | 不可变对象为什么"看起来没事" | 数字、字符串、元组也共享，但**没有任何方法能改它们**，所以永远不会互相影响；`y = x; y = 20` 是"把 y 撕下来贴到 20 上"，不是改了 10；**结论：不可变对象从不需要拷贝** | ★★★ |
| 13.8 | 浅拷贝的三种写法 | 切片 `a[:]`（只有序列能用）、构造函数 `list(a)` / `dict(a)` / `set(a)`、`copy.copy(a)`（通用）；三者效果**完全一样**，选哪个看习惯和容器类型 | ★★★ |
| 13.9 | **浅拷贝的边界** | 浅拷贝只复制**最外面那一层**；里面装的对象还是共享的；**必须用"列表套列表"演示**：改副本的内层，原件的内层也变了；**为什么叫"浅"**：只到第一层就停了 | ★★★ |
| 13.10 | 深拷贝 | `copy.deepcopy(a)` 递归复制每一层，副本和原件彻底无关；**什么时候用**：数据有嵌套结构且要独立修改 | ★★★ |
| 13.11 | 三种情况对照实验 | 用同一份嵌套数据分别做 `b = a`、浅拷贝、深拷贝，各改一次，打印三次结果 + `id` 对比；**这个实验做完，拷贝这件事就通了**，是本章的核心示例 | ★★★ |
| 13.12 | 判断依据 | 唯一要记的判断句：**"改了 b 会不会影响 a？"**——不会 → 什么都不用做（不可变）／改第一层就够 → 浅拷贝／要改嵌套的内层 → 深拷贝 | ★★★ |
| 13.13 | **深拷贝的代价** | ① 慢（要递归遍历整个结构）② 内存翻倍 ③ 有些对象拷不了或拷了没意义（文件句柄、数据库连接、锁）；**结论：不要养成"不确定就 deepcopy"的习惯**，先想清楚要不要改内层 | ★★ |
| 13.14 | 什么时候真的需要深拷贝 | 三个真实场景：① **配置模板**：一份默认配置复制多份分别改 ② **撤销功能**：保存操作前的快照 ③ **传给不信任的代码前保护数据**；都要给具体例子 | ★★ |
| 13.15 | 最常见的数据形状 | **列表套字典**（`[{...}, {...}]`）——从 JSON、CSV、数据库读出来的数据几乎都是这个形状（第 17 章会大量出现）；它的拷贝**必须用 `deepcopy`**，因为要改的往往正是内层字典 | ★★★ |
| 13.16 | 函数参数传递预告 | 把列表传给函数，函数里改了它，外面也变——**这是同一个原理的第三次出现**；完整讨论在 14.6 | ★★ |
| 13.17 | 推导式统一回顾 | 列表 `[...]`、字典 `{k: v ...}`、集合 `{...}` 三种推导式并排对照，**只差括号和有没有冒号**；再提一句 `(...)` 是生成器表达式（→ 第 33 章），**不是"元组推导式"**（元组没有推导式，这是个常见误解） | ★★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 13-1 | 同一个需求，四种容器各写一遍 | B | "记录班上学生"分别用 list / tuple / dict / set 实现，对比各自的取值方式和局限 | 选型对照 |
| 13-2 | 选型决策流程 | B | 四个问题依次问下来，落到具体容器 | 决策表 |
| 13-3 | len / min / max / sum 通用 | A | 同一批函数用在四种容器上 | `len(x)` |
| 13-4 | in 对四种容器分别查什么 | A | 列表查元素、字典**查键**、集合查元素、字符串查子串 | `in` |
| 13-5 | sorted 对字典排的是键 | A | `sorted(d)` 得键列表 | `sorted(d)` |
| 13-6 | 空容器的边界 | A | `sum([])` 得 0；`max([])` ❌ ValueError | 边界情况 |
| 13-7 | any 和 all | A | 基本用法 | `all(lst)` |
| 13-8 | 空列表让 all 返回 True | A | `all([])` 是 `True`，`any([])` 是 `False`；解释"没有反例" | 反直觉边界 |
| 13-9 | b = a 是同一个对象 | A | `id(a) == id(b)`、`a is b`（回收 3.3） | `a is b` |
| 13-10 | 改 b，a 也变了 | A | ❌ 现象重现（回收 9.21、11.23） | 共享修改 |
| 13-11 | 数字和字符串为什么没事 | A | `y = x; y = 20` 后 `x` 不变；解释"撕下标签重贴" | 不可变 |
| 13-12 | 浅拷贝三种写法 | B | `a[:]` / `list(a)` / `copy.copy(a)` 效果相同 | `copy.copy(a)` |
| 13-13 | 浅拷贝管不到里面那一层 | B | ❌ 改副本的内层，原件也变 → ✅ `deepcopy` | 浅拷贝边界 |
| 13-14 | deepcopy 彻底断开 | B | 改内层互不影响 | `copy.deepcopy(a)` |
| 13-15 | 三种情况对照实验 | B | `b = a` / 浅拷贝 / 深拷贝各改一次，三份结果 + id 对照表 | **本章核心** |
| 13-16 | 字典和集合的拷贝 | B | `d.copy()` / `dict(d)` / `s.copy()`；同样只到第一层 | `d.copy()` |
| 13-17 | `[[0]*3]*3` 的真相 | B | 回收 9.20：三行是同一个列表；`id` 验证；正确写法用推导式 | `id(grid[0])` |
| 13-18 | 元组不可变的边界 | B | 回收 10.4：元组里的列表能改，因为引用没变 | `t[1].append(x)` |
| 13-19 | deepcopy 有多慢 | B | 用 `time` 粗测大嵌套结构的深拷贝耗时，与浅拷贝对比 | 代价 |
| 13-20 | 配置模板复制 | B | 一份默认配置深拷贝出多份分别改 | `deepcopy(默认配置)` |
| 13-21 | 列表套字典的拷贝陷阱 | B | ❌ 浅拷贝后改某条记录，原数据被改 → ✅ `deepcopy` | 最常见形状 |
| 13-22 | 三种推导式并排看 | B | 列表 / 字典 / 集合推导式写同一份数据 | 统一对照 |
| 13-23 | `(x for x in ...)` 不是元组推导式 | A | `print` 出来是 generator 对象；一句话指向第 33 章 | 生成器表达式 |
| 13-24 | 学生档案模板批量生成 | C | 深拷贝模板 + 分别修改 + 验证互不影响 | 综合 |
| 13-25 | 撤销功能雏形 | C | 每次操作前 `deepcopy` 存快照，输入 `undo` 恢复上一步 | 综合 |

### 样板示例

**示例 13-13　浅拷贝管不到里面那一层**

先确认浅拷贝在**单层**数据上是好用的：

```python
a = [1, 2, 3]
b = a[:]              # 切片拷贝：造一个新列表

b.append(4)
print(a)              # => [1, 2, 3]      原件没受影响
print(b)              # => [1, 2, 3, 4]
print(a is b)         # => False          确实是两个对象了
```

但数据一嵌套，情况就变了：

```python
a = [[1, 2], [3, 4]]      # 外层列表里装了两个内层列表
b = a[:]                  # 还是切片拷贝

b[0].append(99)           # 注意：改的是 b 里面那个内层列表

print(a)                  # => [[1, 2, 99], [3, 4]]   ！原件的内层也变了
print(b)                  # => [[1, 2, 99], [3, 4]]
```

为什么？因为切片拷贝**只复制了外层那个列表**。新列表里装的仍然是**指向同样两个内层列表的引用**：

```python
a = [[1, 2], [3, 4]]
b = a[:]

print(a is b)             # => False    外层：两个不同的列表 ✅
print(a[0] is b[0])       # => True     内层：还是同一个对象 ❌
```

画出来是这样：

```text
浅拷贝之后：

  a ──→ [ ●, ● ]
          │  │
          │  └──→ [3, 4] ←──┐
          └─────→ [1, 2] ←─┐│
                            ││
  b ──→ [ ●, ● ]───────────┘│
          └────────────────-┘

  外层是两个不同的列表，但里面指向的是同样的内层列表
```

`copy.copy()` 和 `list(a)` 完全一样，都是浅拷贝：

```python
import copy

a = [[1, 2], [3, 4]]
b = copy.copy(a)          # 和 a[:] 效果相同
b[0].append(99)
print(a)                  # => [[1, 2, 99], [3, 4]]   一样的问题
```

要彻底断开，用 `deepcopy`：

```python
import copy

a = [[1, 2], [3, 4]]
b = copy.deepcopy(a)      # 递归复制每一层

b[0].append(99)
print(a)                  # => [[1, 2], [3, 4]]       ✅ 原件安然无恙
print(b)                  # => [[1, 2, 99], [3, 4]]
print(a[0] is b[0])       # => False                  连内层都是独立的了
```

**三种做法一张表看清**（这张表要背下来）：

| 写法 | 外层 | 内层 | 什么时候够用 |
|---|---|---|---|
| `b = a` | 共享 | 共享 | 只是想换个名字用，不打算改 |
| `b = a[:]` / `list(a)` / `copy.copy(a)` | 独立 | **共享** | 数据只有一层，或只改最外层的增删 |
| `b = copy.deepcopy(a)` | 独立 | 独立 | 数据有嵌套，且要改里面的内容 |

> **记住这条**：**判断只需问一句——"我要改的东西在第几层？"** 只在第一层增删元素，浅拷贝够；要改嵌套里面的内容，必须深拷贝。
>
> **回收前面的伏笔**：第 9.20 节 `[[0]*3]*3` 改一行三行全变——现在你知道原因了，`*` 复制的就是引用，三行是同一个列表。第 11.23 节 `d.copy()` 之后内层还共享——同一件事。第 10.4 节元组里的列表能改——也是同一件事。**这五处怪现象，本质上都是"引用被复制了，对象没有被复制"。**

**示例 13-24　学生档案模板批量生成**

一个必须用深拷贝的真实场景：有一份默认档案模板，要为每个学生生成一份并分别填写。

```python
import copy

# 默认模板：注意它是嵌套的——里面有字典，还有列表
template = {
    "name": "",
    "scores": {"数学": 0, "语文": 0},
    "notes": [],
}

# ❌ 错误做法：直接用赋值或浅拷贝
a = template                      # 同一个对象
b = template.copy()               # 浅拷贝：scores 和 notes 还是共享的

a["name"] = "小明"
b["name"] = "小红"
b["scores"]["数学"] = 95          # 改的是共享的那个 scores 字典
b["notes"].append("需要补课")      # 改的是共享的那个 notes 列表

print(template)
# => {'name': '小明', 'scores': {'数学': 95, '语文': 0}, 'notes': ['需要补课']}
#    模板被污染了：名字被 a 改了，成绩和备注被 b 改了
```

模板被彻底污染，后面再拿它生成档案全是错的。而且这种 bug 特别隐蔽——`name` 是第一层，`b.copy()` 保护住了；`scores` 在第二层，没保护住。**同一份代码里一半正常一半出错，最难查。**

正确做法：

```python
import copy

template = {
    "name": "",
    "scores": {"数学": 0, "语文": 0},
    "notes": [],
}

# ✅ 每份档案都从模板深拷贝一份，彼此完全独立
a = copy.deepcopy(template)
b = copy.deepcopy(template)

a["name"] = "小明"
a["scores"]["数学"] = 88

b["name"] = "小红"
b["scores"]["数学"] = 95
b["notes"].append("需要补课")

print(a)         # => {'name': '小明', 'scores': {'数学': 88, '语文': 0}, 'notes': []}
print(b)         # => {'name': '小红', 'scores': {'数学': 95, '语文': 0}, 'notes': ['需要补课']}
print(template)  # => {'name': '', 'scores': {'数学': 0, '语文': 0}, 'notes': []}
#                     ✅ 模板干干净净
```

**这个模式在实战中很常见**，值得记住它的名字叫"原型复制"：

| 场景 | 为什么需要深拷贝 |
|---|---|
| 默认配置生成多个实例 | 各实例要独立修改嵌套的配置项 |
| 撤销/回滚功能 | 快照必须和当前数据彻底断开 |
| 单元测试的测试数据 | 每个测试用例都要拿到干净的初始数据 |
| 把数据交给别处处理前 | 防止对方在你不知道的情况下改了它 |

> **记住这条**：**只要一份数据要"复制出多份分别修改"，而且它有嵌套结构，就用 `deepcopy`。**
>
> **但别滥用**：`deepcopy` 要递归遍历整个结构，数据量大时明显慢，内存也翻倍。判断标准始终是 13.12 那一句——**我要改的东西在第几层**。只改第一层，浅拷贝就够，不必付深拷贝的代价。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `b = a` 以为复制了 | 改 `b` 影响 `a` | 按需用浅拷贝或深拷贝 | `=` 是贴标签 |
| 嵌套数据用浅拷贝 | 改内层时原件也变 | `copy.deepcopy(a)` | 浅拷贝只到第一层 |
| `d.copy()` 后改内层字典 | 原字典的内层也变 | `deepcopy` | 同上 |
| `[[0]*3]*3` | 改一行三行全变 | `[[0]*3 for _ in range(3)]` | `*` 复制引用 |
| 不确定就 `deepcopy` | 程序变慢、内存翻倍 | 先问"要改第几层" | 深拷贝代价高 |
| `deepcopy` 含文件句柄/连接的对象 | 报错或拷出无效副本 | 只拷数据部分 | 有些对象不可复制 |
| `all([])` 以为是 False | 空数据通过了校验 | 先判断非空 | `all` 是"没有反例" |
| `sum(["a","b"])` | `TypeError` | 拼字符串用 `"".join()` | `sum` 只加数字 |
| `reversed(集合)` | `TypeError: not reversible` | 先 `sorted()` 变成列表 | 集合无序 |
| 以为 `(x for x in lst)` 是元组推导式 | 得到 generator 对象 | 要元组写 `tuple(x for x in lst)` | 元组没有推导式 |
| 把列表传进函数后被改 | 外面的数据变了 | 函数内先拷贝，或不改传入的数据（14.6） | 传的是引用 |

### 下一章预告

到这里，数据（四个容器）和流程（判断、循环）都齐了，你已经能写出几十行能干实事的脚本。但代码开始出现重复——同一段逻辑复制粘贴到三个地方。第 14 章讲函数，把重复的代码收成一个能反复调用的整体。顺便，本章的"传引用"问题会以第三种面貌再出现一次：**把列表传进函数，函数改了它，外面也变**。

---

## 第 14 章　函数基础 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会定义带参数和返回值的函数；分清位置参数与关键字参数；避开可变默认值陷阱；理解参数传的是引用；掌握作用域与 `UnboundLocalError`；会把函数存进字典做命令分发 |
| 前置 | 第 13 章（尤其 13.5 拷贝语义） |
| 预计用时 | 6~7 小时（全书最重的一章之一，建议分两到三次） |
| 可用语法边界 | 第 13 章全部 + `def` / `return` / 全部参数形式 / `global` / `nonlocal`（只提不展开）/ docstring / `help()`。**不得使用**：`try`（第 15 章）、`*args` / `**kwargs`（第 25 章）、装饰器（第 31 章）、类（第 26 章）。**受限使用**：`lambda` 与类型注解**只做一瞥**，各一个示例并指路 |
| 目录调整 | 从第 7 章移入"**提前返回**式扁平化"（14.17）；新增 14.14（`UnboundLocalError` 这个经典报错）、14.10（如何保护调用者的数据）、14.16（函数存进字典做命令分发——把 11.18 的查找表升级成真正的分发器）。原目录 10 个小节漏了 `UnboundLocalError`，而它是新手最困惑的报错之一 |
| 编写要点 | 本章有三处必须重笔：<br>① **可变默认值陷阱**（14.8）——现象极诡异，原因很简单（默认值只在定义时算一次），必须画出"默认值被记在函数身上"的图景；<br>② **`UnboundLocalError`**（14.14）——"函数里只要给变量赋过值，它就是局部的"，这条规则不讲清，读者会反复撞墙；<br>③ **函数存进字典**（14.16）——这是第 11 章查找表的终极形态，也是验收项目 A2 的核心，读者会第一次感到"代码可以这样组织"。<br><br>另外 14.9 是"传引用"这件事的**第三次**出现（3.3 标签 → 13.5 拷贝 → 本章参数），要显式回收，让读者确认这是同一个原理 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 14.1 | 为什么需要函数 | 三个理由，按重要性排：① **给一段逻辑起个名字**（`calc_bmi()` 比五行公式好读）② 消除重复代码 ③ 能单独测试和替换；**反面例子**：把第 9 章的成绩统计脚本贴三遍，改一处要改三处 | ★★★ |
| 14.2 | 定义与调用 | `def 名字(参数):` + 缩进体；**必须先定义后调用**（调用写在定义前面会 `NameError`）；**为什么**：Python 是从上往下执行的，`def` 那一行执行时才创建函数 | ★★★ |
| 14.3 | `return` 的行为 | 返回值并**立即结束函数**（后面的代码不再执行）；**没有 `return` 时返回 `None`**（回收 3.5、9.14）；**容易错在哪**：函数里只 `print` 没 `return`，调用方拿到 `None` 却以为拿到了结果——**这个错必须演示** | ★★★ |
| 14.4 | 返回多个值 | `return a, b` 的本质是返回元组（回收 10.13 的 `divmod`）；配合解包 `x, y = f()` | ★★★ |
| 14.5 | 位置参数与关键字参数 | 按顺序传 vs 按名字传；**关键字参数的真正价值是可读性**：`create_user("小明", True, False, 3)` 完全看不懂，`create_user("小明", is_admin=True, ...)` 一目了然；**经验规则**：超过两个参数、或有布尔参数时，调用方就该用关键字传 | ★★★ |
| 14.6 | 默认参数 | `def f(x, n=2)`；**必须放在无默认值的参数之后**，否则直接 `SyntaxError`；**为什么**：否则 Python 无法确定位置参数该对应谁 | ★★★ |
| 14.7 | 默认值只计算一次 | `def f(t=time.time())` 里的时间是**定义时**的时间，不是调用时的；**这是理解下一条陷阱的钥匙**，必须先讲清楚 | ★★★ |
| 14.8 | **可变默认值陷阱** | `def f(items=[])` 的列表**在多次调用间共享**，会越攒越多；**原因**：默认值只在 `def` 执行时创建一次，之后一直是那一个对象；**正确写法**：默认值写 `None`，函数体内 `if items is None: items = []`；**这是 Python 最著名的坑**，任何一本书都会讲，必须讲透 | ★★★ |
| 14.9 | 参数传的是引用 | 传不可变对象（数字、字符串）→ 函数内的重新赋值不影响外面；传可变对象（列表、字典）→ **函数内的修改会影响外面**；**这是 13.5 的第三次现身**，用同一套 `id()` 手法验证 | ★★★ |
| 14.10 | 怎么保护调用者的数据 | 三条策略：① **约定优先**：函数名写清楚会不会改（`sort_scores` vs `sorted_scores`）② 函数内先浅/深拷贝再改 ③ **推荐**：不改传入的数据，返回新对象；**并说明代价**：拷贝有开销，数据大时要权衡 | ★★★ |
| 14.11 | 作用域基础 | 函数内定义的变量是局部的，外面看不见（`NameError`）；函数内**可以读**外面的全局变量；查找顺序简版：**局部 → 全局 → 内置**（完整的 LEGB 含闭包层，留到第 31 章） | ★★★ |
| 14.12 | 遮蔽 | 局部变量与全局同名时，函数内看到的是局部的；**顺带一个真实事故**：给变量取名 `list`、`sum`、`str` 会遮蔽内置函数，之后调用 `list()` 就报错 | ★★★ |
| 14.13 | **`UnboundLocalError`** | 现象：函数里明明有全局变量 `count`，写 `count = count + 1` 却报错；**原因**：**函数里只要对某个名字赋过值，Python 就在整个函数内把它当局部变量**，于是等号右边的 `count` 还没赋值就被读了；**解法**：`global`（不推荐）或改用参数+返回值（推荐）；**这个报错必须专门讲**，它和 `NameError` 长得像但成因完全不同 | ★★★ |
| 14.14 | `global` 及其危害 | 语法怎么写，然后讲**为什么尽量别用**：谁都能改的变量导致出错时无法定位；`nonlocal` 只提一句（第 31 章闭包再讲）；**替代方案**：把状态作为参数传入、结果用返回值传出 | ★★ |
| 14.15 | docstring | 三引号写在函数第一行；`help(函数名)` 和 `函数名.__doc__` 能读到；**内容写什么**：一句话说明做什么、参数含义、返回什么、什么情况会报错；**不写"怎么实现"**（那是注释的事） | ★★★ |
| 14.16 | **函数是对象** | 可以赋值给变量、可以作参数传递、可以放进列表和字典；**杀手级用法**：`{"add": do_add, "del": do_del}` 把 11.18 的查找表升级成**命令分发器**——连"执行什么动作"都能查表决定；**注意**：存函数时**不要加括号**（`do_add` 是函数本身，`do_add()` 是调用结果，这是高频错误） | ★★★ |
| 14.17 | 提前返回扁平化 | 从第 7 章移来：把"层层嵌套的合法性检查"改写成"不合法就 `return`"，主流程始终留在最外层缩进；**对照演示三层嵌套 vs 三次提前返回** | ★★★ |
| 14.18 | 一个函数只做一件事 | 三条可操作的判断标准：① 函数名能否用**一个动词短语**说清（说不清就是干了太多事）② 参数是否超过 4 个 ③ 能否脱离上下文单独测试；**经验值**：函数体超过屏幕一屏（约 30 行）就该考虑拆 | ★★★ |
| 14.19 | 命名约定 | 动词开头（`get_` / `calc_` / `is_` / `has_` / `load_` / `save_`）；返回布尔值的用 `is_` / `has_` 开头；**函数名说清"做什么"，不说"怎么做"** | ★★ |
| 14.20 | 递归入门 | 两要素：终止条件 + 向终止条件靠近的递归调用；用阶乘举例；`RecursionError` 与默认约 1000 层的限制；**说实话**：日常业务代码 95% 的递归都能用循环写得更清楚，递归真正好用的场景是**树形结构**（遍历目录、解析嵌套 JSON），到第 16、17 章会遇到 | ★ |
| 14.21 | `lambda` 一瞥 | 一行匿名函数；只给 `key=lambda x: x[1]` 这一个用法（回收 10.18、11.20 用过的形式）；完整讨论见第 25 章 | ★ |
| 14.22 | 类型注解一瞥 | `def add(a: int, b: int) -> int:`；**强调它不影响运行**，只是给人和工具看的；完整讨论见第 33 章 | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 14-1 | 第一个函数 | A | `def` + 调用 | `def greet():` |
| 14-2 | 带参数的函数 | A | 形参与实参 | `def greet(name):` |
| 14-3 | 用 return 交出结果 | A | 返回值可以被接收、参与运算 | `return a + b` |
| 14-4 | 只 print 不 return 的坑 | A | ❌ 调用方拿到 `None` → ✅ 用 `return` | `return` vs `print` |
| 14-5 | return 之后的代码不执行 | A | `return` 立即结束函数 | 立即结束 |
| 14-6 | 一次返回两个值 | A | `return a, b` + 解包接收 | `x, y = f()` |
| 14-7 | 调用写在定义前面 | A | ❌ `NameError` → ✅ 先定义 | 执行顺序 |
| 14-8 | 位置传参和按名字传参 | A | 两种调用方式等价 | `f(n=3)` |
| 14-9 | 关键字参数救可读性 | B | ❌ `f("小明", True, False, 3)` → ✅ 全部写成关键字 | 可读性 |
| 14-10 | 默认参数 | A | 不传就用默认值 | `def f(x, n=2):` |
| 14-11 | 默认参数放错位置 | A | ❌ `SyntaxError: non-default argument follows default` | 参数顺序 |
| 14-12 | 默认值只算一次 | B | `def f(t=time.time())` 每次调用时间都一样 | 定义时求值 |
| 14-13 | **可变默认值陷阱** | B | ❌ 列表越攒越多 → ✅ 默认值用 `None` | `if x is None:` |
| 14-14 | 传数字进函数：外面不受影响 | B | 函数内重新赋值，外面的值不变 | 不可变 |
| 14-15 | 传列表进函数：外面跟着变 | B | ❌ 函数偷偷改了调用者的数据 | 传引用 |
| 14-16 | 三种保护数据的做法 | B | 命名约定 / 内部拷贝 / 返回新对象，三段对照 | `lst = lst[:]` |
| 14-17 | 局部变量出了函数就不存在 | A | ❌ `NameError` | 作用域 |
| 14-18 | 函数里能读全局变量 | A | 读可以，不用声明 | 读全局 |
| 14-19 | **UnboundLocalError** | B | ❌ `count = count + 1` 报错 → ✅ 参数+返回值 或 `global` | 赋值使其局部化 |
| 14-20 | global 怎么写、为什么别用 | B | 写法 + 一个"谁都能改导致查不出问题"的例子 | `global count` |
| 14-21 | 用返回值代替 global | B | ✅ 推荐写法对照 | `count = incr(count)` |
| 14-22 | 别拿 list、sum 当变量名 | A | ❌ 遮蔽内置函数后 `list()` 报错 | 遮蔽 |
| 14-23 | 给函数写说明书 | B | docstring + `help(f)` + `f.__doc__` | `"""..."""` |
| 14-24 | 函数可以赋值给变量 | A | `f = greet` 后 `f()` 能用；**不要加括号** | `f = greet` |
| 14-25 | 把函数传给另一个函数 | B | `sorted(records, key=取分数函数)` | 函数作参数 |
| 14-26 | **函数存进字典：命令分发器** | B | 回收 11.18，把查找表升级成能执行动作的分发器 | `actions[cmd]()` |
| 14-27 | 提前返回，告别三层嵌套 | B | ❌ 三层嵌套检查 → ✅ 三次提前 `return` | 提前返回 |
| 14-28 | 一个函数只做一件事 | B | ❌ 一个函数又读输入又算又打印 → ✅ 拆成三个 | 单一职责 |
| 14-29 | 递归算阶乘 | B | 终止条件 + 递归调用；同时给循环版对照 | `return n * f(n-1)` |
| 14-30 | 递归太深会报错 | A | `RecursionError`；`sys.getrecursionlimit()` | `RecursionError` |
| 14-31 | lambda 长什么样 | A | `key=lambda t: t[1]`；一瞥即可 | `lambda` |
| 14-32 | 给函数加类型注解 | A | 注解不影响运行，`f("a", "b")` 照样能跑 | `def f(a: int) -> int:` |
| 14-33 | 把成绩统计脚本重构成函数 | C | 回收第 9 章：拆成 `load` / `calc` / `report` 三个函数 | 综合重构 |
| 14-34 | 命令行计算器 | C | 函数 + 字典分发 + 输入解析，**验收项目 A2 的核心** | 综合 |
| 14-35 | 可复用的输入校验函数 | C | 把 8-27 的重试逻辑包成函数，供多处调用；**指出它还有一个缺陷**：靠 `isdigit` 不能处理小数和负数（→ 第 15 章） | 综合 |

### 样板示例

**示例 14-13　可变默认值陷阱**

这是 Python 最著名的坑。先看现象：

```python
# ❌ 错误：默认参数用了可变对象（列表）
def add_item(item, box=[]):
    box.append(item)
    return box

print(add_item("苹果"))      # => ['苹果']              看着正常
print(add_item("香蕉"))      # => ['苹果', '香蕉']       ！上次的苹果还在
print(add_item("橘子"))      # => ['苹果', '香蕉', '橘子'] ！越攒越多
```

每次调用都应该得到一个只装一样东西的新列表，结果它把历史全记住了。

**原因只有一句话：默认值在 `def` 执行时创建一次，之后永远是那一个对象。**

`def` 这一行执行的时候，Python 就把 `[]` 创建好，**挂在函数身上**了。之后每次调用不传 `box`，用的都是挂在函数上那个**同一个列表**：

```python
def add_item(item, box=[]):
    box.append(item)
    return box

# 默认值被记在函数对象上，可以直接看到它
print(add_item.__defaults__)     # => ([],)
add_item("苹果")
print(add_item.__defaults__)     # => (['苹果'],)   默认值本身被改了！
add_item("香蕉")
print(add_item.__defaults__)     # => (['苹果', '香蕉'],)
```

画出来是这样：

```text
def 执行时：
    add_item ──→ 默认值 box ──→ [ ]      ← 只创建这一次

第 1 次调用（不传 box）：
    box 指向那个列表 → append("苹果") → ['苹果']

第 2 次调用（不传 box）：
    box 还是指向同一个列表 → append("香蕉") → ['苹果', '香蕉']
```

**正确写法**：默认值用 `None`，在函数体里创建新列表——因为**函数体是每次调用都执行的**：

```python
# ✅ 正确：默认值写 None，每次调用时才创建新列表
def add_item(item, box=None):
    if box is None:        # 用 is None 判断，不要用 if not box（空列表也会被当成 None）
        box = []           # 这一行每次调用都会执行，所以每次都是新列表
    box.append(item)
    return box

print(add_item("苹果"))      # => ['苹果']
print(add_item("香蕉"))      # => ['香蕉']     ✅ 干净的新列表
print(add_item("橘子"))      # => ['橘子']

# 传入自己的列表也照样能用
my_box = ["已有的"]
print(add_item("新的", my_box))   # => ['已有的', '新的']
```

**哪些默认值有这个问题？** 判断标准就是第 13 章的老问题——**它可变吗**：

| 默认值 | 安全吗 | 原因 |
|---|---|---|
| `0`、`""`、`None`、`True`、`(1, 2)` | ✅ 安全 | 不可变，改不了 |
| `[]`、`{}`、`set()` | ❌ 危险 | 可变，会被累积修改 |
| `time.time()`、`datetime.now()` | ❌ 危险 | 只在定义时算一次，永远是启动时刻 |

> **记住这条**：**默认参数只能用不可变的值。** 需要可变默认值时，写 `None`，在函数体里创建。
>
> **顺带一个真实事故**：`def log(msg, timestamp=datetime.now())` —— 这个函数记录的时间戳永远是**程序启动的时刻**，所有日志时间都一样。程序跑一小时都不会变。正确写法同样是默认 `None`，函数体里再取当前时间。

**示例 14-26　函数存进字典：命令分发器**

第 11.18 节我们用字典替掉了一串 `elif`，但那时字典的值只能是字符串——只能"查出说明"，不能"执行动作"。现在函数可以当值了：

```python
def do_add():
    print("执行：添加一条记录")

def do_list():
    print("执行：列出全部记录")

def do_help():
    print("可用命令：" + ", ".join(actions))     # 直接从字典里读出所有命令名

def do_unknown():
    print("未知命令，输入 help 查看帮助")

# 命令 → 函数 的映射表。注意值是函数名，后面【不加括号】
# 加了括号就变成"先调用再把结果存进去"，那样字典里存的是 None
actions = {
    "add": do_add,
    "list": do_list,
    "help": do_help,
}

cmd = input("请输入命令：").strip().lower()

# get 取出对应的函数（取不到就用兜底函数），再加括号调用它
actions.get(cmd, do_unknown)()
#                            ↑ 这一对括号才是真正的调用
```

运行：

```text
请输入命令：list
执行：列出全部记录

请输入命令：xyz
未知命令，输入 help 查看帮助

请输入命令：help
可用命令：add, list, help
```

**最容易错的地方是括号**：

```python
# ❌ 错误：加了括号，字典构造时函数就被调用了，存进去的是返回值 None
actions = {"add": do_add()}      # 屏幕上立刻打印了"执行：添加一条记录"
print(actions)                    # => {'add': None}
actions["add"]()                  # ❌ TypeError: 'NoneType' object is not callable

# ✅ 正确：不加括号，存的是函数本身
actions = {"add": do_add}
print(actions)                    # => {'add': <function do_add at 0x...>}
actions["add"]()                  # ✅ 现在才调用
```

**记住这个区分**：`do_add` 是"这个函数"，`do_add()` 是"调用它得到的结果"。要把函数存起来备用，就不能加括号。

这个模式的好处，和 11.18 一脉相承但更强：

| 需求 | `if/elif` 版 | 分发器版 |
|---|---|---|
| 加一个命令 | 插一段 `elif` | 写个函数 + 字典加一行 |
| 列出所有命令 | 手工再维护一份 | `", ".join(actions)` |
| 每个命令的逻辑变复杂 | `elif` 块越来越长 | 各自在自己的函数里，互不干扰 |
| 单独测试某个命令 | 做不到，逻辑埋在分支里 | 直接调 `do_add()` |

> **记住这条**：**"根据某个值决定执行哪段逻辑"——用字典存函数，不要用一长串 `elif`。**
>
> **这就是验收项目 A2（命令行计算器）的核心结构**：`{"+" : add, "-": sub, "*": mul, "/": div}`，取出函数直接算，加新运算符只需加一行。
>
> **这个模式还能走更远**：把函数放进字典是"用数据描述行为"的开始。第 31 章的装饰器、第 34 章的 `functools.partial`，都建立在"函数是普通对象"这个事实上。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 函数里只 `print` 不 `return` | 调用方拿到 `None` | 用 `return` 交出结果 | 没有 return 就返回 None |
| `def f(x=[])` | 多次调用间数据累积 | 默认值用 `None`，体内创建 | 默认值只创建一次 |
| `def f(n=2, x)` | `SyntaxError` | 有默认值的参数放后面 | 位置无法对应 |
| 函数里改传入的列表 | 调用者的数据被悄悄改了 | 内部拷贝或返回新对象 | 参数传的是引用 |
| `count = count + 1`（count 是全局的） | `UnboundLocalError` | 用参数+返回值，或 `global` | 赋值使变量局部化 |
| 变量取名 `list` / `sum` / `str` | 之后调用 `list()` 报错 | 换名字 | 遮蔽了内置函数 |
| 字典里存 `do_add()` | 存进去的是 `None`，调用时 TypeError | 存 `do_add`（不加括号） | 括号表示"立即调用" |
| 调用写在 `def` 前面 | `NameError` | 先定义后调用 | 自上而下执行 |
| 递归忘了终止条件 | `RecursionError` | 补上终止条件 | 无限递归 |
| 一个函数干五件事 | 没法复用、没法测试、名字取不出来 | 按 14.18 三条标准拆开 | 单一职责 |
| `if not box:` 判断默认值 | 传进来的空列表被当成"没传" | `if box is None:` | 空列表也是 Falsy |
| 函数名用名词（`data()`） | 读代码时看不出它会做事 | 动词开头（`load_data()`） | 命名约定 |

### 下一章预告

函数让代码有了结构，但它还很脆弱：用户输一个字母，`int()` 就崩了；文件不存在，程序直接退出。第 15 章讲异常处理——**让程序在意外面前站得住**。学完之后，我们会回头把 8-27 和 14-35 那个"靠 `isdigit` 凑合"的输入校验重写一遍，代码会更短也更可靠。这一章学完，阶段一就结束了，你就该动手做验收项目 A 了。

---

## 第 15 章　异常处理入门 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 分清语法错误与运行时异常；会用 `try/except/else/finally`；会捕获特定异常并拿到错误信息；懂两条铁律（不写裸 `except`、不吞异常）；会用 `raise`；理解 Python 偏好"先做后补救"的 EAFP 风格 |
| 前置 | 第 14 章 |
| 预计用时 | 4~5 小时 |
| 可用语法边界 | 第 14 章全部 + `try` / `except` / `else` / `finally` / `raise` / `assert`。**不得使用**：自定义异常类（需要 `class`，第 22、26 章）、`with`（第 16 章）、`logging`（第 21 章）、`raise ... from`（第 22 章） |
| 目录调整 | 新增 15.11（`except Exception` 与裸 `except` 的区别：`Ctrl+C` 不该被吞掉）、15.14（**EAFP vs LBYL** 两种风格）、15.16（`try` 块要小）、15.17（`assert` 的正确用途与危险）。原目录 7 个小节没有覆盖"为什么 Python 鼓励用异常而不是先检查"，而这正是 Python 与 C#/Java 习惯差别最大的地方 |
| 编写要点 | 本章是阶段一的收尾，有三个重点：<br>① **兑现两次承诺**：8-27 和 14-35 那个靠 `isdigit` 凑合的输入校验，本章用 `try` 重写，代码更短且支持负数小数——**这个对照必须写足，它让读者亲眼看到异常处理的价值**；<br>② **EAFP 思维**（15.14）：Python 的习惯是"直接干，出错再说"，而不是"先检查一遍再干"。从 C#/Java 转来的人会本能地先检查，这一节要把两种风格并排对比；<br>③ **两条铁律**（15.10~15.12）：裸 `except` 会吞掉 `Ctrl+C`，`except: pass` 会让 bug 彻底消失——**这两个反面示范要给足篇幅**。<br><br>章末要加**阶段一总结**和**验收项目 A 的开工指引** |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 15.1 | 语法错误 vs 运行时异常 | 语法错误：**代码根本跑不起来**，一行都不执行；运行时异常：**跑到那一行才崩**，前面的代码已经执行完了（回收 2.9 的观察）；**为什么要分清**：前者靠读代码修，后者靠 `try` 处理 | ★★★ |
| 15.2 | 异常的传播 | 不处理的异常会**一层层往上抛**，直到程序退出并打印 Traceback；**这就解释了 Traceback 为什么是多行的**（回收 2.10）：每一行是它经过的一层 | ★★★ |
| 15.3 | `try` / `except` 最简形式 | `try` 里放可能出错的代码，`except` 里放出错后的应对；**关键认知**：`try` 里一旦出错，**剩下的语句直接跳过**，不会执行完 | ★★★ |
| 15.4 | 捕获特定异常 | `except ValueError:` 只管这一种；**为什么要指定类型**：捕获范围越大，越容易把不该处理的错误也吃掉，问题就被藏起来了；**边界情况**：捕获的类型不对，异常照样逃出去 | ★★★ |
| 15.5 | 捕获多种异常 | `except (ValueError, TypeError):` 一并处理；或写多个 `except` 分别处理 | ★★★ |
| 15.6 | `except` 的顺序 | **具体的写前面，宽泛的写后面**；把 `except Exception` 写在最前面会让后面所有 `except` 永远不执行（不报错，静默失效） | ★★★ |
| 15.7 | `except ... as e` | 拿到异常对象；`str(e)` 看错误描述、`type(e).__name__` 看异常类名；**什么时候需要**：要把原因显示给用户或记录下来时 | ★★★ |
| 15.8 | `else` 子句 | `try` **没出错**才执行；**什么时候用**：把"成功后才做的事"和"可能出错的事"分开，让 `try` 块保持最小 | ★★ |
| 15.9 | `finally` | **无论如何都会执行**（出错、没出错、甚至中途 `return`）；**用途**：清理资源（关文件、关连接）；**说明**：第 16 章的 `with` 就是它的语法糖，学了 `with` 之后 `finally` 用得会少很多 | ★★★ |
| 15.10 | **铁律一：不写裸 `except`** | `except:` 会捕获**一切**，包括 `Ctrl+C`（`KeyboardInterrupt`）和内存耗尽（`MemoryError`）；**后果**：用户按 `Ctrl+C` 都停不下程序；**正确写法**：`except Exception:`——它不包括 `KeyboardInterrupt` 和 `SystemExit`；**为什么**：这两个不是"程序出错"，是"外部要求退出" | ★★★ |
| 15.11 | **铁律二：不吞异常** | `except Exception: pass` 是最危险的三行代码：出了错什么都不说，程序继续用错误的数据跑下去；**为什么危险**：bug 从"崩溃"变成"结果悄悄错了"，后者难查一百倍；**最低要求**：至少 `print` 出来（有了第 21 章的 `logging` 后改成 `logger.exception`） | ★★★ |
| 15.12 | `raise` 主动抛出 | `raise ValueError("消息")`；**什么时候用**：函数收到不合理的参数、遇到无法继续的状态时，**主动崩比返回一个错误值更好**（返回值会被忽略，异常不会）；空 `raise` 可以在 `except` 里**重新抛出**当前异常（处理一半，剩下交给上层） | ★★★ |
| 15.13 | 异常不是流程控制 | **反例**：用 `try/except IndexError` 代替"判断列表长度"；**判断标准**：这个情况是"意料之外"还是"日常分支"？日常分支用 `if`，意料之外用异常 | ★★★ |
| 15.14 | **EAFP vs LBYL** | LBYL（Look Before You Leap，先检查再动手）：`if os.path.exists(f): open(f)`；EAFP（Easier to Ask Forgiveness than Permission，先动手出错再说）：`try: open(f) except FileNotFoundError:`；**Python 偏好 EAFP**，两个理由：① 检查和使用之间状态可能已经变了（文件刚检查完就被删了）② 少一次重复工作；**但不是绝对**：纯数据判断（键在不在字典里）用 `if` 更清楚；**这一节对有 C#/Java 背景的读者尤其重要** | ★★★ |
| 15.15 | 常见内置异常速查 | 回收 2.11 的十个报错，这次补上"该怎么处理"一列：`ValueError`（转换失败 → 提示用户重输）、`KeyError` / `IndexError`（取值越界 → 用 `get` 或先判断）、`TypeError`（类型不对 → 一般是代码 bug，不该捕获而该修）、`ZeroDivisionError`、`FileNotFoundError`（第 16 章）；**重点提醒**：`TypeError` / `NameError` / `AttributeError` **通常是代码写错了，不该用 `try` 掩盖**，应该去改代码 | ★★★ |
| 15.16 | `try` 块要小 | 只把**真正可能出错的那一两行**放进 `try`；**为什么**：块越大，越搞不清是哪一行出的错，也越容易误捕获别的错误；配合 `else` 把后续逻辑挪出去 | ★★★ |
| 15.17 | `assert` 的正确用途 | 语法与用途：检查"绝不该发生"的内部状态，帮助开发时尽早发现 bug；**危险**：`python -O` 运行时 `assert` **会被整体移除**！所以**绝不能用 `assert` 做用户输入校验或权限检查**——那些必须用 `if` + `raise` | ★★ |
| 15.18 | 自定义异常预告 | 一句话：需要表达"我的业务出了什么问题"时可以定义自己的异常类，需要 `class`，见第 22 章 | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 15-1 | 语法错误和运行时异常的区别 | A | 前者一行都不执行；后者前面的 `print` 已经输出了 | 对照 |
| 15-2 | 不处理异常：程序崩在这一行 | A | 崩溃 + Traceback，后面的代码不执行 | 传播 |
| 15-3 | 第一个 try / except | A | `int("abc")` 被接住，程序继续跑 | `try:` / `except:` |
| 15-4 | 只捕获想管的那一种 | A | `except ValueError` | `except ValueError:` |
| 15-5 | 捕获的类型不对 | A | ❌ 抓 `TypeError` 却来了 `ValueError`，照样崩 | 类型要对上 |
| 15-6 | 多个 except 分别处理 | B | `ValueError` 和 `ZeroDivisionError` 各给不同提示 | 多分支 |
| 15-7 | 一个 except 管多种 | A | `except (ValueError, TypeError):` | 元组形式 |
| 15-8 | except 顺序错了会静默失效 | B | ❌ `except Exception` 写最前面 → ✅ 具体的在前 | 顺序 |
| 15-9 | 拿到错误信息给用户看 | B | `except ValueError as e: print(e)` | `as e` |
| 15-10 | else：成功了才做的事 | B | 与把代码堆在 `try` 里对照 | `else:` |
| 15-11 | finally：一定会执行 | B | 正常、出错两种情况下都执行 | `finally:` |
| 15-12 | 连 return 都拦不住 finally | B | 函数里 `return` 之后 `finally` 仍执行 | 执行顺序 |
| 15-13 | 裸 except 把 Ctrl+C 也吞了 | B | ❌ 死循环里裸 `except` 导致按不停 → ✅ `except Exception` | 铁律一 |
| 15-14 | except: pass 让 bug 消失 | B | ❌ 静默失败后数据是错的 → ✅ 至少打印出来 | 铁律二 |
| 15-15 | 主动抛出异常 | B | 参数不合理时 `raise ValueError` | `raise ValueError(...)` |
| 15-16 | 处理一半，剩下交给上层 | B | `except` 里记录后用空 `raise` 重新抛出 | `raise` |
| 15-17 | 别用异常代替 if | B | ❌ 用 `except IndexError` 判断列表空 → ✅ `if lst:` | 流程控制 |
| 15-18 | 两种风格：先检查 vs 先动手 | B | 字典取值和文件打开各写两种，说明各自适用场合 | EAFP / LBYL |
| 15-19 | try 块塞太多东西 | B | ❌ 十行全包起来，出错不知在哪 → ✅ 只包一行 + `else` | 粒度 |
| 15-20 | assert 与它的危险 | B | 用途示例 + `python -O` 后断言消失的演示 | `assert` |
| 15-21 | **用 try 重写输入校验** | C | **兑现 8-27 / 14-35 的承诺**：比 `isdigit` 版更短，且支持负数和小数 | `try: float(s)` |
| 15-22 | 安全转换工具函数 | C | `to_int(s, default=0)`：转不成就返回默认值，供全程序复用 | 综合 |
| 15-23 | 健壮的成绩录入程序 | C | 函数 + `while` + `try` + 字典存储，**验收项目 A3 的最终形态** | 综合 |

### 样板示例

**示例 15-21　用 try 重写输入校验（兑现承诺）**

第 8 章我们用 `isdigit()` 做校验，当时留了一句"它挡不住负数和小数"。先看那个版本的问题：

```python
# ❌ 第 8 章的版本：isdigit 只认纯数字字符
for s in ["35", "-5", "3.5", "abc", ""]:
    print(f"{s!r:8} isdigit={s.isdigit()}")
```

```text
'35'     isdigit=True
'-5'     isdigit=False      ← 负数被误判为非法
'3.5'    isdigit=False      ← 小数被误判为非法
'abc'    isdigit=False
''       isdigit=False
```

想支持负数和小数，用 `isdigit` 就得开始打补丁：去掉负号再判断、数一数有几个小数点、小数点不能在开头也不能在结尾……越写越长，而且总有漏的（`1e5`、`+5`、`1_000` 都是合法数字）。

**换个思路：不预先判断，直接交给 Python 转，转不成它会告诉我们。**

```python
while True:
    raw = input("请输入体重（公斤）：").strip()

    try:
        weight = float(raw)          # 直接转，成不成让 float 自己说
    except ValueError:               # 转不成会抛 ValueError，正好接住
        print("请输入一个数字，例如 65 或 65.5")
        continue                     # 重新问

    if weight <= 0:                  # 格式没问题了，再查业务规则（这属于日常分支，用 if）
        print("体重必须大于 0")
        continue

    break

print(f"记录体重：{weight} 公斤")
```

跑一遍：

```text
请输入体重（公斤）：abc
请输入一个数字，例如 65 或 65.5
请输入体重（公斤）：-5
体重必须大于 0
请输入体重（公斤）：65.5
记录体重：65.5 公斤
```

**对照一下两个版本**：

| | `isdigit` 版 | `try` 版 |
|---|---|---|
| 支持小数 | ❌ 要自己打补丁 | ✅ 天然支持 |
| 支持负数 | ❌ 要自己打补丁 | ✅ 天然支持 |
| 支持 `1e5`、`+5` 这类写法 | ❌ 几乎不可能覆盖全 | ✅ 天然支持 |
| 代码行数 | 越写越多 | 固定三行 |
| 谁来定义"什么是合法数字" | 你自己（容易漏） | Python 的 `float()`（权威且完整） |

**注意这段代码的一个设计**：格式校验用 `try`，业务规则（体重必须大于 0）用 `if`。这不是随意的——

- "用户输了字母" 是**意料之外**的输入错误 → 用异常
- "体重是负数" 是**预期内**的一种情况，你本来就要判断它 → 用 `if`

这个区分就是 15.13 说的"异常不是流程控制"。

> **记住这条**：**要判断"这段文本能不能转成数字"，不要自己写规则，直接 `try` 转一下。** 同理，判断"能不能转成日期""是不是合法 JSON"也都是这个套路。
>
> **这段代码可以直接包成函数**（示例 15-22），以后全程序复用：
> ```python
> def ask_float(prompt, min_value=None):
>     """反复询问直到用户输入一个合法的浮点数。min_value 为 None 时不检查下界。"""
>     while True:
>         try:
>             value = float(input(prompt).strip())
>         except ValueError:
>             print("请输入一个数字")
>             continue
>         if min_value is not None and value < min_value:
>             print(f"不能小于 {min_value}")
>             continue
>         return value        # 用 return 直接结束函数，比 break 更干净
> ```

**示例 15-18　两种风格：先检查 vs 先动手**

这是 Python 和 C# / Java 习惯差别最大的地方，值得单独理解。

**LBYL（Look Before You Leap，先检查再动手）**——从其他语言过来的人的本能：

```python
scores = {"小明": 92}

# LBYL：先问"有没有"，再取
if "小红" in scores:
    print(scores["小红"])
else:
    print("没有小红的成绩")
```

**EAFP（Easier to Ask Forgiveness than Permission，先动手，出错再说）**——Python 的习惯：

```python
scores = {"小明": 92}

# EAFP：直接取，取不到再处理
try:
    print(scores["小红"])
except KeyError:
    print("没有小红的成绩")
```

**这个例子里两种都行，`if` 版甚至更清楚**（字典的键在不在，是个稳定的事实）。但换成文件操作，差别就出来了：

```python
import os

# ❌ LBYL 的隐患：检查和使用之间有个时间缝隙
if os.path.exists("data.txt"):
    # 就在这一瞬间，文件可能被别的程序删了 / 权限被改了 / 磁盘被拔了
    f = open("data.txt")        # 照样可能崩
```

```python
# ✅ EAFP 没有这个缝隙：打开这个动作本身就是检查
try:
    f = open("data.txt")
except FileNotFoundError:
    print("文件不存在")
except PermissionError:         # 顺手把"存在但没权限"也处理了，LBYL 版还得再检查一次
    print("没有读取权限")
```

`os.path.exists()` 只回答了"存在吗"，没回答"我能打开吗"。而 `open()` 一次性把所有问题都暴露出来。

**怎么选，给一张判断表**：

| 情况 | 推荐 | 理由 |
|---|---|---|
| 字典键、列表长度等**纯内存数据**的判断 | `if`（LBYL） | 状态不会突变，`if` 更直白 |
| 文件、网络、数据库等**外部资源** | `try`（EAFP） | 检查和使用之间状态会变 |
| 类型转换（`int()` / `float()`） | `try`（EAFP） | 规则复杂，别自己写检查 |
| 判断条件很便宜、失败很常见 | `if`（LBYL） | 异常的开销比 `if` 大 |
| 失败是罕见情况 | `try`（EAFP） | 正常路径上一行检查都不用做 |

> **记住这条**：**Python 的习惯是"直接干，出错再补救"。** 尤其是碰到文件、网络这类外部资源，以及类型转换，用 `try` 比先检查更可靠。
>
> **给有 C#/Java 经验的读者**：那些语言里异常比较"贵"，所以习惯是尽量避免；Python 里异常很轻量，用异常做错误处理是主流风格，标准库自己就大量这么写（比如 `int()` 就是用抛异常来报告失败的）。这不是"滥用异常"，这是 Python 的正常写法。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 裸 `except:` | 连 `Ctrl+C` 都被吞掉，程序停不下来 | `except Exception:` | 裸 except 捕获一切 |
| `except Exception: pass` | 出错无声无息，数据悄悄错了 | 至少 `print(e)`，日后用 `logger.exception` | 吞掉了错误信息 |
| `except Exception` 写在最前面 | 后面的 `except` 永远不执行，且不报错 | 具体异常在前，宽泛在后 | 从上往下匹配 |
| 捕获的异常类型不对 | 异常照样逃出去 | 先跑一次看清报的是什么异常 | 类型必须匹配 |
| `try` 块包了十几行 | 不知道是哪一行出的错 | 只包可能出错的一两行 + `else` | 粒度太粗 |
| 用 `except IndexError` 判断列表空 | 逻辑绕、性能差 | `if lst:` | 异常不是流程控制 |
| 捕获 `TypeError` / `NameError` 掩盖问题 | bug 被藏起来了 | 这类错误应该去**改代码** | 它们是代码 bug，不是意外 |
| 用 `assert` 做用户输入校验 | `python -O` 运行时校验全部消失 | `if` + `raise ValueError` | `-O` 会移除断言 |
| `raise ValueError` 忘了写消息 | 报错时不知道原因 | `raise ValueError("体重不能为负")` | 消息是给排查的人看的 |
| `finally` 里写 `return` | 会覆盖 `try` 里的返回值，行为诡异 | `finally` 里只做清理 | 执行顺序陷阱 |
| 用 `if os.path.exists()` 后再 open | 中间文件可能已变化 | 直接 `try: open(...)` | 检查与使用之间有缝隙 |

### 下一章预告与阶段一收尾

**恭喜——阶段一到这里结束了。** 回头看，你已经掌握了：

| 能力 | 章节 |
|---|---|
| 数据：数字、文本、四种容器 | 3~5、9~13 |
| 流程：判断、循环 | 7、8 |
| 结构：函数 | 14 |
| 健壮性：异常处理 | 15 |
| 生存技能：读报错、查边界、防坑 | 2 及各章易错点 |

**这些加起来，已经足够写出几百行、能干实事的脚本了。** 现在必须动手——

> ### 🎯 开工：验收项目 A（三个至少做两个）
> - **A1 猜数字游戏**：`while True` + `input` + `try` 转换 + 多分支判断 + 计数
> - **A2 命令行计算器**：函数 + **字典分发器**（14.16）+ 除零异常处理
> - **A3 学生成绩管理**：字典套字典（11.10）+ 函数拆分（14.18）+ 输入校验（15.21）+ 排序报表
>
> **做不出来不要往下推进**，回到对应章节重看。做出来了，你就真的会写 Python 了——只是还不会**交付** Python。

**下一章预告**：你的程序现在有个致命问题——**一关就什么都没了**。成绩录了一小时，程序退出，数据全丢。第 16 章讲文件读写，让数据能存下来。阶段二的主题就是：从"能跑的脚本"变成"别人也能用的程序"。

---

# 阶段二　从脚本到程序（第 16~25 章）

**阶段目标**：让代码能复用、能处理外部文件、出错能定位、能带参数运行、能留下运行痕迹。
**阶段验收**：项目 B（命令行记账本 + 文件归档脚本），交付标准是"别人拿到你的目录，照 README 就能跑起来"。
**与阶段一的根本区别**：阶段一写的是"**能跑的代码**"，阶段二写的是"**能交付的程序**"。这一阶段的每一章都在补一样"交付"必需的东西：数据要能存（16、17）、代码要能分文件（18）、环境要能重建（19）、出错要能定位（20、21、22）、时间要能算对（23）、要能接受参数（24），最后把函数写得更地道（25）。

**阶段二的语法基础**：阶段一全部内容（四大容器、函数、异常）都可以自由使用，不再有"还没学到"的限制。图纸中的"可用语法边界"从此主要用来标注**跨阶段二各章的依赖**（如第 16 章不能用 `json`，那是第 17 章的内容）。

---

## 第 16 章　文件读写与路径 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会用 `with` 读写文本文件；知道 `w` 模式会清空文件；能正确处理中文编码；会用 `pathlib` 操作路径与遍历目录；理解"工作目录 ≠ 脚本目录"这个陷阱 |
| 前置 | 第 15 章（异常）、第 5 章（字符串与编码） |
| 预计用时 | 5~6 小时 |
| 可用语法边界 | 阶段一全部 + `open` / `with` / `pathlib` / `os` / `os.path` / `shutil`（仅 `move`）。**不得使用**：`json` / `csv`（第 17 章）、`logging`（第 21 章）、`argparse`（第 24 章）。**预支**：`import` 只用最简形式，完整机制见第 18 章 |
| 目录调整 | 新增 16.10（默认编码的平台差异——**Windows 上默认不是 UTF-8，这是中文乱码的头号原因**）、16.12（文件指针：为什么读第二次是空的）、16.15（**工作目录 ≠ 脚本目录**，`__file__` 的用法）、16.17（常见任务速查）。原目录 9 个小节漏掉了这几个高频事故点 |
| 编写要点 | 本章有四处必须重笔：<br>① **`w` 模式会清空文件**——这是能造成真实数据损失的操作，要用最醒目的方式警告；<br>② **`with` 为什么必须用**——不能只说"这是好习惯"，要演示不用 `with` 时数据真的丢了；<br>③ **编码问题**——Windows 与 Linux/macOS 的默认编码不同，同一段代码在两台机器上结果不一样，必须讲清"永远显式写 `encoding='utf-8'`"；<br>④ **相对路径陷阱**——脚本从不同目录启动，`open("data.txt")` 找的位置就不同，这是新手最困惑的问题之一。<br><br>本章开始，示例会产生真实文件。**每个示例都要说明它会创建什么文件，章末给清理说明。** |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 16.1 | 为什么需要文件 | 内存里的数据**程序一关就没了**（回收第 15 章末尾的"成绩录了一小时全丢"）；文件是最简单的持久化手段；**什么时候用文件、什么时候该用数据库**（一句话：单机、数据量小、结构简单用文件，多人并发或需要查询用数据库 → 专项 C） | ★★★ |
| 16.2 | `open` 的六种模式 | `r` 读（默认，文件不存在报错）、**`w` 写（文件存在会被清空！）**、`a` 追加、`x` 独占创建（存在就报错）、`b` 二进制、`+` 读写；**必须用一张表配"文件不存在时怎样/文件已存在时怎样"两列**——这是选模式的真正依据 | ★★★ |
| 16.3 | **`w` 模式会清空文件** | 用最醒目的方式警告：`open("data.txt", "w")` 这一行执行完，原文件内容就没了，**即使你之后什么都不写**；**怎么避免**：想追加用 `a`，想确保不覆盖用 `x`，不确定就先 `exists()` 判断 | ★★★ |
| 16.4 | **`with` 语句** | 语法 `with open(...) as f:`；**为什么必须用**——不关文件的三个后果：① 写入的数据可能还在缓冲区没落盘 ② Windows 上文件被占用，别的程序打不开 ③ 打开太多文件会耗尽系统句柄；**关键**：`with` 块结束时**自动关闭，即使中间抛异常也会关** | ★★★ |
| 16.5 | 不用 `with` 的写法 | `f = open(...)` + `f.close()`，以及为什么它不可靠（中间抛异常就不会执行到 `close`）；用 `try/finally` 手写等价代码（回收 15.9），**让读者看到 `with` 替他省掉了什么**；一句话：`with` 是 `try/finally` 的语法糖（原理见第 32 章） | ★★★ |
| 16.6 | 读的四种方式 | `read()` 一次读全部（返回一个字符串）、`read(n)` 读 n 个字符、`readline()` 读一行、`readlines()` 读成列表、**`for line in f` 逐行迭代（首选）**；**四者对照表 + 什么时候用哪个** | ★★★ |
| 16.7 | 大文件必须逐行读 | `read()` 会把整个文件塞进内存——**读一个 2GB 的日志文件会直接把内存吃光**；`for line in f` 一次只拿一行；**判断标准**：文件可能超过几十 MB 就必须逐行 | ★★★ |
| 16.8 | 每行都带着换行符 | 读到的每一行末尾都有 `\n`（最后一行可能没有）；**不 `strip()` 会怎样**：拼接时出现意外空行、比较时永远不相等（`line == "abc"` 为 False 因为实际是 `"abc\n"`）；回收 5.14 | ★★★ |
| 16.9 | 写文件 | `write()` **不会自动换行**（和 `print` 不同！要自己写 `\n`）；`writelines()` 也不自动换行（名字骗人）；返回值是写入的字符数 | ★★★ |
| 16.10 | **编码：中文乱码的根源** | `encoding="utf-8"` 参数；**不写会怎样**：用系统默认编码，而**Windows 的默认编码是 GBK/cp936，Linux/macOS 是 UTF-8**——同一段代码在两台机器上行为不同，这是中文乱码的头号原因；**铁律：读写文本文件永远显式写 `encoding="utf-8"`**；顺带讲 `UnicodeDecodeError` 的读法 | ★★★ |
| 16.11 | 二进制模式 | `rb` / `wb`；读到的是 `bytes`（回收 5.12）；**什么时候用**：图片、Excel、压缩包、以及"只是复制文件不关心内容"的场合；**二进制模式不能有 `encoding` 参数** | ★★ |
| 16.12 | 文件指针 | 读完一遍后指针在末尾，**再读一次得到空字符串**（这是新手常见的困惑："我明明读到了，怎么第二次没了"）；`f.seek(0)` 回到开头、`f.tell()` 看当前位置 | ★★ |
| 16.13 | **`pathlib.Path`：现代方案** | `Path("data") / "a.txt"` 用 `/` 拼路径（**跨平台，不用管斜杠方向**）；属性 `name` / `stem` / `suffix` / `parent`；判断 `exists()` / `is_file()` / `is_dir()`；**一行读写 `read_text(encoding="utf-8")` / `write_text(...)`**（小文件的最简写法）；`mkdir(parents=True, exist_ok=True)`；`unlink()` 删除、`rename()` 改名 | ★★★ |
| 16.14 | 遍历目录 | `iterdir()` 列出一层、`glob("*.txt")` 按模式匹配一层、**`rglob("*.txt")` 递归匹配所有子目录**；配合 `sorted()` 得到稳定顺序；**这是验收项目 B2 的核心工具** | ★★★ |
| 16.15 | **工作目录 ≠ 脚本目录** | `open("data.txt")` 里的相对路径，是相对于**运行时的工作目录**（`Path.cwd()`），不是脚本所在目录；**症状**：在 PyCharm 里能跑，在命令行里 `cd` 到别处就 `FileNotFoundError`；**解法**：用 `Path(__file__).parent / "data.txt"` 定位到脚本旁边的文件；**这个坑必须专门讲**，它是"在我这儿能跑"的经典来源 | ★★★ |
| 16.16 | `os` / `os.path` 对照 | `os.path.join` / `os.path.exists` / `os.listdir` / `os.makedirs` / `os.remove`；**为什么还要学**：老项目里全是它，你得看得懂；给一张 `os.path` → `pathlib` 的对照表；**新代码一律用 `pathlib`** | ★★ |
| 16.17 | 安全写入 | 直接写目标文件的风险：写一半程序崩了 → 文件半新半旧，原数据也没了；**做法**：先写 `xxx.tmp`，写完再 `replace()` 覆盖（`Path.replace` 是原子操作）；**什么时候值得这么做**：配置文件、数据文件等"损坏了会造成麻烦"的文件 | ★ |
| 16.18 | 常见任务速查 | 一张表把六个高频任务的最简写法列出来：读整个文件、逐行处理、追加一行、写列表、判断存在、遍历目录下所有 `.txt`——**读者以后大概率只回来查这张表** | ★★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 16-1 | 写下第一个文件 | A | `with open(..., "w", encoding="utf-8")` + `write` | `f.write("hi")` |
| 16-2 | 把它读回来 | A | `with open(..., "r", encoding="utf-8")` + `read` | `f.read()` |
| 16-3 | `write` 不会自动换行 | A | ❌ 三次 `write` 挤在一行 → ✅ 自己加 `\n` | `f.write(s + "\n")` |
| 16-4 | **`w` 模式清空了我的文件** | A | ❌ 只是 `open(..., "w")` 什么都没写，原内容已经没了 → ✅ 用 `a` 或先判断 | `open(p, "a")` |
| 16-5 | 追加模式 | A | `a` 模式在末尾添加，不动原有内容 | `"a"` |
| 16-6 | `x` 模式：绝不覆盖 | A | 文件已存在时抛 `FileExistsError` | `"x"` |
| 16-7 | 六种模式对照 | B | 一张表：文件不存在时/已存在时分别怎样 | 模式表 |
| 16-8 | 不用 with 会怎样 | B | ❌ 忘了 `close()`，数据没落盘 → ✅ `with` 自动关 | `f.close()` |
| 16-9 | with 等价于 try/finally | B | 手写 `try/finally` 版本，对照 `with`（回收 15.9） | `finally: f.close()` |
| 16-10 | 出异常时 with 照样关文件 | B | 块内故意抛异常，验证文件仍被关闭 | 异常安全 |
| 16-11 | 读的四种方式 | B | `read` / `read(n)` / `readline` / `readlines` / 逐行迭代，输出对照 | `for line in f:` |
| 16-12 | 大文件为什么要逐行读 | B | 说明 `read()` 的内存代价；用一个稍大的文件对比（不必真造 2GB，讲清原理 + 给判断标准） | 逐行迭代 |
| 16-13 | 每行都带着 `\n` | B | ❌ `line == "abc"` 为 False → ✅ `line.strip() == "abc"` | `line.rstrip("\n")` |
| 16-14 | 读第二次是空的 | B | 文件指针在末尾；`f.seek(0)` 回到开头 | `f.seek(0)` |
| 16-15 | 写多行的两种方式 | B | 循环 `write` vs `writelines`；后者也不自动换行 | `writelines` |
| 16-16 | **不写 encoding 的后果** | B | ❌ 中文在 Windows 上乱码或 `UnicodeDecodeError` → ✅ 显式 `encoding="utf-8"`；**说明两个平台默认值不同** | `encoding="utf-8"` |
| 16-17 | 读懂 `UnicodeDecodeError` | B | 真实报错文本 + 三种成因（编码不对、其实是二进制文件、文件被截断） | `UnicodeDecodeError` |
| 16-18 | 二进制方式复制一个文件 | B | `rb` / `wb`；不能带 `encoding` | `open(p, "rb")` |
| 16-19 | pathlib 上手 | A | `Path("data") / "a.txt"`、`exists()`、`name` / `stem` / `suffix` / `parent` | `Path(...) / "..."` |
| 16-20 | 一行读写小文件 | A | `read_text` / `write_text`，比 `with open` 更短 | `p.read_text(encoding="utf-8")` |
| 16-21 | 建目录（含多层） | A | `mkdir(parents=True, exist_ok=True)`；不加 `exist_ok` 会报错 | `mkdir(parents=True, exist_ok=True)` |
| 16-22 | 列出目录内容 | B | `iterdir` / `glob("*.txt")` / `rglob("*.txt")` 三者区别 | `p.rglob("*.txt")` |
| 16-23 | 删除与改名 | A | `unlink()` / `rename()`；`missing_ok=True` | `p.unlink(missing_ok=True)` |
| 16-24 | **工作目录不是脚本目录** | B | ❌ 换个目录启动就 `FileNotFoundError` → ✅ `Path(__file__).parent / "data.txt"`；打印 `Path.cwd()` 和 `__file__` 对比 | `Path(__file__).parent` |
| 16-25 | os.path 与 pathlib 对照 | B | 同一批操作两种写法并排；说明老代码为什么长那样 | `os.path.join` |
| 16-26 | 安全写入 | B | 先写 `.tmp` 再 `replace()` | `tmp.replace(target)` |
| 16-27 | 统计一个文本文件的词频 | C | 逐行读 + `strip` + `split` + 字典计数（回收 11.16）+ 按值排序输出 Top 5 | 综合 |
| 16-28 | 给程序加一份"运行记录" | C | 每次运行 `a` 模式追加一行（含时间——时间用 `time.time()` 凑合，正规做法见第 23 章）；**这是第 21 章 logging 的手工版**，为那一章做对比铺垫 | 综合 |
| 16-29 | 按后缀分类整理一个目录 | C | `rglob` 遍历 + 按 `suffix` 分组 + `mkdir` + `rename`；**先做"只打印不动手"的干跑版**，这是验收项目 B2 的雏形 | 综合 |
| 16-30 | 把成绩管理的数据存下来 | C | 回收验收项目 A3：把内存里的字典写成一行一条记录的文本文件，下次启动读回来；**指出它的缺陷**（自己拼格式很脆弱）→ 第 17 章用 JSON 解决 | 综合 |

### 样板示例

**示例 16-4　`w` 模式清空了我的文件**

演示重点：**只要以 `w` 打开，文件立刻被清空，哪怕你一个字都没写。** 要用醒目的警告框，并给出"三种安全做法"（`a` 追加 / `x` 独占 / 先 `exists()` 判断）。这是本章唯一能造成**真实数据损失**的操作，篇幅和警示强度都要给足。

**示例 16-16　不写 encoding 的后果**

演示重点：同一段写中文的代码，在 Windows（默认 GBK）和 Linux/macOS（默认 UTF-8）上表现不同——**你在自己机器上测得好好的，同事一跑就乱码**。要给出真实的 `UnicodeDecodeError` 报错文本，并把结论写成铁律：**读写文本文件永远显式写 `encoding="utf-8"`**。
补充说明 Python 3.15 起默认编码将改为 UTF-8（PEP 686），但**现在写代码不能依赖它**。

**示例 16-24　工作目录不是脚本目录**

演示重点：这是"在我这儿能跑"的经典来源。要实际演示两种启动方式的不同结果：

```text
在脚本所在目录运行     python app.py        → 正常
从上一级目录运行       python code/app.py   → FileNotFoundError
```

然后给出 `Path(__file__).parent / "data.txt"` 这个解法，并说明什么时候该用它（读程序自带的资源文件）、什么时候不该用（处理用户指定的文件，那就该用用户给的路径）。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 用 `w` 模式打开已有文件 | **原内容全部丢失** | 追加用 `a`，不覆盖用 `x` | `w` 打开即清空 |
| 忘了 `close()`，也没用 `with` | 数据没落盘 / 文件被占用 | 一律用 `with` | 缓冲区未刷新 |
| 不写 `encoding` | 中文乱码或 `UnicodeDecodeError` | 显式 `encoding="utf-8"` | 平台默认编码不同 |
| 二进制模式带 `encoding` | `ValueError: binary mode doesn't take an encoding argument` | 去掉 `encoding` | 二进制不涉及编码 |
| `line == "abc"` 比较失败 | 永远是 `False` | `line.strip() == "abc"` | 行尾有 `\n` |
| `write` 后各行挤在一起 | 全在一行 | 自己加 `\n` | `write` 不自动换行 |
| 读第二次得到空字符串 | 以为文件空了 | `f.seek(0)` 或重新打开 | 文件指针在末尾 |
| 用 `read()` 读大文件 | 内存暴涨甚至崩溃 | `for line in f` | 一次性载入 |
| 相对路径在不同目录启动结果不同 | `FileNotFoundError` | `Path(__file__).parent / "..."` | 工作目录 ≠ 脚本目录 |
| `mkdir()` 目录已存在 | `FileExistsError` | `mkdir(exist_ok=True)` | —— |
| `mkdir()` 建多层目录 | `FileNotFoundError` | `mkdir(parents=True)` | 不会自动建父目录 |
| Windows 路径写成 `"C:\new"` | 路径被转义破坏 | `r"C:\new"` 或 `Path("C:/new")` | 回收 5.2 |
| 遍历目录时顺序不固定 | 每次运行输出顺序不同 | `sorted(p.iterdir())` | 文件系统不保证顺序 |
| 边遍历目录边改名/删除 | 行为不可预料、漏文件 | 先 `list()` 收集再处理 | 回收 9.18 的同类问题 |

### 下一章预告

现在数据能存下来了，但**格式是你自己拼的**——用逗号分隔？那内容里带逗号怎么办？存嵌套的字典怎么办？自己定格式，就得自己写解析，还得自己处理所有边界情况。

第 17 章讲 JSON 和 CSV 两种**标准格式**：一行代码存、一行代码读，而且和其他语言、其他系统通用。示例 16-30 那个自己拼格式的成绩存储，会在第 17 章被三行代码取代。

---

## 第 17 章　数据格式：JSON 与 CSV ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会用 JSON 存取嵌套数据；知道 JSON 的类型限制（字典键会变字符串、集合和日期不支持）；会用 `DictReader` / `DictWriter` 读写 CSV；能解决"Excel 打开中文乱码"和"CSV 多空行"两个高频问题 |
| 前置 | 第 16 章（文件读写） |
| 预计用时 | 4~5 小时 |
| 可用语法边界 | 阶段一全部 + 第 16 章 + `json` / `csv` 模块。**不得使用**：`logging`（第 21 章）、`argparse`（第 24 章）、`datetime`（第 23 章——本章需要时间戳时用 `time.time()` 凑合，并注明"正规做法见第 23 章"）、`openpyxl`（第三方，只做一瞥） |
| 目录调整 | 新增 17.8（**字典的 int 键会被转成字符串**）、17.9（JSON 不支持的类型及其 `TypeError`）、17.13（**`newline=""` 与 CSV 多空行**）、17.15（**内容含逗号时手工 `split` 必错**）、17.17（JSON vs CSV 选型）。原目录 6 个小节漏掉的正好都是实战中最常出问题的点 |
| 编写要点 | 本章有四个"必踩且难查"的坑，是全章重点：<br>① **中文被转义成 `\u5c0f\u660e`**——不写 `ensure_ascii=False` 的默认行为，文件打开一看全是乱码般的转义符；<br>② **字典的 int 键存进去再读出来变成了字符串**——静默发生，不报错，之后 `d[1]` 就 `KeyError`；<br>③ **`newline=""`**——不写会导致 CSV 每行之间多一个空行。**这个坑只在 Windows 上出现**，在 Linux/macOS 上写代码的人根本测不出来，交给 Windows 同事就炸；<br>④ **Excel 打开 UTF-8 的 CSV 中文乱码**——要用 `utf-8-sig` 写 BOM。<br><br>**平台差异必须明确标注**：③ 只在 Windows 出现，正文要说清"你在 Mac 上看不到这个现象，但代码必须这么写"。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 17.1 | 为什么要用标准格式 | 回收 16-30：自己拼格式（逗号分隔）的三个问题——① **内容里出现分隔符就散架** ② 嵌套结构没法表达 ③ 解析代码要自己写、边界情况要自己扛；**用标准格式的好处**：读写各一行、和其他语言/系统通用、边界情况别人已经处理好了 | ★★★ |
| 17.2 | JSON 是什么 | 来自 JavaScript，现在是**通用数据交换格式**；**你会在哪儿遇到它**：几乎所有 Web 接口返回的都是 JSON、大量配置文件是 JSON；长得就像 Python 的字典和列表（回收 11.10 的"嵌套字典就是 JSON 的形状"） | ★★★ |
| 17.3 | 四个函数 | `dumps` / `loads` 处理**字符串**，`dump` / `load` 处理**文件**；**记忆法：带 `s` 的是 string**；四者的参数与返回值列一张表 | ★★★ |
| 17.4 | 类型映射 | Python ↔ JSON 对照表：`dict`↔object、`list`↔array、`str`↔string、`int`/`float`↔number、`True`↔`true`、`None`↔`null`；**注意 `True` / `None` 在 JSON 里是小写的 `true` / `null`** | ★★★ |
| 17.5 | **中文被转义** | 默认 `json.dumps({"name": "小明"})` 得到 `{"name": "\u5c0f\u660e"}`——**能用但人看不懂**；加 `ensure_ascii=False` 才输出中文；**为什么默认是转义**：为了保证输出全是 ASCII，在任何环境下传输都不出问题；**结论：给人看的一律加 `ensure_ascii=False`** | ★★★ |
| 17.6 | 美化输出 | `indent=2` 缩进换行、`sort_keys=True` 键排序；**什么时候用**：配置文件、要提交到 Git 的文件（缩进+排序能让版本差异看得清）；**什么时候不用**：网络传输（多余的空格是浪费） | ★★★ |
| 17.7 | 元组会变成列表 | `(1, 2)` 存进去、读出来变成 `[1, 2]`；**为什么**：JSON 没有"元组"这个概念；**什么时候会咬你**：把元组当字典键（第 11 章可以）存 JSON 就完了 | ★★ |
| 17.8 | **字典的 int 键会变成字符串** | `json.dumps({1: "a"})` 得到 `{"1": "a"}`，读回来键是**字符串 `"1"`**；**为什么**：JSON 规定 object 的键必须是字符串；**症状**：存之前 `d[1]` 能用，读之后 `d[1]` 抛 `KeyError`，而 `d["1"]` 才行——**静默发生，不报错**；**解法**：要么统一用字符串键，要么读回来后手工转换 | ★★★ |
| 17.9 | 不支持的类型 | `set`、`datetime`、自定义对象直接抛 `TypeError: Object of type set is not JSON serializable`；**解法**：存之前转成支持的类型（`set` → `list`、日期 → 字符串）；提一句可以用 `default=` 参数自定义转换（细节留给需要时查） | ★★★ |
| 17.10 | 解析失败 | `json.JSONDecodeError`（它是 `ValueError` 的子类）；报错信息里的 `line 1 column 2 (char 1)` 怎么读；**回收第 2 章的多层 Traceback 示例**——那个例子就是 `json.loads("{bad}")`；**实战写法**：读外部来的 JSON 一律用 `try` 包起来（回收第 15 章） | ★★★ |
| 17.11 | CSV 是什么 | 逗号分隔的纯文本表格；**优势**：Excel 能直接打开、体积小、任何工具都支持；**局限**：只能表达"二维表"，**没有类型**（读出来全是字符串）、不能嵌套 | ★★★ |
| 17.12 | 两套 API | `csv.reader` / `csv.writer`（按**列表**处理，靠下标取值）vs **`csv.DictReader` / `csv.DictWriter`（按字典处理，靠列名取值）**；**推荐用 Dict 版**：列顺序变了代码不用改，`row["姓名"]` 比 `row[0]` 好读 | ★★★ |
| 17.13 | **`newline=""` 参数** | 打开 CSV 文件时必须写 `newline=""`；**不写的后果**：Windows 上每行之间多一个空行（csv 模块写的 `\r\n` 又被文本模式转成 `\r\r\n`）；**关键提醒**：**这个现象只在 Windows 上出现**，在 Linux/macOS 上开发的人测不出来，代码交给 Windows 同事就炸；**结论：读写 CSV 一律加 `newline=""`，两个平台都加** | ★★★ |
| 17.14 | **Excel 打开中文乱码** | 用 `encoding="utf-8"` 写的 CSV，Excel 打开中文是乱码；**原因**：Excel 默认按系统编码（GBK）读，不认 UTF-8，除非文件开头有 BOM 标记；**解法**：写文件时用 `encoding="utf-8-sig"`，它会在开头加三个字节 `\xef\xbb\xbf` 告诉 Excel"这是 UTF-8"；**注意**：这个文件用 Python 读时要用 `utf-8-sig` 或 `utf-8`（后者会在第一个字段前留下 `\ufeff`） | ★★★ |
| 17.15 | **内容含逗号时手工 split 必错** | `"小明,数学,语文,92".split(",")` 会把"数学,语文"拆成两段；**csv 模块的处理**：写的时候自动给含逗号的字段加引号（`小明,"数学,语文",92`），读的时候自动去掉；**结论：只要是 CSV，就用 `csv` 模块，不要用 `split(",")`**——这是"标准格式"价值的最好证明 | ★★★ |
| 17.16 | 分隔符与方言 | `delimiter="\t"` 读写 TSV、`delimiter=";"`（某些地区的 Excel 用分号）；`quotechar`、`quoting` 简介 | ★★ |
| 17.17 | **JSON vs CSV 怎么选** | 决策表：数据有嵌套 → JSON；要给 Excel 看 / 数据是规整的二维表 → CSV；要保留类型 → JSON（CSV 读出来全是字符串）；数据量很大且只追加 → CSV（可以逐行追加，JSON 得整个重写）；**这一条要给具体场景**，不能只列特性 | ★★★ |
| 17.18 | Excel 文件一瞥 | `openpyxl` 读写 `.xlsx`（第三方库）；**只做认知**：说明 CSV 和 Excel 不是一回事（CSV 是纯文本、没有格式和公式）；什么时候必须用 openpyxl（要保留格式、多个 sheet、公式） | ★ |
| 17.19 | 其他格式一览 | 一句话各表过：`YAML`（配置，可读性好但要装库）、`TOML`（Python 3.11+ 内置 `tomllib`，`pyproject.toml` 用它）、`pickle`（Python 专有，**有安全风险，不要读不可信来源的 pickle**）、`XML`（老系统）；详见工具箱 T3 | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 17-1 | 字典变成 JSON 字符串 | A | `json.dumps` | `json.dumps(d)` |
| 17-2 | JSON 字符串变回字典 | A | `json.loads`，`type()` 验证 | `json.loads(s)` |
| 17-3 | **中文变成了 `\u5c0f\u660e`** | A | ❌ 默认转义 → ✅ `ensure_ascii=False` | `ensure_ascii=False` |
| 17-4 | 让 JSON 好看点 | A | `indent=2`、`sort_keys=True` | `indent=2` |
| 17-5 | 直接存进文件、直接读出来 | A | `dump` / `load`（不带 s，第一个参数是数据、第二个是文件对象） | `json.dump(d, f, ...)` |
| 17-6 | 类型映射一览 | B | 一个含各种类型的字典存出来，看 JSON 长什么样（`None`→`null`、`True`→`true`） | 映射表 |
| 17-7 | 元组存进去变成了列表 | A | `(1,2)` → `[1,2]` | 类型退化 |
| 17-8 | **int 键变成了字符串** | B | ❌ 存 `{1: "a"}` 读回来 `d[1]` 抛 `KeyError` → ✅ 统一用字符串键 | `{"1": "a"}` |
| 17-9 | 集合和日期存不进去 | A | `TypeError: Object of type set is not JSON serializable`；转成 `list` / 字符串再存 | `list(s)` |
| 17-10 | JSON 格式错误 | B | `json.JSONDecodeError`，读懂 `line 1 column 2`；回收第 2 章的 Traceback 示例 | `JSONDecodeError` |
| 17-11 | 读外部 JSON 要包 try | B | 文件不存在、格式错误、编码错误三种异常一起处理 | `try/except` |
| 17-12 | **把成绩管理存成 JSON** | C | **兑现 16-30 的承诺**：三行代码取代自己拼格式；嵌套字典原样保存、原样读回 | 综合 |
| 17-13 | csv.writer 写一张表 | A | `writerow` / `writerows`；**必须带 `newline=""`** | `csv.writer(f)` |
| 17-14 | csv.reader 读回来 | A | 每行是一个列表；**所有值都是字符串** | `csv.reader(f)` |
| 17-15 | **不加 `newline=""` 会多空行** | B | ❌ 字节变成 `a,b\r\r\n`，读出来多出空行 → ✅ 加 `newline=""`；**注明只在 Windows 出现**，并给出在任何平台都能看到差异的验证方法（看原始字节） | `newline=""` |
| 17-16 | 用列名取值：DictReader | B | `row["姓名"]` 比 `row[0]` 好读；列顺序变了代码不用改 | `csv.DictReader(f)` |
| 17-17 | DictWriter 写 | B | `fieldnames` + `writeheader()` + `writerow(dict)` | `csv.DictWriter(f, fieldnames=...)` |
| 17-18 | **内容里有逗号：手工 split 出错** | B | ❌ `split(",")` 把一个字段拆成两个 → ✅ csv 模块自动加引号并正确还原 | `csv.reader` |
| 17-19 | **Excel 打开中文乱码** | B | ❌ `utf-8` → Excel 乱码 → ✅ `utf-8-sig`（看字节差异：多出 `\xef\xbb\xbf`） | `encoding="utf-8-sig"` |
| 17-20 | 制表符分隔的文件 | A | `delimiter="\t"` | `delimiter="\t"` |
| 17-21 | CSV 读出来全是字符串 | B | 数字要手工 `int()` / `float()` 转换（配合第 15 章的 `try`） | 类型转换 |
| 17-22 | JSON vs CSV 选型 | B | 同一份数据用两种格式各存一次，对照优劣 | 决策表 |
| 17-23 | 把成绩导出成 Excel 能看的报表 | C | `DictWriter` + `utf-8-sig` + `newline=""`，三个坑一次全避开；**这是最接近真实工作的一个示例** | 综合 |
| 17-24 | 读一份"接口返回的数据" | C | 用一段写死的 JSON 字符串模拟接口响应（真实请求见专项 D）→ `loads` → 遍历嵌套结构 → 提取字段 → 存成 CSV；**这是验收项目 C 的数据处理骨架** | 综合 |
| 17-25 | openpyxl 一瞥 | A | 三行读一个 xlsx；说明什么时候才需要它 | `load_workbook` |

### 样板示例

**示例 17-3　中文变成了 `\u5c0f\u660e`**

演示重点：默认行为 vs `ensure_ascii=False`，两个输出并排。要解释**为什么默认要转义**（保证纯 ASCII，任何环境传输都安全），以及**结论**：存给人看/要提交到 Git 的文件一律加 `ensure_ascii=False`；纯机器间传输可以不加（省事，且体积略小）。

真实输出：

```text
默认：      {"name": "\u5c0f\u660e"}
加参数后：  {"name": "小明"}
```

**示例 17-8　int 键变成了字符串**

演示重点：这个坑**静默发生**，是本章最难查的问题。要完整走一遍"存之前能用 → 存了读回来就 KeyError"的过程：

```text
存之前：d[1]        → 'a'      正常
JSON：  {"1": "a"}            键已经变成字符串了
读回来：d[1]        → KeyError
        d["1"]     → 'a'      只能用字符串取
```

给出两条解法（统一用字符串键 / 读回后手工转换），并说明**为什么 JSON 这么规定**（JSON object 的键在标准里就是字符串）。

**示例 17-15　不加 `newline=""` 会多空行**

演示重点：这是**平台相关**的坑，必须说清三件事：

1. **症状**：Windows 上 CSV 每行之间多一个空行，Excel 打开一半是空行
2. **原因**：`csv` 模块自己写 `\r\n` 作为行尾，而 Windows 的文本模式会把其中的 `\n` 再转成 `\r\n`，结果变成 `\r\r\n`
3. **在 Mac/Linux 上看不到这个现象**——但代码必须写 `newline=""`，否则交给 Windows 同事就出问题

**在任何平台都能验证的方法**（直接看字节，不依赖平台）：

```text
错误写法的字节：  b'a,b\r\r\nc,d\r\r\n'    读出来 4 行，其中 2 行是空的
正确写法的字节：  b'a,b\r\nc,d\r\n'        读出来 2 行
```

**示例 17-19　Excel 打开中文乱码**

演示重点：`utf-8` vs `utf-8-sig` 的字节差异（多出 BOM 三字节 `\xef\xbb\xbf`），以及为什么这三个字节能让 Excel 认出 UTF-8。要提醒一个连带问题：**用 `utf-8-sig` 写的文件，用 `utf-8` 读时第一个字段会带上一个看不见的 `\ufeff`**，导致 `row["姓名"]` 抛 `KeyError`——读的时候也要用 `utf-8-sig`。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 不加 `ensure_ascii=False` | 文件里全是 `\uXXXX` | `json.dumps(d, ensure_ascii=False)` | 默认转义非 ASCII |
| 字典用 int 当键存 JSON | 读回来 `d[1]` 抛 `KeyError` | 统一用字符串键 | JSON 键必须是字符串 |
| 存 `set` / `datetime` | `TypeError: not JSON serializable` | 先转成 `list` / 字符串 | JSON 不支持这些类型 |
| 元组存进 JSON | 读回来变成列表 | 知道会退化，或本来就用列表 | JSON 没有元组 |
| 读外部 JSON 不做异常处理 | 格式一错程序就崩 | `try/except JSONDecodeError` | 外部数据不可信 |
| CSV 不加 `newline=""` | **Windows 上多空行** | 一律加 `newline=""` | 换行被二次转换 |
| CSV 用 `utf-8` 给 Excel 看 | 中文乱码 | 写文件用 `utf-8-sig` | Excel 需要 BOM |
| `utf-8-sig` 写、`utf-8` 读 | 第一个列名带 `\ufeff`，`KeyError` | 读也用 `utf-8-sig` | BOM 被当成内容 |
| 用 `split(",")` 解析 CSV | 字段里含逗号就散架 | 用 `csv` 模块 | 引号规则要按标准处理 |
| 以为 CSV 读出来有类型 | 数字变成字符串，算术报错 | 手工 `int()` / `float()` | CSV 无类型 |
| 用 `DictWriter` 忘了 `writeheader()` | 文件里没有列名行 | 补 `writeheader()` | 不会自动写 |
| `DictWriter` 的字典多了个键 | `ValueError: dict contains fields not in fieldnames` | 对齐 `fieldnames`，或用 `extrasaction="ignore"` | 严格校验 |
| 直接覆盖写数据文件，中途崩了 | 数据半新半旧 | 先写 `.tmp` 再 `replace`（16.17） | 非原子写入 |

### 下一章预告

到这里，你的程序能读能写、数据能存能取了。但所有代码还堆在**一个 `.py` 文件**里——写到三百行就开始上下翻找，想复用某个函数只能复制粘贴。

第 18 章讲模块与包：把代码拆到多个文件里，互相 `import`。同时会解开一个你可能已经在别人代码里见过、但完全不知道干什么用的谜语：

```python
if __name__ == "__main__":
```

---

## 第 18 章　模块与包 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会把代码拆成多个文件并互相 `import`；理解 `import` 会执行整个模块文件；彻底搞懂 `if __name__ == "__main__"`；会排查 `ModuleNotFoundError`；知道给文件起名不能和标准库撞车 |
| 前置 | 第 17 章 |
| 预计用时 | 4~5 小时 |
| 可用语法边界 | 阶段一全部 + 第 16、17 章 + `import` 全部形式 + `sys` / `dir` / `help`。**不得使用**：`venv`（第 19 章）、`logging`（第 21 章）、`argparse`（第 24 章） |
| 特别说明 | **本章大量示例需要多个文件**。每个示例必须先画出目录结构（用代码块画树），再逐个给文件内容，最后给运行命令和输出。这是本书第一次出现"多文件示例"，格式要立规矩：<br><br>`目录结构 → 各文件内容（标明文件名）→ 运行命令 → 输出` |
| 目录调整 | 新增 18.5（**`import` 会执行整个模块文件**——这是理解一切 import 行为的钥匙）、18.6（模块只执行一次）、18.9（**自己的文件名和标准库同名的惨剧**）、18.14（`python -m` 的用法）、18.18（`__pycache__` 是什么）。原目录 8 个小节缺了这几个高频问题的解释 |
| 编写要点 | 本章有四处重笔：<br>① **`import` 的本质是"执行那个文件"**（18.5）——不讲清这点，后面所有现象（为什么模块里的 `print` 会输出、为什么只执行一次、`__name__` 为什么能区分）都成了魔法；<br>② **`if __name__ == "__main__"`**（18.7）——新手在别人代码里见过一百次却不知道干什么用，必须讲透；<br>③ **文件名撞车**（18.9）——把自己的文件叫 `random.py` / `json.py` / `test.py` 会导致极其诡异的报错，是真实高频事故；<br>④ **相对导入不能直接运行**（18.13）——`ImportError: attempted relative import with no known parent package`，这个报错几乎每个学包结构的人都会撞一次。<br><br>18.20 要把验收项目 A3（成绩管理）真正拆成多文件，这是**从"脚本"到"程序"的关键一跃**，也是验收项目 B 的结构基础。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 18.1 | 为什么要拆文件 | 单文件写到两三百行就开始上下翻找；想复用某个函数只能复制粘贴（**改一处要改三处**）；多人协作时同一个文件会不断冲突；**拆分的判断标准**：一个文件只装"一类事"（数据存取 / 业务计算 / 界面交互） | ★★★ |
| 18.2 | 模块就是 `.py` 文件 | 模块名 = 文件名去掉 `.py`；**文件名必须是合法的标识符**（不能有减号、不能数字开头——回收 2.5）；同目录下直接 `import 文件名` 就能用 | ★★★ |
| 18.3 | `import` 的四种写法 | ① `import m` → 用 `m.func()` ② `import m as alias` ③ `from m import func` → 直接用 `func()` ④ `from m import *`；**四者对照表**：写法、调用方式、命名空间污染程度 | ★★★ |
| 18.4 | 推荐做法 | **首选 `import m` 或 `from m import 具体名字`**；`import m as alias` 用于长名字（`import pandas as pd` 是社区惯例）；**`from m import *` 一律不要用**——理由：看不出某个名字是从哪来的、可能覆盖你自己的变量、IDE 也没法帮你跳转 | ★★★ |
| 18.5 | **`import` 会执行整个文件** | 这是本章的钥匙：`import m` 的动作是"**把 m.py 从头到尾执行一遍**"，函数定义、类定义、以及**任何顶层的语句**都会执行；**演示**：在模块里放一句 `print("我被执行了")`，`import` 时它就会输出；**推论**：模块顶层不要放耗时操作、不要放会产生副作用的代码（改文件、发请求） | ★★★ |
| 18.6 | 模块只执行一次 | 同一个模块 `import` 十次也只执行一次，之后从 `sys.modules` 缓存里拿；**为什么**：避免重复执行和无限循环；**什么时候会咬你**：改了模块代码，在同一个 REPL / Notebook 里重新 `import` 不生效（**解法：重启，或用 `importlib.reload`**）——这是 Jupyter 用户的高频困惑 | ★★★ |
| 18.7 | **`if __name__ == "__main__"`** | `__name__` 是每个模块都有的内置变量：**直接运行这个文件时它等于 `"__main__"`，被别人 `import` 时它等于模块名**；所以这一行的含义是"**只在直接运行时才执行下面的代码**"；**三个实际用途**：① 放测试/演示代码，被导入时不打扰别人 ② 让一个文件既能当模块又能当脚本 ③ 作为程序入口，让人一眼看出"从这里开始读"；**必须配"两种运行方式下 `__name__` 的实际值"的对照演示** | ★★★ |
| 18.8 | 模块搜索路径 | `import` 时 Python 按 `sys.path` 的顺序找：**① 脚本所在目录（或当前目录）② `PYTHONPATH` 环境变量 ③ 标准库 ④ 第三方库目录 `site-packages`**；`print(sys.path)` 看实际顺序；**`ModuleNotFoundError` 的三步排查**：文件名对不对 → 在不在搜索路径里 → 是不是解释器选错了（回收 1.6） | ★★★ |
| 18.9 | **文件名撞车的惨剧** | 把自己的文件命名为 `random.py`、`json.py`、`csv.py`、`time.py`、`test.py`、`email.py` 会怎样：**因为当前目录排在标准库前面，`import random` 会导入你自己那个文件**；**症状极其诡异**：`AttributeError: module 'random' has no attribute 'randint'`，或者第三方库莫名报错（因为它内部 import 了标准库，结果拿到了你的文件）；**还会留下 `__pycache__` 里的缓存让问题更难查**；**铁律：给文件起名前先想一下标准库里有没有同名的** | ★★★ |
| 18.10 | 包：装模块的目录 | 目录 + `__init__.py` = 包；`import 包.模块`、`from 包 import 模块`；**为什么需要包**：模块多了要分类（`storage/`、`report/`）；**目录层级不要超过三层**，否则 import 语句长到没法读 | ★★★ |
| 18.11 | `__init__.py` 的作用 | ① **标记这是一个包**（3.3 起可以省略，但建议保留——见下条）② `import 包` 时它会被执行 ③ **常见用法：在里面做统一导出**，让用户写 `from mypkg import Foo` 而不用关心 Foo 在哪个子模块；**可以为空**，空文件也是合法的 | ★★ |
| 18.12 | 隐式命名空间包 | Python 3.3+ 没有 `__init__.py` 的目录也能当包用；**但建议保留 `__init__.py`**，理由：① 意图明确 ② 有些工具（老版本的打包工具、测试发现）对隐式包支持不好 ③ 需要统一导出时总归要它 | ★ |
| 18.13 | 绝对导入 vs 相对导入 | 绝对：`from mypkg.storage import load`；相对：`from . import storage`、`from .storage import load`（`.` 是当前包，`..` 是上一级）；**PEP 8 推荐绝对导入**（更清楚、移动文件时不容易错）；**相对导入的致命限制**：**含相对导入的文件不能直接 `python 文件.py` 运行**，会报 `ImportError: attempted relative import with no known parent package`——必须用 `python -m 包.模块` | ★★★ |
| 18.14 | `python -m` | 把模块当脚本运行；**什么时候必须用**：跑包内的模块（因为相对导入）、跑标准库的工具（`python -m venv`、`python -m json.tool`、`python -m http.server`）；**它和 `python 文件.py` 的区别**：`-m` 会把当前目录加进 `sys.path` 并按包的方式解析 | ★★ |
| 18.15 | import 语句的组织规范 | PEP 8 规定的三组顺序，**组间空一行**：① 标准库 ② 第三方库 ③ 自己的代码；每组内按字母序；**为什么**：一眼看出这个文件依赖了什么外部东西；`ruff` / `isort` 能自动整理（第 37 章） | ★★ |
| 18.16 | 循环导入 | A 导入 B、B 又导入 A → `ImportError: cannot import name X from partially initialized module`；**根因**：两个模块职责划分不清；**三种解法**：① 重新划分职责（**首选**，循环导入通常是设计问题的信号）② 把 import 移到函数内部（能用但是补丁）③ 抽出公共部分成第三个模块 | ★ |
| 18.17 | 探索一个陌生模块 | `dir(模块)` 列出它有什么、`help(模块)` 看文档、`模块.__file__` 看它在哪个文件；**什么时候用**：想知道某个库能干什么，比翻文档快 | ★★ |
| 18.18 | `__pycache__` 是什么 | 里面是 `.pyc` 字节码缓存，**用来加快下次导入**（不是编译成机器码，只是省掉解析步骤）；**要不要管它**：不用管，但**要加进 `.gitignore`**（第 37 章）；**删掉它没有任何损失**，下次导入会重新生成；改了 `.py` 后缓存会自动失效 | ★★ |

### 代码示例清单

> **本章示例的固定格式**：先画目录结构，再列各文件内容（每个代码块上方标明文件名），再给运行命令和输出。

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 18-1 | 把函数搬到另一个文件 | A | 建 `tools.py` + `main.py`，`import tools` 后 `tools.add(1,2)` | `import tools` |
| 18-2 | 四种 import 写法对照 | B | 同一个函数用四种方式导入，调用形式的差别 | `from m import f` |
| 18-3 | 给长名字起别名 | A | `import mymodule as m`；提一句 `import pandas as pd` 是社区惯例 | `as` |
| 18-4 | `from m import *` 的问题 | B | ❌ 覆盖了自己的同名变量、看不出名字来源 → ✅ 显式导入 | 命名空间污染 |
| 18-5 | **import 会执行整个文件** | B | 模块顶层放 `print("模块被执行了")`，`import` 时就输出；**推论：顶层别放副作用代码** | 执行时机 |
| 18-6 | 模块只执行一次 | B | `import` 三次，"被执行了"只打印一次；说明 `sys.modules` 缓存；提 Jupyter 改代码不生效的解法 | `sys.modules` |
| 18-7 | **`__name__` 的两种值** | B | 同一个文件：直接运行时 `__name__` 是 `"__main__"`；被 import 时是 `"模块名"`。**两种运行方式的输出并排对照** | `print(__name__)` |
| 18-8 | **`if __name__ == "__main__"` 的三个用途** | B | ① 自测代码 ② 双重身份 ③ 程序入口；演示"被导入时测试代码不打扰别人" | `if __name__ == "__main__":` |
| 18-9 | 看看 Python 从哪里找模块 | A | `print(sys.path)`，指出第一项是脚本目录 | `sys.path` |
| 18-10 | **把文件命名为 random.py 的惨剧** | B | ❌ 自己的 `random.py` 遮蔽了标准库 → `AttributeError: module 'random' has no attribute 'randint'` → ✅ 改名 + 删 `__pycache__`；**列出十个不能用的文件名** | 命名冲突 |
| 18-11 | ModuleNotFoundError 三步排查 | B | 文件名 → 搜索路径 → 解释器（回收 1.6） | 排查清单 |
| 18-12 | 建一个包 | B | 目录 + `__init__.py`；`import mypkg.storage` 与 `from mypkg import storage` | 包结构 |
| 18-13 | `__init__.py` 做统一导出 | B | 在 `__init__.py` 里 `from .storage import load`，用户就能 `from mypkg import load` | 统一导出 |
| 18-14 | 绝对导入与相对导入 | B | 同一个包内两种写法对照 | `from . import x` |
| 18-15 | **相对导入不能直接运行** | B | ❌ `python mypkg/storage.py` → `ImportError: attempted relative import with no known parent package` → ✅ `python -m mypkg.storage` | `python -m` |
| 18-16 | python -m 的其他用途 | A | `python -m http.server`、`python -m json.tool` 格式化 JSON | `python -m` |
| 18-17 | import 语句的规范顺序 | A | 三组分开、组间空行 | PEP 8 |
| 18-18 | 循环导入的样子 | B | 两个文件互相 import 的真实报错；给出"重新划分职责"的修法 | 循环导入 |
| 18-19 | 探索一个陌生模块 | A | `dir(json)`、`help(json.dumps)`、`json.__file__` | `dir()` |
| 18-20 | `__pycache__` 里是什么 | A | 看一眼目录；说明可以随便删、要加进 `.gitignore` | `__pycache__` |
| 18-21 | **把成绩管理拆成三个模块** | C | **验收项目 A3 → 多文件版**：`storage.py`（JSON 读写，用第 17 章）+ `report.py`（统计与格式化）+ `main.py`（交互入口，含 `if __name__ == "__main__"`）；**这是从"脚本"到"程序"的关键一跃，也是验收项目 B 的结构基础** | 综合 |
| 18-22 | 写一个自己的工具模块 | C | `utils.py` 收集前面章节写过的可复用函数（安全转换 15-22、输入校验 15-21、格式化输出）；配 docstring；**说明这就是"积累自己的工具箱"的开始** | 综合 |

### 样板示例

**示例 18-7 + 18-8　`__name__` 与 `if __name__ == "__main__"`**

这是本章最重要的样板，要用"制造疑问 → 观察现象 → 解释原理 → 给出用途"的顺序写：

**第一步：制造疑问。** 先展示一个不加这一行的模块——它里面有自测代码，被 import 时自测代码也跟着执行了，打扰了调用方：

```text
运行 main.py 时的输出：
  === 开始自测 ===        ← 这是 tools.py 里的自测代码，不该出现
  测试通过
  3
```

**第二步：观察现象。** 在 `tools.py` 里加一行 `print(f"{__name__=}")`（用第 5 章的调试写法），然后用两种方式运行，对照输出：

```text
python tools.py    →  __name__='__main__'
python main.py     →  __name__='tools'      （tools 是被 import 的那一方）
```

**第三步：解释原理。** `__name__` 是 Python 自动给每个模块设置的变量，规则只有一条：**直接运行的那个文件，它的 `__name__` 是 `"__main__"`；被导入的模块，它的 `__name__` 是模块名。**

**第四步：给出用途。** 于是 `if __name__ == "__main__":` 的含义就清楚了——"只在直接运行我的时候才做下面的事"。三个用途各给一个具体例子。

**示例 18-10　把文件命名为 random.py 的惨剧**

演示重点：这是真实高频事故，症状诡异到让人怀疑 Python 坏了。完整还原过程：

1. 目录里有一个自己写的 `random.py`（哪怕只是随手做的小实验）
2. 另一个文件里 `import random; random.randint(1, 10)`
3. 报错：`AttributeError: module 'random' has no attribute 'randint'`
4. **困惑点**：`randint` 明明是标准库的函数，怎么会没有
5. **原因**：`sys.path` 第一项是当前目录，所以 `import random` 拿到的是你自己那个文件
6. **修法**：改名，**并且删掉 `__pycache__`**（否则缓存还在，问题继续）

要列出一张"**别用这些文件名**"的清单：`random.py`、`json.py`、`csv.py`、`time.py`、`datetime.py`、`email.py`、`string.py`、`test.py`、`types.py`、`logging.py`、`os.py`、`socket.py`。

**怎么判断一个名字有没有被占用**：`python -c "import 名字"`，不报错就说明标准库里有。

**示例 18-21　把成绩管理拆成三个模块**

演示重点：这是本章的落地示例，也是全书第一个"像真项目"的结构。

```text
grades/
├── main.py         ← 入口：菜单交互，if __name__ == "__main__"
├── storage.py      ← 只管存取：load_data() / save_data()（用第 17 章的 JSON）
└── report.py       ← 只管统计与格式化：average() / top_n() / format_table()
```

要讲清**为什么这样拆**（三条判断标准）：

| 模块 | 职责 | 判断依据 |
|---|---|---|
| `storage.py` | 数据进出 | 换成数据库存储时，**只改这一个文件** |
| `report.py` | 计算与格式化 | 纯函数，**不碰文件也不碰输入**，最容易单独测试（第 36 章） |
| `main.py` | 与人交互 | 只做"问用户要什么、调用另两个模块、把结果显示出来" |

**并且要说明依赖方向**：`main` → `storage` / `report`，而 `storage` 和 `report` **互不依赖**——这样就不会出现循环导入（18.16）。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 自己的文件叫 `random.py` | `AttributeError: module 'random' has no attribute ...` | 改名 + 删 `__pycache__` | 当前目录优先于标准库 |
| 文件名带减号 `my-tools.py` | 无法 `import my-tools` | 用下划线 `my_tools.py` | 模块名必须是合法标识符 |
| 模块顶层放了耗时代码 | 一 `import` 就卡住 | 挪进函数或 `if __name__` 块 | import 会执行整个文件 |
| 在 Jupyter 里改了模块代码 | 重新 `import` 不生效 | 重启内核，或 `importlib.reload` | 模块只执行一次 |
| 直接运行含相对导入的文件 | `ImportError: attempted relative import with no known parent package` | `python -m 包.模块` | 相对导入需要包上下文 |
| `from m import *` | 名字被覆盖、来源不明 | 显式导入需要的名字 | 命名空间污染 |
| 两个模块互相 import | `ImportError: cannot import name ... partially initialized` | 重新划分职责 | 循环导入 |
| `ModuleNotFoundError` 但文件就在旁边 | —— | 检查工作目录（回收 16.15）、检查解释器（回收 1.6） | 搜索路径不含那个目录 |
| 把 `__pycache__` 提交进 Git | 仓库里全是二进制垃圾 | 加进 `.gitignore` | 它是本地缓存 |
| 包目录忘了 `__init__.py` 又用了旧工具 | 找不到包 | 补上 `__init__.py` | 老工具不支持隐式包 |
| import 顺序混乱 | 看不出依赖了什么 | 按标准库/第三方/本地三组排列 | PEP 8 |

### 下一章预告

代码拆成多个文件之后，一个新问题浮现了：**你的程序依赖了哪些第三方库？**

你自己机器上装过 `requests`，所以跑得起来。同事拿到代码，一运行 `ModuleNotFoundError`。更麻烦的是——你的 A 项目要 `requests` 2.28，B 项目要 2.32，装哪个？

第 19 章讲虚拟环境：**让每个项目有一套自己的库，互不干扰**，并且能用一个文件把"需要什么库"记录下来，别人照着一条命令就能装齐。

---

## 第 19 章　虚拟环境与依赖管理 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 理解为什么每个项目要有独立环境；会创建、激活、退出虚拟环境；会用 `requirements.txt` 记录和恢复依赖；知道 `.venv` 不能提交到 Git；能解决 Windows 上激活失败的问题 |
| 前置 | 第 18 章 |
| 预计用时 | 3~4 小时（含实际操作） |
| 可用语法边界 | 阶段一全部 + 第 16~18 章。**本章 90% 是终端命令，不是 Python 代码**——示例清单中用「终端」标注 |
| 目录调整 | 新增 19.5（**怎么确认激活成功**）、19.7（**Windows PowerShell 执行策略导致激活失败**——这是 Windows 用户的头号卡点）、19.8（`.venv` 不能提交 Git）、19.12（`pip freeze` 的问题：会写入间接依赖）、19.17（两个完整工作流）。原目录 6 个小节只讲了"怎么做"，没讲"做错了什么症状" |
| 编写要点 | 本章的难点不是概念（虚拟环境很好懂），而是**操作会卡住**。三个必须写足的地方：<br>① **激活命令三平台不同**（19.4）——Windows / PowerShell / macOS / Linux 四套写法，写错了就报"找不到文件"；<br>② **Windows PowerShell 执行策略**（19.7）——`Activate.ps1` 默认被禁止运行，报一句让人完全看不懂的错，无数人卡在这里放弃；<br>③ **怎么确认真的激活了**（19.5）——很多人以为激活了，其实没有，然后把库装到了全局环境，又回到 1.6 那个"装了库却 import 失败"的坑。<br><br>另外本章要**明确回收 1.6 的伏笔**：那时说"这个问题在第 19 章有更彻底的解决办法"，现在就是。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 19.1 | 为什么需要虚拟环境 | **三个真实问题**：① **版本冲突**——A 项目要 `requests` 2.28，B 项目要 2.32，全局只能装一个 ② **同事跑不起来**——你机器上装过的库，别人没有 ③ **全局环境越来越脏**——装了几十个库，不知道哪个项目在用，不敢删；**一句话总结**：让项目之间别互相毒害 | ★★★ |
| 19.2 | 虚拟环境是什么 | 就是**一个目录**（通常叫 `.venv`），里面有一份独立的解释器入口和一个独立的 `site-packages`；**没有魔法**：激活的本质是把这个目录的 `bin`/`Scripts` 放到 `PATH` 最前面，于是 `python` 和 `pip` 指向了它；**演示**：`sys.executable` 在激活前后指向不同路径 | ★★★ |
| 19.3 | 创建 | `python -m venv .venv`（回收 18.14 的 `python -m`）；**为什么叫 `.venv`**：社区惯例，点开头表示隐藏、IDE 能自动识别；创建后看一眼目录结构（`Scripts`/`bin`、`Lib`/`lib`、`pyvenv.cfg`） | ★★★ |
| 19.4 | **激活：四套命令** | 一张表列清楚：<br>Windows CMD → `.venv\Scripts\activate.bat`<br>Windows PowerShell → `.venv\Scripts\Activate.ps1`<br>macOS / Linux → `source .venv/bin/activate`<br>Git Bash on Windows → `source .venv/Scripts/activate`<br>**写错的症状**：`找不到文件` / `command not found` | ★★★ |
| 19.5 | **怎么确认激活成功** | 三重验证：① 提示符前面出现 `(.venv)` ② `where python`（Win）/ `which python`（Unix）指向 `.venv` 里面 ③ **最可靠**：`python -c "import sys; print(sys.executable)"`；**为什么要专门讲**：很多人以为激活了其实没有，然后把库装到全局，回到 1.6 那个坑 | ★★★ |
| 19.6 | 退出 | `deactivate`（四个平台都一样）；**关掉终端窗口也等于退出**——虚拟环境不是"永久设置"，每开一个新终端都要重新激活 | ★★★ |
| 19.7 | **Windows 激活失败** | 症状：`无法加载文件 ...\Activate.ps1，因为在此系统上禁止运行脚本`；**原因**：PowerShell 默认的执行策略禁止运行脚本文件；**两个解法**：① 一次性放开当前用户 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` ② 改用 CMD 或 Git Bash；**这是 Windows 用户最容易卡住并放弃的地方**，必须给完整的报错原文和解法 | ★★★ |
| 19.8 | **`.venv` 不要提交 Git** | 理由：① 体积大（几十上百 MB）② 里面是二进制，跨平台不通用（Windows 建的环境 Mac 上用不了）③ **完全可以重建**——这才是关键；**正确做法**：`.gitignore` 里写 `.venv/`，提交 `requirements.txt`；**类比**：提交"配料表"而不是"做好的菜" | ★★★ |
| 19.9 | pip 常用命令 | `install` / `install -U`（升级）/ `uninstall` / `list` / `show` / `list --outdated`；**`pip show` 的 `Location` 字段**可以验证装到哪了（回收 1.7） | ★★★ |
| 19.10 | 记录依赖 | `pip freeze > requirements.txt`；文件长什么样（`包名==精确版本`，一行一个）；**为什么要有它**：这是"配料表"，别人和未来的你靠它重建环境 | ★★★ |
| 19.11 | 恢复依赖 | `pip install -r requirements.txt`；**接手别人项目的标准三步**：建环境 → 激活 → 装依赖 | ★★★ |
| 19.12 | **`pip freeze` 的问题** | 它会把**间接依赖**也写进去——你只装了 `requests`，freeze 出来有五六个包（`urllib3`、`certifi`…）；**后果**：看不出哪些是你真正需要的，升级时容易冲突；**两种应对**：① 手工维护一份只写直接依赖的 `requirements.txt`（推荐，配合注释说明每个库干什么）② 用现代工具（`uv` / `poetry`）自动区分直接与间接依赖 | ★★ |
| 19.13 | 版本约束写法 | `requests==2.32.5`（锁死）、`>=2.28`（不低于）、`>=2.28,<3.0`（区间）、`~=2.32.0`（兼容版本，等价于 `>=2.32.0,<2.33.0`）；**该用哪个**：应用锁死（可复现），库放宽（避免和别人冲突） | ★★ |
| 19.14 | 国内镜像源 | 临时：`pip install X -i 镜像地址`；永久：`pip config set global.index-url 镜像地址`；给两三个常用镜像（清华、阿里）；**注意**：镜像有同步延迟，刚发布的新版本可能拿不到 | ★★ |
| 19.15 | pip 的常见麻烦 | ① `pip` 自己要升级：`python -m pip install -U pip`（**用 `python -m pip` 而不是 `pip`**，避免升级过程中把自己删了）② 权限错误（`Permission denied`）→ 说明你没在虚拟环境里，正在往系统目录装 ③ 装了但 import 失败 → 回收 1.6 与 19.5 | ★★ |
| 19.16 | 现代工具概览 | `uv`（Rust 写的，**快一到两个数量级**，能同时管 Python 版本和依赖）、`poetry`、`pdm`；**本教程的态度**：先把 `venv` + `pip` 用熟，因为它们是内置的、任何环境都有；等你需要"锁定间接依赖版本""管理多个 Python 版本"时再上工具；`pyproject.toml` 见第 37 章 | ★ |
| 19.17 | 在 IDE 里选中虚拟环境 | VS Code：`Python: Select Interpreter` 选 `.venv` 里的；PyCharm：Settings → Project → Python Interpreter；**回收 1.6**：这就是那个问题的彻底解法——项目一开始就建虚拟环境，IDE 认准它，从此不会搞混 | ★★★ |
| 19.18 | 两个标准工作流 | **① 开新项目**：建目录 → `python -m venv .venv` → 激活 → 装库 → `pip freeze > requirements.txt` → 写 `.gitignore`<br>**② 接手别人的项目**：拿到代码 → 建环境 → 激活 → `pip install -r requirements.txt` → 跑起来<br>**这两个流程要做成清单，让读者能照着执行** | ★★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 19-1 | 全局环境为什么会乱 | A · 终端 | `pip list` 看全局装了多少东西；提出"哪些还在用？"这个无法回答的问题 | `pip list` |
| 19-2 | 创建一个虚拟环境 | A · 终端 | `python -m venv .venv`，看生成的目录结构 | `python -m venv .venv` |
| 19-3 | **激活：四个平台四套命令** | A · 终端 | 一张表；写错时的报错 | `source .venv/bin/activate` |
| 19-4 | **确认真的激活了** | A · 终端 | 提示符 `(.venv)` + `which python` + `sys.executable` 三重验证 | `sys.executable` |
| 19-5 | **Windows 上激活失败怎么办** | B · 终端 | 完整报错原文 + `Set-ExecutionPolicy` 解法 + 改用 CMD 的备选方案 | `Set-ExecutionPolicy` |
| 19-6 | 在虚拟环境里装库 | A · 终端 | 激活后 `pip install requests`，`pip list` 看只有几个包（对比全局的一长串） | `pip install` |
| 19-7 | 验证隔离是真的 | B · 终端 | 环境内 `import requests` 成功 → `deactivate` → 再 import 失败（如果全局没装） | `deactivate` |
| 19-8 | 生成 requirements.txt | A · 终端 | `pip freeze > requirements.txt`，看文件内容 | `pip freeze` |
| 19-9 | 从零恢复一个环境 | B · 终端 | 删掉 `.venv` → 重建 → `pip install -r requirements.txt` → 程序照样能跑；**这个演示最能说明"为什么 .venv 不用提交"** | `pip install -r` |
| 19-10 | freeze 里多出来的包是什么 | B · 终端 | 只装了 `requests`，freeze 出来五六个；解释间接依赖；给出手工维护直接依赖的做法 | 间接依赖 |
| 19-11 | 版本约束的四种写法 | A | `==` / `>=` / 区间 / `~=` 对照表 | `~=2.32.0` |
| 19-12 | 换成国内镜像 | A · 终端 | 临时 `-i` 与永久 `pip config set` | `-i 镜像地址` |
| 19-13 | 升级 pip 的正确方式 | A · 终端 | `python -m pip install -U pip`，解释为什么不用 `pip install -U pip` | `python -m pip` |
| 19-14 | .gitignore 该写什么 | A | `.venv/`、`__pycache__/`、`*.pyc`、`.env`（回收 18.18） | `.gitignore` |
| 19-15 | 让 IDE 用上虚拟环境 | B | VS Code / PyCharm 选解释器；**回收 1.6 的伏笔** | Select Interpreter |
| 19-16 | uv 有多快 | A · 终端 | `uv venv` + `uv pip install` 的耗时对比；一句话说明什么时候值得上它 | `uv` |
| 19-17 | **完整工作流①：开一个新项目** | C · 终端 | 从空目录到"能跑、依赖已记录、Git 已配好"的完整清单 | 工作流 |
| 19-18 | **完整工作流②：接手别人的项目** | C · 终端 | 拿到一个只有代码和 `requirements.txt` 的目录，三步跑起来；**这是验收项目 B 的交付标准** | 工作流 |

### 样板示例

**示例 19-3 + 19-4　激活与确认**

演示重点：**激活命令的平台差异**和**如何确认激活成功**要写在一起，因为它们是同一个动作的两半。

四套命令做成表格（含 Git Bash，很多 Windows 用户用它）：

| 环境 | 激活命令 |
|---|---|
| Windows CMD | `.venv\Scripts\activate.bat` |
| Windows PowerShell | `.venv\Scripts\Activate.ps1` |
| macOS / Linux | `source .venv/bin/activate` |
| Windows + Git Bash | `source .venv/Scripts/activate` |

三重验证要给出真实输出对照——**激活前**和**激活后**的 `sys.executable` 分别指向哪里：

```text
激活前：/usr/bin/python3
激活后：/项目路径/.venv/bin/python
```

**强调最可靠的验证方式是看 `sys.executable`**，因为提示符可能被自定义主题吃掉，而路径不会骗人。

**示例 19-5　Windows 上激活失败**

演示重点：给出**完整的报错原文**（不要简写），因为读者要靠它来对号入座：

```text
.venv\Scripts\Activate.ps1 : 无法加载文件 C:\...\.venv\Scripts\Activate.ps1，
因为在此系统上禁止运行脚本。有关详细信息，请参阅
https:/go.microsoft.com/fwlink/?LinkID=135170 中的 about_Execution_Policies。
```

然后给两条解法并说明各自的代价：

| 解法 | 命令 | 代价 |
|---|---|---|
| 放开当前用户的执行策略 | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` | 需要理解这是在放宽安全设置（只影响当前用户，允许运行本地脚本） |
| 改用 CMD | `.venv\Scripts\activate.bat` | 没有代价，但 CMD 功能比 PowerShell 弱 |

**这一节要写得特别耐心**——它是纯操作问题，但卡住的人极多，而且和编程能力毫无关系。

**示例 19-9　从零恢复一个环境**

演示重点：这是**理解"为什么 `.venv` 不用提交"的最有效方式**——亲手删掉再重建一次。

```text
1. 记录依赖    pip freeze > requirements.txt
2. 删掉环境    rm -rf .venv        （Windows: rmdir /s .venv）
3. 重新创建    python -m venv .venv
4. 激活
5. 装回依赖    pip install -r requirements.txt
6. 程序照样跑起来
```

结论要说透：**环境是"可再生"的，配料表才是要保存的**。这也是为什么交付项目时给的是代码 + `requirements.txt`，而不是一个几百 MB 的环境目录。

**示例 19-17 / 19-18　两个完整工作流**

做成可以照着执行的**清单**（带勾选框），而不是散文。这两个清单是本章最实用的产出，读者以后会反复回来查。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| PowerShell 里激活 | `因为在此系统上禁止运行脚本` | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 或改用 CMD | 执行策略限制 |
| 用了别的平台的激活命令 | `找不到文件` / `command not found` | 按 19.4 的表选对命令 | 平台路径不同 |
| 以为激活了其实没有 | 库装到了全局，`import` 还是失败 | 用 `sys.executable` 验证 | 没看提示符 |
| 忘了激活就 `pip install` | 装进全局环境，污染系统 | 先激活再装 | —— |
| 开了新终端窗口没激活 | 命令找不到库 | 每个新终端都要重新激活 | 激活只对当前会话有效 |
| 把 `.venv` 提交进 Git | 仓库几百 MB，别人还用不了 | `.gitignore` 写 `.venv/` | 环境不跨平台且可重建 |
| 只提交代码不提交 `requirements.txt` | 别人不知道要装什么 | 提交配料表 | —— |
| `pip install -U pip` | Windows 上可能失败（自己删自己） | `python -m pip install -U pip` | pip 正在运行中 |
| `Permission denied` | 往系统目录装库 | 说明没在虚拟环境里 | 权限不足是个信号 |
| 直接用 `freeze` 的输出当依赖清单 | 分不清直接/间接依赖 | 手工维护直接依赖列表 | freeze 记录一切 |
| 虚拟环境目录改名或移动 | 里面的路径失效、`python` 报错 | 删掉重建（**环境不要移动**） | 内部记录的是绝对路径 |
| IDE 没选虚拟环境 | 编辑器报红、运行报 ModuleNotFound | 重新 Select Interpreter | 回收 1.6 |

### 下一章预告

到这里，你的程序有了正经的骨架：多文件、独立环境、依赖可重建。但接下来会遇到一个新阶段的问题——**程序跑起来了，结果却不对**。

你会开始往代码里插一堆 `print` 来看变量到底是什么值，跑一遍、删掉、再插几个、再跑一遍。这个过程极其低效，而且插得越多越乱。

第 20 章讲调试：**怎么让程序在你想看的地方停下来，让你随便查看任何变量**。这一章的位置是刻意前置的——越早会打断点，越早脱离"加一行 print 跑一遍"的循环。

---

## 第 20 章　调试：从 print 到断点 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 摆脱"插一堆 `print` 再删"的低效循环；会在 IDE 里打断点、单步执行、查看变量；会用 `breakpoint()` 和 `pdb`；掌握"最小复现"和"二分定位"两种排错方法；能在程序崩溃后进入现场查看当时的变量 |
| 前置 | 第 19 章 |
| 预计用时 | 4~5 小时（必须动手，光看没用） |
| 可用语法边界 | 阶段一全部 + 第 16~19 章 + `breakpoint()` / `pdb` / `traceback`。**不得使用**：`logging`（第 21 章，本章只在"方法阶梯"里提一句它的位置）、`pytest`（第 36 章） |
| **写作策略** | **本章有个特殊问题：IDE 操作无法用代码演示。** 处理原则：<br>① **以 `pdb` 为主线**——它是命令行工具，示例可运行、输出可验证、不依赖任何 IDE；<br>② **IDE 部分用"操作步骤 + 按键表 + 界面元素说明"的文字形式**，不依赖截图（截图会随 IDE 版本失效，且本书尽量不用图）；<br>③ 明确告诉读者：**两者的概念完全一样**（断点、单步、变量面板），学会 `pdb` 就理解了 IDE 调试器在做什么，反之也成立。 |
| 目录调整 | 新增 20.3（`print` 调试的正确姿势——**不是不能用 print，而是要用对**）、20.7（**条件断点**：循环里只在特定条件停）、20.10（**崩溃后进入现场** `pdb.pm()`，这是最被低估的调试技巧）、20.13（常见 bug 类型 → 对应查法的对照表）、20.14（调试的心理纪律）。原目录 5 个小节偏薄，撑不起"这一章要改变读者的工作方式"这个目标 |
| 编写要点 | 本章的目标不是教工具，而是**改变工作方式**。三处重笔：<br>① **开篇要让读者认出自己的低效模式**——插 print、跑、删、再插，一小时过去了。不先制造这个共鸣，读者不会有动力学断点；<br>② **`Step Over` / `Step Into` / `Step Out` 的区别**（20.5）——这是新手用调试器时最迷惑的地方，必须用一段有函数调用的代码逐步演示；<br>③ **20.15 的完整排查实录**——给一段有隐蔽 bug 的代码，把"现象 → 假设 → 验证 → 定位 → 修复"整个过程写出来，包括**走过的弯路**。这是全章最有价值的部分，因为排错思维只能通过看别人怎么想来学。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 20.1 | `print` 调试的困境 | 四个具体问题：① 插了要删，删漏了就污染输出 ② 只能看到你**想到要看**的变量，看漏了得重跑 ③ 看不到调用是从哪儿来的 ④ 循环里打印上千行，找不到关键那次；**共鸣式开场**：描述"插 print → 跑 → 没看到关键信息 → 再插 → 再跑"这个循环，一小时就没了 | ★★★ |
| 20.2 | 调试方法阶梯 | 四级：`print`（临时、一次性）→ `logging`（长期、可开关，第 21 章）→ **IDE 断点**（交互、看全貌）→ `pdb`（无 IDE 时、远程服务器上）；**关键认知**：不是越高级越好，**是场合不同**——查一个值用 print 最快，理不清流程必须用断点 | ★★★ |
| 20.3 | `print` 调试的正确姿势 | 既然还会用它，就要用对：① **用 `f"{x=}"`**（回收 5.10）自动带变量名，改名不用改两处 ② 加醒目前缀 `print("### 进到这里了", x)` 便于搜索和删除 ③ 打印**类型**而不只是值（`f"{x=} {type(x)=}"`——很多 bug 是类型不对）④ 用 `repr()` 看清空白字符和引号（回收 5.10 的 `repr` 用法） | ★★★ |
| 20.4 | 断点的概念 | **断点 = 让程序在这一行暂停，把当时的一切交给你看**；三件事：暂停位置、当时所有变量的值、调用栈（谁调用了这里）；**为什么比 print 强**：不用事先猜要看什么，停下来后**所有**变量都能看 | ★★★ |
| 20.5 | **单步执行的三个动作** | `Step Over`（执行这一行，**不进入函数内部**）、`Step Into`（**进入**这一行调用的函数）、`Step Out`（把当前函数**执行完并跳回**调用处）；**必须用一段有函数调用的代码演示三者的区别**；**经验规则**：默认用 Step Over，只在"怀疑这个函数内部有问题"时才 Step Into；**别 Step Into 到标准库里去**（回收 2.6：库的代码不用看） | ★★★ |
| 20.6 | 变量面板与表达式求值 | 变量面板自动显示当前作用域的所有变量；**Watch / Evaluate 功能可以现场算任意表达式**——比如停在断点上直接算 `len(data)`、`data[3]["name"]`，不用改代码重跑；**这是断点最大的价值**：探索式排查 | ★★★ |
| 20.7 | **条件断点** | 循环跑一万次，只有第 8000 次出错时怎么办？给断点加条件（如 `i == 8000` 或 `item is None`），只在条件成立时停；**为什么重要**：没有它，循环里的断点只能一次次按继续，实际用不了 | ★★★ |
| 20.8 | `breakpoint()` | 内置函数（3.7+），在代码里写一行 `breakpoint()`，运行到这里就进入 `pdb`；**优点**：不依赖 IDE、在服务器上也能用；**可以用环境变量 `PYTHONBREAKPOINT=0` 一键禁用所有断点**（这个设计很实用，讲一句） | ★★★ |
| 20.9 | `pdb` 常用命令 | 八个够用：`n`（下一行，相当于 Step Over）、`s`（进入函数，Step Into）、`r`（执行到函数返回，Step Out）、`c`（继续运行到下一个断点）、`l`（看当前代码位置）、`p 表达式`（打印）、`w`（看调用栈）、`q`（退出）；**做成表格，并标出与 IDE 按钮的对应关系** | ★★★ |
| 20.10 | **崩溃后进入现场** | 程序抛异常退出后，**变量已经没了**——除非你用这两招：① `python -m pdb 脚本.py`，崩溃后自动停在出错位置 ② 在 REPL / Notebook 里出错后立刻 `import pdb; pdb.pm()`（post-mortem）进入现场；**为什么重要**：偶发的 bug 你没法预先在正确的地方打断点，崩溃现场是唯一机会；**这是最被低估的调试技巧** | ★★ |
| 20.11 | 最小复现 | 把出问题的代码**删到只剩能复现问题的最少行数**；**为什么有效**：① 删的过程中一半情况会自己发现原因 ② 干扰因素消失，问题暴露 ③ 需要求助时，别人能直接跑你的代码；**具体做法**：新建一个空文件，把可疑部分抄进去，逐步删减 | ★★★ |
| 20.12 | 二分定位 | 不知道问题在哪一段时：**在中间插一个检查点**（print 或断点），确认问题在前半还是后半，然后对半段继续；**为什么高效**：一千行代码，十次二分就能定位到具体一行；**也适用于数据**（一万条记录里哪条有问题）和**版本**（哪次改动引入的 bug，Git 的 `bisect` 就是这个思路） | ★★★ |
| 20.13 | 先怀疑数据 | 新手的默认假设是"我的逻辑错了"，但实际上**很大一部分 bug 是数据不符合预期**——多了空格、类型是字符串不是数字、有 `None`、编码不对、少了字段；**具体做法**：在处理数据前先把它原样打印出来（用 `repr()`），确认它真的长你以为的样子 | ★★★ |
| 20.14 | bug 类型 → 查法对照 | 一张表：<br>**值不对** → 断点看输入输出，用二分定位哪一步开始错<br>**流程不对**（没进 if / 循环次数不对）→ 断点 + 单步走一遍<br>**抛异常** → 读 Traceback（第 2 章）+ 崩溃后进现场<br>**偶发** → 加日志（第 21 章），断点抓不住<br>**慢** → 不是调试问题，是性能分析（专项 B） | ★★★ |
| 20.15 | 调试的心理纪律 | 四条：① **一次只验证一个假设**（改五处再跑，分不清是哪处生效）② **不要瞎改**——看不懂就先看懂，随机改动能"碰对"但留下隐患 ③ **记下你试过什么**（超过十分钟的排查就该记录，否则会重复试）④ **卡住 30 分钟就换方式**：休息、换人看、或回到最小复现 | ★★ |
| 20.16 | 读第三方库的报错 | 回收 2.6：Traceback 里只看属于自己文件的行；**在调试器里同理**：不要 Step Into 到库内部；调用栈面板里**先找自己的那一帧** | ★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 20-1 | print 调试的困境 | B | 给一段结果不对的代码，用 print 找问题——**故意演示"插了三轮才找到"的过程**，制造共鸣 | `print(x)` |
| 20-2 | print 调试的四个改进 | B | `f"{x=}"`、醒目前缀、打印类型、用 `repr()` 看空白字符 | `f"{x=} {type(x)=}"` |
| 20-3 | 第一个断点：breakpoint() | B | 在代码里写 `breakpoint()`，运行后进入 `(Pdb)` 提示符，用 `p` 查看变量 | `breakpoint()` |
| 20-4 | pdb 八个命令实操 | B | `l` 看位置 → `n` 下一行 → `p x` 打印 → `c` 继续 → `q` 退出，**给完整的交互记录** | `(Pdb) n` |
| 20-5 | **`n` 和 `s` 的区别** | B | 一段有函数调用的代码：用 `n` 直接跨过函数调用；用 `s` 进入函数内部；**输出并排对照** | `n` vs `s` |
| 20-6 | 在断点里现场算表达式 | B | 停下后 `p len(data)`、`p data[2]["name"]`——**不改代码就能探索** | `p 表达式` |
| 20-7 | 看调用栈 | B | `w` 命令显示"谁调用了这里"，配合多层函数调用 | `(Pdb) w` |
| 20-8 | 条件断点（pdb 版） | B | `b 行号, 条件` 只在条件成立时停；配合一个跑 1000 次的循环 | `b 12, i == 800` |
| 20-9 | 一键禁用所有断点 | A | `PYTHONBREAKPOINT=0 python app.py`，`breakpoint()` 全部失效 | `PYTHONBREAKPOINT=0` |
| 20-10 | **崩溃后进入现场** | B | `python -m pdb app.py` 崩溃后自动停住，查看当时的变量；以及 REPL 里的 `pdb.pm()` | `python -m pdb` |
| 20-11 | IDE 调试器操作步骤 | B · 文字 | VS Code / PyCharm 的操作步骤 + **按键与 pdb 命令的对应表**（不用截图） | F9 / F10 / F11 |
| 20-12 | IDE 的条件断点与 Watch | B · 文字 | 右键断点填条件；Watch 面板加表达式 | 界面操作 |
| 20-13 | 二分定位法实战 | B | 一段 60 行的处理流程，用三次二分找到出错的那一步 | 二分 |
| 20-14 | 最小复现实战 | B | 把一个 40 行的 bug 删到 5 行，**在删的过程中发现原因** | 删到最小 |
| 20-15 | **完整排查实录：平均分算低了** | C | **本章核心示例**。给一段成绩统计代码，现象是"平均分明显偏低"。完整走一遍：<br>现象 → 假设①（除法用错？验证：不是）→ 假设②（数据有问题？）→ 断点查看数据 → 发现有 `None` 值被当成 0 → 定位到数据来源 → 修复 → 验证。**要写出走过的弯路** | 综合 |
| 20-16 | 调试"边遍历边删除" | C | 回收 9.18：用断点观察循环变量和列表长度的变化，**亲眼看到下标错位的过程**——这比读文字解释深刻得多 | 综合 |

### 样板示例

**示例 20-5　`n` 和 `s` 的区别**

演示重点：这是新手用调试器最迷惑的地方。用一段有函数调用的简单代码，把两种走法的**完整交互记录**并排给出：

```text
用 n（Step Over）：直接跨过 calc() 这一行，下一步就到了 print
用 s（Step Into）：进入 calc() 内部，可以看到函数里的局部变量
```

要给出**经验规则**：默认用 `n`，只在"怀疑这个函数内部有问题"时用 `s`。并提醒**别 `s` 进标准库**——按 `r` 或 `c` 赶快出来。

**示例 20-15　完整排查实录：平均分算低了**

这是本章最重要的样板，**要把思考过程完整写出来，包括错误的假设**。

**场景**：一份成绩数据，统计平均分，结果明显比手算的低。

**代码骨架**（含一个隐蔽 bug）：

```text
data = load_scores()          # 从 JSON 读，某些学生的成绩字段是 null（第 17 章的 None）
total = 0
for name, score in data.items():
    total += score or 0       # ← bug 在这里：用了 4.9 的 or 取默认值
avg = total / len(data)       # 分母是全部人数，分子却少算了缺考的人
```

**排查过程要这样写**：

| 步骤 | 做了什么 | 结果 |
|---|---|---|
| 1 | 观察现象：平均分 62，手算应该是 78 | 差得多，不是精度问题 |
| 2 | **假设①：除法写错了**（回收 4.2 的 `/` vs `//`） | 检查发现用的是 `/`，没错。**假设排除** |
| 3 | **假设②：数据有问题**（20.13 的"先怀疑数据"） | 打断点看 `data` |
| 4 | 在断点里 `p data` | 发现有几个人的成绩是 `None` |
| 5 | `p total` 逐轮观察 | `None or 0` 把缺考当成了 0 分 |
| 6 | 定位根因 | 分子把缺考算成 0，分母却算了全部人数——**这是业务逻辑错误，不是语法错误** |
| 7 | 修复 | 明确决定缺考怎么算：**排除**（分母也减去）还是**算 0 分**（业务说了算） |
| 8 | 验证 | 重跑，平均分 78，与手算一致 |

**要点提示要写清三件事**：

1. **假设①虽然错了，但排查它是必要的**——排除法就是这么工作的，不要因为"猜错了"而气馁
2. **这个 bug 的根源是第 4.9 节警告过的 `or` 取默认值**——`None or 0` 和 `0 or 0` 无法区分"缺考"和"考了 0 分"
3. **最终的修复是业务决策，不是技术决策**——调试的终点常常是"去问清楚需求"

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 一次改五个地方再跑 | 分不清是哪个改动生效 | 一次只验证一个假设 | —— |
| 看不懂就随机改动 | 偶尔"碰对"，留下隐患 | 先看懂再改 | 碰对不等于修好 |
| 只怀疑代码不怀疑数据 | 在正确的代码里找了两小时 | 先 `repr()` 打印输入数据 | 大量 bug 来自数据 |
| 循环里打无条件断点 | 按一万次继续 | 用条件断点 | —— |
| `s` 进了标准库内部 | 迷失在别人的代码里 | 按 `r` / `c` 出来 | 库的代码不用看 |
| 调试完忘了删 `breakpoint()` | 程序在生产环境卡住等输入 | 删掉，或用 `PYTHONBREAKPOINT=0` 兜底 | —— |
| 调试完留下一堆 print | 输出污染、日后困惑 | 加醒目前缀便于搜索删除，或改用 `logging` | —— |
| 程序崩了才想看变量 | 变量已经没了 | `python -m pdb` 或 `pdb.pm()` | 进程已退出 |
| 卡住两小时死磕 | 效率极低、情绪变差 | 30 分钟没进展就换方式 | —— |
| 偶发 bug 硬用断点抓 | 蹲守半天碰不到 | 加日志（第 21 章）等它自己出现 | 断点需要可复现 |

### 下一章预告

断点解决的是"**开发时**"的问题——你坐在电脑前，能随时暂停程序。

但交付出去的程序不是这样：它可能在半夜自动运行、可能在别人的机器上、可能在服务器上跑了三天才出错一次。这时候你没法打断点，**只能靠它自己留下的痕迹来判断发生了什么**。

第 21 章讲 `logging`——给程序装上"行车记录仪"。同时会回答一个你可能已经在想的问题：**为什么不直接用 `print` 记录？**

---

## 第 21 章　日志 logging ★★

| 项 | 内容 |
|---|---|
| 本章目标 | 知道交付的程序为什么不能用 `print`；会用三行 `basicConfig` 起步；理解五个级别与"默认只显示 WARNING 以上"这个坑；会把日志写进文件并轮转；会用 `logger.exception` 记录完整异常堆栈；知道日志里不能写什么 |
| 前置 | 第 20 章（调试）、第 15 章（异常） |
| 预计用时 | 3~4 小时 |
| 可用语法边界 | 阶段一全部 + 第 16~20 章 + `logging` / `logging.handlers`。**不得使用**：`argparse`（第 24 章）、`datetime`（第 23 章——日志的时间戳由 logging 自己格式化，不需要手动处理时间） |
| 目录调整 | 新增 21.4（**默认级别是 WARNING，所以 `logger.info()` 什么都不显示**——这是新手用 logging 的头号困惑）、21.12（**`%s` 与 f-string 之争**，要客观呈现）、21.13（`basicConfig` 只生效一次的坑）、21.14（日志文件中文乱码）、21.15（关掉第三方库的日志噪音）、21.16（**日志里不能写密码**——安全问题）。原目录 6 个小节只覆盖了"怎么配"，没覆盖"配了不生效怎么办" |
| 编写要点 | 本章最容易写成"配置参数说明书"，那就失败了。三个必须做到的地方：<br>① **开篇必须回答"为什么不用 print"**（21.1），而且要给具体场景——半夜自动运行的脚本出错了，你第二天来查，`print` 的输出早就没了；<br>② **21.4 的坑要放在最前面讲**——新手照文档写了 `logger.info("开始")`，运行后什么都没有，直接以为 logging 坏了。这个坑必须在读者踩之前就点明；<br>③ **21.19 要产出一个能直接复制到项目里用的配置**——本章的实用价值最终体现在这里，读者以后大概率只回来抄这一段。<br><br>另外 21.12 是有争议的话题，**要客观呈现两种观点和各自的理由**，不要简单站队。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 21.1 | 为什么不能用 `print` | **六个具体理由**：① 关不掉（要么全留要么全删）② 分不出重要程度（正常信息和错误混在一起）③ 没有时间——出错了不知道什么时候的事 ④ 不知道来源（哪个模块哪一行）⑤ 不能同时输出到屏幕和文件 ⑥ 交付后跑在别人机器上，输出根本看不到；**关键场景**：半夜自动跑的脚本出错了，第二天你来查，`print` 的输出早已随终端关闭而消失 | ★★★ |
| 21.2 | 三行起步 | `logging.basicConfig(level=logging.INFO, format=...)` + `logging.info("消息")`；**先给能跑的最小配置**，再讲原理——不要一上来就 Logger/Handler/Formatter | ★★★ |
| 21.3 | 五个级别 | `DEBUG` < `INFO` < `WARNING` < `ERROR` < `CRITICAL`；**关键是"什么时候用哪个"**，给一张判断表：<br>`DEBUG`——排查时才需要看的细节（变量值、中间结果）<br>`INFO`——程序正常运行的关键节点（开始处理、处理了多少条、完成）<br>`WARNING`——**不影响运行但需要注意**（配置缺失用了默认值、数据有异常但能跳过）<br>`ERROR`——**这次操作失败了**，但程序还能继续<br>`CRITICAL`——程序没法继续了 | ★★★ |
| 21.4 | **默认只显示 WARNING 以上** | 不配 `level` 时，`logging.info()` / `debug()` **什么都不输出**；**症状**：照着文档写了日志，运行后一片空白，以为 logging 坏了；**原因**：默认级别是 `WARNING`；**解法**：`basicConfig(level=logging.INFO)`；**这个坑必须在读者踩之前讲** | ★★★ |
| 21.5 | 格式化 | `format="%(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s"`；把常用占位符做成表（`asctime` / `levelname` / `name` / `module` / `funcName` / `lineno` / `message` / `process` / `thread`）；`datefmt` 控制时间格式；**推荐一个够用的默认格式**并解释每一段为什么需要 | ★★★ |
| 21.6 | 输出到文件 | `basicConfig(filename="app.log")`；**注意**：加了 `filename` 之后**屏幕上就不显示了**（这也是一个常见困惑）；`filemode="w"` 会每次清空（回收 16.3 的 `w` 模式警告），默认 `"a"` 追加 | ★★★ |
| 21.7 | 同时输出到屏幕和文件 | `basicConfig(handlers=[FileHandler(...), StreamHandler()])`；**这是实际项目的标准配置**——开发时看屏幕，出问题查文件；顺带引出下一节的三件套概念 | ★★★ |
| 21.8 | Logger / Handler / Formatter | 三件套的职责：**Logger** 是你调用的入口（决定级别、名字）、**Handler** 决定输出到哪（文件/屏幕/网络）、**Formatter** 决定长什么样；**一个 Logger 可以挂多个 Handler**，各自可以有不同级别（比如屏幕只看 INFO，文件记 DEBUG）；**用一张两层图或表说明数据流向**（不用 Mermaid，表格更清楚） | ★★ |
| 21.9 | `getLogger(__name__)` | **多模块项目的标准做法**：每个模块开头写 `logger = logging.getLogger(__name__)`；**好处**：日志里自动带上模块名（回收 18.7 的 `__name__`），一眼看出是哪个文件打的；**不要在模块里调用 `basicConfig`**——配置是应用入口的事，模块只负责"打日志" | ★★★ |
| 21.10 | Logger 的层级 | 名字里的点表示父子关系（`app.storage` 的父是 `app`）；子 logger 的消息会**向上传递**给父的 handler（`propagate`）；**为什么要知道**：这解释了"为什么我只配了根 logger，所有模块的日志都出来了"，也解释了"为什么日志重复输出两遍"（同时配了父和子的 handler） | ★★ |
| 21.11 | **记录异常堆栈** | `logger.exception("出错了")` **自动附上完整 Traceback**（只能在 `except` 块里用）；等价写法 `logger.error("...", exc_info=True)`；**对照演示**：只用 `logger.error(e)` 只能得到一行错误消息，**丢掉了最有价值的栈信息**；**这是 logging 相比 print 最有价值的能力之一** | ★★★ |
| 21.12 | **`%s` 还是 f-string** | 官方推荐 `logger.info("处理了 %s 条", n)` 而不是 `logger.info(f"处理了 {n} 条")`；**理由**：① **延迟格式化**——级别不够时字符串根本不会被拼接，省掉开销 ② 某些日志聚合系统能利用参数结构；**反方观点**：f-string 可读性明显更好，性能差异在绝大多数应用里可以忽略；**本书建议**：**普通应用用 f-string 没问题**；但如果在**热路径**（每秒几千次）打 DEBUG 日志、或团队用 `ruff` 的 `G` 规则检查，就用 `%s` 风格。**要客观给出两方理由，让读者自己决定** | ★★ |
| 21.13 | `basicConfig` 只生效一次 | 第二次调用**默认被忽略**（除非 `force=True`）；**症状**：在 Jupyter 里改了配置不生效、或者某个库先调用了 `basicConfig` 把你的配置顶掉了；**解法**：`force=True`，或者干脆不用 `basicConfig` 而是手工配 handler | ★★ |
| 21.14 | 日志文件中文乱码 | `FileHandler(..., encoding="utf-8")`；**不写会怎样**：Windows 上用系统编码写，用别的工具打开就乱码（回收 16.10 的编码铁律）；**`basicConfig` 也支持 `encoding` 参数**（3.9+） | ★★★ |
| 21.15 | 关掉第三方库的噪音 | 装了 `requests` / `urllib3` 之后，把级别设成 DEBUG 会刷出大量库内部日志；**解法**：`logging.getLogger("urllib3").setLevel(logging.WARNING)` 单独压低某个库的级别 | ★★ |
| 21.16 | **日志里不能写什么** | **绝对不要记**：密码、密钥、Token、完整的身份证/银行卡号、用户隐私数据；**为什么**：日志文件常常权限宽松、会被复制、会被发给同事排查、会进日志聚合系统；**做法**：需要记录时脱敏（`138****5678`）；**这是安全问题，不是风格问题** | ★★★ |
| 21.17 | 日志轮转 | `RotatingFileHandler`（按大小，`maxBytes` + `backupCount`）、`TimedRotatingFileHandler`（按时间，`when="midnight"`）；**为什么需要**：不轮转的日志文件会一直长，几个月后几个 GB，磁盘满了程序就崩了 | ★★ |
| 21.18 | 日志该记什么 | 四条实用建议：① **记录"发生了什么"而不是"代码走到哪"**（`处理订单 1234 失败` 比 `进入 process 函数` 有用）② 带上关键标识（订单号、文件名、用户 ID）——出问题时靠它筛查 ③ 循环里不要每次都记（一万条数据打一万行），改成每 1000 条记一次进度 ④ **成对记录**（开始/结束、成功/失败），才能算出耗时和成功率 | ★★★ |
| 21.19 | `print` vs `logging` 怎么选 | 判断标准：**这个输出是给"用户"看的，还是给"排查问题的人"看的**？给用户看的（命令行工具的正常结果、进度提示）用 `print`；给排查用的一律 `logging`；**注意**：命令行工具的正常输出确实应该用 `print`（第 24 章会讲），日志走 `stderr` 或文件 | ★★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 21-1 | print 记录的困境 | B | 一个"半夜自动运行"的脚本用 print 记录，第二天什么都查不到；列出六个理由 | —— |
| 21-2 | 三行日志起步 | A | `basicConfig(level=INFO)` + `logging.info` | `logging.basicConfig(level=logging.INFO)` |
| 21-3 | **为什么 info 什么都不显示** | A | ❌ 不设 `level` 时 `info` / `debug` 无输出 → ✅ 设 `level=logging.INFO` | `level=logging.INFO` |
| 21-4 | 五个级别都试一遍 | A | 五行日志 + 级别设成 WARNING 时只出来后三条 | `logger.debug/info/warning/error/critical` |
| 21-5 | 什么时候用哪个级别 | B | 一段真实处理流程，五个级别各用在恰当的地方 | 级别判断表 |
| 21-6 | 让日志带上时间和位置 | A | `format` 常用占位符；给出推荐的默认格式 | `format="%(asctime)s ..."` |
| 21-7 | 写进文件 | A | `filename="app.log"`；**注意屏幕上就不显示了** | `filename=` |
| 21-8 | 屏幕和文件都要 | B | `handlers=[FileHandler, StreamHandler]`；两个 handler 设不同级别 | `handlers=[...]` |
| 21-9 | 日志文件中文乱码 | A | ❌ 不写 encoding → ✅ `encoding="utf-8"` | `encoding="utf-8"` |
| 21-10 | 多模块项目的日志 | B | 两个模块各写 `getLogger(__name__)`，日志里能看出来源；入口统一配置 | `logging.getLogger(__name__)` |
| 21-11 | **记录完整异常堆栈** | B | ❌ `logger.error(e)` 只有一行 → ✅ `logger.exception("...")` 带完整 Traceback；两种输出并排对照 | `logger.exception(...)` |
| 21-12 | 日志重复输出两遍 | B | 同时配了父子 handler 导致重复；用 `propagate=False` 或只配一处 | `propagate` |
| 21-13 | basicConfig 第二次不生效 | B | ❌ 改配置没反应 → ✅ `force=True` | `force=True` |
| 21-14 | `%s` 与 f-string | B | 两种写法并列；**客观给出各自理由**；说明什么场合选哪个 | `logger.info("x=%s", x)` |
| 21-15 | 关掉库的日志噪音 | A | `getLogger("urllib3").setLevel(WARNING)` | `setLevel` |
| 21-16 | 按大小轮转 | B | `RotatingFileHandler(maxBytes=1_000_000, backupCount=3)`，看生成的 `.log.1` `.log.2` | `RotatingFileHandler` |
| 21-17 | 按天轮转 | A | `TimedRotatingFileHandler(when="midnight")` | `TimedRotatingFileHandler` |
| 21-18 | **日志里的密码泄漏** | B | ❌ 直接记录整个请求参数（含密码）→ ✅ 脱敏后再记 | 脱敏 |
| 21-19 | 循环里的日志 | B | ❌ 一万条数据打一万行 → ✅ 每 1000 条记一次进度 | 进度日志 |
| 21-20 | **可直接复用的日志配置** | C | **本章最实用的产出**：一个 `log_setup.py`，屏幕 INFO + 文件 DEBUG + UTF-8 + 按天轮转 + 统一格式，一个函数调用就配好 | 综合 |
| 21-21 | 给成绩管理加上日志 | C | 回收 18.21 的多模块项目：入口配置日志，各模块 `getLogger(__name__)`，关键节点记 INFO，异常记 exception | 综合 |
| 21-22 | 把 16-28 的手工记录换成 logging | C | **兑现 16-28 的伏笔**：那时用 `a` 模式手工追加运行记录，现在用 logging 三行搞定，还多了级别、时间、来源、轮转 | 综合 |

### 样板示例

**示例 21-3　为什么 info 什么都不显示**

演示重点：这是新手用 logging 的第一个坑，要在读者踩之前点明。

```text
❌ 不设 level：
    import logging
    logging.info("开始处理")
    → 屏幕上什么都没有

✅ 设了 level：
    logging.basicConfig(level=logging.INFO)
    logging.info("开始处理")
    → INFO:root:开始处理
```

要解释**为什么默认是 WARNING**：logging 的设计假设是"库不应该在正常情况下往用户屏幕上刷东西"，所以默认只报警告和错误。这个默认值对**库**是合理的，但对**应用**不合适——应用要自己设级别。

**示例 21-11　记录完整异常堆栈**

演示重点：这是 logging 相比 print 最有价值的能力，要用并排对照说明差距。

```text
用 logger.error(e)：
    ERROR:root:division by zero
    ← 只有一行，不知道哪个文件哪一行出的错

用 logger.exception("计算失败")：
    ERROR:root:计算失败
    Traceback (most recent call last):
      File "app.py", line 12, in calc
        return a / b
               ~~^~~
    ZeroDivisionError: division by zero
    ← 完整堆栈，和你在终端看到的一样（回收第 2 章读 Traceback）
```

**要强调**：`logger.exception()` **只能在 `except` 块里用**（它靠当前的异常上下文取堆栈），在别处调用会记录 `NoneType: None`。

**示例 21-20　可直接复用的日志配置**

这是本章的最终产出，读者以后大概率只回来抄这一段。要求：

- **一个函数调用就配好**：`setup_logging()`
- **屏幕 INFO + 文件 DEBUG**（开发时看关键信息，出问题查文件里的细节）
- **UTF-8 编码**（回收 16.10）
- **按天轮转，保留 7 天**
- **统一格式**：时间 + 级别 + 模块名 + 行号 + 消息
- **代码要带详尽注释**，说明每个参数为什么这么设
- **配套说明"在项目里怎么用"**：入口调用一次 `setup_logging()`，各模块只写 `logger = logging.getLogger(__name__)`

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 不设 `level` 就用 `info` | 什么都不输出 | `basicConfig(level=logging.INFO)` | 默认级别是 WARNING |
| 加了 `filename` 后屏幕没输出了 | 以为日志没生效 | 用 `handlers=[...]` 同时配两个 | `filename` 会替换默认的屏幕输出 |
| `filemode="w"` | 每次运行清空历史日志 | 用默认的 `"a"` | 回收 16.3 |
| 不写 `encoding` | 日志文件中文乱码 | `encoding="utf-8"` | 回收 16.10 |
| 用 `logger.error(e)` 记异常 | **丢掉了完整堆栈** | `logger.exception("...")` | 只记了异常消息 |
| 在 `except` 块外用 `exception()` | 记录成 `NoneType: None` | 只在 `except` 里用 | 它依赖当前异常上下文 |
| 在模块里调用 `basicConfig` | 配置互相覆盖、行为不可预测 | 配置只在应用入口做一次 | 模块只负责打日志 |
| 第二次 `basicConfig` 不生效 | 改了配置没反应 | `force=True` | 默认忽略重复调用 |
| 同时配了父子 handler | 日志重复输出两遍 | `propagate=False` 或只配一处 | 消息向上传递 |
| 日志文件不轮转 | 几个月后几个 GB，磁盘满 | `RotatingFileHandler` | —— |
| 循环里每次都打日志 | 一万条数据一万行，有用信息被淹没 | 每 N 条记一次进度 | —— |
| **日志里记了密码/Token** | 安全事故 | 脱敏后再记 | 日志会被复制、会发给别人 |
| 日志只记"进入某函数" | 出问题时没有可用信息 | 记"发生了什么"+ 关键标识（订单号等） | —— |
| 把给用户看的结果也走日志 | 用户看到一堆 `INFO:root:` 前缀 | 用户输出用 `print`，排查信息用 `logging` | 两者受众不同 |

### 下一章预告

有了日志，你能知道程序出了什么错。但**光知道不够**——很多错误其实可以更早、更清楚地暴露出来。

第 22 章回到异常，讲第 15 章没讲完的部分：**怎么定义属于你自己业务的异常类型**（"库存不足"比 `ValueError` 精确得多）、**怎么在包装异常时不丢掉原始原因**、以及**失败了要不要重试、怎么重试**。这一章会和刚学的 `logging` 配合起来，形成一套完整的"出错处理"方案。

---

## 第 22 章　异常处理进阶 ★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会定义表达业务含义的异常类；会用 `raise ... from` 保留原始原因；知道该在哪一层捕获异常（而不是每层都 `try`）；会写重试和兜底；理解 `assert` 为什么不能用于校验用户输入 |
| 前置 | 第 15 章（异常入门）、第 21 章（日志） |
| 预计用时 | 3~4 小时 |
| 可用语法边界 | 阶段一全部 + 第 16~21 章。**不得使用**：装饰器（第 31 章——本章的重试用"循环 + `try`"实现，装饰器版本留到 31.6）、`with` 自定义（第 32 章）、`contextlib`（第 32 章） |
| **重要：一处必要的预支** | **自定义异常需要 `class`，而类要到第 26 章才讲。** 处理方式：<br>本章只用**一行样板形式**：<br><br>`class 名字(Exception):` + 缩进一行 `pass`<br><br>正文必须明确写出：**"这是本书第一次出现 `class`。你现在只需要照抄这个形式，把它当成'声明一种新的错误类型'的固定写法；`class` 的完整知识在第 26 章，那时你会明白这一行到底在做什么。"**<br><br>**不要**在本章讲继承、`__init__`、方法——凡是需要这些的内容（如给异常附带结构化数据）一律推到第 26 章之后，或用最朴素的方式绕开。 |
| 目录调整 | 新增 22.2（为什么需要自定义异常）、22.6（**异常链的三种形式**：`from e` / `from None` / 隐式）、22.7（**该在哪一层捕获**——比"怎么捕获"更重要）、22.8（异常转换：技术异常包装成业务异常）、22.13（异常处理的分层策略总结）。原目录 6 个小节缺了"在哪捕获"这个最关键的设计问题 |
| 编写要点 | 本章的核心不是语法（`raise from` 一行就讲完），而是**设计判断**。三处重笔：<br>① **该在哪一层捕获**（22.7）——新手的典型毛病是"每个函数都包一层 `try`"，结果每层都在处理同一个错误、日志重复、还掩盖了问题。这一节要给出明确原则：**底层抛出、上层处理，中间层什么都不做**；<br>② **`raise ... from`**（22.5）——不写 `from` 会丢失原始原因或产生令人困惑的双段 Traceback，要把三种形式的实际输出都展示出来；<br>③ **重试的正确写法**（22.10）——要讲清"什么错误该重试"（网络超时该重试，参数错误重试一万次也没用）和"退避"（不要疯狂重试把对方打死）。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 22.1 | 本章补什么 | 第 15 章解决的是"程序别崩"；本章解决的是"**出错时能说清是什么错、错在哪、要不要重试**"；一句话定位：从"能捕获"到"会设计" | ★★ |
| 22.2 | 为什么需要自定义异常 | 用 `ValueError("库存不足")` 的问题说明：① 调用方**没法区分**"库存不足"和"价格格式错"（都是 `ValueError`）② 只能靠**匹配错误消息文字**来判断，消息一改就全坏 ③ 表达不出业务概念；**什么时候值得自定义**：这个错误调用方需要**区别对待**时 | ★★★ |
| 22.3 | 定义自己的异常 | 样板形式（**预支 `class`，见上方说明**）；**命名约定**：以 `Error` 结尾（`StockError`、`ConfigError`）；**必须继承 `Exception`**（不要直接继承 `BaseException`——那会连 `Ctrl+C` 一起接管，回收 15.10） | ★★★ |
| 22.4 | 异常层次设计 | 一个业务基类 + 若干子类：`AppError` → `StockError` / `PaymentError`；**好处**：调用方可以选择粒度——想统一处理就捕获基类，想区别对待就捕获子类；**演示**：`except AppError` 能抓住所有子类（回收 15.6 的"具体在前，宽泛在后"）；**层次不要超过两层**，业务代码用不到更深的 | ★★ |
| 22.5 | **`raise ... from`** | 场景：底层抛了 `KeyError`，你想改抛成业务异常，但**不想丢掉原始原因**；`raise ConfigError("配置缺少 host") from e` 会在 Traceback 里保留两段，中间写 `The above exception was the direct cause of...`；**为什么重要**：丢了原始异常，排查时就不知道底层到底发生了什么 | ★★★ |
| 22.6 | 异常链的三种形式 | ① **`raise X from e`**——显式说明"由 e 导致"，Traceback 写 `direct cause` ② **`raise X`（在 except 块里，不写 from）**——Python 自动隐式链接，Traceback 写 `During handling of the above exception, another exception occurred`（**这句话让很多人困惑，要专门解释**）③ **`raise X from None`**——明确切断链条，只显示新异常（**什么时候用**：底层原因是实现细节，不想暴露给调用方） | ★★★ |
| 22.7 | **该在哪一层捕获** | 新手的典型毛病：每个函数都包一层 `try`；**后果**：同一个错误被处理三次、日志重复三遍、真正的问题被掩盖；**原则**：<br>**底层函数只管抛**（它不知道该怎么处理）<br>**最上层统一处理**（它知道要不要重试、要不要告诉用户、要不要退出）<br>**中间层什么都不做**（让异常穿过去）<br>**唯一的例外**：中间层需要**补充信息**或**转换成业务异常**时才捕获，且必须 `raise ... from e` 重新抛出 | ★★★ |
| 22.8 | 异常转换 | 把底层技术异常包装成业务异常：`KeyError` → `ConfigError`、`JSONDecodeError` → `DataFormatError`；**为什么**：调用方不该关心你内部用的是 JSON 还是 YAML；**代价**：多一层包装，所以只在"跨越模块边界"时做，模块内部不必 | ★★ |
| 22.9 | 内置异常该不该捕获 | 回收 15.15 并深化，做成三类：<br>**该捕获**——外部原因导致的：`FileNotFoundError`、`JSONDecodeError`、网络超时、`ValueError`（用户输入）<br>**不该捕获**——你自己代码写错了：`TypeError`、`NameError`、`AttributeError`、`IndexError`（**捕获它们等于把 bug 藏起来**，应该去改代码）<br>**绝不捕获**——`KeyboardInterrupt`、`SystemExit`（回收 15.10） | ★★★ |
| 22.10 | **重试模式** | 用"循环 + `try` + `break`"实现（装饰器版本见 31.6）；**三个关键设计**：<br>① **什么错该重试**——只重试"可能自己恢复"的错误（网络超时、临时锁冲突）；参数错误、文件格式错误重试一万次也没用<br>② **重试次数要有上限**，否则程序永远卡住<br>③ **要退避**——每次重试前等一会儿，且等待时间递增（`1s → 2s → 4s`），否则会把对方服务打死<br>**配合 logging**：每次重试记 `WARNING`，最终失败记 `ERROR` | ★★★ |
| 22.11 | 兜底与降级 | 三种兜底策略：① **默认值**（配置读不到就用默认配置）② **降级**（拿不到实时数据就用缓存的旧数据，并**明确告知用户这是旧数据**）③ **跳过并记录**（批量处理时单条失败不影响整批，但要记下来事后核对）；**关键提醒**：兜底必须**留痕**，静默兜底等于隐瞒问题（回收 15.11） | ★★★ |
| 22.12 | 与 logging 配合 | 三种组合：<br>① **记录并重抛**：`logger.exception(...)` + `raise`（回收 15.12 的空 `raise`）——我记下来了，但处理不了，交给上层<br>② **记录并兜底**：`logger.warning(...)` + 用默认值继续<br>③ **只在最上层记录**：避免每层都记导致日志里同一个错误出现五遍<br>**推荐**：中间层用①或什么都不做，最上层用 `logger.exception` 统一记录 | ★★★ |
| 22.13 | `assert` 的正确用途 | 回收 15.17 并展开：`assert` 是**给开发者的内部检查**，断言"这里绝不可能发生"的情况；**致命限制**：`python -O` 运行时**所有 assert 被整体移除**；**所以绝不能用于**：校验用户输入、检查权限、验证外部数据——那些必须用 `if` + `raise`；**可以用于**：单元测试（第 36 章）、开发期的内部一致性检查 | ★★ |
| 22.14 | 分层策略总结 | 一张表把本章的设计原则收口：<br>**数据层/工具层**——抛具体异常，不捕获、不记日志<br>**业务层**——转换成业务异常（`raise ... from`），必要时重试<br>**入口层**（`main` / CLI）——统一捕获、记日志、决定退出码、给用户友好提示<br>**这张表是本章最有价值的产出** | ★★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 22-1 | 内置异常表达不了业务 | B | ❌ 全都抛 `ValueError`，调用方只能匹配错误消息文字来区分 → 引出自定义异常 | `ValueError("库存不足")` |
| 22-2 | 定义第一个自定义异常 | A | 样板形式（**说明这是预支的 `class`，照抄即可**）；命名以 `Error` 结尾 | `class StockError(Exception): pass` |
| 22-3 | 抛出并捕获自己的异常 | A | `raise StockError("库存不足")` + `except StockError` | `raise StockError(...)` |
| 22-4 | 异常层次：一个基类带几个子类 | B | `AppError` → `StockError` / `PaymentError`；捕获基类能抓住所有子类 | `except AppError` |
| 22-5 | **`raise ... from` 保留原始原因** | B | ❌ 不写 from：原始的 `KeyError` 信息丢失或产生困惑的双段 Traceback → ✅ `from e` 明确因果 | `raise X("...") from e` |
| 22-6 | 三种异常链的实际输出 | B | `from e`（direct cause）/ 不写 from（During handling）/ `from None`（切断），**三段真实 Traceback 并排** | `from None` |
| 22-7 | **每层都 try 的后果** | B | ❌ 三层函数每层都捕获+记日志 → 日志里同一个错误出现三遍，还掩盖了根因 → ✅ 底层抛、上层处理、中间层不管 | 分层 |
| 22-8 | 让异常穿过中间层 | B | 中间层不写 `try`，异常自动向上传播（回收 15.2） | 传播 |
| 22-9 | 异常转换 | B | `KeyError` → `ConfigError`、`JSONDecodeError` → `DataFormatError`，都用 `from e` | 转换 |
| 22-10 | 哪些异常不该捕获 | B | ❌ 捕获 `TypeError` 把 bug 藏起来 → ✅ 让它崩，去改代码 | 三类对照表 |
| 22-11 | **重试模式** | B | 循环 + `try` + `break` + 次数上限；配合 `logger.warning` 记录每次重试 | `for attempt in range(3)` |
| 22-12 | 退避：不要疯狂重试 | B | `time.sleep(2 ** attempt)` 递增等待；说明为什么不能连续重试 | 指数退避 |
| 22-13 | 什么错误值得重试 | B | 对照表：超时/临时锁冲突 → 重试；参数错误/格式错误 → 重试无意义 | 判断表 |
| 22-14 | 兜底：配置读不到用默认值 | B | 读配置失败 → `logger.warning` + 用默认配置继续 | 兜底 |
| 22-15 | 批量处理时单条失败不中断 | B | 循环里 `try`，失败记录到"错误清单"，最后统一报告；**这是数据处理的标准模式** | 批量容错 |
| 22-16 | 记录并重抛 | B | `logger.exception(...)` + 空 `raise`（回收 15.12、21.11） | `raise` |
| 22-17 | assert 被 `-O` 移除 | A | 正常运行 vs `python -O` 运行，断言消失；**结论：绝不能用于输入校验** | `python -O` |
| 22-18 | **给成绩管理加业务异常** | C | 回收 18.21 / 21.21：定义 `GradeError` 层次，`storage` 层抛、`main` 层统一处理并给用户友好提示 | 综合 |
| 22-19 | 带重试和兜底的数据读取 | C | 读文件失败重试 3 次 → 仍失败则用缓存数据降级 → 全程记日志；**这是验收项目 C 的健壮性骨架** | 综合 |

### 样板示例

**示例 22-5 + 22-6　`raise ... from` 与三种异常链**

演示重点：三种形式的**真实 Traceback 输出**必须完整给出，因为读者需要靠"看到那句英文"来认出自己遇到的是哪种情况。

**场景**：读配置时字典里缺少 `host` 键，想改抛成 `ConfigError`。

| 写法 | Traceback 里的关键句 | 什么时候用 |
|---|---|---|
| `raise ConfigError(...) from e` | `The above exception was the direct cause of the following exception` | **推荐**：明确说明"新异常由旧异常导致" |
| `raise ConfigError(...)`（在 except 里，不写 from） | `During handling of the above exception, another exception occurred` | 不推荐——这句话字面意思是"处理旧异常时又出了个新错"，容易被误读成"你的错误处理代码有 bug" |
| `raise ConfigError(...) from None` | 只显示新异常，旧的完全不出现 | 底层原因是实现细节、不想暴露给调用方时 |

**要特别解释第二种**：很多人第一次看到 `During handling of the above exception...` 会以为自己的 `except` 块写错了。其实这是 Python 在说"这两个异常有关系，但你没告诉我什么关系，我只能说它们前后发生"。**加一个 `from e` 就变成明确的因果陈述。**

**示例 22-7　每层都 try 的后果**

演示重点：这是本章最重要的设计教学。要用一个三层调用的例子，对照两种写法。

**❌ 每层都捕获**：

```text
read_file()      → try/except，记日志，返回 None
parse_config()   → try/except，记日志，返回 {}
main()           → try/except，记日志，提示用户

结果：
  - 日志里同一个错误出现三遍
  - main() 拿到的是空字典，根本不知道是"文件不存在"还是"格式错误"
  - 返回 None / {} 让错误"静默传播"，后面某个地方才崩，栈信息已经和根因无关了
```

**✅ 分层处理**：

```text
read_file()      → 什么都不做，让 FileNotFoundError 自然抛出
parse_config()   → 只做一件事：转换成业务异常 ConfigError，用 from e 保留原因
main()           → 统一捕获 ConfigError，logger.exception 记一次，给用户友好提示，退出

结果：
  - 日志里错误出现一次，且带完整调用链
  - main() 明确知道是配置问题
  - 根因（FileNotFoundError）在 Traceback 里保留着
```

要把原则写成三句话：**底层只管抛、上层统一处理、中间层除了"补充信息"什么都不做。**

**示例 22-11 + 22-12　重试与退避**

演示重点：重试很容易写错，要讲清三个设计点。给一个能直接用的骨架（循环 + `try` + 上限 + 退避 + 日志），并配一张"什么错该重试"的判断表：

| 错误 | 重试有用吗 | 原因 |
|---|---|---|
| 网络超时、连接被拒 | ✅ 有用 | 对方可能只是暂时忙 |
| 数据库锁冲突 | ✅ 有用 | 等一下锁就释放了 |
| HTTP 500（服务端错误） | ✅ 有用（谨慎） | 可能是临时故障 |
| HTTP 404、401 | ❌ 没用 | 地址错了/没授权，重试一万次还是一样 |
| `ValueError`（数据格式错） | ❌ 没用 | 数据不会自己变对 |
| 磁盘满 | ❌ 没用 | 需要人工介入 |

**并强调退避的必要性**：不加等待地疯狂重试，等于对故障中的服务发起攻击，会让情况更糟。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 用错误消息文字区分异常类型 | 消息一改逻辑就坏 | 定义不同的异常类 | 字符串匹配很脆弱 |
| 自定义异常继承 `BaseException` | 连 `Ctrl+C` 都被接管 | 继承 `Exception` | 回收 15.10 |
| 在 `except` 里 `raise X` 不写 `from` | Traceback 出现令人困惑的 `During handling...` | `raise X from e` | 未声明因果关系 |
| 包装异常时丢掉原始异常 | 排查时不知道底层发生了什么 | `from e` | —— |
| 每层函数都 `try/except` | 日志重复、根因被掩盖 | 底层抛、上层处理 | 职责混乱 |
| 出错返回 `None` / `{}` 代替抛异常 | 错误静默传播，崩在无关的地方 | 抛异常 | 返回值会被忽略，异常不会 |
| 捕获 `TypeError` / `AttributeError` | 把自己的 bug 藏起来 | 让它崩，去改代码 | 那不是"意外"，是错误 |
| 重试没有次数上限 | 程序永远卡住 | 设上限 | —— |
| 连续重试不等待 | 把故障中的服务彻底打死 | 指数退避 | —— |
| 重试不该重试的错误 | 白等几秒，问题照旧 | 按 22-13 的表判断 | —— |
| 兜底但不记日志 | 问题被隐瞒，事后无法核对 | 兜底必须 `logger.warning` | 回收 15.11 |
| 用 `assert` 校验用户输入 | `python -O` 下校验全部失效 | `if` + `raise` | 断言会被移除 |
| 每层都 `logger.exception` | 同一个错误在日志里出现五遍 | 只在最上层记录 | —— |

### 下一章预告

出错处理这条线到这里就完整了：能捕获（15）、能定位（20）、能留痕（21）、能设计（22）。

接下来是一个看起来简单、实际坑极多的主题：**时间**。

第 23 章会告诉你为什么"今天减去 30 天"不能用 `day - 30`、为什么"两个日期相差几天"要用专门的类型、以及为什么处理时间时**时区**这件事会让人如此头疼。日志里的时间戳、账单的账期、数据的有效期——只要涉及时间，这一章的内容就绕不开。

---

## 第 23 章　日期与时间 ★★★

| 项 | 内容 |
|---|---|
| 本章目标 | 分清 `date` / `time` / `datetime` / `timedelta`；会做日期加减和差值计算；会格式化和解析日期字符串；知道"加一个月"为什么不能用 `timedelta`；理解 naive 与 aware 的区别并知道"存 UTC、显示本地"这条原则 |
| 前置 | 第 22 章 |
| 预计用时 | 4~5 小时 |
| 可用语法边界 | 阶段一全部 + 第 16~22 章 + `datetime` / `time` / `zoneinfo` / `calendar`。**不得使用**：`argparse`（第 24 章）、第三方 `dateutil`（只做一瞥）、`perf_counter` 的性能分析用途（专项 B） |
| 目录调整 | 新增 23.4（**`datetime.utcnow()` 在 3.12 已废弃**——大量教程还在教它）、23.7（**没有 `timedelta(months=1)`**，以及"加一个月"到底怎么算）、23.12（`%A` / `%B` 输出英文的本地化问题）、23.13（**ISO 格式是存储的首选**）、23.16（**naive vs aware 不能混用**）、23.18（存 UTC 显示本地）、23.21（`dateutil.relativedelta`）。原目录 6 个小节远远撑不住这一章的实际坑量 |
| 编写要点 | 本章是"看起来简单、实际坑极多"的典型。四处重笔：<br>① **"加一个月"的困难**（23.7）——`timedelta` 根本没有 `months` 参数，因为"一个月"不是固定长度。实测：`1月31日 + 30天 = 3月2日`（**整个 2 月被跳过了**）。这一节要讲清"月"这个概念在计算上的模糊性，并给出三种实用做法；<br>② **naive vs aware**（23.16）——两者相减直接 `TypeError`，而且这个错误往往在"本地测试没问题、上线拿到带时区的数据就崩"的时候才出现；<br>③ **`utcnow()` 已废弃**（23.4）——网上大量教程还在教 `datetime.utcnow()`，3.12 起会发 `DeprecationWarning`，要明确给出替代写法；<br>④ **存储用 ISO 格式**（23.13）——回收 17.9 那个"`datetime` 存不进 JSON"的问题，给出标准答案。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 23.1 | 时间为什么难 | 五个客观原因：① 月份长度不同（28/29/30/31）② 闰年规则（回收 4-17 的闰年判断）③ **时区**（同一时刻各地时间不同）④ 夏令时（某些地区一年有一天是 23 小时、一天 25 小时）⑤ 各国日期格式不同（`08/01` 在美国是 8 月 1 日，在欧洲是 1 月 8 日）；**结论**：时间计算不能靠直觉，必须用标准库 | ★★ |
| 23.2 | 四个类型各管什么 | `date`（只有年月日）、`time`（只有时分秒）、`datetime`（两者都有）、**`timedelta`（时间长度，不是时间点）**；**关键区分**：前三个是"什么时候"，`timedelta` 是"多久"；**选型建议**：只关心哪一天就用 `date`（更简单、不用操心时区），需要精确到时刻才用 `datetime` | ★★★ |
| 23.3 | 获取当前时间 | `datetime.now()`、`date.today()`；`now()` 带微秒（打印出来很长，格式化时通常要去掉） | ★★★ |
| 23.4 | **`utcnow()` 已废弃** | `datetime.utcnow()` 在 **3.12 起发出 `DeprecationWarning`**，未来会移除；**为什么废弃**：它返回一个"内容是 UTC 时间但不带时区标记"的 naive 对象，**极易被误当成本地时间**，是无数时区 bug 的根源；**替代写法**：`datetime.now(timezone.utc)`——它返回带时区的 aware 对象；**必须讲这条**，因为网上大量教程还在教 `utcnow()` | ★★★ |
| 23.5 | 访问各部分 | `.year` / `.month` / `.day` / `.hour` / `.minute` / `.second` / `.weekday()`（周一是 0）/ `.isoweekday()`（周一是 1）；**注意两个星期几方法的起点不同**，用错会差一天 | ★★★ |
| 23.6 | `timedelta` 做加减 | `date + timedelta(days=30)`、`- timedelta(weeks=2)`；支持的参数：`days` / `seconds` / `minutes` / `hours` / `weeks`；**日期加减必须用 `timedelta`**，不能直接 `day - 30`（那是在改"日"这个数字，1 月 5 日减 30 会直接报错） | ★★★ |
| 23.7 | **没有 `timedelta(months=1)`** | 实测报错：`TypeError: 'months' is an invalid keyword argument`；**为什么没有**：一个月是 28/29/30/31 天，"加一个月"没有唯一答案；**实测演示**：`1月31日 + 30天 = 3月2日`（**整个 2 月被跳过**）；**三种实用做法**：① 用 `calendar.monthrange` 算出目标月有多少天，取 `min(原日, 月末)` ② 存"年+月"两个整数自己算 ③ 用第三方 `dateutil.relativedelta(months=1)`（**它把"月末对齐"的规则实现好了**）；**还要说清业务问题**：1 月 31 日加一个月应该是 2 月 28 日还是 3 月 3 日？**这是业务决策，不是技术问题** | ★★★ |
| 23.8 | 两个日期相减 | `date - date` 得到 **`timedelta`**，用 `.days` 取天数；**边界情况**：`datetime - datetime` 得到的 `timedelta` 里还有 `seconds`，只取 `.days` 会丢掉不足一天的部分；`.total_seconds()` 取总秒数 | ★★★ |
| 23.9 | 日期比较与排序 | 可以直接用 `<` / `>` / `==`（回收 4.6）；可以直接 `sorted()`；**注意**：`date` 和 `datetime` **不能互相比较**（`TypeError`） | ★★★ |
| 23.10 | `strftime` 格式化 | 常用格式符表：`%Y`（四位年）`%y`（两位年）`%m` `%d` `%H`（24 时）`%I`（12 时）`%M` `%S` `%p`（AM/PM）`%A`（星期全称）`%a`（缩写）`%B`（月份全称）`%j`（一年第几天）`%%`（百分号）；**中文日期直接写**：`"%Y年%m月%d日"` 完全可以 | ★★★ |
| 23.11 | `strptime` 解析 | `datetime.strptime("2026-08-01", "%Y-%m-%d")`；**格式必须完全匹配**，差一个分隔符就 `ValueError: time data ... does not match format ...`；**记忆法**：`f` = format（转成字符串）、`p` = parse（从字符串解析） | ★★★ |
| 23.12 | `%A` / `%B` 输出英文 | 实测 `%A` 得 `Saturday`、`%B` 得 `August`；**为什么**：它们跟随系统 locale，而多数环境是英文；**要中文星期怎么办**：自己建一个列表 `["周一", "周二", ...]` 用 `weekday()` 取——**这比折腾 locale 可靠得多**（locale 设置在不同系统上行为不一致） | ★★ |
| 23.13 | **ISO 格式：存储的首选** | `isoformat()` 输出 `2026-08-01T14:30:00`、`fromisoformat()` 解析回来；**为什么它是存储首选**：① 国际标准，无歧义（不像 `08/01` 那样有美式欧式之争）② **字符串排序等于时间排序**（这个特性极实用）③ 各语言各数据库都支持；**回收 17.9**：`datetime` 存不进 JSON，标准答案就是存 `isoformat()` 字符串 | ★★★ |
| 23.14 | 时间戳 | `time.time()` 得到从 1970 年起的秒数（浮点）；`datetime.fromtimestamp(ts)` 转回来、`dt.timestamp()` 转过去；**什么时候用时间戳**：需要跨语言/跨系统传递、需要做数值运算、或者存数据库时；**注意**：时间戳本身**没有时区概念**（它就是一个绝对时刻），转成 `datetime` 时才涉及时区 | ★★★ |
| 23.15 | `time` 模块 | `time.time()` 时间戳、`time.sleep(秒)` 暂停、**`time.perf_counter()` 高精度计时**（测代码耗时用它，不要用 `time.time()`）；**为什么不用 `time.time()` 计时**：它可能受系统时间调整影响（比如 NTP 同步时跳变），`perf_counter` 是单调递增的 | ★★★ |
| 23.16 | **naive vs aware** | **naive**（`tzinfo` 是 `None`）= 不知道自己属于哪个时区；**aware** = 带时区信息；**两者不能相减、不能比较**，实测报错：`TypeError: can't subtract offset-naive and offset-aware datetimes`；**这个坑的典型场景**：本地测试用 `datetime.now()`（naive），从接口/数据库拿到的是带时区的（aware），一相减就崩；**判断方法**：`dt.tzinfo is None` | ★★★ |
| 23.17 | `zoneinfo` | Python 3.9+ 内置（以前要装 `pytz`）；`ZoneInfo("Asia/Shanghai")`；`astimezone()` 转换时区；**时区名用 IANA 标准**（`Asia/Shanghai` 而不是 `CST`——后者有歧义，可能指中国标准时间也可能指美国中部时间） | ★★ |
| 23.18 | **存 UTC，显示本地** | 这是时间处理的核心原则：**存储和内部计算一律用 UTC（aware），只在给人看的最后一步转成本地时区**；**为什么**：① 服务器可能在任何时区 ② 用户可能在任何时区 ③ 夏令时切换时本地时间会重复或跳跃，UTC 不会；**什么时候可以偷懒**：纯单机、纯本地、只关心"哪一天"的场景，用 `date` 就够，不必上时区 | ★★★ |
| 23.19 | 日期存进 JSON / 作字典键 | 回收 17.9：`datetime` 不能直接 `json.dumps`；**标准做法**：存 `isoformat()` 字符串，读回来 `fromisoformat()`；作字典键同理（键必须可哈希且 JSON 键必须是字符串，回收 17.8） | ★★★ |
| 23.20 | `calendar` | `monthrange(年, 月)` 返回（当月第一天是星期几，当月天数）——**这是 23.7 手工算月份的关键工具**；`isleap()` 判断闰年（回收 4-17）；`monthcalendar` 打印月历 | ★ |
| 23.21 | `dateutil` 一瞥 | 第三方库；`relativedelta(months=1)` 正确处理月末对齐、`parser.parse()` 能自动识别多种日期格式；**什么时候值得装**：业务里有大量"按月"计算（账期、订阅、还款日）；**注意** `parser.parse` 的自动识别对 `01/08/2026` 这类有歧义的格式会猜错，重要场合仍应显式指定格式 | ★ |
| 23.22 | 常见业务场景 | 五个场景给出标准写法：① **算年龄**（要判断今年生日到没到，不能简单相减年份）② **账期**（本月第一天到最后一天）③ **有效期**（判断是否过期）④ **文件按日期命名**（`report_20260801.csv`，用 `strftime("%Y%m%d")`）⑤ **计算工作日**（`weekday() < 5`；法定节假日需要额外数据） | ★★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 23-1 | 四个类型各建一个 | A | `date` / `time` / `datetime` / `timedelta` 并列，看打印结果 | `date(2026, 8, 1)` |
| 23-2 | 现在几点 | A | `datetime.now()`、`date.today()`；注意 now 带微秒 | `datetime.now()` |
| 23-3 | **`utcnow()` 已经不推荐了** | A | ❌ `datetime.utcnow()` 发 DeprecationWarning → ✅ `datetime.now(timezone.utc)` | `now(timezone.utc)` |
| 23-4 | 取出年月日星期 | A | `.year` / `.month` / `.weekday()`；**两个星期几方法起点不同** | `.weekday()` |
| 23-5 | 三十天后是哪天 | A | `date + timedelta(days=30)` | `timedelta(days=30)` |
| 23-6 | 两周前 | A | `- timedelta(weeks=2)` | `timedelta(weeks=2)` |
| 23-7 | **`timedelta(months=1)` 不存在** | A | ❌ `TypeError: 'months' is an invalid keyword argument` | TypeError |
| 23-8 | **1 月 31 日加一个月是哪天** | B | 实测 `+30 天 = 3月2日`（**跳过整个 2 月**）、`+31 天 = 3月3日`；引出"一个月"的模糊性 | 月份陷阱 |
| 23-9 | 手工算"下个月同一天" | B | `calendar.monthrange` 取目标月天数 + `min(原日, 月末)` | `monthrange` |
| 23-10 | 两个日期相差几天 | A | `date - date` → `timedelta`，`.days` | `.days` |
| 23-11 | 相差不足一天的部分 | B | `datetime - datetime` 的 `timedelta` 含 `seconds`；`.total_seconds()` | `.total_seconds()` |
| 23-12 | 日期能直接比大小 | A | `<` / `>` / `sorted()`；**`date` 与 `datetime` 不能比较** | 比较 |
| 23-13 | 格式化成各种样子 | A | `%Y-%m-%d %H:%M:%S`、`%Y年%m月%d日`、12 小时制 | `strftime` |
| 23-14 | 星期和月份是英文 | A | `%A` → `Saturday`；给出自建中文列表的做法 | `%A` |
| 23-15 | 从字符串解析日期 | A | `strptime`；`f`=format、`p`=parse 的记忆法 | `strptime` |
| 23-16 | 格式差一个字符就报错 | A | ❌ 用 `%Y-%m-%d` 解析 `2026/08/01` → `ValueError` | ValueError |
| 23-17 | **ISO 格式：存储首选** | B | `isoformat()` / `fromisoformat()`；**演示"字符串排序等于时间排序"** | `isoformat()` |
| 23-18 | 时间戳互转 | B | `time.time()`、`fromtimestamp`、`.timestamp()` | `fromtimestamp` |
| 23-19 | 暂停与计时 | A | `time.sleep(1)`；`perf_counter` 测耗时 | `perf_counter()` |
| 23-20 | 为什么计时不用 `time.time()` | B | 说明单调性问题（系统对时会跳变） | `perf_counter` |
| 23-21 | **带时区和不带时区不能相减** | B | ❌ `TypeError: can't subtract offset-naive and offset-aware datetimes` → ✅ 统一成 aware | `tzinfo is None` |
| 23-22 | 给时间加上时区 | B | `ZoneInfo("Asia/Shanghai")`；打印带 `+08:00` | `ZoneInfo` |
| 23-23 | 时区转换 | B | 上海时间 → UTC → 纽约时间 | `astimezone` |
| 23-24 | **存 UTC，显示本地** | B | 完整流程：存的时候 `now(timezone.utc)`，显示的时候 `astimezone(ZoneInfo("Asia/Shanghai"))` | 核心原则 |
| 23-25 | 日期存进 JSON | B | **兑现 17.9 的承诺**：`isoformat()` 存、`fromisoformat()` 读 | 综合 |
| 23-26 | 算年龄 | C | 必须判断今年生日到没到，不能简单减年份；给出正确写法和错误写法对照 | 综合 |
| 23-27 | 算账期 | C | 本月第一天（`replace(day=1)`）到本月最后一天（`monthrange`）；跨年月的边界 | 综合 |
| 23-28 | 按日期命名归档文件 | C | `report_20260801.csv`；配合第 16 章的 `pathlib` 建目录；**这是验收项目 B2 的一部分** | 综合 |
| 23-29 | `dateutil` 一瞥 | A | `relativedelta(months=1)` 正确处理 1 月 31 日 | `relativedelta` |

### 样板示例

**示例 23-8 + 23-9　"加一个月"到底是哪天**

演示重点：这是本章最能体现"时间很难"的例子。

**第一步：先证明 `timedelta` 帮不了你**

```text
timedelta(months=1)  →  TypeError: 'months' is an invalid keyword argument
```

**第二步：用天数凑，看看会发生什么**

```text
1 月 31 日 + 30 天  =  3 月 2 日      ← 整个 2 月被跳过了
1 月 31 日 + 31 天  =  3 月 3 日
```

**第三步：讲清为什么**——"一个月"不是固定长度。所以问题的本质是：**1 月 31 日加一个月，应该是 2 月 28 日、3 月 3 日，还是报错？** 这三个答案在不同业务里都是对的：

| 业务场景 | 合理答案 |
|---|---|
| 订阅到期日 | 2 月 28 日（月末对齐，用户不会觉得少了几天） |
| 还款日 | 2 月 28 日（同上，银行通常这么算） |
| "30 天后提醒" | 3 月 2 日（本来就是按天算的，别用"月"来描述） |

**结论要写明**：**这是业务决策，不是技术问题。** 遇到"加一个月"的需求，先去问清楚要哪种语义。

**第四步：给三种实现**

| 做法 | 适用 |
|---|---|
| `calendar.monthrange` + `min(原日, 月末)` | 不想装库，手工实现月末对齐 |
| 存"年 + 月"两个整数，需要时再组装 | 只按月统计的场景（账期、月报） |
| `dateutil.relativedelta(months=1)` | 业务里有大量按月计算，值得装这个库 |

**示例 23-21　带时区和不带时区不能相减**

演示重点：这个坑的可怕之处在于**本地测试发现不了**。

```text
naive = datetime(2026, 8, 1, 12, 0)                        # tzinfo 是 None
aware = datetime(2026, 8, 1, 12, 0, tzinfo=timezone.utc)   # tzinfo 是 UTC

aware - naive  →  TypeError: can't subtract offset-naive and offset-aware datetimes
```

**要讲清典型的出错路径**：

1. 本地开发时全用 `datetime.now()`（naive），一切正常
2. 上线后从接口/数据库读到的时间**带时区**（aware）
3. 两者一相减 → `TypeError`，而且是在生产环境才出现

**判断方法**要给出：`dt.tzinfo is None` 就是 naive。

**解法**要给出原则：**在系统边界（读入数据的地方）就统一成 aware**，内部一律用 aware，不要让两种混在一起流动。

**示例 23-24　存 UTC，显示本地**

演示重点：把 23.18 的原则落成可复制的代码流程：

```text
① 产生时间：datetime.now(timezone.utc)              → 带时区的 UTC
② 存储：    isoformat() 存成字符串（或存时间戳）      → 无歧义
③ 读取：    fromisoformat() 还原                     → 仍是 UTC aware
④ 显示：    astimezone(ZoneInfo("Asia/Shanghai"))    → 转成本地再格式化
```

**要说明什么时候可以不这么麻烦**：纯单机脚本、只关心"哪一天"的场景，用 `date` 就够了——**不要为了"规范"给简单场景强加时区复杂度**。判断标准：**这个时间会不会跨时区被人看到？**

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| `datetime.utcnow()` | 3.12 起 DeprecationWarning；返回的 naive 对象易被误读 | `datetime.now(timezone.utc)` | 已废弃 |
| `timedelta(months=1)` | `TypeError` | 用 `monthrange` 或 `dateutil` | 月长度不固定 |
| 用加天数模拟加月份 | 1 月 31 日 +30 天跳到 3 月 | 明确业务语义后再选实现 | 同上 |
| 直接改 `.day` 做日期加减 | 越界报错或结果错误 | 用 `timedelta` | 日期不是普通数字 |
| naive 和 aware 相减 | `TypeError: can't subtract offset-naive and offset-aware` | 在系统边界统一成 aware | 缺少时区信息无法比较 |
| `date` 和 `datetime` 比较 | `TypeError` | 统一类型再比 | 类型不同 |
| `strptime` 格式差一个字符 | `ValueError: time data ... does not match format` | 检查分隔符和顺序 | 必须完全匹配 |
| 混用 `weekday()` 和 `isoweekday()` | 星期差一天 | 记住前者周一是 0，后者是 1 | 起点不同 |
| 用 `%A` 期望中文 | 输出 `Saturday` | 自建中文列表 | 跟随 locale |
| 用 `time.time()` 测代码耗时 | 系统对时会导致结果异常 | `time.perf_counter()` | 非单调 |
| 把 `datetime` 直接存 JSON | `TypeError: not JSON serializable` | 存 `isoformat()` 字符串 | 回收 17.9 |
| 用 `08/01/2026` 这类格式存储 | 美式/欧式歧义 | 用 ISO 格式 | 国际标准 |
| 只取 `.days` 忽略了小时 | 差 23 小时被算成 0 天 | 用 `.total_seconds()` | timedelta 含时分秒 |
| 时区名用 `CST` | 有歧义（中国/美国中部都叫 CST） | 用 IANA 名 `Asia/Shanghai` | 缩写不唯一 |
| 算年龄直接减年份 | 生日没到的人多算一岁 | 判断月日是否已过 | —— |

### 下一章预告

你的程序现在功能完整了——能读写文件、能记日志、能处理时间。但它还有一个"最后一公里"的问题：**怎么让别人用**？

现在要改一个参数（处理哪个文件、输出到哪里），只能打开代码改常量。这对你自己也许还行，交给别人就完全不可接受了。

第 24 章讲 `argparse`：让程序能从命令行接受参数，自动生成帮助信息，像个正经工具一样被调用。学完这一章，验收项目 B 的骨架就齐了。

---

## 第 24 章　命令行程序 ★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会用 `argparse` 定义位置参数、可选参数、开关和子命令；知道 `--help` 是自动生成的；会用退出码表示成功失败；知道配置的三层来源；**知道密码不能用命令行参数传** |
| 前置 | 第 21 章（日志）、第 23 章 |
| 预计用时 | 4~5 小时 |
| 可用语法边界 | 阶段一全部 + 第 16~23 章 + `argparse` / `sys` / `os.environ` / `getpass`。**不得使用**：第三方 CLI 库（`click` / `typer` 只做一瞥）、`python-dotenv`（只提一句） |
| 目录调整 | 新增 24.6（**`type` 参数自动转换并自动报错**——这是 argparse 最省事的地方）、24.10（`store_true` 开关型参数）、24.15（**退出码**——脚本被自动化调用时的唯一沟通方式）、24.17（**配置的三层来源**：默认值 < 配置文件 < 命令行）、24.20（**密码不能用命令行参数传**，`ps` 能看到）、24.22（CLI 设计惯例：`--help` / `--version` / `--verbose` / `--dry-run`）、24.23（`--dry-run` 的实现）。原目录 4 个小节远远不够，而这一章直接决定验收项目 B 的形态 |
| 编写要点 | 本章要传达一个核心观念：**`argparse` 不只是"解析参数"，它是在帮你把脚本变成"工具"**。三处重笔：<br>① **自动生成的 `--help`**（24.7）——这是 argparse 最大的价值。你只写了几行 `add_argument`，它免费给你一份格式规范的帮助文档、类型检查、错误提示。要把"手工解析 `sys.argv`" 和 "argparse" 的代码量并排对比；<br>② **子命令**（24.14）——记账本 `add` / `list` / `report` 这种结构，是验收项目 B1 的骨架；<br>③ **`--verbose` 控制日志级别**（24.21）——把第 21 章的 logging 和本章的参数解析连起来，这是真实工具的标配。<br><br>另外 24.20 是**安全问题**：命令行参数会出现在 `ps` 输出和 shell 历史里，密码绝不能这样传。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 24.1 | 为什么需要参数 | 现状的问题：改一个输入文件名要**打开代码改常量**；同一个脚本想跑不同数据得复制多份；别人拿到不知道该改哪里；**目标**：让脚本像 `git` / `pip` 那样被调用 | ★★★ |
| 24.2 | `sys.argv` 的局限 | `sys.argv` 是一个列表，`[0]` 是脚本名；**手工解析的痛苦**：要自己数位置、自己转类型、自己写帮助、自己处理"参数漏了/多了/顺序反了"；**给一段手工解析的代码看它有多长**，然后引出 argparse | ★★★ |
| 24.3 | argparse 三步 | ① `parser = argparse.ArgumentParser(description=...)` ② `parser.add_argument(...)` ③ `args = parser.parse_args()`；**先给能跑的最小例子**再讲细节 | ★★★ |
| 24.4 | 位置参数 | `add_argument("filename")`；**必须提供，按顺序**；`args.filename` 取值；**什么时候用**：这个参数是命令的"主角"（要处理的文件、要执行的目标） | ★★★ |
| 24.5 | 可选参数 | `add_argument("-o", "--output")`；**短选项一个横线、长选项两个**；取值用长名去掉横线（`args.output`）；**什么时候用**：可以省略、或者有多个平级选项时 | ★★★ |
| 24.6 | **`type` 自动转换与报错** | `type=int` 会自动转换，**转不成时 argparse 自己报错并退出**，错误消息还很友好（`invalid int value: 'abc'`）；`type=float`、`type=Path`（回收第 16 章）；**这一条要强调**：你不用写任何 `try`，参数校验免费得到（对比第 15 章手工校验输入的代码量） | ★★★ |
| 24.7 | **自动生成的 `--help`** | 你只写 `help="..."`，argparse 免费给你：格式规范的用法说明、参数列表、`-h` / `--help` 两种触发方式、错误时的提示；**必须展示真实的 `--help` 输出**；**这是 argparse 最大的价值**——它让你的脚本对别人（和三个月后的自己）可用 | ★★★ |
| 24.8 | `default` 与 `required` | 可选参数默认是 `None`，用 `default=` 给默认值；`required=True` 让可选参数变成必填（**但通常这时候该用位置参数**） | ★★★ |
| 24.9 | `choices` 限定取值 | `choices=["json", "csv"]`，传别的值 argparse 直接报错并列出合法值；**好处**：省掉自己写校验，帮助信息里也会自动显示可选值 | ★★★ |
| 24.10 | **`action="store_true"` 开关** | `add_argument("--dry-run", action="store_true")`——**不带值的开关**，出现就是 `True`，不出现就是 `False`；**最常用的三个开关**：`--verbose`、`--dry-run`、`--force`；**注意**：命令行里的 `--dry-run` 在代码里是 `args.dry_run`（**横线变下划线**） | ★★★ |
| 24.11 | `nargs` 接受多个值 | `nargs="+"`（一个或多个）、`nargs="*"`（零个或多个）、`nargs=2`（固定两个）；结果是列表；**典型场景**：一次处理多个文件 | ★★ |
| 24.12 | 自定义 `type` 做校验 | `type` 可以是任何"接收字符串返回值"的函数——**包括你自己写的校验函数**（校验失败抛 `argparse.ArgumentTypeError`）；**例子**：检查文件必须存在、检查端口在 1-65535 之间；**这是把第 15 章的校验逻辑接到 CLI 上的标准方式** | ★★ |
| 24.13 | 修饰帮助信息 | `description`（开头说明）、`epilog`（结尾示例）、`metavar`（帮助里显示的占位名）、`prog`（程序名）；**建议在 `epilog` 里放两三个使用示例**——这是最有用的帮助内容 | ★★ |
| 24.14 | **子命令** | `subparsers = parser.add_subparsers(dest="command")`，每个子命令有自己的参数；用法像 `git commit` / `pip install`；**验收项目 B1 的结构**：`money add 30 午餐` / `money list --month 8` / `money report`；**注意**：要处理"没给子命令"的情况（`if not args.command: parser.print_help()`） | ★★★ |
| 24.15 | **退出码** | `sys.exit(0)` 成功、`sys.exit(1)` 失败（或其他非 0）；**为什么重要**：脚本被自动化调用时（cron、CI、shell 脚本），**退出码是唯一的沟通方式**——`&&` 和 `||` 就靠它判断；**约定**：0 成功，1 一般错误，2 参数错误（argparse 自己用的就是 2） | ★★★ |
| 24.16 | 输出分流 | **正常结果 → `stdout`（`print`）；错误和日志 → `stderr`**（回收 6.11、21.19）；**为什么**：这样用户可以 `python app.py > result.txt` 只保存结果，而错误依然显示在屏幕上；**别把日志混进 stdout**，否则会污染管道 | ★★★ |
| 24.17 | **配置的三层来源** | 优先级从低到高：**内置默认值 < 配置文件 < 环境变量 < 命令行参数**；**为什么要分层**：默认值让程序开箱能跑，配置文件放长期设置，命令行做临时覆盖；**实现方式**：`add_argument` 的 `default` 取自配置文件/环境变量，命令行不传就用它 | ★★★ |
| 24.18 | 环境变量 | `os.getenv("API_KEY")`、`os.environ.get(..., 默认值)`；**什么时候用**：密钥、部署相关的配置（数据库地址）；**优点**：不进代码库、不出现在命令行历史里 | ★★★ |
| 24.19 | `.env` 文件 | 项目根目录放 `.env` 写本地配置，用 `python-dotenv` 加载；**必须加进 `.gitignore`**（回收 19.8）；**只做一瞥**，说明它的定位是"本地开发方便" | ★★ |
| 24.20 | **密码不能用命令行参数传** | **原因**：① 命令行参数出现在 `ps` 输出里，**同机器上任何用户都能看到** ② 会留在 shell 历史文件里 ③ 可能被日志记录；**正确做法**：环境变量、配置文件（权限设 600）、或 `getpass` 交互式输入；**这是安全问题，要写得明确** | ★★★ |
| 24.21 | `getpass` | `getpass.getpass("密码：")` 输入时不回显；**局限**：不能用于自动化场景（无人交互时会卡住） | ★★ |
| 24.22 | CLI 设计惯例 | 五条通行惯例：① **`-h` / `--help`** 自动有了 ② **`--version`**（`action="version"`）③ **`-v` / `--verbose`** 控制输出详细程度 ④ **`--dry-run`** 只显示会做什么、不真做 ⑤ **`-f` / `--force`** 跳过确认；**遵守惯例的价值**：用户不用学，看一眼就知道怎么用 | ★★★ |
| 24.23 | **`--dry-run` 的实现** | 把"决定做什么"和"真的去做"分开：先算出待执行的操作列表，`--dry-run` 时只打印，否则执行；**为什么必须有它**：任何会**删除、覆盖、移动**文件的工具都应该提供干跑模式（回收 16.3 的 `w` 模式警告）；**验收项目 B2 强制要求这个功能** | ★★★ |
| 24.24 | 第三方 CLI 库一瞥 | `click`（装饰器风格）、`typer`（基于类型注解，第 33 章之后会更容易理解）；**本书态度**：先用内置的 `argparse` 把概念学会，它零依赖、任何环境都有；子命令多到十几个、需要复杂交互时再考虑换 | ★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 24-1 | 改参数要改代码的痛苦 | A | 一个把文件名写死的脚本；提出三个问题 | —— |
| 24-2 | `sys.argv` 手工解析 | B | 自己数位置、自己转类型、自己判断个数——**代码很长且不健壮** | `sys.argv` |
| 24-3 | argparse 三行起步 | A | 三步骤最小例子，跑起来 | `parse_args()` |
| 24-4 | 位置参数 | A | `add_argument("filename")`，不传时 argparse 自动报错 | 位置参数 |
| 24-5 | 可选参数与短选项 | A | `-o` / `--output`；`args.output` | `-o, --output` |
| 24-6 | **看看免费得到的 `--help`** | A | 真实 `--help` 输出；对比 24-2 手工版什么都没有 | `--help` |
| 24-7 | **`type=int` 自动转换和报错** | A | 传 `abc` 时的真实报错 `invalid int value: 'abc'`；**不用写一行 try** | `type=int` |
| 24-8 | 默认值 | A | `default=10`；不传时的值 | `default=` |
| 24-9 | 限定取值 | A | `choices=["json","csv"]`；传错值的报错和帮助里的显示 | `choices=` |
| 24-10 | 开关型参数 | A | `action="store_true"`；**命令行 `--dry-run` → 代码 `args.dry_run`** | `store_true` |
| 24-11 | 一次传多个文件 | B | `nargs="+"`，结果是列表 | `nargs="+"` |
| 24-12 | 自定义校验：文件必须存在 | B | 自己写 `type` 函数，抛 `ArgumentTypeError` | `ArgumentTypeError` |
| 24-13 | 在帮助里放使用示例 | A | `epilog` + `RawDescriptionHelpFormatter` 保留换行 | `epilog=` |
| 24-14 | **子命令** | B | `add_subparsers`；三个子命令各有参数；处理"没给子命令"的情况 | `add_subparsers` |
| 24-15 | 加上 `--version` | A | `action="version"` | `action="version"` |
| 24-16 | **退出码** | B | 成功 `sys.exit(0)`、失败 `sys.exit(1)`；在 shell 里用 `&&` 验证 | `sys.exit(1)` |
| 24-17 | 结果和错误分两路 | B | 结果 `print`、错误 `print(..., file=sys.stderr)`；演示 `> result.txt` 时的差别 | `file=sys.stderr` |
| 24-18 | 从环境变量读配置 | B | `os.getenv("API_KEY")`；作为 `default` 的来源 | `os.getenv` |
| 24-19 | 三层配置合并 | B | 默认值 < 环境变量 < 命令行，**用真实的优先级演示** | 三层 |
| 24-20 | **密码不要用参数传** | B | ❌ `--password 123456` 会出现在 `ps` 里 → ✅ 环境变量或 `getpass` | 安全 |
| 24-21 | 交互式输入密码 | A | `getpass.getpass()` 不回显 | `getpass` |
| 24-22 | **`--verbose` 控制日志级别** | B | **把第 21 章接进来**：`-v` 时 `level=DEBUG`，否则 `INFO`；`-vv` 更详细（`action="count"`） | `action="count"` |
| 24-23 | **`--dry-run` 的实现** | B | 先算出操作列表 → 干跑时只打印 → 否则执行；**任何会删改文件的工具都该有** | `--dry-run` |
| 24-24 | 交互确认与 `--force` | B | 危险操作前 `input("确认? [y/N] ")`；`--force` 跳过 | `--force` |
| 24-25 | **文件归档脚本的完整 CLI** | C | **验收项目 B2 的骨架**：位置参数（目录）+ `--by`（后缀/日期，用 `choices`）+ `--dry-run` + `-v`；结合第 16 章的 `pathlib` 和第 21 章的日志 | 综合 |
| 24-26 | **记账本的子命令结构** | C | **验收项目 B1 的骨架**：`add` / `list` / `report` 三个子命令，各自的参数；配合第 17 章的 JSON 存储 | 综合 |

### 样板示例

**示例 24-2 + 24-6 + 24-7　argparse 到底替你做了什么**

演示重点：这三个示例要连起来讲，形成一个完整的说服过程。

**第一步：手工解析的样子**（24-2）——要写出真实的代码，包括所有必须自己处理的情况：

```text
需要自己做的事：
  □ 判断参数个数够不够
  □ 按位置取值（顺序错了就全错）
  □ 手工 int() 转换 + try/except
  □ 手工判断可选参数在不在
  □ 手工写帮助文本
  □ 手工处理 -h
  □ 参数错误时给出有用的提示
```

**第二步：argparse 版**（24-3~24-5）——同样的功能，代码短得多。

**第三步：展示免费得到的东西**（24-6、24-7）——真实的 `--help` 输出，以及类型错误时的自动报错：

```text
$ python app.py --help
usage: app.py [-h] [-o OUTPUT] [-n COUNT] filename

处理一个数据文件

positional arguments:
  filename              要处理的文件

options:
  -h, --help            show this help message and exit
  -o OUTPUT, --output OUTPUT
                        输出文件（默认打印到屏幕）
  -n COUNT, --count COUNT
                        处理前 N 条（默认 10）

$ python app.py data.txt -n abc
usage: app.py [-h] [-o OUTPUT] [-n COUNT] filename
app.py: error: argument -n/--count: invalid int value: 'abc'
```

**结论要写明**：上面这一整套（用法行、参数说明、类型检查、友好报错、退出码 2）**你一行都没写**，是 `add_argument` 附带的。这就是用标准库的价值——和第 17 章"用标准格式而不是自己拼格式"是同一个道理。

**示例 24-14　子命令**

演示重点：这是验收项目 B1 的骨架，要给出完整可运行的结构。

```text
用法长这样：
    money add 30 午餐
    money add 5.5 咖啡 --date 2026-08-01
    money list --month 8
    money report

代码结构：
    parser = ArgumentParser(prog="money")
    sub = parser.add_subparsers(dest="command")

    p_add = sub.add_parser("add", help="记一笔")
    p_add.add_argument("amount", type=float)
    p_add.add_argument("note")
    p_add.add_argument("--date", default=今天)

    p_list = sub.add_parser("list", help="列出记录")
    p_list.add_argument("--month", type=int)

    p_report = sub.add_parser("report", help="统计报表")
```

**必须讲的三个细节**：

1. **`dest="command"`** 让你能用 `args.command` 判断用户调了哪个子命令
2. **没给子命令时** `args.command` 是 `None`，要 `parser.print_help()` 而不是崩掉
3. **每个子命令的参数互相独立**——`add` 的 `--date` 和 `list` 的 `--month` 不会互相干扰

**示例 24-22　`--verbose` 控制日志级别**

演示重点：把第 21 章的 logging 和本章连起来，这是真实工具的标配。

```text
python app.py data.txt          → 日志级别 INFO，只看关键信息
python app.py data.txt -v       → 日志级别 DEBUG，看到全部细节
python app.py data.txt -vv      → 更详细（连第三方库的日志也放开）
```

实现要点：`action="count"` 让 `-v` 可以重复（`args.verbose` 是 0/1/2），然后据此选择 `logging` 级别。

**这个模式要强调**：**日志级别应该由使用者决定，而不是写死在代码里**——开发时想看细节，正常运行时只想看关键信息，出问题时又想看细节。`-v` 就是那个开关。

**示例 24-23　`--dry-run` 的实现**

演示重点：这是**安全性设计**，不只是功能。

关键是**把"算"和"做"分开**：

```text
❌ 边算边做：
    for 每个文件:
        算出目标位置
        立刻移动          ← 没法预览，出错了已经移了一半

✅ 先算后做：
    plan = []
    for 每个文件:
        plan.append((源, 目标))     ← 只是算，不动手

    if args.dry_run:
        打印 plan，结束
    else:
        执行 plan
```

**要说明这个结构的额外好处**：`plan` 算出来之后可以检查冲突（两个文件要移到同一个位置）、可以统计数量、可以让用户确认——**这些都是"边算边做"做不到的**。

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 用 `args.dry-run` 取值 | `SyntaxError` | `args.dry_run` | 横线自动转下划线 |
| 忘了 `parse_args()` | 参数全是 `None` 或报错 | 必须调用它 | 它才真正解析 |
| 位置参数和可选参数搞反 | 用户必须写一堆 `--xxx` 才能跑 | 主角用位置参数 | 设计问题 |
| 用 `required=True` 的可选参数 | 用户困惑（既然必填为什么带 `--`） | 改成位置参数 | 语义矛盾 |
| 手工写 `if "-h" in sys.argv` | 重复劳动且不规范 | argparse 自动处理 | —— |
| 自己写类型转换和校验 | 代码长、报错不友好 | 用 `type=` 和 `choices=` | —— |
| 没给子命令时直接用 `args.command` | `None` 导致后续崩溃 | 判断并 `print_help()` | —— |
| 程序失败仍然 `exit(0)` | 自动化脚本以为成功了 | 失败时 `sys.exit(1)` | 退出码是唯一沟通方式 |
| 日志和结果都输出到 stdout | 管道和重定向被污染 | 日志走 stderr | —— |
| **密码用 `--password` 传** | `ps` 和 shell 历史里明文可见 | 环境变量或 `getpass` | 安全事故 |
| `.env` 提交进 Git | 密钥泄漏 | 加进 `.gitignore` | 回收 19.8 |
| 删改文件的工具没有 `--dry-run` | 用户误操作无法挽回 | 提供干跑模式 | —— |
| `--dry-run` 时仍然写了文件 | 干跑不"干"，失去意义 | 把"算"和"做"分开 | 实现结构问题 |
| 日志级别写死在代码里 | 想看细节得改代码 | `-v` 控制 | —— |

### 下一章预告

阶段二的工具链到这里齐了：数据能存（16、17）、代码能分（18）、环境能建（19）、错误能查能记能设计（20、21、22）、时间能算对（23）、参数能传（24）。

**现在你已经能交付一个真正可用的命令行工具了**——这也是验收项目 B 的全部要求。

第 25 章是阶段二的最后一章，不引入新的"工具"，而是回头把**函数**写得更好：`*args` / `**kwargs`、`lambda`、高阶函数、推导式进阶、解包技巧。学完之后，你写的代码会从"能跑的翻译式代码"变成"地道的 Python"——然后就可以动手做验收项目 B 了。

---

## 第 25 章　函数进阶与 Pythonic 写法 ★★

| 项 | 内容 |
|---|---|
| 本章目标 | 会用 `*args` / `**kwargs` 和参数转发；会用解包调用函数；掌握 `sorted(key=...)` 包括多级排序；知道推导式为什么通常优于 `map`/`filter`；能把"翻译式代码"改写成地道的 Python |
| 前置 | 第 14 章（函数基础）、第 24 章 |
| 预计用时 | 5~6 小时 |
| 可用语法边界 | 阶段一全部 + 第 16~24 章 + `lambda` / `map` / `filter` / `functools.reduce` / `operator.itemgetter`。**不得使用**：装饰器（第 31 章——但 25.5 的参数转发是它的前置知识，要点明这层关系）、生成器函数 `yield`（第 30 章——本章只讲生成器**表达式**）、`collections`（第 34 章，只做预告） |
| 目录调整 | 新增 25.5（**参数转发**——装饰器的前置知识）、25.6（**解包调用** `f(*list)` / `f(**dict)`）、25.12（**多级排序**：`key` 返回元组，极实用但很多人不知道）、25.18（**`any` / `all` + 生成器表达式**——最地道的"批量判断"写法）、25.20（字典合并 `{**a, **b}` 与 `|`）、25.24（**什么时候不该追求"地道"**）。原目录 8 个小节缺了多级排序和参数转发这两个高价值内容 |
| 编写要点 | 本章是阶段二的收尾，也是**审美教育**：同样能跑的代码，有"看着就懂"和"要读三遍"的差别。三处重笔：<br>① **多级排序**（25.12）——`key` 返回元组就能实现"先按 A 排、A 相同再按 B 排"，这个技巧极实用而知道的人不多；<br>② **推导式 vs `map`/`filter`**（25.14）——要客观：不是 `map` 不好，而是在 Python 里推导式更易读；`map` 在"函数已经存在"时仍然是好选择；<br>③ **25.23 的改写清单**——**本章的高潮和全书阶段二的收口**。给出十几组"从其他语言带来的写法 → 地道 Python"的对照，每组都要说明**为什么后者更好**（不是"因为这样才 Pythonic"，而是具体的好处：更短、更不容易错、意图更明显）。<br><br>**同时要给出反面提醒**（25.24）：追求"地道"不能牺牲可读性。嵌套三层的推导式虽然"很 Python"，但不如一个老实的循环。 |

### 知识点清单

| # | 知识点 | 必须讲到（含四问要点） | 优先级 |
|---|---|---|---|
| 25.1 | 本章定位 | 不引入新工具，而是**把已有的东西写好**；**为什么值得单独一章**：代码写完只是开始，它会被读几十次（包括三个月后的你）；本章所有内容的判断标准都是**"读的人更容易懂吗"** | ★★ |
| 25.2 | `*args` | 接收任意多个位置参数，在函数内是一个**元组**；**什么时候用**：参数个数不确定（`sum_all(1,2,3)`）、写通用的包装函数；**注意**：`args` 只是惯例名，`*` 才是语法 | ★★★ |
| 25.3 | `**kwargs` | 接收任意多个关键字参数，在函数内是一个**字典**；**什么时候用**：转发配置项、写通用包装；**惯例名** `kwargs` | ★★★ |
| 25.4 | 参数的完整顺序 | `def f(位置参数, *args, 关键字参数, **kwargs)`；**顺序不能乱**，否则 `SyntaxError`；给一张完整的参数种类表（位置、默认、可变位置、仅关键字、可变关键字） | ★★ |
| 25.5 | **参数转发** | `def wrapper(*args, **kwargs): return func(*args, **kwargs)`——**原封不动把参数传给另一个函数**；**为什么重要**：这是**装饰器的核心机制**（第 31 章），也是写通用包装函数的基础；**要点明这层关系**，让读者知道现在学的东西马上会用上 | ★★★ |
| 25.6 | **解包调用** | `f(*列表)` 把列表拆成多个位置参数、`f(**字典)` 把字典拆成关键字参数；**典型场景**：参数存在配置字典里，直接 `f(**config)`；**与定义时的 `*` / `**` 区分**：定义时是"收集"，调用时是"拆开"——**同一个符号，两个方向** | ★★★ |
| 25.7 | 仅关键字与仅位置 | `def f(a, *, b)` 中 `b` **必须用关键字传**（防止调用方写出 `f(1, True, False)` 这种看不懂的代码）；`def f(a, /, b)` 中 `a` 只能按位置传（3.8+，标准库里常见）；**实用建议**：**布尔参数一律设成仅关键字**（回收 14.5 的可读性讨论） | ★★ |
| 25.8 | `lambda` | 语法 `lambda 参数: 表达式`；**三个限制**：只能是一个表达式、不能有语句、没有名字（报错栈里显示 `<lambda>`）；**什么时候用**：作为 `key=` / 回调的一次性小函数；**什么时候别用**：赋值给变量（那就该用 `def`）、逻辑超过一行 | ★★★ |
| 25.9 | `lambda` vs `def` | 判断标准：**这个函数需要名字吗？会被复用吗？需要写文档字符串吗？** 三个有一个是"是"就用 `def`；**PEP 8 明确不建议** `f = lambda x: x*2` 这种写法——它比 `def f(x): return x*2` 没有任何优势，还丢了函数名 | ★★★ |
| 25.10 | **`sorted(key=...)` 正式讲** | 回收 10.18、11.20；`key` 是一个函数，对每个元素调用一次，**按返回值排序**；常用写法：`key=len`、`key=str.lower`、`key=lambda t: t[1]`、`key=operator.itemgetter(1)`；**`reverse=True` 降序**；**关键认知**：`key` 不改变元素本身，只决定比较用什么 | ★★★ |
| 25.11 | 按字段排序 | 列表里装字典/元组时按某个字段排：`key=lambda r: r["score"]`；`itemgetter("score")` 是等价写法且略快；**什么时候用 `itemgetter`**：字段名固定、追求性能时；否则 `lambda` 更直观 | ★★★ |
| 25.12 | **多级排序** | **`key` 返回元组就实现了多级排序**：`key=lambda r: (r["班级"], -r["分数"])`——先按班级升序，班级相同再按分数降序（**数字取负实现降序**）；**字符串字段要降序怎么办**：不能取负，改用两次 `sorted`（**稳定排序**保证先排的次序被保留）；**这个技巧极实用而知道的人不多** | ★★★ |
| 25.13 | 稳定排序 | Python 的排序是**稳定的**——值相等的元素保持原有相对顺序；**这个性质的用法**：多级排序可以拆成多次 `sorted`，从**最次要的字段开始排** | ★★ |
| 25.14 | `map` 与 `filter` | `map(函数, 可迭代对象)`、`filter(函数, 可迭代对象)`；**它们返回迭代器**（`print` 出来看不到内容，要 `list()` 包一层——这是新手困惑点）；**性能上和推导式差不多**，差别主要在可读性 | ★★ |
| 25.15 | **为什么推导式通常更好** | 对照同一需求的两种写法：`[f(x) for x in lst]` vs `list(map(f, lst))`、`[x for x in lst if p(x)]` vs `list(filter(p, lst))`；**推导式的优势**：① 不用 `list()` 包一层 ② 表达式可以直接写在里面（`map` 需要先有一个函数或临时 lambda）③ 过滤和转换能一次写完；**`map` 什么时候仍然更好**：**函数已经存在且不需要 lambda** 时（`map(int, 字符串列表)` 比 `[int(x) for x in ...]` 略简洁）；**要客观，不要一刀切** | ★★★ |
| 25.16 | 推导式进阶 | 多重循环 `[（i,j) for i in a for j in b]`（**顺序等同于嵌套 for，从左到右**）；嵌套推导式（二维列表展平）；带 `if` 过滤；`if/else` 在表达式位置（`[x if x>0 else 0 for x in lst]`——**注意这个 `if` 在前面，和过滤的 `if` 位置不同**） | ★★★ |
| 25.17 | **推导式的可读性红线** | 三条硬线：① **超过两层循环就改写成普通 for** ② **同时有多重循环和条件时改写** ③ **写完自己读一遍读不顺就改写**；**给一个"炫技型"推导式的反面例子**，然后用普通循环重写，让读者对比哪个更容易懂；**核心观点**：推导式的价值是"一眼看出在做什么"，一旦看不出来，它就失去了全部意义 | ★★★ |
| 25.18 | 生成器表达式 | `(x*2 for x in lst)` 圆括号版；**它不是元组推导式**（回收 13.23）；**区别**：推导式立刻算出所有结果，生成器表达式**按需生成**；**什么时候用**：数据量大、只遍历一次、或者作为 `sum` / `any` / `max` 的参数（**这时连括号都可以省**）；完整的生成器知识在第 30 章 | ★★★ |
| 25.19 | **`any` / `all` + 生成器表达式** | `any(x > 100 for x in lst)`、`all(r["score"] >= 60 for r in records)`；**为什么这是最地道的"批量判断"写法**：① 比循环 + flag 变量短得多 ② **短路求值**——`any` 找到第一个真就停，不用遍历完（回收 4.8）③ 意图一目了然；回收 13.4 的空容器边界（`all([])` 是 `True`） | ★★★ |
| 25.20 | 解包全家桶 | `a, b = b, a`（回收 10.11）、`first, *rest = lst`（回收 10.12）、`a, *mid, z = lst`、嵌套解包 `(a, (b, c)) = ...`、**`_` 表示"不需要这个值"**（`_, name = pair`）；`for _ in range(3)` 表示"重复三次但不用计数器" | ★★★ |
| 25.21 | 字典的合并与解包 | `{**a, **b}`（后者覆盖前者）、`a | b`（3.9+，更清楚）、`{**默认配置, **用户配置}` **这是配置合并的标准写法**（回收 24.17 的三层配置） | ★★ |
| 25.22 | `zip` 的进阶用法 | 三个用法：① 配对遍历（回收 8.15）② **转置** `list(zip(*矩阵))`（**这里的 `*` 是解包调用**，回收 25.6）③ `strict=True`（3.10+）防止长度不等时静默丢数据（回收 8.15 的坑） | ★★ |
| 25.23 | **从"翻译式代码"到地道 Python** | **本章高潮**：十几组对照改写，每组说明具体好处（不是"这样才 Pythonic"，而是"更短/更不容易错/意图更明显"）；清单见示例 25-28 | ★★★ |
| 25.24 | **什么时候不该追求"地道"** | 四条反面提醒：① 团队里没人看得懂的写法就不要用 ② 嵌套推导式不如老实的循环 ③ 为了一行而牺牲可读性是本末倒置 ④ **"地道"的目的是让代码更容易读，不是显示水平**；**一句话收口**：如果一个写法需要你在旁边加注释解释它怎么工作，那就换个写法 | ★★★ |

### 代码示例清单

| # | 示例标题 | 段位 | 演示要点 | 核心写法 |
|---|---|---|---|---|
| 25-1 | `*args` 接收任意多参数 | A | 函数内是元组；`sum_all(1,2,3)` | `def f(*args)` |
| 25-2 | `**kwargs` 接收任意多关键字 | A | 函数内是字典 | `def f(**kwargs)` |
| 25-3 | 参数的完整顺序 | B | 五种参数按顺序排；顺序写错的 `SyntaxError` | 参数顺序表 |
| 25-4 | **参数转发** | B | `wrapper(*args, **kwargs)` 原封不动传给另一个函数；**点明这是装饰器的核心机制** | 转发 |
| 25-5 | **解包调用** | B | `f(*列表)` / `f(**字典)`；与定义时的 `*` 对比（收集 vs 拆开） | `f(**config)` |
| 25-6 | 布尔参数设成仅关键字 | B | ❌ `f(1, True, False)` 看不懂 → ✅ `def f(a, *, verbose=False, force=False)` | `*` 分隔 |
| 25-7 | `lambda` 基础与限制 | A | 只能一个表达式；试着写语句会报错 | `lambda x: x*2` |
| 25-8 | 不要把 lambda 赋值给变量 | A | ❌ `f = lambda x: x*2` → ✅ `def f(x)`；说明前者丢了函数名 | PEP 8 |
| 25-9 | `sorted(key=...)` 正式用法 | B | `key=len`、`key=str.lower`（回收 4.10 的大小写排序问题） | `key=str.lower` |
| 25-10 | 按字典字段排序 | B | `key=lambda r: r["score"]`；`itemgetter` 等价写法 | `itemgetter` |
| 25-11 | **多级排序** | B | `key=lambda r: (r["班级"], -r["分数"])`；先班级升序、再分数降序 | `key` 返回元组 |
| 25-12 | 字符串字段要降序怎么办 | B | 不能取负 → 用两次 `sorted`，利用**稳定排序** | 稳定排序 |
| 25-13 | `map` / `filter` 返回迭代器 | A | `print(map(...))` 看不到内容，要 `list()` 包一层 | `list(map(...))` |
| 25-14 | **推导式 vs map/filter** | B | 同一需求四种写法并排；说明各自适用场合；**`map(int, ...)` 是 map 仍然好用的例子** | 对照 |
| 25-15 | 多重循环推导式 | B | `[(i,j) for i in a for j in b]`；**顺序等同嵌套 for** | 多重循环 |
| 25-16 | 展平二维列表 | B | `[x for row in matrix for x in row]` | 嵌套 |
| 25-17 | 表达式位置的 `if/else` | B | `[x if x>0 else 0 for x in lst]`；**和过滤的 `if` 位置不同** | `if/else` |
| 25-18 | **推导式的可读性红线** | B | ❌ 一个三层嵌套带条件的"炫技"推导式 → ✅ 用普通循环重写，对比哪个好懂 | 红线 |
| 25-19 | 生成器表达式 | B | `(x*2 for x in lst)`；不是元组推导式（回收 13.23）；作为 `sum` 参数时可省括号 | `sum(x*2 for x in lst)` |
| 25-20 | **`any` / `all` + 生成器表达式** | B | ❌ 循环 + flag 变量（六行）→ ✅ `any(x > 100 for x in lst)`（一行）；说明短路求值 | `any(... for ...)` |
| 25-21 | 解包全家桶 | B | 交换、`first, *rest`、`a, *mid, z`、嵌套解包 | `first, *rest = lst` |
| 25-22 | `_` 表示不需要 | A | `_, name = pair`；`for _ in range(3)` | `_` |
| 25-23 | 字典合并 | A | `{**默认, **用户}`、`a | b`（3.9+）；**配置合并的标准写法**（回收 24.17） | `{**a, **b}` |
| 25-24 | `zip` 转置矩阵 | B | `list(zip(*matrix))`；**这里的 `*` 就是 25-5 的解包调用** | `zip(*matrix)` |
| 25-25 | `zip(strict=True)` | A | 长度不等时报错而不是静默丢数据（回收 8.15） | `strict=True` |
| 25-26 | **十四组改写对照** | C | **本章核心示例**，见下方样板说明 | 改写清单 |
| 25-27 | 重构前面章节的代码 | C | 挑三段前面写过的代码（第 9 章成绩统计、第 11 章词频、第 16 章文件处理）用本章技巧重写，**对比行数和可读性** | 综合 |
| 25-28 | 一个"过度地道"的反面例子 | B | 一段用了五个技巧挤在两行里的代码；说明为什么它比朴素版本更糟 | 反面 |

### 样板示例

**示例 25-11 + 25-12　多级排序**

演示重点：这是本章最实用的技巧之一，而且很多写了几年 Python 的人也不知道。

**核心认知**：`key` 返回什么，就按什么排。**返回元组时，Python 按元组的顺序逐个比较**——这正好就是多级排序。

```text
需求：先按班级升序，班级相同的按分数降序

key=lambda r: (r["班级"], -r["分数"])
                  ↑           ↑
              第一排序键   第二排序键（取负实现降序）
```

**要讲清"取负"这个技巧的边界**：只能用于数字。字符串字段要降序时不能取负，得用**稳定排序**的性质分两次排：

```text
先按次要字段排 → 再按主要字段排
（因为排序是稳定的，第二次排序不会打乱第一次的结果）
```

**这个"从最次要的字段开始排"的顺序容易记反**，要专门强调。

**示例 25-20　`any` / `all` + 生成器表达式**

演示重点：这是"地道 Python"最有说服力的例子之一，因为改进非常明显。

**❌ 翻译式写法**（从其他语言带来的习惯）：

```text
found = False
for x in lst:
    if x > 100:
        found = True
        break
if found:
    print("有超过 100 的")
```

**✅ 地道写法**：

```text
if any(x > 100 for x in lst):
    print("有超过 100 的")
```

**要说明三个具体好处**（不要只说"更 Pythonic"）：

1. **六行变一行**，而且这一行读起来就是需求本身
2. **不需要 flag 变量**——flag 变量是 bug 的常见来源（忘了初始化、忘了 break、在错误的地方重置）
3. **短路求值**：`any` 找到第一个满足的就停止（回收 4.8），和手写 `break` 效果一样，不会白跑

**并给出 `all` 的对应用法**和空容器的边界（回收 13.4：`all([])` 是 `True`，校验空数据时要小心）。

**示例 25-26　十四组改写对照**

**这是本章的高潮，也是阶段二的收口。** 要求：每一组都给出「翻译式写法 → 地道写法 → **具体好处**」，好处必须是可验证的（更短、更不容易错、意图更明显），**不能只说"这样才 Pythonic"**。

清单（每组都要标出回收了哪一章的知识点）：

| # | 翻译式写法 | 地道写法 | 好处 | 回收 |
|---|---|---|---|---|
| 1 | `for i in range(len(lst)): lst[i]` | `for x in lst:` | 不会下标越界，意图更直接 | 8.5 |
| 2 | 手工维护计数器 `i = 0; i += 1` | `for i, x in enumerate(lst)` | 不会忘记自增 | 8.14 |
| 3 | 循环累加求和 | `sum(lst)` | 一行，且不会写错初始值 | 9.13 |
| 4 | `if len(lst) > 0:` | `if lst:` | 更短；对所有容器统一 | 7.5 |
| 5 | `if x == True:` | `if x:` | 避免 `1 == True` 的陷阱 | 3.5 |
| 6 | 建空列表再循环 `append` | 列表推导式 | 一行说清"从什么变成什么" | 9.22 |
| 7 | `try: d[k] except KeyError:` | `d.get(k, 默认值)` | 不用异常做流程控制 | 11.5 / 15.13 |
| 8 | 循环里 `s += 每一段` | `"".join(列表)` | 字符串不可变，`+=` 每次都造新对象 | 5.3 / 5.21 |
| 9 | 临时变量交换 | `a, b = b, a` | 少一个变量 | 10.11 |
| 10 | 循环 + flag 判断"有没有" | `any(...)` / `all(...)` | 一行 + 短路求值 | 25.19 |
| 11 | `if k in d.keys():` | `if k in d:` | 更短；`keys()` 是多余的 | 11.6 |
| 12 | 一长串 `elif x == 值` | 字典分发（值是函数） | 加分支只改数据 | 11.18 / 14.16 |
| 13 | `f = open(...)` + `f.close()` | `with open(...)` | 异常时也保证关闭 | 16.4 |
| 14 | 手工计数字典 `if k in d: d[k]+=1 else: d[k]=1` | `d[k] = d.get(k, 0) + 1`（并预告 `Counter`） | 三行变一行 | 11.16 / 34.1 |

**每组的写法都要给真实可运行的代码**，不能只写描述。

**示例 25-28　一个"过度地道"的反面例子**

演示重点：这一节是给 25-26 打的补丁，防止读者从"翻译式"直接冲到"炫技式"。

给一段挤了五个技巧的代码（嵌套推导式 + 多重条件 + lambda + 解包 + 三元表达式），然后用朴素写法重写，让读者自己判断哪个好读。

**收口的话要说明白**：**本章教的所有技巧都是手段，目的是"让读代码的人更快理解"。** 判断标准很简单——**如果你需要在旁边加注释解释这个写法怎么工作，那就换个写法。**

---

### 易错点

| ❌ 情况 | 症状 | ✅ 解法 | 原因 |
|---|---|---|---|
| 参数顺序写错 | `SyntaxError` | 按"位置 → `*args` → 仅关键字 → `**kwargs`" | 语法规定 |
| 定义时和调用时的 `*` 搞混 | 行为不符合预期 | 定义是"收集"，调用是"拆开" | 同符号两个方向 |
| `f = lambda x: ...` | 能跑，但丢了函数名、栈里显示 `<lambda>` | 用 `def` | PEP 8 不建议 |
| lambda 里想写语句 | `SyntaxError` | 改用 `def` | lambda 只能是表达式 |
| `print(map(...))` | 显示 `<map object at 0x...>` | `list(map(...))` 或用推导式 | map 返回迭代器 |
| map / filter 的结果用了两次 | 第二次是空的 | 转成列表，或重新生成 | 迭代器只能遍历一次 |
| 多级排序时取负排字符串 | `TypeError` | 用两次 `sorted` 配合稳定排序 | 字符串不能取负 |
| 两次 `sorted` 顺序反了 | 排序结果不对 | **从最次要的字段开始排** | 稳定排序的性质 |
| 推导式嵌套三层 | 自己都读不懂 | 改写成普通循环 | 超过可读性红线 |
| 把过滤的 `if` 和三元的 `if` 位置搞混 | 语法错误或逻辑错误 | 过滤在后、三元在前 | 两种 `if` 位置不同 |
| 以为 `(x for x in lst)` 是元组推导式 | 得到 generator 对象 | 要元组用 `tuple(...)` | 回收 13.23 |
| 生成器表达式遍历两次 | 第二次是空的 | 需要多次用就转成列表 | 一次性 |
| 用 `all()` 校验空数据 | 空列表返回 `True`，校验被绕过 | 先判断非空 | 回收 13.4 |
| 为了"地道"写出没人看懂的代码 | 团队维护困难 | 可读性优先 | 手段和目的搞反了 |

---

## 阶段二收尾

**阶段二到这里结束了。** 回头看这十章补上的东西：

| 能力 | 章节 |
|---|---|
| 数据能持久化（文件、JSON、CSV） | 16、17 |
| 代码能分文件、能复用 | 18 |
| 环境能重建、依赖能记录 | 19 |
| 出错能定位（断点）、能留痕（日志）、能设计（异常分层） | 20、21、22 |
| 时间能算对 | 23 |
| 能接受参数、像个正经工具 | 24 |
| 代码写得地道 | 25 |

**这些加起来，正好是"交付一个别人也能用的程序"所需的全部基础。**

> ### 🎯 开工：验收项目 B（两个都要做）
>
> **B1 命令行记账本**
> - `argparse` 子命令：`add` / `list` / `report`（第 24 章）
> - JSON 持久化（第 17 章）
> - 模块拆分：`storage.py` / `report.py` / `cli.py`（第 18 章）
> - 业务异常 + 分层处理（第 22 章）
> - 日志写文件（第 21 章）
> - 日期处理用 ISO 格式存储（第 23 章）
>
> **B2 文件自动归档脚本**
> - `pathlib` 递归遍历（第 16 章）
> - 按后缀或日期分类（第 16、23 章）
> - **`--dry-run` 干跑模式**（第 24 章，强制要求）
> - 重名冲突处理
> - 操作日志（第 21 章）
>
> **交付标准（这是本阶段的真正考核）**：
> 把项目目录交给别人，他只看你的 `README.md`，就能完成——**建虚拟环境 → 装依赖 → 跑起来 → 看懂怎么用**。
>
> 做不到这一点，说明阶段二没有真正掌握。

**下一阶段预告**：阶段三（第 26~37 章）解决的是另一类问题——**代码活得久了怎么办**。

当程序从三百行长到三千行、当你三个月后回来改它、当别人要接手它，你会需要：类（26~29）、迭代器与生成器（30）、装饰器（31）、上下文管理器（32）、类型注解（33）、高效标准库（34）、正则（35）、**测试**（36）、代码规范与工具链（37）。

阶段二让你能"交付"，阶段三让你能"维护"。
