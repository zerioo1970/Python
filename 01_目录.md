# Python 3 新手完全教程 · 目录与读法

> 本文件是教程的**总纲**：读者画像、教程结构、阅读方法，以及四阶段完整目录。
> 写作用的详细图纸（每章知识点清单、代码示例清单、样板示例、易错点）见 `02_施工图.md`。

---

## 1. 这份教程写给谁

**主要读者**
- 完全没写过代码，想把 Python 作为第一门语言的人
- 会一点语法但写不出完整程序、不知道"下一步该学什么"的人
- 有其他语言经验（C# / Java / SQL / VB），需要快速迁移到 Python 的人

**不适合**
- 想速成三天上手的人（本教程主线约 10 周）
- 找算法题解或框架深度源码分析的人

**假设的前提**：会用电脑、会装软件、会开命令行窗口。不假设任何数学基础，全书不出现推导。

---

## 2. 这份教程有什么不一样

| 常见教程的问题 | 本教程的做法 |
|---|---|
| 语法讲完就结束，不教怎么交付程序 | 阶段二专门讲日志、参数、调试、模块化，标准是"别人拿去能跑" |
| 不讲测试、类型注解、代码风格 | 阶段三把它们当必学内容，不当"高级话题" |
| 把元类、GIL、垃圾回收当"进阶精华" | 这些明确标注【面试向】并下沉到选修专项 |
| 只在最后放一个大项目 | 每阶段末尾都有验收项目，且项目逐阶段迭代升级 |
| 报错怎么读没人教 | 第 2 章就教读 Traceback 和十个必认报错 |
| 深浅拷贝放到最后讲 | 提到第 13 章（容器学完立刻讲），因为它会造成真 bug |

---

## 3. 教程结构：三层

```
主线（必学，约 10 周）
├── 阶段一  第 1~15 章   基础入门        → 能写出解决问题的脚本
├── 阶段二  第 16~25 章  从脚本到程序    → 能交付一个可用的命令行工具
└── 阶段三  第 26~37 章  地道且可维护    → 能写出团队能接手的代码

专项（选修，用到再学）
└── 阶段四  A 并发 | B 性能内存 | C 数据库 | D 网络 | E 打包 | F 底层元编程

工具箱（不读，只查）
└── T1 系统进程 | T2 数字数学 | T3 数据格式 | T4 第三方生态 | T5 附录速查表
```

每章标注优先级：

- ★★★ **必学**，跳过会影响后续章节
- ★★ **应会**，日常工作高频
- ★ **了解即可**，用到再回来查
- 【面试向】应用开发中极少用到，只为理解原理或应付面试

---

## 4. 怎么读

### 4.1 三条硬规矩

1. **所有代码必须自己敲一遍**，不要复制粘贴。手指记住的语法比眼睛记住的牢十倍。
2. **每章的"易错点"部分要故意写错一次**，亲眼看到那个报错，比读十遍说明有用。
3. **阶段验收项目必须做完**，做不出来说明前面没学会，回头补，不要往下推进。

### 4.2 按背景选路线

| 你的情况 | 建议路线 |
|---|---|
| 零基础 | 老实按 1→37 章顺序走，不跳章 |
| 有其他语言经验 | 先读 T7-E 的语言对照表，阶段一速通（重点看第 2、5、9、11、13、14 章的"易错点"），阶段二三照常细读 |
| 只想写自动化小脚本 | 阶段一 + 第 16~21、24 章即可，其余按需 |
| 目标是数据分析 / 量化 | 主线阶段一二 + 第 29、30、33、34 章，然后转数据方向专项（`numpy` / `pandas`） |
| 目标是 Web 后端 | 主线全部 + 专项 C、D、E |

### 4.3 卡住了怎么办

1. 先把报错信息完整读一遍（第 2.6 节教了怎么读）
2. 用第 20 章的断点调试，看变量到底是什么值
3. 查工具箱 T7-A 的报错速查表
4. 把出错代码缩到最小可复现的 5 行——一半情况下缩的过程中就找到原因了

---

---

## 5. 环境要求

- Python **3.10+**（推荐 3.12 或 3.13）
- 编辑器：VS Code + Python 插件，或 PyCharm 社区版
- 阶段一可选装 Jupyter Notebook 做实验
- 第三方库只在明确说明处使用；主线用到的仅 `requests`、`pytest`、`ruff`、`mypy`

---

## 6. 写作进度

| 部分 | 目录 | 施工图 | 正文 |
|---|---|---|---|
| 阶段一（第 1~15 章） | ✅ | ✅ 全部完成 | 第 1~8 章 ✅　第 9~15 章 ⬜ |
| 阶段二（第 16~25 章） | ✅ | ✅ 全部完成 | ⬜ |
| 阶段三（第 26~37 章） | ✅ | ⬜ | ⬜ |
| 阶段四　专项 A~F | ✅ | ⬜ | ⬜ |
| 工具箱　T1~T7 | ✅ | 手册类不需图纸 | T5 / T6 ✅　其余 ⬜ |

---

# 详细目录

> 优先级：★★★ 必学　★★ 应会　★ 了解即可 / 用到再查
> 【面试向】表示应用开发中极少用到，只为理解原理或应付面试。

## 阶段一　基础入门：能写出解决问题的脚本
**目标**：把 Python 当成计算器和自动化脚本用，建立编程思维。
**建议投入**：3~4 周，每天 1~2 小时。这一阶段每一节都必须自己敲一遍。

### 第 1 章　认识 Python 与环境搭建 ★★★
- 1.1 Python 是什么：解释型、动态类型、跨平台
- 1.2 为什么只学 Python 3；能做什么、不适合做什么
- 1.3 安装 Python 与 `PATH` 配置（Windows / macOS / Linux）
- 1.4 三种运行方式对比
  - REPL 交互式解释器：验证一行代码的最快方式
  - `.py` 脚本：`python script.py`
  - **Jupyter Notebook**：分段执行、留住中间结果，适合做实验和学习
- 1.5 编辑器选择：VS Code（装 Python 插件）/ PyCharm 社区版，最小配置
- 1.6 用 pip 安装第一个第三方库

### 第 2 章　语法规则与读懂报错 ★★★
- 2.1 `Hello, World!` 逐行拆解
- 2.2 缩进：Python 用 4 个空格代替花括号
- 2.3 注释与文档字符串
- 2.4 一行一句、换行续行、分号
- 2.5 标识符命名规范（`user_age` 风格）与关键字
- 2.6 **读懂 Traceback**：从下往上看、定位到自己的代码行
- 2.7 新手前十个必认报错：`SyntaxError` / `IndentationError` / `NameError` / `TypeError` / `ValueError` / `IndexError` / `KeyError` / `AttributeError` / `ZeroDivisionError` / `ModuleNotFoundError`

### 第 3 章　变量与基本数据类型 ★★★
- 3.1 变量是"标签"不是"盒子"：赋值的本质
- 3.2 `int`：任意精度、进制、下划线分隔
- 3.3 `float`：精度陷阱（`0.1 + 0.2 != 0.3`）与 `round`
- 3.4 `bool`：`True` / `False` 首字母大写
- 3.5 `None`
- 3.6 类型转换：`int()` / `float()` / `str()` / `bool()`，显式与隐式
- 3.7 类型查看：`type` / `isinstance`
- 3.8 `complex` 复数（★ 跳过无妨）

### 第 4 章　运算符与表达式 ★★★
- 4.1 算术：`+ - * /` 与 `//` 整除、`%` 取模、`**` 幂
- 4.2 赋值与复合赋值 `+=` `-=` `*=`
- 4.3 比较运算符与链式比较 `0 < x < 10`
- 4.4 逻辑 `and` / `or` / `not` 与短路求值
- 4.5 成员 `in`、身份 `is`；**`is` 与 `==` 的区别**
- 4.6 运算符优先级与"不确定就加括号"
- 4.7 位运算（★ 用到再查）

### 第 5 章　字符串 ★★★
- 5.1 单引号、双引号、三引号（多行文本）
- 5.2 转义字符 `\n` `\t` 与原始字符串 `r''`
- 5.3 **不可变性**：字符串不能就地修改
- 5.4 索引与切片（负索引、步长）
- 5.5 常用方法·查判：`len` / `find` / `index` / `startswith` / `in`
- 5.6 常用方法·变换：`strip` / `replace` / `split` / `join` / `upper` / `lower`
- 5.7 格式化：**f-string 首选**，了解 `.format()` 与旧式 `%`
- 5.8 编码与中文：`encode` / `decode`、UTF-8、`bytes` 初识

### 第 6 章　输入与输出 ★★★
- 6.1 `print` 输出多个值：逗号分隔的真相
- 6.2 `sep` 参数：改变连接符
- 6.3 `end` 参数：让输出不换行
- 6.4 空 `print()` 打空行
- 6.5 三种输出风格对比：逗号 / `+` / f-string（结论：用 f-string）
- 6.6 `input` 基础：程序会停下来等
- 6.7 **`input` 永远返回字符串**（必踩的坑）
- 6.8 输入的清洗与转换：`strip` / `lower` / `int` / `float`
- 6.9 一次输入多个值：`split` + 索引
- 6.10 输出缓冲与 `flush`：为什么输出没马上出现
- 6.11 标准输出与标准错误：`stdout` / `stderr` 为什么要分开
- 6.12 `print` 也能写文件：`file` 参数（★ 正规做法见第 16 章）
- 6.13 中文对齐的现实（★ 回收第 5 章遗留问题）

> 原"输入校验与重试循环"已移出本章：重试需要 `while`（第 8 章）与 `try`（第 15 章），放在示例 8-14 与 15-13。

### 第 7 章　条件判断 ★★★
- 7.1 `if` / `elif` / `else`
- 7.2 真假规则：什么算 `False`（`0`、`""`、`[]`、`{}`、`None`）
- 7.3 三元表达式 `a if cond else b`
- 7.4 嵌套判断的扁平化：`elif` 替代嵌套、`and` 合并条件、边界值排序（"提前返回"式写法需要函数，见第 14 章）
- 7.5 `match-case` 只做认知（★ 详见第 37 章）

### 第 8 章　循环 ★★★
- 8.1 `for` 循环与可迭代对象
- 8.2 `range(start, stop, step)`
- 8.3 `while` 循环与循环条件设计、避免死循环
- 8.4 `break` / `continue` / `pass`
- 8.5 好搭档：`enumerate` / `zip` / `reversed`（`sorted` 需要列表，移到 9.4）
- 8.6 嵌套循环
- 8.7 循环的 `else` 子句（★ Python 特有，少见）

### 第 9 章　列表 list ★★★
- 9.1 创建、索引、切片
- 9.2 增删：`append` / `insert` / `extend` / `pop` / `remove` / `clear` / `del`
- 9.3 查统：`index` / `count` / `in`
- 9.4 排序：`sort` 与 `sorted`、`reverse`、`key` 初步
- 9.5 遍历时修改列表的陷阱（边遍历边删会跳过元素）
- 9.6 二维列表与 `[[0]*3]*3` 陷阱
- 9.7 `b = a` 之后改 b，a 也变了（现象与临时解法，原理见 13.4）
- 9.8 列表推导式**只讲最简形式**（进阶写法见第 25 章）
- 9.9 列表与字符串互转：`split` / `join` / `list()`

### 第 10 章　元组 tuple ★★
- 10.1 创建元组；**逗号才是关键，括号不是**
- 10.2 单元素元组必须加逗号 `(1,)`
- 10.3 索引与切片（与列表一致）
- 10.4 不可变意味着什么；**不可变的边界**：元组里装的列表照样能改
- 10.5 元组只有 `count` 和 `index` 两个方法
- 10.6 **可哈希**：元组能当字典键、列表不能（`hash()` 验证）
- 10.7 解包；个数必须匹配
- 10.8 交换变量 `a, b = b, a` 的原理
- 10.9 星号解包 `first, *rest`（注意 `rest` 是列表）
- 10.10 函数返回多值的本质就是元组（用 `divmod` 演示）
- 10.11 解包遍历一批记录：`for name, score in records`
- 10.12 元组与列表互转
- 10.13 选型：元组表达"结构"，列表表达"集合"
- 10.14 具名元组 `namedtuple` 一瞥（★ 详见第 34 章）

### 第 11 章　字典 dict ★★★
- 11.1 字典是什么：按**名字**取数据（对照列表按位置取）
- 11.2 创建字典；键的要求（唯一 + 可哈希）；键重复会静默覆盖
- 11.3 **取值三法及其失败行为**：`[]` 抛异常 / `get` 返回默认值 / `setdefault` 会写入
- 11.4 `in` 判断的是**键**，不是值
- 11.5 增和改是同一个语法；`update` 合并
- 11.6 删除：`del` / `pop` / `pop(k, 默认值)` / `clear`
- 11.7 遍历：直接遍历得到键；`keys` / `values` / `items`
- 11.8 **遍历时增删直接报错**（与列表的"静默出错"对比）
- 11.9 字典推导式
- 11.10 嵌套字典 = JSON 的形状；嵌套取值的安全写法
- 11.11 **高频用法①：计数**（对照第 9 章 `count` 的效率问题）
- 11.12 **高频用法②：分组**（`setdefault` 唯一好用的场合）
- 11.13 **高频用法③：查找表替代一串 `elif`**
- 11.14 3.7+ 保持插入顺序（"字典无序"是过时信息）
- 11.15 按值排序出排行榜
- 11.16 字典与列表互转
- 11.17 视图对象是"活的"（`keys()` 不是快照）
- 11.18 拷贝问题：`copy()` 只复制第一层（详见 13.4）

### 第 12 章　集合 set ★★
- 12.1 集合是什么：**只有键没有值的字典**
- 12.2 创建；**空集合必须用 `set()`**（`{}` 是字典）
- 12.3 元素必须可哈希；无序意味着不能索引
- 12.4 增删：`add` / `update` / `remove` / `discard` / `pop`（随机删）
- 12.5 去重：`set(lst)` 丢顺序；**保序去重用 `dict.fromkeys`**
- 12.6 成员测试的效率优势：什么时候值得转成集合
- 12.7 集合运算：`&` 交、`|` 并、`-` 差（**有方向**）、`^` 对称差
- 12.8 子集/超集判断与权限检查场景
- 12.9 运算符 vs 方法：`a & b` 要求两边都是集合，`a.intersection(lst)` 不要求
- 12.10 集合推导式；遍历时用 `sorted()` 固定顺序
- 12.11 **实战：两批数据比对**（新增/删除/保留，三行代替双层循环）
- 12.12 `frozenset`（★ 不可变集合，可作字典键）

### 第 13 章　容器综合与拷贝语义 ★★★
- 13.1 四大容器选型对照表（什么时候用哪个）
- 13.2 可迭代 / 序列 / 映射：概念梳理
- 13.3 通用函数：`len` / `min` / `max` / `sum` / `sorted` / `zip`；空容器的边界
- 13.4 `any` / `all` 的反直觉边界：`all([])` 是 `True`
- 13.5 **拷贝语义实用篇（必须现在学，否则会写出真 bug）**
  - `b = a` 只是多一个标签
  - 切片拷贝 `a[:]`、`list(a)`、`copy.copy` 浅拷贝
  - `copy.deepcopy` 深拷贝与嵌套可变对象的坑
  - 判断依据：改了 b 会不会影响 a
  - 原理（引用计数、GC）留到阶段四，这里只讲"怎么做"
  - **深拷贝的代价**：慢、内存翻倍、有些对象拷不了
  - 什么时候真的需要深拷贝：配置模板、撤销快照、保护数据
  - 最常见的数据形状"列表套字典"的拷贝陷阱
  - 回收五处伏笔：3.3 标签模型、9.20 `[[0]*3]*3`、9.21、10.4 元组边界、11.18
- 13.6 推导式统一回顾：列表 / 字典 / 集合；`(...)` 不是元组推导式

### 第 14 章　函数基础 ★★★
- 14.1 为什么需要函数：给逻辑起名字、消除重复、能单独测试
- 14.2 定义与调用；必须先定义后调用
- 14.3 `return` 的行为：立即结束函数；无 `return` 返回 `None`
- 14.4 返回多个值（本质是元组）
- 14.5 位置参数与关键字参数；关键字参数的真正价值是可读性
- 14.6 默认参数；**必须放在无默认值的参数之后**
- 14.7 默认值只在定义时计算一次
- 14.8 **可变默认值陷阱**（`def f(items=[])` 为什么危险）
- 14.9 参数传的是引用（13.5 的第三次现身）
- 14.10 怎么保护调用者的数据：命名约定 / 内部拷贝 / 返回新对象
- 14.11 作用域：局部 → 全局 → 内置
- 14.12 遮蔽：别拿 `list` / `sum` / `str` 当变量名
- 14.13 **`UnboundLocalError`**：函数里赋过值的变量就是局部的
- 14.14 `global` 及其危害；用参数 + 返回值替代
- 14.15 文档字符串 docstring 与 `help()`
- 14.16 **函数是对象**：赋值、传参、**存进字典做命令分发器**（11.13 查找表的升级版）
- 14.17 提前返回式扁平化（从第 7 章移入）
- 14.18 一个函数只做一件事：三条可操作的判断标准
- 14.19 命名约定：动词开头，`is_` / `has_` 返回布尔
- 14.20 递归入门与深度限制（★）
- 14.21 `lambda` 一瞥（★ 详见第 25 章）
- 14.22 类型注解一瞥（★ 详见第 33 章）

### 第 15 章　异常处理入门 ★★★
- 15.1 语法错误 vs 运行时异常：一个跑不起来，一个跑到那行才崩
- 15.2 异常的传播：不处理就一路往上抛（这就是 Traceback 多行的原因）
- 15.3 `try` / `except` 最简形式；出错后 `try` 里剩下的语句会被跳过
- 15.4 捕获特定异常；捕获类型不对异常照样逃出去
- 15.5 捕获多种异常：`except (ValueError, TypeError)`
- 15.6 `except` 的顺序：**具体在前，宽泛在后**
- 15.7 `except ... as e` 拿到异常信息
- 15.8 `else` 子句：成功了才做的事
- 15.9 `finally`：无论如何都执行（`with` 是它的语法糖，见第 16 章）
- 15.10 **铁律一：不写裸 `except`**（会吞掉 `Ctrl+C`）
- 15.11 **铁律二：不吞异常**（`except: pass` 让 bug 消失）
- 15.12 `raise` 主动抛出；空 `raise` 重新抛出
- 15.13 异常不是流程控制：日常分支用 `if`，意料之外用异常
- 15.14 **EAFP vs LBYL**：Python 偏好"先动手，出错再补救"
- 15.15 常见内置异常速查（含"该不该捕获"）
- 15.16 `try` 块要小：只包真正可能出错的那一两行
- 15.17 `assert` 的正确用途与危险（`python -O` 会移除它）
- 15.18 自定义异常预告（需要 `class`，见第 22 章）

> ### 🎯 验收项目 A（做完才算过关，三个至少做两个）
> - **A1 猜数字游戏**：`while` + `input` 类型转换 + 条件判断 + 异常处理
> - **A2 命令行计算器**：函数拆分 + 字典做运算符分发表 + 除零异常
> - **A3 学生成绩管理**：列表 + 字典嵌套 + 增删改查 + 排序 + 统计（本项目在阶段二会被升级成带文件持久化的版本）

---

## 阶段二　从脚本到程序：能交付一个别人也能用的工具
**目标**：代码能复用、能处理外部文件、出错能定位、能带参数运行、能留下运行痕迹。
**建议投入**：3 周。这一阶段决定你写的是"能跑的代码"还是"能用的程序"。

### 第 16 章　文件读写与路径 ★★★
- 16.1 `open` 模式：`r` / `w`（覆盖！）/ `a` / `x` / `b` / `+`
- 16.2 **`with` 语句**与为什么必须用它
- 16.3 读：`read` / `readline` / `readlines` / 逐行迭代（大文件首选）
- 16.4 写：`write` / `writelines`、换行处理
- 16.5 `encoding='utf-8'` 与中文乱码
- 16.6 二进制文件读写
- 16.7 **`pathlib.Path`（现代方案，推荐）**：`Path.cwd()`、`exists()`、`/` 拼接、`glob`
- 16.8 **编码：中文乱码的根源**——Windows 默认 GBK、Linux/macOS 默认 UTF-8
- 16.9 文件指针：为什么读第二次是空的
- 16.10 遍历目录：`iterdir` / `glob` / `rglob`
- 16.11 **工作目录 ≠ 脚本目录**：`Path(__file__).parent` 的用法
- 16.12 `os` / `os.path` 对照（老代码里全是它）
- 16.13 安全写入：先写临时文件再替换（★）
- 16.14 常见任务速查表（六个高频操作的最简写法）

### 第 17 章　数据格式：JSON 与 CSV ★★★
- 17.1 JSON 四函数：`dumps` / `loads` / `dump` / `load`
- 17.2 `ensure_ascii=False` 与 `indent`（中文与美化）
- 17.3 JSON 与 Python 类型映射表
- 17.4 元组会退化成列表；**字典的 int 键会变成字符串**（静默发生）
- 17.5 不支持的类型：`set` / `datetime` 直接抛 `TypeError`
- 17.6 解析失败 `JSONDecodeError`；读外部 JSON 必须包 `try`
- 17.7 CSV 是什么：能给 Excel 看，但**没有类型**、不能嵌套
- 17.8 两套 API：`reader`/`writer` 与 **`DictReader`/`DictWriter`（推荐）**
- 17.9 **`newline=""`**：不加会导致 Windows 上多空行
- 17.10 **Excel 打开中文乱码**：用 `utf-8-sig` 写 BOM
- 17.11 **内容含逗号时手工 `split` 必错**——这就是用标准格式的理由
- 17.12 分隔符与方言：TSV、分号分隔
- 17.13 **JSON vs CSV 选型决策表**
- 17.14 Excel 文件一瞥（`openpyxl`，第三方，★）
- 17.15 其他格式一览：YAML / TOML / pickle / XML（★ 详见工具箱 T3）

### 第 18 章　模块与包 ★★★
- 18.1 一个 `.py` 就是一个模块
- 18.2 `import` 的四种写法与推荐做法（别用 `from x import *`）
- 18.3 **`if __name__ == "__main__"` 到底做什么**
- 18.4 包与 `__init__.py`（3.3+ 允许隐式命名空间包，但建议保留）
- 18.5 **`import` 会执行整个模块文件**（理解 import 的钥匙）
- 18.6 模块只执行一次（Jupyter 里改代码不生效的原因）
- 18.7 模块搜索路径 `sys.path` 与"明明有文件却导入失败"
- 18.8 **文件名撞车的惨剧**：自己的 `random.py` 会遮蔽标准库
- 18.9 包与 `__init__.py`；统一导出
- 18.10 绝对导入 vs 相对导入；**相对导入的文件不能直接运行**
- 18.11 `python -m` 的用途
- 18.12 import 语句的规范顺序：标准库 / 第三方 / 本地
- 18.13 循环导入的症状与解法（★）
- 18.14 探索陌生模块：`dir()` / `help()` / `__file__`
- 18.15 `__pycache__` 是什么（可以删，要加进 `.gitignore`）

### 第 19 章　虚拟环境与依赖管理 ★★★
- 19.1 为什么需要虚拟环境（一句话：项目之间别互相毒害）
- 19.2 虚拟环境是什么：就是一个目录，激活只是改 `PATH`
- 19.3 创建 `python -m venv .venv`
- 19.4 **激活：四个平台四套命令**
- 19.5 **怎么确认真的激活了**（`sys.executable` 最可靠）
- 19.6 **Windows PowerShell 激活失败**：执行策略问题
- 19.7 **`.venv` 不要提交 Git**：提交配料表而不是做好的菜
- 19.8 `pip` 常用：`install` / `list` / `show` / `uninstall` / 升级
- 19.9 `pip freeze > requirements.txt` 与 `pip install -r`
- 19.10 **`freeze` 会写入间接依赖**，怎么办
- 19.11 版本约束写法：`==` / `>=` / 区间 / `~=`
- 19.12 国内镜像源；升级 pip 用 `python -m pip`
- 19.13 让 IDE 用上虚拟环境（**回收 1.6 的伏笔**）
- 19.14 现代工具概览：`uv` / `poetry`（★ 知道存在即可）
- 19.15 **两个标准工作流**：开新项目 / 接手别人的项目

### 第 20 章　调试：从 print 到断点 ★★★
> 位置刻意前置——越早会打断点，越早脱离"加一行 print 跑一遍"的低效循环。
- 20.1 `print` 调试的四个困境
- 20.2 调试方法阶梯：`print` → `logging` → IDE 断点 → `pdb`（场合不同，不是越高级越好）
- 20.3 `print` 调试的正确姿势：`f"{x=}"`、打印类型、用 `repr()`
- 20.4 断点的概念：让程序停下，把当时的一切交给你看
- 20.5 **单步执行三个动作**：Step Over / Into / Out 的区别
- 20.6 变量面板与**现场求值**（断点最大的价值）
- 20.7 **条件断点**：循环一万次只在第 8000 次停
- 20.8 `breakpoint()` 与 `PYTHONBREAKPOINT=0` 一键禁用
- 20.9 `pdb` 八个够用的命令（含与 IDE 按钮的对应）
- 20.10 **崩溃后进入现场**：`python -m pdb` 与 `pdb.pm()`
- 20.11 最小复现：删到只剩能复现的几行
- 20.12 二分定位：一千行十次定位到一行
- 20.13 **先怀疑数据，再怀疑代码**
- 20.14 bug 类型 → 查法对照表
- 20.15 调试的心理纪律：一次一个假设、不要瞎改、卡住就换方式
- 20.16 读第三方库报错：只看栈里属于自己的那几行

### 第 21 章　日志 logging ★★
- 21.1 为什么交付的程序不能用 `print`（六个具体理由）
- 21.2 三行 `basicConfig` 起步
- 21.3 五个级别与「什么时候用哪个」判断表
- 21.4 **默认只显示 WARNING 以上**——为什么 `info` 什么都不输出
- 21.5 格式化：时间、级别、模块、行号
- 21.6 输出到文件；**加了 `filename` 屏幕就没输出了**
- 21.7 同时输出到屏幕和文件（实际项目的标准配置）
- 21.8 Logger / Handler / Formatter 三件套
- 21.9 **`getLogger(__name__)`**：多模块项目的标准做法
- 21.10 Logger 层级与日志重复输出两遍的原因
- 21.11 **`logger.exception` 记录完整堆栈**（只能在 `except` 里用）
- 21.12 `%s` 还是 f-string：客观对比两种写法
- 21.13 `basicConfig` 只生效一次
- 21.14 日志文件中文乱码：`encoding="utf-8"`
- 21.15 关掉第三方库的日志噪音
- 21.16 **日志里绝对不能写密码、Token、隐私数据**
- 21.17 日志轮转：按大小 / 按天
- 21.18 日志该记什么：记「发生了什么」+ 关键标识
- 21.19 `print` vs `logging`：受众不同

### 第 22 章　异常处理进阶 ★★
- 22.1 本章补什么：从「能捕获」到「会设计」
- 22.2 为什么需要自定义异常（内置异常表达不了业务含义）
- 22.3 定义自己的异常（**本书第一次出现 `class`，照抄即可，详见第 26 章**）
- 22.4 异常层次设计：一个业务基类 + 若干子类
- 22.5 **`raise ... from`**：保留原始原因
- 22.6 **异常链的三种形式**：`from e` / 不写 from / `from None`
- 22.7 **该在哪一层捕获**：底层抛、上层处理、中间层不管
- 22.8 异常转换：技术异常包装成业务异常
- 22.9 内置异常该不该捕获（三类对照：该 / 不该 / 绝不）
- 22.10 **重试模式**：什么错该重试、次数上限、指数退避
- 22.11 兜底与降级：默认值 / 旧数据 / 跳过并记录（**必须留痕**）
- 22.12 与 `logging` 配合：记录并重抛 / 记录并兜底 / 只在最上层记录
- 22.13 `assert` 的正确用途与危险（`python -O` 会移除它）
- 22.14 **分层策略总结**：数据层 / 业务层 / 入口层各做什么

### 第 23 章　日期与时间 ★★★
- 23.1 时间为什么难：月份长度、闰年、时区、夏令时、格式歧义
- 23.2 四个类型各管什么：`date` / `time` / `datetime` / **`timedelta`（长度不是时刻）**
- 23.3 获取当前时间：`now()` / `today()`
- 23.4 **`datetime.utcnow()` 在 3.12 已废弃**，改用 `now(timezone.utc)`
- 23.5 访问各部分；`weekday()` 与 `isoweekday()` 起点不同
- 23.6 `timedelta` 做加减（日期加减必须用它）
- 23.7 **没有 `timedelta(months=1)`**——「加一个月」是业务决策
- 23.8 两个日期相减得 `timedelta`；`.days` 与 `.total_seconds()`
- 23.9 日期比较与排序；`date` 和 `datetime` 不能互比
- 23.10 `strftime` 格式化与常用格式符表
- 23.11 `strptime` 解析：格式必须完全匹配
- 23.12 `%A` / `%B` 输出英文；中文星期自建列表更可靠
- 23.13 **ISO 格式是存储首选**（字符串排序 = 时间排序）
- 23.14 时间戳 `timestamp` / `fromtimestamp`
- 23.15 `time` 模块：`sleep`；**计时用 `perf_counter` 而不是 `time()`**
- 23.16 **naive vs aware**：不带时区与带时区不能相减
- 23.17 `zoneinfo`（3.9+）与 IANA 时区名
- 23.18 **存 UTC，显示本地**——时间处理的核心原则
- 23.19 日期存进 JSON（**兑现 17.9 的承诺**）
- 23.20 `calendar`：`monthrange` 是手工算月份的关键（★）
- 23.21 `dateutil.relativedelta` 一瞥（★ 第三方）
- 23.22 五个常见业务场景：年龄、账期、有效期、按日期命名、工作日

### 第 24 章　命令行程序 ★★
- 24.1 为什么需要命令行参数（改参数要改代码的痛苦）
- 24.2 `sys.argv` 的局限：手工解析要自己做的七件事
- 24.3 argparse 三步：创建 / `add_argument` / `parse_args`
- 24.4 位置参数（命令的「主角」）
- 24.5 可选参数与短选项 `-o` / `--output`
- 24.6 **`type=` 自动转换并自动报错**（不用写一行 `try`）
- 24.7 **自动生成的 `--help`**——argparse 最大的价值
- 24.8 `default` 与 `required`
- 24.9 `choices` 限定取值
- 24.10 **`action="store_true"` 开关**；命令行 `--dry-run` → 代码 `args.dry_run`
- 24.11 `nargs` 接受多个值
- 24.12 自定义 `type` 做校验（抛 `ArgumentTypeError`）
- 24.13 修饰帮助信息：`description` / `epilog` 放使用示例
- 24.14 **子命令**（验收项目 B1 的骨架）
- 24.15 **退出码**：0 成功、非 0 失败——自动化调用的唯一沟通方式
- 24.16 输出分流：结果走 stdout、日志走 stderr
- 24.17 **配置的三层来源**：默认值 < 配置文件 < 环境变量 < 命令行
- 24.18 环境变量 `os.getenv`
- 24.19 `.env` 文件与 `.gitignore`（★）
- 24.20 **密码不能用命令行参数传**（`ps` 能看到）
- 24.21 `getpass` 交互式输入密码
- 24.22 CLI 设计惯例：`--help` / `--version` / `-v` / `--dry-run` / `--force`
- 24.23 **`--verbose` 控制日志级别**（把第 21 章接进来）
- 24.24 **`--dry-run` 的实现**：把「算」和「做」分开
- 24.25 第三方 CLI 库一瞥：`click` / `typer`（★）

### 第 25 章　函数进阶与 Pythonic 写法 ★★
- 25.1 本章定位：不加新工具，把已有的写好
- 25.2 `*args` 接收任意多位置参数（函数内是元组）
- 25.3 `**kwargs` 接收任意多关键字参数（函数内是字典）
- 25.4 参数的完整顺序（写错就 `SyntaxError`）
- 25.5 **参数转发** `wrapper(*args, **kwargs)`——装饰器的核心机制
- 25.6 **解包调用** `f(*列表)` / `f(**字典)`（定义是收集，调用是拆开）
- 25.7 仅关键字 `*` 与仅位置 `/`；**布尔参数一律设成仅关键字**
- 25.8 `lambda`：语法与三个限制
- 25.9 `lambda` vs `def`：不要把 lambda 赋值给变量
- 25.10 **`sorted(key=...)` 正式讲**：`key` 决定按什么排
- 25.11 按字典字段排序；`itemgetter` 等价写法
- 25.12 **多级排序**：`key` 返回元组；数字取负实现降序
- 25.13 稳定排序：字符串字段降序用两次 `sorted`（从最次要的排起）
- 25.14 `map` / `filter` 返回迭代器（`print` 看不到内容）
- 25.15 **为什么推导式通常更好**；以及 `map` 仍然好用的场合
- 25.16 推导式进阶：多重循环、展平、表达式位置的 `if/else`
- 25.17 **推导式的可读性红线**（三条硬线）
- 25.18 生成器表达式（不是元组推导式）
- 25.19 **`any` / `all` + 生成器表达式**：最地道的批量判断
- 25.20 解包全家桶；`_` 表示「不需要这个值」
- 25.21 字典合并 `{**a, **b}` 与 `|`（配置合并的标准写法）
- 25.22 `zip` 进阶：转置、`strict=True`
- 25.23 **从「翻译式代码」到地道 Python：十四组改写对照**
- 25.24 **什么时候不该追求「地道」**：可读性优先

> ### 🎯 验收项目 B
> - **B1 命令行记账本**：JSON/CSV 持久化 + `argparse` 子命令（add / list / report）+ 异常防护 + 日志文件 + 模块拆分（`storage.py` / `report.py` / `cli.py`）
> - **B2 文件自动归档脚本**：`pathlib` 遍历 + 按后缀/日期分类 + 干跑模式（`--dry-run`）+ 冲突重命名 + 操作日志
> - 交付标准：别人拿到你的目录，照 README 建虚拟环境、装依赖、就能跑起来

---

## 阶段三　写出地道、可维护的 Python
**目标**：从"能用"到"像 Python 程序员写的"。这一阶段的内容是工作中真正高频的进阶部分。
**建议投入**：4 周。

### 第 26 章　类与对象 ★★★
- 26.1 为什么需要类：从"一堆函数 + 字典"到对象
- 26.2 `class` 定义与实例化
- 26.3 `__init__` 与 `self` 到底是什么
- 26.4 实例属性 vs 类属性（**共享可变类属性的坑**）
- 26.5 实例方法的调用机制
- 26.6 `__dict__` 与动态属性（★）

### 第 27 章　继承与多态 ★★
- 27.1 继承语法与 `super().__init__()`
- 27.2 方法重写
- 27.3 多态与鸭子类型
- 27.4 `isinstance` / `issubclass`
- 27.5 **组合优于继承**：什么时候不该用继承
- 27.6 多重继承与 MRO（★）

### 第 28 章　特殊方法（魔术方法）★★
- 28.1 `__str__`（给人看）与 `__repr__`（给开发者看）
- 28.2 容器化：`__len__` / `__getitem__` / `__setitem__` / `__delitem__` / `__contains__`
- 28.3 相等与哈希：`__eq__` / `__hash__` 必须成对考虑
- 28.4 比较运算符重载与 `functools.total_ordering`
- 28.5 算术运算符重载
- 28.6 `__call__`、`__iter__`
- 28.7 常用魔术方法速查表

### 第 29 章　让类写得更省：属性与现代工具 ★★
- 29.1 命名约定：`_protected` 与 `__private`（名称改写的真相）
- 29.2 `@property` / `@x.setter`：把方法伪装成属性
- 29.3 `@classmethod`（`cls`）与 `@staticmethod`
- 29.4 **`dataclass`：少写一半样板代码（强烈推荐）**
- 29.5 `Enum` 枚举：替代满天飞的魔法字符串
- 29.6 类设计小结与常见反模式（万能类、贫血类、继承过深）

### 第 30 章　迭代器与生成器 ★★
- 30.1 可迭代对象 vs 迭代器：`iter` / `next`
- 30.2 迭代器协议：`__iter__` / `__next__`
- 30.3 生成器函数与 `yield`：延迟计算
- 30.4 生成器表达式
- 30.5 实战：逐行处理超大文件、无限序列、数据流管道
- 30.6 `yield from` 与 `send`（★）

### 第 31 章　闭包与装饰器 ★★
- 31.1 前置知识：闭包与 `nonlocal`
- 31.2 最简装饰器与 `@` 语法糖
- 31.3 带参数的装饰器（三层嵌套）
- 31.4 `functools.wraps` 保留元信息
- 31.5 类装饰器（★）
- 31.6 实战五例：计时、缓存、重试、日志、参数校验

### 第 32 章　上下文管理器 ★★
- 32.1 `with` 的原理：`__enter__` / `__exit__`
- 32.2 用类实现
- 32.3 `contextlib.contextmanager` 装饰器写法（更常用）
- 32.4 `suppress` / `ExitStack`（★）
- 32.5 实战：数据库连接、计时器、临时切换目录、临时改配置

### 第 33 章　类型注解 ★★
> 现代 Python 的基本素养，注解不影响运行，是给人和工具看的。
- 33.1 基本语法：变量、参数、返回值
- 33.2 容器：`list[int]` / `dict[str, int]` / `tuple[int, ...]`
- 33.3 `Optional` / `Union` 与 `X | None` 新写法
- 33.4 `Any` / `Callable` / `TypeVar` / 泛型入门
- 33.5 `TypedDict` / `Protocol` / `Literal`（★）
- 33.6 用 `mypy` 检查，以及"注解到什么程度就够了"

### 第 34 章　高效标准库三件套 ★★
- 34.1 `collections`：`defaultdict`、`Counter`、`deque`、`namedtuple`、`OrderedDict`
- 34.2 `itertools`：`chain`、`groupby`、`product`、`combinations`、`islice`、`count`、`cycle`
- 34.3 `functools`：`lru_cache`、`partial`、`reduce`、`cached_property`
- 34.4 `operator`（★）
- 34.5 对照练习：同一需求用"手写循环"与"三件套"各写一遍

### 第 35 章　正则表达式 re ★★
- 35.1 什么时候该用正则，什么时候**别用**（HTML、嵌套结构）
- 35.2 元字符与字符类
- 35.3 数量词、贪婪与非贪婪
- 35.4 分组、命名分组、反向引用
- 35.5 API：`match` / `search` / `findall` / `finditer` / `sub` / `split`
- 35.6 `compile` 与标志位（`IGNORECASE` / `MULTILINE` / `DOTALL`）
- 35.7 实战：校验、提取、批量替换、日志解析
- 35.8 调试正则的方法与可读性（`re.VERBOSE`）

### 第 36 章　测试 ★★
- 36.1 没有测试的代码，改一次就要手工验一次
- 36.2 从 `assert` 到测试函数
- 36.3 **`pytest`（推荐）**：命名约定、断言、`-k`、参数化 `parametrize`、`fixture`
- 36.4 `unittest`：`TestCase`、`setUp` / `tearDown`（老项目里常见）
- 36.5 `mock` 入门：隔离网络、时间、数据库
- 36.6 覆盖率与"测多少才够"
- 36.7 `doctest`（★）

### 第 37 章　代码风格与工程习惯 ★★
- 37.1 PEP 8 要点速览
- 37.2 命名、注释、文档字符串规范
- 37.3 自动化工具：**`ruff`（检查 + 格式化 + 整理 import，已取代 black/isort/flake8）**、`mypy` 类型检查、pre-commit
- 37.4 项目目录结构惯例与 `pyproject.toml`
- 37.5 `.gitignore`、配置分离、`.env`
- 37.6 少见但会遇到的语法：海象 `:=`、`match-case`、`__pycache__`、shebang、编码声明
- 37.7 Python 3.9~3.13 主要新特性年表

> ### 🎯 验收项目 C（综合验收）
> **接口数据采集入库工具**
> - `requests` 抓取公开 JSON 接口 → 清洗为 `dataclass` → 写入 `sqlite3`（参数化查询）
> - 增量更新 + 失败重试装饰器 + 完整日志 + `argparse` 参数
> - 全程带类型注解，`pytest` 覆盖核心逻辑（mock 掉网络）
> - 通过 `ruff` + `mypy` 检查，`pyproject.toml` 规范化，可 `pip install -e .`

---

## 阶段四　按需专项（不必按顺序，也不必都学）
**判断标准**：你手上的活儿用到了才学。没用到就先跳过，知道"有这么个东西"即可。

### 专项 A　并发与异步 ★
- A.1 概念：并发 vs 并行、IO 密集 vs CPU 密集
- A.2 **GIL**：为什么多线程救不了计算密集任务
- A.3 `threading`：线程、`Lock` / `RLock`、`queue.Queue`
- A.4 `multiprocessing`：进程、进程池、`Queue` / `Pipe` 进程间通信
- A.5 **`concurrent.futures`：先学这个**（`ThreadPoolExecutor` / `ProcessPoolExecutor`）
- A.6 `asyncio`：事件循环、`async def` / `await`、`gather`、并发限流
- A.7 选型决策表：什么场景用哪个（附"大多数人其实只需要线程池"的结论）

### 专项 B　性能与内存 ★
- B.1 先测量再优化：`time` / `timeit` 微基准
- B.2 `cProfile` 找热点；`line_profiler` 逐行分析（第三方）
- B.3 常见优化手段：选对数据结构 > 一切微技巧
- B.4 内存机制：**引用计数**、**GC 标记清除与分代回收**、循环引用
- B.5 `__slots__` 省内存、`weakref` 弱引用
- B.6 小整数对象池与字符串驻留【面试向】
- B.7 "不要过早优化"的具体含义

### 专项 C　数据库 ★★
- C.1 DB-API 2.0：Python 访问数据库的统一模型
- C.2 `sqlite3` 全流程：连接、游标、增删改查
- C.3 **参数化查询与 SQL 注入**（永远不要拼字符串）
- C.4 事务、提交、回滚、上下文管理
- C.5 连接 SQL Server / MySQL / PostgreSQL 的驱动选择
- C.6 ORM 是什么、要不要用：SQLAlchemy 概览

### 专项 D　网络与 HTTP ★★
- D.1 `requests`：GET / POST、params、headers、`Session`、超时
- D.2 处理 JSON 接口、状态码、错误重试
- D.3 `urllib.request` 标准库方案
- D.4 起一个最小 HTTP 服务；`socket` 极简介（★）
- D.5 爬虫的边界：robots、频率、法律与道德

### 专项 E　打包与交付 ★★
- E.1 `pyproject.toml` 与项目骨架
- E.2 本地可编辑安装 `pip install -e .`
- E.3 发布到 PyPI 概览
- E.4 打包成 exe：`PyInstaller`
- E.5 用 Docker 交付概览（★）

### 专项 F　底层与元编程 ★【面试向，应用开发极少用】
- F.1 一切皆对象：`type` 与 `object`
- F.2 属性查找顺序完整版；描述符协议
- F.3 `__getattr__` / `__getattribute__` / `__setattr__`
- F.4 动态属性 `getattr` / `setattr` / `hasattr`；`importlib` 动态导入
- F.5 `inspect` 运行时自省
- F.6 `type()` 三参数动态建类；**元类 metaclass**（只有写框架/ORM 才需要）
- F.7 `eval` / `exec` / `compile` 及其安全风险
- F.8 抽象基类 `abc`
- F.9 `ctypes` / `struct` / Cython 概览

---

## 工具箱（随查手册，不按顺序读）

### T1　系统与进程
`os`（环境变量、进程信息）、`sys`（`argv` / `exit` / 标准流）、`shutil`（复制移动压缩）、`glob`、`subprocess`（调外部命令）、`tempfile`

### T2　数字与数学
`math`、`random`（含随机种子）、**`decimal`（金额计算为什么不能用 float）**、`statistics`、`fractions`

### T3　其他数据格式
`configparser`（ini）、`pickle`（及其安全风险）、`xml` / HTML 解析、`hashlib`、`base64`、`uuid`

### T4　第三方生态导览（知道该找谁）
- 数据处理：`numpy`、`pandas`
- 可视化：`matplotlib`、`plotly`
- Web：`FastAPI`、`Flask`、`Django` 对比
- 办公自动化：`openpyxl`、`python-docx`、`schedule`
- 机器学习：`scikit-learn`、`LightGBM`
- 如何评估一个第三方库值不值得用（维护活跃度、依赖、许可）

### T5　编辑器实用指南（VS Code 为主）
- T5.1 装哪几个扩展就够了（不要装一堆）
- T5.2 必改的六个设置（含 `settings.json` 原文）
- T5.3 十二个高频快捷键
- T5.4 `launch.json`：**带参数运行**、设置工作目录、调试模块
- T5.5 集成终端与虚拟环境的配合
- T5.6 八个常见问题：报红但能跑、import 报红、格式化不生效……
- T5.7 PyCharm 对应操作速查（换 IDE 时查这张表）

### T6　pip 疑难速查
- T6.1 常用命令一览（含不常用但关键的几个）
- T6.2 **装库失败的六种原因与解法**
- T6.3 `pip check`：依赖冲突怎么看
- T6.4 缓存：`pip cache dir` / `purge`
- T6.5 离线安装：`pip download` + `--no-index --find-links`
- T6.6 从 Git / 本地路径 / 压缩包安装
- T6.7 `pip install -e .` 可编辑安装
- T6.8 `requirements.txt` 进阶：分层、注释、嵌套 `-r`
- T6.9 查看依赖树
- T6.10 什么时候都不该用 `sudo pip`
- T6.11 用 `uv` 替代 pip（★ 快一到两个数量级）

### T7　附录
- A　常见报错速查表：症状 → 原因 → 解法
- B　内置函数按用途分类速查
- C　字符串 / 列表 / 字典 / 集合方法速查
- D　`strftime` 格式符表、正则元字符表、`open` 模式表
- E　**给有其他语言经验者的对照表**（C# / Java / SQL → Python）
- F　新手最常踩的 30 个坑汇总
- G　推荐文档、书籍、练习网站

---

## 节奏总表

| 阶段 | 章节 | 建议投入 | 完成后你能做什么 | 可否跳过 |
|---|---|---|---|---|
| 一　基础入门 | 1~15 | 3~4 周 | 写出解决具体问题的单文件脚本 | 不可 |
| 二　脚本→程序 | 16~25 | 3 周 | 交付一个别人也能跑起来的命令行工具 | 不可 |
| 三　地道 Python | 26~37 | 4 周 | 写出团队能接手、能测试、能维护的代码 | 不可，但可调序 |
| 四　按需专项 | A~F | 按需 | 解决特定方向的问题 | **可以，用到再学** |
| 工具箱 | T1~T5 | 不限 | 当手册查 | 不必通读 |

**剪裁原则**：阶段一 + 阶段二 ≈ 日常工作的 80%，先用两个月全力打通，不要在冷门细节上纠缠。阶段三是"从会写到写好"的分水岭，不要因为"能跑就行"而跳过。阶段四里除了 C（数据库）和 D（网络）之外，多数人可以长期不碰；专项 F 基本只在面试和造框架时用得上。
