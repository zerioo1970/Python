# 写作交接（新会话第一步读这一页）

> **给 AI 的指令**：如果你是新会话，**先完整读这一页**，再按"下次从哪继续"开工。这一页只反映当前状态，每次写完要改写它。
>
> **给人的说明**：这个文件的作用是让跨天、跨会话的写作保持一致。AI 不会自动记住上次的对话，但读完这一页就能恢复全部约定。

---

## 1. 这是什么项目

一份 **Python 3 中文新手教程**，从零开始覆盖到工程实践。目标读者是零基础或半途卡住的人，以及**有其他语言经验（C# / Java / SQL）需要迁移的人**。

**结构**：主线 37 章（三阶段）+ 选修专项 A~F + 工具箱 T1~T7。

**核心定位**：不是语法手册，而是"能交付、能维护"的实践教程。三个阶段分别对应：能写脚本（1~15）→ 能交付工具（16~25）→ 能维护代码（26~37）。

---

## 2. 当前进度

| 部分 | 目录 | 图纸 | 正文 |
|---|---|---|---|
| 阶段一（第 1~15 章） | ✅ | ✅ | **✅ 全部完成（15/15）** |
| 阶段二（第 16~25 章） | ✅ | ✅ | **第 16 章 ✅**，17~25 ⬜ |
| 阶段三（第 26~37 章） | ✅ | **⬜ 未写** | ⬜ |
| 专项 A~F | ✅ | ⬜ | ⬜ |
| 工具箱 T1~T7 | ✅ | 手册类不写图纸 | **T5、T6 ✅**，其余 ⬜ |

**已完成正文的体量**：15 章共 **91741+ 汉字 / 361 个示例**（实测，非估算）；T5 / T6 约 5200 字。
**最长的三章**：第 14 章函数（10102 字 / 35 例）、第 13 章拷贝语义（8691 字 / 25 例）、第 15 章异常（8555 字 / 23 例）。
**质检状态**：`tools/check_outputs.py` 对 12 章共 **570 处**行内标注 + 35 个输出块全部核对通过。

---

## 3. 工作方式：跟着读者的学习进度写

**用户明确要求的方式**（2026-08-01 确认）：

> 他先看正文学习，看到第几章发现问题就提出来，我随即修改；正文和图纸**不必一次性全写完**，跟着他的阅读进度往下写。

### 3.1 为什么这样可行

跨章设计（承诺兑现链、语法边界、示例复用链）**必须提前做**，否则边写边想会前后打架。而**第 1~25 章的图纸已经全部写完**，这些设计已就位——所以前 25 章的正文可以按需逐章写，写一章停一章都不影响一致性。

**第 26~37 章图纸暂时不写是安全的**：目录已定稿、章号稳定，前面正文里"详见第 30 章"这类指向不会失效。等写到阶段二末尾再补图纸即可。

### 3.2 用户提出问题时的两种处理

| 类型 | 表现 | 处理 |
|---|---|---|
| **A：局部问题** | 某处讲得不清楚、有错、示例不好 | 改该章正文 → **同步更新该章图纸** → 跑质检 → 推送。影响范围仅一章 |
| **B：结构性调整** | 某概念该提前/推后/拆章 | **先告知影响面再动手**：会牵连目录、图纸、以及已写正文里的交叉引用（章号变了，所有"详见第 N 章"都要改）。**越早提越便宜** |

**重要**：改正文时**必须同步改对应的图纸**，否则两者会不一致，后续接手时会照着过时的图纸写。

### 3.3 ⚠️ 已和用户约定的推进顺序（2026-08-01 确认）

用户说"等我学习到 25 章，再补充后边的施工图和正文"。**这句话的正确理解是**：

```
① 用户现在读第 1~16 章（正文已有）
② 他读到哪一章有问题就提出来，我随即修改
③ 我按他的阅读进度，逐章写第 17~25 章正文（一次一章）
④ 等他读到第 25 章，才开始补第 26~37 章的图纸 + 正文
```

**不要在他读到第 25 章之前去补第 26~37 章的图纸。** 理由是他自己说的那个道理：图纸的价值只在"即将要写正文"时兑现，提前 12 章写出来，等真写到那儿很可能还要改（前 25 章图纸在打样阶段就调整过 5 次目录结构）。

**补第 26~37 章图纸时要分三批，每批一个新对话**（12 章一次写完，对话自己会变长被压缩，导致前后语法边界打架、示例编号引用错）：

| 批次 | 章节 | 内在联系 |
|---|---|---|
| 第一批 | 26~29 | 类与对象、`dataclass`、魔术方法、测试——面向对象 |
| 第二批 | 30~33 | 迭代器生成器、装饰器、上下文管理器、类型注解——语言进阶 |
| 第三批 | 34~37 | 标准库精选、并发一瞥、代码组织、工程化收尾 |

每批写完要把**跨批约定**记进本文件：该批定的可用语法边界、埋了哪些伏笔待回收、有没有因此需要调整目录。

### 3.4 下次开工的默认动作

1. 问用户读到哪一章、有什么问题
2. 若无待改问题 → **写下一章正文**（当前应写第 17 章：数据格式 JSON 与 CSV）
3. **别主动去补第 26~37 章图纸**，见 3.3 的约定
3. 若有待改问题 → 先按 3.2 分类处理，再往下写

## 4. 写作流程

### 4.1 章节（第 1~37 章、专项 A~F）

**先图纸，后正文。** 图纸写在 `02_施工图.md`，正文写在 `chapters/`。

图纸每章五块：章头信息表（含**可用语法边界**）→ 知识点清单（带四问要点）→ 代码示例清单（带 A/B/C 段位）→ 样板示例 → 易错点。

### 4.2 工具箱（T1~T7）

**直接写正文，不写图纸。** 理由：图纸的价值在于控制教学顺序、示例梯度、语法边界，而工具箱是"随查手册"，内容本身就是确定的。写在 `toolbox/`。

### 4.3 每写完一章要做的事

1. 跑 `python3.12 tools/check_outputs.py`，必须全部通过
2. 用 `python3 tools/sync_toc.py 章号 内容文件` 同步目录小节
3. 更新本文件第 2 节的进度表，以及 `01_目录.md` / `README.md` 里的进度表
4. 提交并推送

---

## 5. 十条铁律

这些是用户明确要求或已定的规则，**不要重新讨论，直接执行**：

1. **不设字数上限。** 用户明确要求过。唯一标准是把概念讲清楚——宁可长，不可漏。
2. **A/B/C 段位不设比例指标。** 用户明确要求过。前几章 A 段为主是正常的（还没有语法可供组合），后面章节 B/C 段为主。唯一硬要求：有 A 段起步、至少一个 C 段收尾。
3. **每个概念必答四问**：是什么 / 为什么 / 什么时候用 / 容易错在哪。★★★ 概念额外补边界情况与常见误解。（规范 2.1）
4. **重要行必须有注释**，判定标准见规范 3.3；不给显而易见的代码加注释。
5. **所有输出必须实测**，用 Python 3.12，写完跑质检脚本。（规范 6.7）
6. **每章末尾必须有「给有其他语言经验者」折叠块**，重点写"看着一样其实不同"的地方。（规范 6.6）
7. **必须维护两条主线**：伏笔回收链、承诺兑现链。（规范 6.5）
8. **每个概念收口给一句"记住这条"**，读者合上书能想起来的就是这一句。
9. **图纸里的"可用语法边界"是硬约束**：某章的示例不得使用后面章节才讲的语法。已因此调整过 5 处目录结构。
10. **不回避难点，也不吹嘘 Python。** 该说"Python 慢"就说，该说"少了编译器保护"就说。

---

## 6. 已定的关键决策（附理由，防止被推翻重议）

| 决策 | 理由 |
|---|---|
| **目录用四阶段 + 工具箱结构** | 用户对比了自己的三阶段方案和我的 51 章平铺方案，综合而成。阶段制有终点感，工具箱不打断主线 |
| **报错前置到第 2 章** | 新手要犯的错比要写的代码多，读报错是最先用得上、最省时间的技能 |
| **深浅拷贝提到第 13 章**（不放到最后） | 它会产生真 bug，第二周就会踩 |
| **元类 / GIL / 垃圾回收下沉到选修专项并标【面试向】** | 用户原方案把它们放"高级进阶"，我指出这是面试八股不是工作进阶，用户接受 |
| **VS Code 为主，PyCharm 只给对照表** | 用户问过选哪个，结论：AI 编码工具生态、轻量、免费 MSSQL 扩展（用户有 20 年 SQL Server 背景）、Jupyter 成熟。**注意：2025 年起 PyCharm 已合并为单一产品，Jupyter 支持免费**，旧的"社区版不支持 Jupyter"论据已失效 |
| **只推荐 Ruff，明确不要装 Black/isort/Flake8** | Ruff 到 2026 年已取代这三个，一个工具搞定 lint + format + 整理 import |
| **第 22 章允许预支一行 `class`** | 自定义异常需要它，而类在第 26 章。正文必须写明"照抄这个形式，第 26 章会讲原理"，且禁止在第 22 章讲继承/`__init__`/方法 |
| **工具箱编号**：T1~T4 标准库、T5 编辑器、T6 pip、**T7 附录** | 附录原为 T5，新增两节后重排，附录归到最后 |
| **第 5 章不拆分**（字符串 + f-string 合为一章） | 用户 2026-08-01 拍板。我原倾向拆（实测约 5900 字偏长），用户看过正文后认为一章读下来更连贯。**不要再提拆章** |
| **推送一律直推 `main`，不开 PR** | 用户 2026-08-01 拍板。这是教程仓库、单人写作，PR 只增加操作负担。写完一章就提交推送，不必询问 |
| **不加专项 G（数据分析）** | 用户 2026-08-01 先同意后改主意：数据分析**不进这份教程**，以后**单独写一篇专门的文章**。理由是它和"Python 语言入门"不是一件事，混在一起两头都不到位。**曾经写进目录的 18 节大纲已整块删除**，要点见第 13 节备忘。**不要再提议加进教程** |

---

## 7. 用户情况（影响写作judgment的部分）

- **20 年 C# ASP.NET WebForm + SQL Server 经验**，SQL 与业务理解扎实 → **折叠块里的 C#/SQL 对照有真实价值，不是摆设**
- **Python 入门阶段**，大学数学已遗忘 → **不做数学推导**（浮点精度用"十进制写不出 1/3"类比讲，不推导 IEEE 754）
- **实际目标**：用 LightGBM 在 A 股盘中判断下跌转折点（应用开发，不做算法研究）→ 数据分析方向的内容对他有实际价值
- **每天最多 2 小时** → 减少配置折腾、给可照抄的清单比给原理更实用
- **用中文回答**

---

## 8. 实测与质检

```bash
# 写正文前：先把要用的输出跑出来（不要凭记忆写）
python3.12 -c "..."

# 写完后：三个脚本【都要跑】，它们查的是互补的盲区
python3.12 tools/check_outputs.py     chapters/ch15_*.md   # 逐行比对，定位最准
python3.12 tools/check_blocks.py      chapters/ch15_*.md   # 整块真跑，专治 is 比较
python3.12 tools/check_text_blocks.py chapters/ch15_*.md   # 核对多行 ```text 输出块

# 不带参数 = 全部章节（全书跑一遍约 2~4 分钟，建议放后台）
```

### ⚠️ 只跑一个脚本会漏——三个盲区都是打样时踩出来的

| 脚本 | 强项 | 盲区 |
|---|---|---|
| `check_outputs.py` | **逐行 exec**，能指出是哪一行不符 | 含 ` is ` 的行被当"易变"整类跳过 |
| `check_blocks.py` | **整块写成 .py 用子进程真跑**，与读者存成文件运行完全一致，`is` 结果可信 | 只能块级定位 |
| `check_text_blocks.py` | 核对多行输出（对齐报表、`help()`），手工估算的空格数肉眼极难发现错 | 只认紧邻配对 |

**三个盲区的来历，别再踩**：

1. **第 12 章**：CPython 对字符串哈希逐进程随机化 → 集合顺序每次不同。首轮"运气好"通过，连跑 20 轮才暴露 → 加了 `DECLARED_UNSTABLE` 机制。
2. **第 13 章**：`is` 比较被整类跳过，而该章 **16 处标注全是 `is`** → 等于核心内容一处也没核对上 → 新增 `check_blocks.py`。
3. **第 15 章**：`# =>` **独占一行**时（多行 `try/except` 后统一标注输出），逐行模式因"这行没有代码可执行"而**静默丢掉标注**，写错也发现不了。本章一个错别字就是靠 `check_blocks.py` 抓出来的 → 已补宽松核对。

**补完之后全书核对数从 570 涨到 832**（第 15 章一章从 28 涨到 63）——说明此前漏掉的不是个别情况。

> **教训**：**每次改质检脚本后都要看核对数有没有涨。** 涨了说明之前在漏。

**会自动跳过的**分两类：

| 类型 | 怎么识别 | 你要做的 |
|---|---|---|
| **自动跳过** | 脚本靠 `VOLATILE` 名单认出 `id()`、时间、随机数、`is` 比较 | 在正文里注明"你运行时结果会不同"；`is` 另用 `check_blocks.py` 核对 |
| **手工声明** | 脚本靠 `DECLARED_UNSTABLE` 名单，认标注里的"顺序不定 / 你运行时 / 每次运行都不同"等字样 | **在 `# =>` 标注里写上这类字样**，并在正文向读者解释它为什么会变 |

### ⚠️ 含集合的章节必须连跑 20 轮

```bash
for i in $(seq 1 20); do python3.12 tools/check_outputs.py chapters/ch12_集合.md; done
```

**原因**：CPython 对**字符串哈希做了逐进程随机化**，`print(set("hello"))` 的顺序**每次运行都不同**。
第 12 章第一轮质检"运气好"地通过了，连跑 20 轮才抓出 `print(set(("a","b","a")))` 这一行漏标。
**整数集合是稳定的**（小整数哈希值就是它自己），只有**字符串**参与时才飘——最容易误判的正是这一点。

### 三个辅助脚本

| 脚本 | 用途 |
|---|---|
| `tools/check_outputs.py` | 自动核对正文里的 `# =>` 标注和多行输出块 |
| **`tools/run_in_tty.py`** | **在伪终端里跑交互示例**，拿到读者真实看到的画面（含输入回显）。<br>含 `input()` 的示例**必须**用它验证——管道输入时用户敲的字符不回显，输出会变成 `第一个数字：第二个数字：105` 这种读者根本看不到的样子。<br>用法：`python3 tools/run_in_tty.py 脚本.py 输入1 输入2` |
| `tools/sync_toc.py` | 按章节标题范围整块替换目录小节 |

### 两个已经用上的实测技巧

- **死循环**：`timeout -s INT 1 python3.12 loop.py` 模拟 `Ctrl+C`，能拿到真实的 `KeyboardInterrupt` 输出（第 8 章用过）
- **随机数**：测试时用 `random.seed(42)` 固定结果以便复现（第 8 章猜数字的答案是 82），但**正文代码里不要留 seed**，并注明"你运行时结果会不同"

**实测已经救过的错**（说明这个流程必要）：`19.9 * 3` 的浮点误差、价格表空格数、`257 is 257` 在整块/逐行执行时结果相反、`newline=""` 只在 Windows 出问题、第 2 章"报错行号会骗你"在 3.10+ 已不成立、**`set(("a","b","a"))` 的输出顺序每次都不同**。

---

## 9. 推送约定

- 仓库：`zerioo1970/Python`，工作副本在 `/projects/sandbox/Python`
- **一律直推 `main`，不开 PR**（用户 2026-08-01 明确决定）
- 用户不熟 Git 且不愿为流程费心 → **每次写完主动提交并推送，不要询问**
- 推送用 `kiro_powers` → `github` → `push_to_remote`，参数：
  `{"owner":"zerioo1970","path":"/projects/sandbox/Python","remote_branch_name":"main","repository_name":"Python"}`

---

## 10. AI 自己容易犯的操作错误（避坑）

| 坑 | 表现 | 做法 |
|---|---|---|
| **`fs_append` 会让文件在浏览器预览里变成 "Binary file"** | 用户打不开文件 | **不要用 `fs_append`**。要追加内容就：写临时文件 → 用 python 脚本拼接后整体写回 → 删临时文件 |
| **heredoc 里的 Python 字符串引号冲突** | `SyntaxError: invalid syntax` | 含双引号的文本用单引号包裹，或改用中文书名号「」；**内容多时写进临时文件再由脚本读取** |
| **目录同步时匹配原文失败** | `AssertionError` | 不要手工猜原文，用 `tools/sync_toc.py 章号 内容文件`（按章节标题范围整块替换） |
| **两份副本不同步** | 改了一份忘了另一份 | **唯一工作副本是 git 仓库 `/projects/sandbox/Python`**，不要另建目录 |
| **用"截断到某个标记再拼接"的方式改文件末尾** | **把那个标记后面的内容整段删掉了** | 改末尾时先确认标记之后还有没有别的内容；**宁可用精确的 `str.replace(旧段, 新段)`**。<br>已经犯过一次：改"最后更新"那段时，把它后面的**第 13 节整节删掉了**，靠 `git show 上一个提交:文件` 才找回来 |
| **忘了新对话怎么恢复上下文** | 下次接手时风格、决策全丢 | 仓库里有 `.kiro/steering/python-tutorial.md`（`inclusion: auto`），会自动提示先读本文件。**改了本文件的结构后，记得看那个引导文件是否还准确** |

---

## 11. 要细节去哪个文件

| 想知道 | 看 |
|---|---|
| **新对话怎么恢复上下文** | `.kiro/steering/python-tutorial.md`（自动加载，会让你先读本文件） |
| 教程定位、读者画像、读法、四阶段完整目录 | `01_目录.md` |
| **写作规范**（模板、四问法、注释判定、示例梯度、术语表、自检清单、两条主线、实测流程） | `02_施工图.md` 第一部分（规范 1~7 节） |
| 某一章要讲什么、示例怎么排 | `02_施工图.md` 第二部分（第 1~25 章图纸） |
| 已完成正文的风格实物 | `chapters/ch01`~`ch15`（**最新风格看 ch13 / ch14 / ch15**） |
| 工具箱的写法参考 | `toolbox/T5`、`T6` |

---

## 12. 待用户确认的问题

**当前没有。** 原先挂着的三个问题已于 2026-08-01 全部由用户拍板，结论见第 6 节已定决策表：

| 原问题 | 结论 |
|---|---|
| 第 5 章要不要拆两章 | **不拆** |
| 直推 main 还是走 PR | **直推 main** |
| 要不要加专项 G | **要加**（已写入 `01_目录.md`，18 个小节） |

以后有新的待确认问题追加到这里；**但不要因为没答就停下写作**。

---

---

## 13. 另行成文：数据分析（不属于本教程）

用户决定**数据分析单独写一篇文章**，不作为教程的专项。这里只留骨架，等他提起时再展开。

**定位**：教用 Python 处理真实数据，**不教算法**。重点是数据加工（真实项目 80% 的时间在这里），不是建模。

**曾拟定的内容骨架**（讨论过一轮，用户看过）：

| 块 | 内容 |
|---|---|
| 入门与"先看一眼" | 为什么需要 pandas；`Series`/`DataFrame`；**索引不是行号**；读 CSV 时**股票代码 `000001` 被读成 `1`**；拿到新数据的固定动作（`info`/`describe`/`value_counts`/`isna().sum()`） |
| 数据加工核心 | `loc` vs `iloc`；布尔筛选（多条件每个都要加括号）；**`SettingWithCopyWarning` = 教程第 13 章"视图 vs 拷贝"的 pandas 版**；缺失值（`NaN != NaN`、**别无脑填 0**）；向量化（**别用 `for` 遍历 DataFrame**）；`groupby`=`GROUP BY`；`merge`=`JOIN`（**连接后行数变多 → 右表有重复键**） |
| 时间序列 | `to_datetime` / `resample` / `rolling`；**`shift(1)` 取过去（安全），`shift(-1)` 取未来（几乎总是错的）**；交易日不连续，别用自然日做偏移 |
| **工程陷阱（最有价值的两节）** | **① 数据泄漏**：三种形式——未来函数、标准化/填充做在切分之前、时序数据用随机切分。自检：**效果好得反常时先怀疑泄漏**。<br>**② 训练-推理一致性**：训练时构造特征的方式必须与实盘那一刻的信息边界完全相同；做法是**把特征构造写成一个函数，训练和推理调用同一个** |
| 收尾 | 最小流程（`fit`/`predict`、LightGBM 适合表格数据）；**类别不平衡时 `accuracy` 会骗人**；**换模型的提升通常远小于修一个数据问题的提升**；探索代码要搬进 `.py` 写成函数 |

**故意不写**：数学推导（用户大学数学已忘，应用开发不需要）、深度学习（表格数据 + 几千行样本没优势）、调参技巧、Spark/Flink（数据装得进内存）。

**写作要点**：用户有 20 年 SQL Server 经验 → **每节都给 SQL 对照**（`groupby`=`GROUP BY` 等），这是他最省力的入口。

**红线**：属于技术学习，**不构成投资建议**；不得建议用真实资金验证未经样本外检验的模型。

**待定**（他当时没答，将来写文章时要先问）：要不要包含建模的最小流程、画图讲多深、例子用股票日线还是通用销售数据。

---

**最后更新**：完成**第 16 章**正文（文件读写与路径），阶段二开篇。图纸已回填打样记录。

**本轮新增的两条自检要求**（已写进规范第 7 节）：
1. **代码块尽量自包含**，不依赖前一块的执行结果——三个质检脚本都是逐块独立执行的，跨块依赖会被判为不一致；而且**读者复制单独一块也应该能跑出结果**。第 16 章的示例 16-30 就因此报了错，已改成每块自带 `save`/`load` 定义。
2. **会产生真实文件的章节（16、17），质检必须在临时目录里跑**并预先备好前置文件，否则示例产物（`note.txt` / `out/` / `messy/`）会污染仓库：
   ```bash
   cd 临时目录 && python3.12 ../../tools/check_outputs.py ../../chapters/ch16_*.md
   ```
   正文也要给"创建了哪些文件 + 清理说明"。

**下一章应写第 17 章（JSON 与 CSV）**。它要兑现第 16 章末尾那个承诺：16-30 那三十行自拼格式的 `save`/`load`，用 JSON 两行取代，且不怕任何特殊字符。第 16 章已经把"自拼格式为什么脆弱"演示到位了（名字带 `|` 直接崩、带 `=` 侥幸对），第 17 章开篇可以直接接上。

**仍然建议提醒用户**：阶段一已完结，**该动手做验收项目 A 了**（第 15 章示例 15-23 是 A3 的骨架）。看懂和写出来是两件事。
